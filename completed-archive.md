# **Done**

* **[2026-07-05 — 🧪 Group 32 Bulletproof-Sweep Flake Cleanup COMPLETE → on `feature/group-32-bulletproof-flake-cleanup` (merged to `dev` + `main`)]** — The 3 load-sensitive flakes from the Group-26 verification sweep (all passed in isolation, failed only under the saturated `RUN_INTEGRATION=1` sweep). Test-infra + one small cross-platform product hardening. Root cause of 32.1 pinned down: the `data/*.json` store files are **bind-mounted into the Docker container**, so the host pytest and the in-container cancel-monitor daemon `os.replace` the same target at the same instant → Windows `PermissionError` (WinError 5/32). Verified: the 3 targets are GREEN in the full sweep; the remaining 5 sweep failures were **A/B-proven pre-existing** (fail identically with this diff stashed) and filed as a fresh Testing item (Group-33 candidate). Full spec: [docs/agent_docs/tasks/32-bulletproof-sweep-flake-cleanup.md](docs/agent_docs/tasks/32-bulletproof-sweep-flake-cleanup.md).

  * ✅ **32.1 — atomic-write race across the bind-mounted JSON stores.** New shared `atomic_store.replace_with_retry(src, dst)` — `os.replace` with a bounded retry on Windows `PermissionError` (rides out the host↔container sharing collision), a true **no-op on POSIX/prod** (Linux `os.replace` is atomic), re-raising after N attempts so a persistently-held target isn't masked. Routed **all four** same-pattern stores through it (`cancel_fetch_store`, `cancel_review_store`, `print_deduct_ledger`, `print_tracker_store`) — killing the whole class, not just the one test that flaked. Pinned by `tests/test_atomic_store_retry.py` (5 cases: retry-then-succeed, re-raise-after-exhaustion, non-PermissionError-fast-fail, `attempts` guard, + a `cancel_fetch_store._save` integration proving it routes through the retry). The `encoding='utf-8'` half of the buglist item was already present since the file's creation — the buglist misdiagnosed it.
  * ✅ **32.2 — bulk_set integration read-timeouts under sweep load.** New `_req(method, url, …)` helper in `test_filament_attributes_bulk_api.py` — a bounded retry on **transport errors only** (`ReadTimeout`/`ConnectTimeout`/`ConnectionError`, never an HTTP status), 15 s timeout floor for headroom, re-raising after N attempts so it rides out transient saturation **without masking a real slowdown**. The two flaky `bulk_set` tests + the shared `_pick_target`/`_snapshot_attrs`/`_restore` helpers route through it. All 11 bulk_api tests green.

* **[2026-07-05 — ⚖️ Group 31 Weight-Entry Fixes & Unification COMPLETE → on `feature/group-31-weight-entry-fixes` (merged to `dev` + `main`)]** — Three weight-surface items from Derek's 2026-07-03 buglist, executed as the CLAUDE.md-mandated "route new weight input through the unified `<WeightEntry>`, don't bolt on a 4th ad-hoc form" migration slice. Built → **5-lens adversarial diff-review** (write-surface/outage-class, unknown-initial math, preserve-text, wizard-launcher, test-adequacy → per-finding verify; 17 agents / ~966K tokens / 12 raw findings → **4 confirmed, all fixed**) → re-tested. **0 regressions:** 94 math/overlay/launcher/archive-helper tests + the weigh-out E2Es all green in isolation (the batch-run buffer-population failures are the pre-existing buffer-poll flake — the untouched `test_weigh_out_e2e.py` fails identically under load, passes alone). Full spec: [docs/agent_docs/tasks/31-weight-entry-fixes-unification.md](docs/agent_docs/tasks/31-weight-entry-fixes-unification.md).

  * ✅ **31.3 (N4) — Bulk weigh-out no longer loses keyed-in text on redraw.** `renderWeighOutList()` (`inv_weigh_out.js`) now snapshots every `.weigh-input` value before the `innerHTML` rebuild and restores them (+ re-runs each preview) — one change covers BOTH redraw triggers (per-row save AND the `inventory:buffer-updated` pulse); the just-saved row is intentionally dropped. **Review fix:** the modal now clears `#weigh-out-list` on open so an abandoned-then-reopened session starts fresh (the snapshot must not resurrect stale un-submitted text into a perceived-fresh weigh-out — a weight-write-surface footgun). Guarded by `test_weigh_out_preserve_text_e2e.py` (sibling-preserve + close→reopen-discards).
  * ✅ **31.2 (N3) — Setting remaining→0 in the wizard now auto-archives/unassigns.** Root cause: an inherit-weight spool (`initial_weight=null`, net inherited from its filament) defeated `_auto_archive_on_empty`/`_auto_unarchive_on_refill` — they read the spool's own null initial and bailed. Both helpers now fall back to `filament.weight` (the effective initial Spoolman itself uses for `remaining_weight`), fixing wizard + quick-weigh + weigh-out uniformly. Per Derek's steer, archive stays a **save-time server-side** consequence (never a live-UI toggle) and the symmetric unarchive-on-refill fix is the recovery net so a mistyped weight can't lock a spool archived. Pinned by +9 cases in `test_archive_unarchive_helpers.py` (inherit-weight archive/unarchive + `update_spool` wiring).
  * ✅ **31.1 (N2) — Wizard "⚖️ Weigh from scale…" launcher.** A button atop the wizard weight grid (`modals_wizard.html`) opens the shared `<WeightEntry>` overlay (`inv_wizard.js:wizardOpenWeightEntry`): type one gross reading → the empty-spool tare is auto-deducted into remaining/used with a live preview, written back to the grid — no more hunting across six fields. Derek's exact case (gross + tare, **no known original net** — the reason we dropped hard-coded 1000g) is handled by a guarded `allowUnknownInitial` extension to `computeUsedWeight`/`<WeightEntry>`: gross − tare = available, recorded as a full spool (`derived_initial`=available, used=0). `openQuickWeigh` never passes the flag → byte-for-byte unaffected. **Review fixes:** no "Skip → save as Used" when net is unknown (a gross reading can't be reinterpreted as net without over-booking the tare); vendor context read from the visible search field (the hidden `-sel` has no `selectedIndex`); mode-aware clamp warning. Pinned by +6 `test_compute_used_weight.py` cases + `test_wizard_weigh_launcher_e2e.py` (known-net, unknown-net, missing-tare-no-skip, vendor-context).

* **[2026-07-05 — 🎨 Group 30 Color Display & Printer-Status Polish COMPLETE → on `feature/group-30-color-display-printer-status` (merged to `dev`)]** — Four small frontend-only polish items. Grounded via a 2-agent subsystem map (color hex/RGB + printer-status widget) → built → 4-lens adversarial diff-review (correctness / regression / dark-theme / completeness → per-finding verify) → the ONE confirmed finding fixed → **29 live behavioral checks + 28 E2E** (visual baseline / printer-status widget / contrast-guard) all green, 0 regressions. Backend untouched (material was already in the pulse payload). Full spec: [docs/agent_docs/tasks/30-color-display-printer-status-polish.md](docs/agent_docs/tasks/30-color-display-printer-status-polish.md).

  * ✅ **30.1 — RGB alongside the hex near color swatches.** Both the spool AND filament details modals now render the RGB triplet (`rgb(r, g, b)`) under the hex (`#detail-rgb` / `#fil-detail-rgb`), via a new shared `window.rgbText()`.
  * ✅ **30.2 — search-box hex live-sync.** Typing/pasting a hex into the global-search color-location field now syncs the `<input type=color>` picker swatch + a new RGB readout span (`#global-search-color-rgb`); handles 3-digit shorthand + '#'-optional, hides the readout when empty (no flex-gap shift), and never clobbers the picker on an invalid partial. **Review-caught contrast fix:** the readout used `text-secondary` but the search offcanvas root is `text-bg-dark`, which the dark-contrast guard (global.css) didn't cover — extended the guard to `text-bg-dark` so the readout renders at #aaa (~6.6:1) like the modal readout (verified computed color).
  * ✅ **30.3 — printer-status panel breathing room.** The expanded toolhead panel (`.fcc-ps-th`) gained 5px bottom padding so the lower color/material text isn't clipped by `overflow:hidden` — small, not a resize (Derek's constraint).
  * ✅ **30.4 — material on the expanded panel.** The expanded printer-status chip now shows the loaded filament's material (e.g. "PETG") on a compact second line (`.fcc-ps-chip-mat`, `ui_builder.js`), sourced from the existing `item.details.material` (no backend change); added `material` to the widget repaint fingerprint (`inv_printer_status.js`) so a material-only change repaints. Renders nothing when material is absent.
  * ✅ **Shared color helper (consolidation).** ONE client-side hex path: new `normalizeHex()` (trim / strip# / lowercase / 3-digit-expand / 6-digit-validate → `#rrggbb` or null) in `inv_core.js`, and hardened `hexToRgb` to route through it (it previously mis-parsed 3-digit shorthand + returned NaN channels for garbage). `hexToRgb` / `normalizeHex` / `rgbText` exposed on `window`; the pre-existing print-label consumer (`inv_cmd.js`) is unchanged.

* **[2026-07-05 — 🟡 Group 29 L316 Characterization Yellows (Attrs Manager + Bindings + State/Pulse) COMPLETE → on `feature/group-29-l316-charact-yellows` (merged to `dev`)]** — The FINAL L316 characterization tier: 10 actionable fixes + 4 notes (findings 36–39, 42–45, 47, 48, 50, plus notes 26/28/29), across `routes_config_attrs.py`, `routes_bindings.py`, `routes_state_pulse.py`, `routes_scan.py`, and `print_deduct.py`. Each fix flips its `tests/test_l316_charact_*.py` pin to the CORRECTED behavior in the same change (the pin-in-same-commit contract); several new pins were added. Three low-stakes decisions were made with Derek (marked below). **Verified 0 regressions:** 117 L316-charact pins + 95 + 90 + 36 adjacent/E2E suites = **338 passed / 0 failed** (1 environmental skip); the E2E batches ran against the live dev container, so the fixes are confirmed end-to-end (incl. the live `test_filament_attributes_bulk_api` report-shape test that finding 36 targeted). A 4-lens adversarial diff-review (correctness / pin-fidelity / consumer-compat / conventions → per-finding verify; 114 tool calls / 417K tokens) returned ZERO confirmed findings. **With Groups 27 + 28, all 50 L316 characterization findings are now resolved.** Full per-item spec: [docs/agent_docs/tasks/29-l316-charact-yellows.md](docs/agent_docs/tasks/29-l316-charact-yellows.md).

  * ✅ **Cluster A — Filament-attributes manager (`routes_config_attrs.py`), 4 fixes.** (36) `report` `counts` restricted to the schema choice list — a rogue attribute a record still carries is surfaced in a new additive `rogue_counts` map instead of silently inflating `counts` (keeps the live report-shape SUBSET invariant); (37) **`remove_choice` docstring corrected to match the shipped code order** (snapshot→DELETE→recreate→strip-during-restore — the earlier strip-BEFORE-delete claim was never what shipped; _decision: code is intended, docstring aligned_); (38) **`bulk_set` `success = updated>0 or unchanged>0`** so an all-errored call reports `success:false` (partial success ≥1 processed stays true; _decision_); (39) `remove_choice` + `sweep_unused` restore-loop catch broadened `RequestException`→`Exception`, so a non-request error mid-restore lands in `restore_failures` instead of escaping as a bare 500 over a half-migrated schema. Pins: `test_report_counts_restricted_to_choices_rogue_surfaced_separately`, `test_bulk_set_all_errored_reports_success_false`, `test_remove_choice_restore_non_requestexception_collected`, `test_sweep_restore_non_requestexception_collected` (+ the happy-path/order pins updated).
  * ✅ **Cluster B — Bindings / quickswap (`routes_bindings.py`), 3 fixes + 1 note.** (42) quickswap-return fan-out now NATURAL/numeric sort (`XL-2` before `XL-10`) via a new `_natural_key` — fixes both probe order and which loaded toolhead the return acts on for the 10+-toolhead indxx future; (43) printer_creds PUT only logs "Printer connection updated" on an ACTUAL change (no no-op log noise); (44) **`return_no_binding` response taxonomy made consistent** — `toolhead` now uniformly means the REQUESTED value in every error branch, with the resolved one reported separately as `active_toolhead` (`requested` kept as a back-compat alias; _decision: the frontend reads none of these fields, so pure internal consistency_); (45/N1) the dead `cfg = config_loader.load_config()` in `quickswap_return` (vestigial FilaBridge residue) was REMOVED — no longer part of the vestigial-FB-artifacts cleanup item. Pins: the return-taxonomy + printer-creds pins in `test_l316_charact_bindings_errors.py`.
  * ✅ **Cluster C — State/pulse + misc (`routes_state_pulse.py`, `routes_scan.py`, `print_deduct.py`), 3 fixes + 3 notes.** (50) `/api/state/buffer` + `/api/state/queue` POST now 400 on a non-list payload (mirrors `/api/spools/refresh`) — one malformed client write can no longer poison the buffer/queue served to every dashboard (the frontend always posts arrays via `persistBuffer`/`persistQueue`, so no consumer break); (26) `api_update_filament` generic-Exception branch → HTTP 500 (was a bare 200); (47) multi-spool picker display built conditionally so legitimate content hyphens (`-EOL-`) survive (was `.strip(' -')`); (48/N2) `backfill_spool_weights` skips a malformed spool with no `id` (no more `PATCH /spool/None`); (29/N4) the 23.6 `product_url` idempotency compare is now trailing-slash tolerant (`rstrip('/')`), so a QR-form stored URL no longer fires one spurious self-healing upgrade write; (28/N3) doc-note — CLAUDE.md's Group-23.4 section ALREADY cited `test_l316_charact_filament_edit_log.py` for the `(cleared)` render, so NO change was needed (verified). Pins: `test_state_buffer_post_non_list_value_rejected_400`, `test_state_queue_post_non_list_value_rejected_400`, `test_update_filament_generic_exception_returns_500_with_str`, `test_multi_spool_display_preserves_content_hyphens`, `test_backfill_malformed_spool_no_id_skipped`, `test_prusament_product_url_upgrade_skips_when_stored_has_trailing_slash`.

* **[2026-07-05 — 🟠 Group 28 L316 Characterization Oranges (Labels/CSV + Deletes/Wizard + Scan/Queue) COMPLETE → on `feature/group-28-l316-charact-oranges` (merged to `dev`+`main`)]** — All 25 🟠 findings surfaced by the L316 app.py-modularization characterization layer (findings 1–7, 9–17, 19, 20, 23, 24, 30–33, 35), across three module clusters. Each fix flips its `tests/test_l316_charact_*.py` pin to assert the CORRECTED behavior in the same change (the pin-test-in-same-commit contract; several pins renamed to reflect the fixed contract). Four behavior forks were decided with Derek — all the safe/honest option (see per-cluster below). **Verified 0 regressions:** 285 L316-charact + route-table pins, plus the label / print-queue / audit-scan / manage-contents / wizard / delete / weight-log / delete-sentinel suites and 17 add_choice/delete E2E happy-paths (the status-code changes don't break the frontend flows); the only sweep failure was a Playwright timing flake (`test_quickswap_button_disabled_when_slot_empty`) that passes in isolation. Full per-item spec: [docs/agent_docs/tasks/28-l316-charact-oranges.md](docs/agent_docs/tasks/28-l316-charact-oranges.md).

  * ✅ **Cluster A — Labels + CSV (`labels_csv.py`), 12 fixes.** (1) `hex_to_rgb` strips `#` BEFORE the length guard (`'#AABBC'` rejects); (2) `get_smart_type` conditional-joins so an empty material leaves no trailing space; (3) `get_color_name` quote-strips the name fallback; (4) `get_best_hex` skips empty comma-segments; (5) `sanitize_label_text` matches each emoji in both bare + emoji-presentation (`+U+FE0F`) form (bare ⚠ maps, ⚡️ leaves no stray VS16); (6) `flatten_json` scalar → `'value'` header; (7) **`flatten_json` no-silent-loss** — nested empty containers preserved as an empty column, mangled-key collisions disambiguated with a `__N` suffix (_Derek's fork_); (9) batch location-mode uses case-insensitive `LocationID` key access; (10) swatch mode runs `sanitize_label_text` on Brand/Color/Type; (11) a literal 0 bed/extruder temp renders `'0°C'`; (12) the single location-label writes the row's STORED casing + QR (round-trips); (13) `'Overwritten 1 item.'` singular grammar.
  * ✅ **Cluster B — Deletes & wizard (`routes_locations.py` / `routes_inventory.py`), 6 fixes.** (14) DELETE `/api/locations` blank id → 400 + msg; (15) **`api_delete_filament` stops on the FIRST child-spool failure** (abort means abort — leaves a coherent partial state; _Derek's fork_); (16) `api_delete_spool` existence guard → 404 (fail-closed, no blind delete on a blip); (17) `api_create_filament` surfaces `LAST_SPOOLMAN_ERROR` (matches `api_create_vendor`); (19) **`api_edit_spool_wizard` reports partial persist honestly** — a filament rejection after a committed spool write returns `spool_saved:true` + "Spool saved, but filament update rejected: …" (_Derek's fork_); (20) `add_choice` missing-field → 400.
  * ✅ **Cluster C — Scan / queue-flags (`routes_scan.py` / `routes_print_queue.py`), 7 fixes.** (23) unknown `manage_contents` action → accurate "Unknown action: X" msg + dead terminal return dropped; (24) an active-audit scan's error status is propagated to the scanner UI as `{type:'error'}` (the frontend toasts it) instead of a bare `cmd:clear`; (30/31/33) **`set_flag` harmonized to `mark_printed`** — missing-id guard, int-coerce + legacy-reject, and `get_json(silent=True)` so bad JSON/wrong content-type return the JSON error contract (200) not framework 400/415 (_Derek's fork_); (32) `set_flag` fall-throughs (not-found / unknown-type) carry a msg; (35) `id=0` is a real id, not "missing" (both endpoints, `item_id in (None, '')`).

* **[2026-07-05 — 🔴 Group 27 L316 Characterization Priority Reds COMPLETE → on `feature/group-27-l316-charact-reds` (merged to `dev`)]** — All 10 🔴 real-bug reds surfaced by the L316 app.py-modularization characterization layer (findings 18/40/34/41/25/22/46/27/49/21). Each fix flips its `tests/test_l316_charact_*.py` pin to assert the CORRECTED behavior in the same change (the pin-test-in-same-commit contract), so a reviewer confirms "each red became a green that asserts the FIX, not the bug." Grounded against the live carve modules (the findings cite pre-carve `app.py` line numbers; the code now lives in the L316 flat modules). Two UX forks (27.5 / 27.6) were decided with Derek (both the safe, non-destructive option). **562 backend tests pass** across every touched module — all 12 charact files + the deduct/cancel/completion cluster, wizard-clobber-guard, print-queue, prusament-scan, audit-session, delete-sentinel, active-print-enforcement, and `test_route_table_pin.py` (routes unchanged, `app.<name>` identity intact); 0 regressions. Full per-item spec: [docs/agent_docs/tasks/27-l316-charact-reds.md](docs/agent_docs/tasks/27-l316-charact-reds.md).

  * ✅ **27.4 (41) — `_fcc_owns_completion_deduct` mis-parse** — replaced the `bool()`-coerce with a real parser (real bools + numerics + the `"true"/"false"/"1"/"0"/"yes"/"no"/"on"/"off"` string forms; default safe False). The JSON string `"false"` now reads False — pre-fix it was truthy and silently *enabled* the completion deduct. `print_monitor.py`. Pin: `test_completion_flag_parses_bool_string_and_numeric_forms`.
  * ✅ **27.2 (40) — boot creds seed could kill launch** — wrapped the inner `locations_db.seed_printer_credentials` call (try/except→WARNING, return) so a raising seed degrades gracefully and the cancel monitor still starts; belt-and-suspenders wrap at the `__main__` call site too. The docstring's "never blocks startup" now holds. `print_monitor.py` + `app.py`. Pin: `test_seed_inner_seed_exception_is_swallowed`.
  * ✅ **27.1 (18) — wizard slot-clobber window** — `api_edit_spool_wizard` now FAILS CLOSED on a `get_spool→None` pre-fetch blip (surfaces `LAST_SPOOLMAN_ERROR`, no write) instead of forwarding the raw payload incl. `container_slot`/`physical_source` verbatim (the April-outage class). `routes_inventory.py`. Pin: `test_wizard_get_spool_none_fails_closed_no_raw_forward`.
  * ✅ **27.3 (34) — `mark_printed` unhandled 500** — broadened the legacy-ID gate to `except (ValueError, TypeError)` so a JSON-array id returns the JSON error contract at HTTP 200, not a framework 500. `routes_print_queue.py`. Pin: `test_mark_printed_non_intable_id_type_returns_json_error`.
  * ✅ **27.10 (21) — unknown scan id unrenderable** — `identify_scan` now returns `{"type":"error","msg":"… not found"}` (+ a WARNING Activity-Log line) on a `get_spool`/`get_filament` → None, for BOTH the spool and filament branches — the frontend already toasts `res.type==='error'`. Was falling through to the bare, unrenderable resolver dict. `routes_scan.py`. Pins: `test_spool_scan_unknown_spool_returns_error_payload` + new `test_filament_scan_unknown_filament_returns_error_payload`.
  * ✅ **27.9 (49) — pulse `status` section vanished on error** — when the shared `_pulse_section_logs` health check raises, the derived `status` section now carries `{"error": …}` (per the endpoint docstring's per-section-isolation promise) so the nav-bar spoolman/audit/undo dot gets a signal, instead of being silently omitted. `routes_state_pulse.py`. Pin: `test_pulse_status_section_carries_error_when_logs_helper_raises`.
  * ✅ **27.8 (27) — committed write reported as failure** — moved the `_format_filament_edit_log` + `add_log_entry` out of the success-determining try into its own guarded block AFTER the write, so a formatter/logger crash logs the failure but still returns `success:true` for a COMMITTED write (was making the user retry → double-write). `routes_scan.py`. Pin: `test_update_filament_committed_write_survives_raising_logger`.
  * ✅ **27.7 (46) — one bad spool poisoned the whole response** — `api_get_multi_spool_filaments` now guards each spool in a per-spool try/except (+ null-safe filament/vendor reads), so one malformed record (missing `id`, `vendor: null`) is skipped + logged and the valid candidates still return, instead of the blanket except collapsing everything to `[]`. `print_deduct.py`. Pin: `test_multi_spool_malformed_spool_is_skipped_valid_candidates_survive`.
  * ✅ **27.5 (25) — CMD:AUDIT wiped an active audit** — _Derek's call: no-op + info line._ Re-scanning CMD:AUDIT mid-audit is now a no-op that preserves in-progress scanned/expected/rogue state, refreshes the idle watchdog, and logs "🕵️‍♀️ Audit already in progress — scan a Location to continue, or CMD:CANCEL to end." (the active-session check now sits ABOVE the reset). The user ends an audit explicitly via CMD:CANCEL/CMD:DONE. `routes_scan.py`. Pin: `test_cmd_audit_during_active_session_is_noop_preserves_state`.
  * ✅ **27.6 (22) — clear_location silently skipped slotted spools** — _Derek's call: keep slotted, warn + report (a slotted spool is loaded into a toolhead/MMU — auto-ejecting it on a bulk clear would unassign a live feed)._ Still ejects unslotted spools, but now surfaces the slotted survivors in the response (`ejected[]` / `skipped_slotted[]` / `msg`) + a WARNING Activity-Log line naming them; the "Nuke all unslotted" button (`inv_loc_mgr.js`) toasts the honest survivor result instead of always "Cleared!". `routes_scan.py` + `inv_loc_mgr.js`. Pin: `test_clear_location_ejects_unslotted_and_surfaces_slotted_survivors`.

* **[2026-07-03 — 🧪 Group 26 Test-Infra Debt Cleanup COMPLETE → on `feature/group-26-test-infra-debt` (merged to `dev`)]** — All 21 pre-existing reds + both intermittent flakes cleared, plus 7 net-new coverage tests. Test-only (assertions, fixtures, baselines, poll-not-sleep, coverage) — NO product code. A full `RUN_INTEGRATION=1` sweep went from the Group-24 baseline (21 failed / 1759 passed) to **2102 passed / 3 failed / 11 skipped**; all 3 remaining failures are pre-existing load-sensitive *environment* flakes (a Windows `os.replace` race + 2 integration read-timeouts) that PASS in isolation — filed as a new open `## 🧪 Testing` row, NOT part of this group. Commits `8f1feab` (fixes) + `2c48b24` (status) + `16b5b39` (new-flake row) + the archive commit. Full per-item detail: [docs/agent_docs/tasks/26-test-infra-debt-cleanup.md](docs/agent_docs/tasks/26-test-infra-debt-cleanup.md).

  * ✅ **21-red cleanup (6 buckets)** — **26.1** repointed the buffer-card source-canary at `_renderSpoolsRefreshPayload` (L206 moved the diff/mutation loop out of `liveRefreshBuffer`); **26.2** rewrote `test_universal_fallback` to drop the retired FilaBridge `_fb_write` (`requests.post`) assertions while keeping the real invariants (physical_source recording, eject-before-remap, unassign) + a conftest UTF-8 stream reconfigure that kills the cp1252 emoji-logging tracebacks on Windows; **26.3** added shared `bound_loaded_slot` / `real_toolhead` fixtures (dev boxes are bound-but-empty, so `.fcc-qs-slot` stayed disabled + the overlays never mounted) + probe-aware timeouts (the confirm overlay awaits a ~3s active-print probe before it mounts) + a non-destructive route stub on confirm-yes + a bounded-card confirm-overlay visual snapshot (not the live-data full-viewport backdrop); **26.4** recaptured the drifted shortcuts-overlay baseline (eyeballed — benign growth from new FAB/log-pill/CMD:CANCEL shortcuts); **26.5** cleared modal/backdrop/focus pollution + waited for `state.allLocations` before the 3-level escape-walk (passed in isolation, failed mid-sweep); **26.6** flipped to the 23.4 delete-sentinel assertion (`__FCC_DELETE_EXTRA__`).
  * ✅ **26.7 / 26.8 — the two intermittent flakes** — `_force_close_wizard` now survives Bootstrap swallowing `hide()` mid-transition (retry loop); the doassign tests stamp `lastLocalBufferChange` so the 2 s `loadBuffer` poll's "local change wins" grace window can't wipe the injected buffer. Both hammered 3× clean.
  * ✅ **26.9 — Group-23 FE coverage backfill** — 7 new tests pinning behaviors that were code-read-only: import-apply `color_hex` (3-digit expand + color-picker sync) + bad-hex skip-toast + `product_url`; editfil import panel reset on reopen; wizard `📄 product` chip; canonical Prusament `product_url` upgrade + idempotent skip.

* **[2026-07-01 — 🧙 Group 25 Wizard UX: Print/Queue, Tab-Confirm & Spool-Label Parity COMPLETE → on `feature/group-25-wizard-ux-print-tab` (merged to `dev`)]** — All 3 items, pure-frontend `inv_wizard.js`. Grounded (3-agent map) → built → 11-agent adversarial diff-review (4 dimensions → per-finding verify): 6 findings / 1 confirmed, fixed before commit; 5 refuted (25.3 + 25.H verified sound). 13 new tests (`test_wizard_group25.py`) + 34 green across combobox/wizard/queue regression suites, 0 regressions.

  * ✅ **25.I — Tab confirms the highlighted combobox item** — added a `Tab` branch to the shared `wizardBindCombobox` keydown (covers BOTH the location and vendor comboboxes), mirroring Enter but WITHOUT `preventDefault` so focus advances ("confirm and move on"). **Review-caught bug fixed:** the naive branch fired a bubbling `change` on a pass-through Tab, which wrote the `-- Generic --`/`-- Unassigned --` sentinel into an untouched box AND spuriously flipped `wizardState.isDirty` (false "Unsaved Changes" prompt on cancel). Fix: confirm ONLY when an item is highlighted or a query was typed, and dispatch `change` only when the value actually changed (same guard `wizardComboboxSet` uses); applied to the Enter branch too for consistency. Pinned by 2 pass-through-safety regression tests.
  * ✅ **25.3 — spool-label flag parity (`original_color`)** — extracted `window.wizardBuildFilamentLabelDiff(orig, fPayload)` from the inline edit-spool submit diff and ADDED the missing `original_color` comparison (previously only `filament_attributes` was in the wizard's `changedExtras`, so a manufacturer Color-NAME change via the wizard didn't flag spools for reprint while the primary Edit-Filament path did). Quote-strip normalized (stored values are JSON-wrapped; form values raw), delete-sentinel/null → "", and presence-guarded on the key so an unchanged edit can't false-flag. Feeds the SAME shared `_maybePromptLabelReprint` → true parity; no backend change.
  * ✅ **25.H — print/queue affordances** — extracted `window.wizardRenderPostSaveQueueActions(mode, created, editSid)` from the inline post-save chip block. CREATE mode renders one chip per created spool + a **"🖨️ Queue All"** button when >1 (an 8-spool round used to need 8 individual clicks; dedups via `window.labelQueue` + reports the count); EDIT mode now renders a chip for the edited spool (it previously rendered none). The modal stays open post-save in all modes, so both are visible.

* **[2026-07-01 — ⚖️ Group 24 Weight Transparency & 1000g-Default Correctness COMPLETE → on `feature/group-24-weight-transparency-1000g` (merged to `dev`)]** — Both items (24.F + 24.K). Grounded against current code (5-agent parallel-reader map — which also caught the Amazon/3DFP missing-flag bug the task file hadn't), built, then a 22-agent adversarial diff-review (4 dimensions → per-finding verify) surfaced 18 findings / 4 confirmed, all applied before commit. All new/updated tests green; a full `RUN_INTEGRATION=1` sweep (2026-06-30) came back 21 failed / 1759 passed with all 21 confirmed PRE-EXISTING (zero Group-24 regressions; `locations.json` held 55→55, no wipe) → catalogued as a standalone Testing-cleanup item.

  * ✅ **24.F — manual weight adjustments log a before➔after breakdown** — new shared `_log_manual_weight_change(spool_id, pre, post)` helper (app.py) emits `✏️ Weight updated — Spool #N: [Xg ➔ Yg remaining] (used A ➔ B)[ total …]`, mirroring the auto-deduct wording. Wired into BOTH manual funnels: `api_spool_update` (weigh-out / quick-weigh) AND `api_edit_spool_wizard` (the wizard edit-spool save — the review caught this second funnel was initially missing, so the feature now covers *all* manual weight edits). Fires only when a weight field is dirty; skips silently on a missing pre-snapshot (no fabricated `0.0g` before-values). L200 Prusament correction has its own dedicated log and does NOT route through the helper. `test_manual_weight_log.py` (8 cases — both funnels + empty-pre + non-weight-edit + failed-update).
  * ✅ **24.K — audit + kill the hard-coded 1000g assumptions** — (1) **wizard create-path leak**: `extractSpoolFieldsFromTemplate` no longer bakes a defaulted 1000g into the per-spool `initial_weight`, `applyFilamentFieldsFromTemplate` leaves the weight field blank with a "Net weight unknown" prompt, and the used/remaining calc helpers abort instead of assuming 1kg when no net weight is known anywhere (inv_wizard.js). (2) **parser flag bug** (new finding): Amazon + 3DFP parsers now emit `weight_is_default` (Prusament already did) so every downstream guard works uniformly (external_parsers.py). (3) **edit-import sibling** (review finding): `computeFilamentBackfillDiff` skips the `weight` field when the scrape is defaulted, closing the same leak through the Edit-Filament import preview. (4) **printer-status bar** scales fill to the spool's own `initial_weight` (graceful fallback to 1000, 100% cap for over-measured spools); `_build_location_match` now exposes `initial_weight` and it's in the widget dedup fingerprint (spoolman_api.py / inv_printer_status.js). L200 Prusament correction untouched (used-weight-preserving model frozen); no new weight-entry UI (deferred to the unified `<WeightEntry>`); all writes route through `update_spool`. Tests: `test_external_parsers.py` (flag assertions per parser), `test_wizard_per_spool_scan_unit.py` (create-path guard + field-blanking + calc-abort + edit-import guard), `test_printer_status_widget.py::test_bar_scales_to_initial_weight`.

* **[2026-06-29 — ✏️ Group 23 Edit-Filament Import/Scrape, Product-URL & Label-Invalidation COMPLETE → on `feature/group-23-edit-filament-import-scrape` (merged to `dev`)]** — All 6 items + a label-scope follow-up. Each grounded against current code (6-agent verification), then a 17-agent adversarial diff-review (8 confirmed findings fixed before commit) + a focused review of the follow-up (clean). 185+ targeted tests green, 0 regressions; 16 new backend tests. Commits `359e97f` (6 items + review fixes) + `1766573` (spool-label follow-up).

  * ✅ **23.2 — Import-from-External link doesn't reset between uses** — the edit-filament modal reused one set of `editfilExternal*` inputs + a module-scoped `importState` across every filament, so a previous edit's typed query / picked source / multi-result list / previewed template survived into the next edit (wrong-link-to-wrong-filament risk). New `editfilExternalReset()` (clears `importState` + the query/source/results DOM) is called from `_editfilOpenModal` on every open, mirroring `wizardReset` discipline.
  * ✅ **23.1 — missing hex overwrite on apply + product_url not scraped** — root cause was a CONSUMER-side omission, not the parser: `computeFilamentBackfillDiff` already produces `color_hex` + `extra.product_url`/`extra.purchase_url`, but the `inv_details.js` import-apply maps (`FIELD_TO_INPUT_ID`/`FIELD_LABEL`) dropped them. Wired all three in: `color_hex` drives the color-picker via a synthetic `input` event (+ 3-digit-hex expand + a toast instead of silently dropping a bad hex); the two URLs read from the diff's `external_link`/`purchase_link` source. PrusamentParser already scrapes `color_hex` + both URLs — confirmed, no parser change needed.
  * ✅ **23.5 — product-link "filled" badge in the wizard metadata panel** — the `wiz-spool-metadata-panel` summary read only `wiz-spool-purchase_url` (one `🔗 link` chip). Added a parallel `📄 product` chip for `wiz-spool-product_url` (icons match the form labels).
  * ✅ **23.6 — Prusament scan persists/normalizes the scanned URL** — `_handle_prusament_url_scan`'s matched-spool branch never wrote the scanned URL to `extra.product_url`. Since the matcher already requires the stored URL to contain the scanned id/hash, this is an idempotent canonical UPGRADE (`/spool/<id>/<hash>` path form) via the existing per-spool `update_spool` block + a `_pm_norm` differ-guard (no per-scan churn); never clobbers a product-page link (the matched URL is always a spool-instance URL).
  * ✅ **23.4 — blank-a-field-to-clear (delete-sentinel), all 3 write surfaces** — root cause was BACKEND `_merge_extras_with_existing` omit-means-keep (a blanked key, omitted from the payload, was re-added from the existing record). New `DELETE_EXTRA_SENTINEL = "__FCC_DELETE_EXTRA__"` (FE `window.FCC_DELETE_EXTRA`): a key whose value is the sentinel is POPPED by the merge (never forwarded to Spoolman, never stored literally). Wired through edit-filament, wizard spool-edit (EDIT-mode-only — create still omits), and vendor edit. `compute_dirty_extras` suppresses no-op deletes (absent/blank key) so a no-change save stays a no-change save; the merge refuses to pop SYSTEM_MANAGED keys (slot-binding backstop); the filament + vendor edit-log formatters render the sentinel as `→ (cleared)`. New `test_delete_sentinel.py` (12 cases). **⚠️ NEW write-surface contract: omitting an extra key still means "keep"; send the sentinel to DELETE — any future blank-to-clear surface must follow this.**
  * ✅ **23.3 — label-invalidation reprint nudge (label-type-aware)** — the "which fields print on the label" set was read from the actual P-touch templates (`Spoolman Filament Lables.lbx` / `Spoolman Spool Lables.lbx`), not guessed. **Filament/sample label = HEAVY prompt** (`mountOverlay`, "Add to print queue") when a swatch-visible field changes (color/RGB, Name, Vendor, Material, Type), gated on the record having a printed sample/label (`needs_label_print===false` OR `sample_printed===true` — so older/imported records whose flag drifted still nudge; Derek uses the printed RGB as a slicer painting reference). **Spool labels = LIGHT flag** (Derek's call): a change to a spool-VISIBLE field (Brand/Type/Color-name — NOT hex/RGB, which isn't printed on the spool label) flags the filament's unarchived spools' `needs_label_print` via new `POST /api/filament/<id>/flag_spool_labels` (surfaces in spool details + the queue, no popup). Reused by the wizard `edit_spool` path. New `test_flag_spool_labels.py` (4 cases).

* **[2026-06-18 — 🧹 Standalone Sweep #2 (Option B) COMPLETE → on `feature/option-b-standalone-sweep-2026-06-16`]** — Four standalone buglist items + a pre-existing test-debt fix. Each grounded → implemented → 4-lens adversarial review → review-fixes applied → tested. Commits `fd5ac89` (features + tests), `ca3df70` (test-debt + buglist bookkeeping), `de85cac` (lock-message wording). The 4-lens review caught two missed comboboxes (wizard material + multi-choice) **confirmed live** + a feeds empty-list edge — all fixed before commit. Label-CSV lock fix verified by Derek on dev.

  * ✅ **Escape cancels the dropdown, not the modal** (full sweep) — custom comboboxes (input + a div dropdown) kept focus on their `<input>`, so Escape bubbled to the Bootstrap modal and dismissed the whole modal instead of just the dropdown. Added `preventDefault`+`stopPropagation` on Escape — handled AHEAD of the empty-list guard so a "No matches" list can still be escaped out of — to the feeds combo (`inv_loc_mgr`), the wizard location/material/multi-choice comboboxes (`inv_wizard`), and the bind-slot-picker (`inv_quickswap`). The two `inv_details` comboboxes were already correct (the reference pattern). Native `<select>`s are out of scope (Chromium consumes Escape on the open popup, so they don't repro). `test_escape_closes_dropdown_e2e.py` (location / material / feeds-populated / feeds-empty) + extended `test_bind_picker_escape_closes` to assert the manage modal stays open.
  * ✅ **Live printer state in the collapsed Printer Status strip** ("tie into printer status" + "collapsed view") — a compact `.fcc-ps-state-chip` now leads each printer group in the collapsed header strip (the expanded badge already existed, L56), so PRINTING / IDLE / PAUSED / OFFLINE is visible without expanding the bar. A shared `_stateMeta()` keeps the expanded badge + collapsed chip from drifting on vocabulary/colors; the printer-reported state is HTML-escaped at the source. `test_collapsed_strip_shows_printer_state`; visual baselines stayed within tolerance (no recapture).
  * ✅ **Detail-modal footer standardization** — the spool- and filament-details footers now share one canonical order: Spoolman link far-left, then a right-anchored cluster of type-specific create/nav buttons, ✏️ Edit, ➕ Queue Label, the ⚙️ more-actions menu, Close. The shared Edit · Queue Label · ⚙️ · Close cluster sits in identical positions in both. Order-only change (all button ids / onclicks / data-bs-* byte-identical → JS wiring untouched; each modal keeps its theme colors); ids added to the Close buttons. `test_detail_modal_footer_layout.py` pins the byte-offset order incl. Close.
  * ✅ **Label-CSV export robustness (P-touch CSV lock)** — root cause: a P-touch label template links each label CSV as its database source and holds the file open; the OLD overwrite write hit a `PermissionError` and surfaced only a short, mis-named ("Close Excel") 4s toast → read as a silent failure. New `_write_label_csv` writes the overwrite path atomically (temp + `fsync` + `os.replace`, leak-free fd, temp cleaned on failure → no torn CSV); a held handle now fails LOUDLY — Activity Log ERROR + a 7s toast that names the file actually held open (main labels vs `slots_to_print.csv`) and says "P-touch or another program has it open." Success also logs; fixed a latent `folder` NameError on a bare-filename `csv_path`; the queue error toast dropped its redundant `mode:` prefix. `test_label_csv_export.py`. **Empirically pinned** (ctypes share-mode probe + a container→host bind-mount probe): atomic overwrite ALWAYS fails while P-touch holds the file (you can't replace a held file, in any share mode); FCC writes `<LABEL_OUTPUT_PATH>/labels_*.csv`. Derek confirmed he keeps two P-touch files (one dev-linked, one live-linked) → no filename-mismatch problem. Verified on dev. _(One tiny residual kept OPEN in the buglist: give `api_print_location_label` the same friendly lock message.)_
  * ✅ **Test-debt — `test_bind_slot_picker` reds** (found during the sweep, A/B-confirmed pre-existing — failed identically on the clean baseline): **(1)** the single-slot-PUT backend tests mocked `config_loader.load_config` for the printer_map, but the bindings validator reads `locations_db.get_active_printer_map()` after the **L271 Phase-4** cutover (config `printer_map` is now a prime-only boot seed) — the mock was dead, so every PUT was rejected as "not registered in printer_map"; repointed the 5 mocks to `get_active_printer_map`. **(2)** the 3 live chromium tests (rebind / unbind / enter-commits) read the server only 600 ms after an async UI PUT and flaked against a slow / just-restarted dev container (a *different* chromium test failing on different runs = the flake signature, not a bug; the click→`activeIdx` wiring is correct) — replaced the fixed `wait_for_timeout` reads with a `_poll_slot_targets()` helper. 14/14 stable across 2 runs.

* **[2026-06-15 — 🧹 Standalone Cleanup Sweep (Option B) COMPLETE → merged to `dev`]** — Four standalone buglist items shipped on `feature/standalone-cleanup-sweep-2026-06-15` (`19a4a5e` impl → `cc35b72` review-fixes). Each grounded → implemented → adversarially diff-reviewed → tested; the 51-agent ultra-review (10 finder angles → verify → sweep) found only low-severity items, all fixed. 186 backend + Playwright green (incl. new undo/pill/toolhead regression tests); 0 regressions.

  * ✅ **Undo-button log readability** — the undo record now carries each spool's rich display label + source (captured where `format_spool_display` is already computed), so the Activity Log reads `↩️ Undid: moved #225 Brand PETG (Orange) from <src> -> <dst>` (the leading `#225` IS the Spoolman spool ID; multi-spool lists each, destination once; legacy/empty-moves records render their old summary without a double "moved"). Empty-Undo message now signals the move-only scope. **Weight-undo intentionally NOT wired** (a separate multi-surface feature — snapshot every weight write surface + a typed undo stack + ledger reconciliation). `test_logic_undo.py` grew.
  * ✅ **Movable Activity-Log pill** — extracted the FAB drag engine into a shared `static/js/modules/draggable_pill.js` (`window.makeDraggablePill`); `#fcc-log-pill` now drag-to-parks + persists to `localStorage:fcc.logPill.pos` (default bottom-right above the cmd-deck, diagonally opposite the FAB). The engine writes only position, never `display`, so the "N new" show/hide gate is untouched; long-press resets; Enter/Space keyboard activation; a ResizeObserver re-clamps to the real width on first show (the pill is hidden at init so `offsetWidth` is 0). CLAUDE.md `fcc.logPill.pos` preference row added; `test_log_pill_drag.py` new (FAB regression suite stays green).
  * ✅ **`TOOLHEAD_TYPES` dedup** — collapsed the duplicate `set` (@20) + `frozenset` (@547) to ONE canonical `frozenset` in the top constants block. **Pure code, no prod/data/deploy step** (byte-identical; the frozenset already won at import via last-binding-wins, and nothing normalizes a location `Type` from this constant). Guard test pins `frozenset`-ness so a re-added mutable duplicate trips. The parallel JS copies (`inv_quickswap.js:17`, the `inv_loc_mgr.js:1141` literal) left as a separate DRY item.
  * ✅ **Drop the round-trip ⏱️ perf-trace logs — FULL retire** — deleted `perf_trace.py` + `test_perf_trace.py`, unwrapped all 11 `with perf_trace.span(...)` sites across logic/app/config_loader/prusalink_api/spoolman_api, dropped the two ⏱️ Activity-Log emissions. The kept L3 probe-cache fix's outermost-call gating is preserved via a thread-local depth guard (`_smart_move_depth`) replacing the old `perf_trace.start_if_idle` gate (begin/clear once at depth 0, never on the auto-deploy recursion, balanced on every exception path). Adversarial review of the retire returned zero findings.
  * **Spun off, kept standalone (OPEN in the buglist Testing section):** a pre-existing `test_doassign_buffer_safety` E2E flake (buffer-poll-races-local-state; A/B-confirmed NOT caused by this sweep).

* **[2026-06-15 — 🐞 Group 21 New-Bug Sweep COMPLETE (card/modal + toolhead load/unload) → merged to `dev`]** — All 6 items shipped on `feature/new-bug-sweep-2026-06-12`. Each reproduced/verified against the live dev container via Playwright; 17 new/rewritten tests + 122 passed across adjacent suites, 0 regressions.

  * ✅ **21.1 — search badge covered the buffer-card weight** ("Search badge (lower right icon) Now sometimes covers the weight field of a filament in the buffer zone…"). The "lower-right search badge" was the floating `.fcc-fab-search` button (measured overlapping the bottom buffer card's weight). Derek's steer: it's his PRIMARY search affordance (used more than the navbar button), must stay always-available incl. over modals, and getting rid of it is not the answer. Reworked it into a **drag-to-park FAB** (new `fab_drag.js`): Pointer-Events drag (mouse+touch), position persisted to `localStorage:fcc.fab.pos`, viewport-clamped, default = bottom-left cmd-deck band (clear of the buffer weights AND the WEIGH QR), tap=open / drag=move / long-press=reset. Added `/` + `Ctrl/Cmd+K` search shortcuts (none existed). Rewrote `test_fab_no_overlap_with_cmd_deck.py` → 8 tests; visual baseline within tolerance.
  * ✅ **21.2 — eject-from-XL-dryerbox-slot didn't reload QR codes** ("…Causes the attached filament to refresh, but sometimes not reload the QR codes…"). Intermittent dropped-rAF-QR-pass race (could NOT deterministically reproduce in 10+ eject cycles). Added a self-healing QR sweep on `_renderManagePayload`'s anti-wiggle early-return (`inv_loc_mgr.js`): re-fills only EMPTY `qr-pick/print/trash` placeholders from the card's `data-spool-id`, so a dropped pass recovers within one dashboard pulse instead of staying blank. Low-risk; no change to the move/data path.
  * ✅ **21.3 — manual assign to a toolhead should auto-eject the resident** ("…not leave 2 filaments attached to the same toolhead…"). `perform_smart_move`'s single-occupancy `is_toolhead` check (`logic.py`) omitted `'No MMU Direct Load'` (the Core One direct-load type), so a manual assign to such a head — when not also in `printer_map` — skipped the resident eject. Now sourced from canonical `locations_db.TOOLHEAD_TYPES` (∪ `'Printer'`) so it can't drift again. 4 new regression tests (`test_toolhead_resident_eject_21_3.py`).
  * ✅ **21.4 — queue-label closed the spool-details modal** ("…shouldn't cancel out the modal, it should stay open."). Dropped the `modals.spoolModal.hide()` from the `btn-print-action` handler (`scripts.html`); the toast confirms. Sibling of Group 17.2.
  * ✅ **21.5 — double-click a force-location entry to assign** ("Double clicking on a force location overide entry should just assigne that entry…"). Added a `dblclick` handler in `promptEditLocation` (`inv_details.js`) that selects the row + `Swal.clickConfirm()` (reuses the existing commit path); single-click+Force and keyboard nav unchanged.
  * ✅ **21.6 — buffer spools return after scan-into-slot** ("Sometimes filaments… after being scanned into a slot, return back into the buffer. (…two PCs?)"). Deterministically reproduced: a stale `/api/state/buffer` heartbeat (lost/slow persist OR a 2nd client's clobber) resurrected a just-assigned spool once the 3s grace window lapsed. Added a "recently-assigned-out" guard (`inv_cmd.js`): `loadBuffer`'s overwrite filters out spools the client just assigned out + re-asserts the corrected buffer; genuine re-buffering clears the mark. 3 new tests. Makes the assigning client robust regardless of cause; full bidirectional multi-client sync remains L302 (ON HOLD).
  * **Spun off (kept standalone in the buglist):** the `TOOLHEAD_TYPES` double-definition code smell in `locations_db.py` (set @20 + frozenset @547), surfaced while fixing 21.3 — filed for a future `locations_db`/toolhead-typing touch.

* **[2026-06-13 — 🌉 FilaBridge Phase-2 cutover COMPLETE: FilaBridge fully removed from the codebase + decommissioned on prod]** — The FilaBridge absorption epic is DONE end-to-end (dev `67d4477` → main release `92e4d74`; prod on `92e4d749`, cutover executed + verified on the TrueNAS share). FCC is now the single source of truth for the toolhead↔spool map AND owns BOTH cancel and FINISHED-completion deducts; the FilaBridge container is stopped/decommissioned (its `needo37` image kept pinned as a do-nothing fallback). Spanned multiple chats.

  * ✅ **Phase A — credential gate** (`43bb56d`+`a26468c`+`d7e9435`): printer IP/API-key relocated OFF FilaBridge `/printers` onto each first-class `Type:"Printer"` row (`printer_creds` in locations.json), read via `locations_db.get_printer_credentials`; `prusalink_api.fetch_printer_credentials` body rewritten to the local store (signature kept, `filabridge_url` ignored); boot-time prime-only auto-seed from FilaBridge (while still up) + a "🔌 Printer Connections" Settings editor; `/api/locations` redacts the api_key. (= the L271 Phase-4 credential-relocation gate.)
  * ✅ **Phase B — bgcode-aware recovery parser** (`da58e89`): `download_gcode_and_parse_usage` routed through the `.bgcode` decoder (the recovery path previously read zero on the all-binary fleet). Later retired with the rest of the FB surface in Phase E.
  * ✅ **Phase C — FCC owns FINISHED completions** (`c1b0f5e`, shipped DARK behind the `fcc_owns_completion_deduct` flag): a completion = the same per-tool compute at the full slicer footer; `_COMPLETE_TERMINAL_STATES` kept separate from the cancel terminal set; auto-applies the footer with no review (M486 handled by neither side, confirmed); Connect-lock completions defer via `cancel_fetch_store`.
  * ✅ **Detection-layer hardening** (`54f271c`, = buglist Group 22.1 + 22.2): instant-ack log on the STOPPED edge; adaptive poll (10s busy / 30s idle); and the shared **ambiguous-IDLE review** — a latched in-progress job reaching IDLE/READY without an observed STOPPED/FINISHED (incl. ATTENTION-screen hard-reset, power-cycle, and the fast cancel→restart poll-miss) routes to the cancel-review pipeline flagged "couldn't confirm", computed at retained progress, and NEVER auto-deducts. (Group 22.3 spool-swap apportionment remains open in the buglist.)
  * ✅ **Phase E — retire the FilaBridge surface:** Slice 1 the L324 reconcile tool (`5758454`); Slice 2 the `/print-errors` error-poll + auto-recover task (`9a18499`); Slice 3 the four `/api/fb_*` endpoints + recovery modal + `filabridge_error` log handler + snapshot store + orphaned `download_gcode_and_parse_usage`/`acknowledge_filabridge_error` (`c4caca8`, −868 lines); Slice 4 the status dot + `/print-errors` liveness probe + `FILABRIDGE_URL` wiring (`cb7d88a`). Moved the unrelated `_prune_locations_backups` (L347) tests to their own file; preserved footer-parse coverage on the surviving completion source.
  * ✅ **Phase F — kill the map-push** (`3a86700`, −875 lines): removed `_fb_write` + `_fb_spool_location` + all ~12 call sites across the move/eject/cascade/force-unassign/undo pipeline, plus the `origin_toolhead`/`fb_outcomes`/`filabridge_*` result plumbing and the frontend `maybeWarnFilabridge`; rewrote `test_filabridge_move_ordering.py` → `test_smart_move_spoolman.py` (guards the Spoolman move behavior + asserts ZERO FilaBridge POSTs).
  * **Prod cutover (operator step, verified):** deployed with FilaBridge UP so the boot seed could prime the creds onto the Printer rows — the make-or-break gate, since current prod stored no creds (it read them live from FilaBridge); verified the seed + that PrusaLink reads resolve locally, THEN stopped FilaBridge, flipped `fcc_owns_completion_deduct=True`, bounced FCC. Confirmed on the TrueNAS share: prod git `92e4d749`, `🔐 Seeded printer credentials…` logged, both Printer rows carry creds, flag True, monitor live reaching the fleet with FilaBridge stopped. Ordering lesson: the seed needs FilaBridge UP at deploy — do NOT stop it first.
  * **Open follow-ups remain STANDALONE in the buglist** (deliberately not nested under this completed epic): Group 22.3 (mid-print spool-swap apportionment) + a low-priority cosmetic cleanup (orphaned `filabridge_error_snapshots.json` + dead `config.json` keys `auto_recover_filabridge_errors`/`filabridge_port`/`filabridge_ip`). Full detail in the `project_filabridge_absorption_active` memory + `docs/agent_docs/tasks/filabridge-absorption-scoping.md`.

* **[2026-06-12 — Cancelled-print partial-deduct (FilaBridge Phase 0/1) + UI-lockout hardening → `main` `69b06e9`]** — Two features merged `dev`→`main` (`--no-ff`) + pushed; deploy via the TrueNAS cron. Validated end-to-end on **real XL hardware**.

  * ✅ **Cancelled-print partial deduction** (FilaBridge absorption §9). FCC now deducts the ACTUAL partial filament on a CANCELLED print (previously unrecorded → spool drift → manual re-weigh). **Phase 0** print-edge fast-poll (also closes L25); **Phase 1 / Scope 1** repointed `_fb_spool_location` to read Spoolman `location` (FCC owns the toolhead↔spool map). Core: a pure-Python **binary-gcode (`.bgcode`) decoder** (heatshrink+MeatPack, byte-equal to the reference lib), a **per-tool gcode prefix-parse** to the M73-time-mapped cancel point (an un-engaged head = 0 natively — the XL invariant), an exactly-once `(printer, job_id)` ledger, a **dashboard-independent `_cancel_monitor` daemon**, **immediate + deferred-fetch** (the Connect selected-file download-lock) paths, and a **preview-and-confirm review UX** (nudgeable per-spool grams, archive-on-empty discipline). Hardened by cloud adversarial reviews. **Real-XL validation 2026-06-12:** multi-tool decode (`BINS-X~5`), unattended auto-detection (job 691), both fetch paths, multi-spool confirm write, a physical scale cross-check (4.72≈4.70 g), and the **M486 over-estimate** quantified (phantom = the skipped half of the part, isolated to the cancelled head) + a standing review reminder. Plus the **sum-by-sid** resolution fix (same-spool-multi-position under-deduct) and **slice 7** latch persistence/restart-recovery (resume → restore / no-resume → deduct from last pulled progress / ambiguous → manual review). ~200 cancel/bgcode/recovery tests. Cancel monitor verified to start on prod (the `(not _dev)` guard); new `data/*.json` stores are created on demand (no migration). **Epic continues:** Scope 2 / Phase-2 FilaBridge cutover + Phase-4 toolhead↔printer hard link remain (still in the buglist).

  * ✅ **UI-lockout hardening** (the search→buffer-add lockout class, L28 family). New `window.fetchT` wall-clock-timeout helper (`AbortSignal.timeout`) so a hung request can't strand the z-index:9999 processing-overlay (which gates all input); routed the overlay-setting user-action fetches through it (`processScan` / `doEject` / `manualAddSpool` / `triggerEjectAll`); added the missing `.catch` on `triggerEjectAll` (a genuine stuck-overlay bug) + a `_pickInFlight` guard / `.catch` / `.finally` on `ejectSpool(pickup=true)`. Converts “lock → hard refresh” into “toast → auto-recover.” An 8-agent investigation (adversarially verified + ground-truthed) ruled out the obvious socket-exhaustion theory and corrected a stale L122 note (`_confirmActivePrintAssign` already routes through `mountOverlay`). +5 source-canary tests; root cause not conclusively pinned, so this is the defensive class-killer.

* **[2026-06-07/08 — Buglist sweep `feature/buglist-sweep-2026-06-05`: 7 items shipped to prod]** — A multi-item autonomous sweep (toast / wizard / deck / scan / quickswap / cascade) plus a 4-agent backlog triage. Merged `dev`→`main` (`--no-ff`) and deployed via the TrueNAS update cron. All frontend (JS + one template) + tests; no backend / data-migration changes. Per-item record:

  * ✅ **Wizard filament-attribute add + toast Escape** (orig. buglist 2026-05-30). **(1) Toast Escape-to-dismiss** — `showToast` ([inv_core.js](inventory-hub/static/js/modules/inv_core.js)) gained a capture-phase Escape handler with priority **overlay > toast > modal**; a visible toast owns Escape for its ENTIRE on-screen life incl. the ~300 ms fade-out (keys off the toast node still in the DOM, not opacity), so an Escape pressed "just as the toast cleared" can't leak to a background modal (Derek's 2026-06-07 concern). Bonus latent fix: `window.showToast` was never published (bare `const`), silently no-op'ing every `if (window.showToast)` caller (Settings / Config-Reconcile / duplicate-picker) — now exposed. `test_toast_escape_dismiss_e2e.py` (5). **(2) Missing `➕` button** — the multi-choice chip widget (`filament_attributes`) never rendered the `➕` its own fuzzy-suggestion toast referenced; added it wired to a now-multi-choice-aware `wizardPromptNewChoice` that drops the chip in place (no DOM-wiping schema refetch). `test_wizard_multichoice_add_button_e2e.py` (6). **Residual:** "config filament-attributes manager is clunky" → tracked under the Config epic (L18 Phase 5).

  * ✅ **Shapeshifting QR codes (L299)** — Derek picked Option B: extracted the Audit deck slot's in-place QR "shapeshift" into a reusable `window.registerShapeshiftQR({slot,size,default,states}) → {set,reset,current}` ([inv_cmd.js](inventory-hub/static/js/modules/inv_cmd.js)) and migrated Audit onto it byte-identically (CMD:AUDIT/"AUDIT" ↔ CMD:DONE/"FINISH", panel + classes verified). Groundwork for a future multi-state deck command (Bulk Moves arm→set-dest→commit). `test_shapeshift_qr_helper_e2e.py` (2). Options A (Drop/Eject "EXIT" label) + C (defer) remain available, unbuilt. (Corrected an earlier wrong "no pattern exists" note — Audit *is* the pattern.)

  * ✅ **Empty-location scan warning** (was On-Hold "legacy barcode has no spools attached"). Scanning a location/legacy label that resolves to ZERO spools used to open the Location Manager silently; now raises an info toast + WARNING activity-log entry before opening it (mirrors the existing empty-SLOT path), scoped to the truly-empty case. `test_empty_location_scan_e2e.py` (2).

  * ✅ **Background refresh — Quick-Swap stale weights (L352)** — a triage found the one remaining static spool-data surface: the Quick-Swap grid's live weights didn't tick on the heartbeat. Added an `inventory:sync-pulse` listener calling `renderQuickSwapSection(currentLoc,{silent:true})` ([inv_quickswap.js](inventory-hub/static/js/modules/inv_quickswap.js)), hardened with a content-hash (`_lastQsHtml`, no flicker) + a focus-guard (never yanks in-progress keyboard nav; only runs while the manage modal is `.show`). `test_quickswap_pulse_refresh_e2e.py` (3).

  * ✅ **Spool product-data field (L351)** — verified the existing `product_url` / `purchase_url` spool extras are stored / scan-populated / shown / editable, then added a per-spool `📄 Product Link` input (`#wiz-spool-product_url`, [modals_wizard.html](inventory-hub/templates/components/modals_wizard.html)) in parity with Purchase Link: scan-populated, gated-on-truthy save (empty never clobbers a scan/inherited value), clone/edit fallback via `wizardApplyProductLinkFallback`, reset between sessions.

  * ✅ **Vendor→Filament→Spool cascade (L349)** — the empty-weight half was already fully wired + hardened; added the filament→spool **price** half: blank-gated `wizardPrefillSpoolPrice` inherits the filament's price only when the spool's own price is empty (NEVER overwrites existing data — Derek's explicit constraint), on New-from-Filament / Clone / Edit. `test_wizard_price_product_cascade_e2e.py` (6, covering L349 + L351).

  * ✅ **Flaky E2E fixed — load-sensitive scan-toast wait** — `test_slot_qr_scan_ui_e2e` error-toast assertions flaked when run after network-heavy files (polluter bisected to `test_confirm_qr.py`'s printer-state probes loading the shared dev server; A/B-confirmed pre-existing, NOT caused by the sweep). Bumped the four toast-visibility waits to a documented `_TOAST_WAIT_MS = 8000`. A second pre-existing flake — `test_wizard_group10_session_a.py::test_edit_wizard_cancel_from_spool_details_reopens_details` (wizard-on-details modal-close timing) — was A/B-confirmed to fail on clean dev too and is left OPEN in the buglist's Testing section with a fix direction.

* ✅ **DONE 2026-06-05** (`feature/prusament-spool-hash-matcher`) — **Prusament scan matcher now matches by spool-hash, not just product-id.** A Prusament URL is `prusament.com/spool/<product-id>/<spool-hash>`; the matcher previously keyed off the numeric **product-id** only, so scanning one spool could correct the FIRST owned duplicate of that product (proven live: dev #196/#197, both product 17705 — a spool-level op like L200 weight correction could hit the WRONG physical spool).
  _[**What shipped** — three surfaces + a bonus: **(1)** [`logic.resolve_scan`](inventory-hub/logic.py#L304) regex extended to `prusament\.com/spool/(\d+)(?:/([0-9a-z]+))?` — recognition stays anchored on the product-id `\d+` (so a malformed/absent hash never regresses the scan), the hash captured best-effort (optional, permissive, case-insensitive; `None` when absent; the greedy id group stops at `/` so an all-digit hash isn't swallowed); returns a new `spool_hash` key. **(2)** [`_handle_prusament_url_scan`](inventory-hub/app.py#L2451) matcher keys on the spool-UNIQUE hash → resolves the exact physical spool, not the first duplicate. **(3)** the `prusament_new` onboarding branch carries `spool_hash` through (the full hashed URL already lands in the new spool's `extra.product_url`, so a future rescan exact-matches it — traced end-to-end, no frontend change needed). **Bonus:** matches the unique hash in BOTH URL shapes — path `/spool/<id>/<hash>` AND query `?spoolId=<hash>` — closing a pre-existing gap for the 4 query-form spools that were never matchable by scan under the old code. **Design decision (Derek 2026-06-05):** when a hash is present but NO owned spool (incl. archived) matches it, do NOT fall back to a product-id match (that was the original wrong-sibling bug) — route to onboard (`prusament_new`); an unowned exact spool is a brand-new spool. Only a hash-LESS scan falls back to product-id granularity. This deliberately supersedes the buglist's earlier "keep product-id granularity for filament temp backfill" lean — the L198 temp-backfill still fires on an exact-hash match (i.e. when you re-scan a spool you own). **Verification:** investigation + 5-agent adversarial-review workflows (substring false-match, hashless-stored false-negative, end-to-end onboard-records-exact-spool, semantic regression, new-key safety) — 4/5 clean; the one "medium" finding (hashless-stored duplicate risk) shown THEORETICAL against real data (0 hashless-path `product_url`s in dev inventory — 12 full-hashed, 4 query-form, 0 bare). Tests: [test_prusament_scan.py](inventory-hub/tests/test_prusament_scan.py) = 32 (+7 new: hash-capture / all-digit-hash / none-when-absent parse tests, exact-hash duplicate resolution, unowned-hash-onboards, query-form match, query-form precision); 94 green across all other `resolve_scan` consumers. Filed from buglist line 22 / the SHELVED Prusament-QR data-model note.]_

* **[2026-06-05 — Prusament scan now updates EXISTING inventory: L198 (filament temps) + L200 (spool weights)]** — shipped on `feature/prusament-scan-update-existing`; L200 hardened via a 16-agent adversarial review and live-verified end-to-end. Per-item detail below.

* ✅ **DONE + VERIFIED 2026-06-05** — Scans of Prusament filament also update min/max bed + nozzle temps on existing filaments. (Original ask: "These values should stay the same, but there is some legacy data entry where this data wasn't [filled].")
  _[✅ **CLOSED 2026-06-05** — verified-already-shipped (the 2026-05-25 "open questions" below are all answered by code that landed afterward, mainly `feature/scan-match-pipeline` + the wizard/details backfill work). The temp-backfill is live across **all three** candidate surfaces: **(1) top-bar global scan** → [`_handle_prusament_url_scan`](inventory-hub/app.py#L2254) matches the existing spool by stored `product_url`, silently backfills *blank* `settings_extruder_temp`/`settings_bed_temp`/`nozzle_temp_max`/`bed_temp_max` on the parent filament (via `update_filament_or_raise` + `compute_dirty_extras` + `SYSTEM_MANAGED_EXTRAS` — CLAUDE.md-compliant), and surfaces *differing* temps through the [`promptPrusamentMatched`](inventory-hub/static/js/modules/inv_cmd.js#L337) overlay — gated to only prompt when the filament has no active spools in use; **(2) Add/Edit wizard per-spool scan** → [`computeFilamentBackfillDiff`](inventory-hub/static/js/modules/inv_wizard.js#L3632) silent-fills empties + opt-in mismatch panel; **(3) Filament Details → Import from External** → [`editfilExternalRenderPreview`](inventory-hub/static/js/modules/inv_details.js#L3301) per-field checkbox preview. **Decisions resolved by the shipped code:** (1) all three surfaces wired; (2) multi-match → duplicate picker + Tier-1 product-id auto-pick; (3) policy = fill-empties silently + conflicts opt-in (the "values should stay the same" lean); (4) surface = silent fill + diff/conflict panel. Coverage: [test_prusament_scan.py](inventory-hub/tests/test_prusament_scan.py) (11 tests, all green 2026-06-05 incl. 2 live-server overlay E2E). The remaining spool-side weight half (scan updating an existing spool's total/tare weight) is the separate L200 item.]_
  _[CODE-PATH NOTES 2026-05-25 (superseded by the closure above) — backend Prusament parser ([external_parsers.py:125-216](inventory-hub/external_parsers.py#L125-L216)) already returns `settings_extruder_temp`, `settings_bed_temp`, `extra.nozzle_temp_max`, `extra.bed_temp_max`. Match logic exists at [inv_wizard.js:findFilamentMatches](inventory-hub/static/js/modules/inv_wizard.js) and dedupes via Prusament product id. Open questions before implementing: **(1)** Which UX path triggers the update — top-bar scan, Spool details "Spool data link" QR (`inv_details.js:3057`), Edit Filament > Import-from-External, or all three? **(2)** When multiple existing filaments match the scan (same vendor + material + color), update only the lowest-id, all matches, or surface the duplicate picker? **(3)** Backfill policy — overwrite existing temps, OR only fill empties (preserve any user-set values)? Lean: fill-empties is safest (matches user's "values should stay the same" comment). **(4)** Surface mode — silent toast, or open Edit Filament with the diff pre-applied for confirmation?]_

* ✅ **DONE 2026-06-05** (`feature/prusament-scan-update-existing`) — Scanning a Prusament data-link for an EXISTING spool now corrects its weight fields. (Original ask: update total + other fields on an existing spool, but preserve the current usage amount.)
  _[✅ **IMPLEMENTED 2026-06-05** — built on the **top-bar global scan** path (Derek's pick): when a Prusament QR matches an existing spool, [`_handle_prusament_url_scan`](inventory-hub/app.py#L2254) now also computes a spool weight-field diff via the new pure helper [`_compute_prusament_spool_weight_diff`](inventory-hub/app.py) and the [`promptPrusamentMatched`](inventory-hub/static/js/modules/inv_cmd.js#L337) overlay renders a "📦 Prusament's spec differs from this spool's weights" section (Total/Tare before→after + a recomputed-Remaining line) with an explicit **Update spool weights** button. **Weight model (Derek-confirmed 2026-06-05, AskUserQuestion):** `used_weight` is the source of truth for consumption (FilaBridge auto-deduct writes it; tare-independent) → **preserved**; `initial_weight` ← scanned net, `spool_weight` (tare) ← scanned tare, both resolved against the *effective* current value (spool → filament → vendor `empty_spool_weight`, the same `resolveEmptySpoolWeight` chain). Since Spoolman derives `remaining = initial − used`, correcting the total recomputes remaining "as if the correct weight had been used from the start" without touching the print history. **Decisions resolved:** (1) surface = top-bar matched overlay; (2) tolerance = ±1 g float-noise guard (`_PM_WEIGHT_TOL`); (3) initial overwrites, remaining recomputes, used preserved (the update payload NEVER carries `used_weight`); blocked-with-warning when scanned net < used (would force Spoolman to clamp/zero remaining); (4) sibling-safe — applies via a dedicated `/api/spool/prusament_apply_weights` → `update_spool_or_raise` (surfaces `LAST_SPOOLMAN_ERROR`); confirm-gated, never silent. **Note:** the legacy/used detection is implicit — used_weight already encodes consumption, so an unused spool (used=0) just gets initial set fresh; no separate "weight ≠ card ⇒ used" branch was needed.]_
  _[🛡️ **HARDENED 2026-06-05 (adversarial review — 16-agent workflow, 12 confirmed findings, all fixed)** — the first cut applied via the generic `/api/spool/update`, which runs `_auto_archive_on_empty`/`_auto_unarchive_on_refill`: a correction that drove `remaining ≤ 0` would silently archive + unassign a loaded spool, and correcting an archived spool would resurrect+relocate it. Fixes: (a) NEW dedicated `/api/spool/prusament_apply_weights` endpoint re-validates against the LIVE spool (kills the scan→confirm TOCTOU where FilaBridge deducts in between) — refuses if archived or if the new total would empty the spool, writes only initial/tare via `update_spool_or_raise`; (b) `_compute_prusament_spool_weight_diff` now skips archived spools, blocks any total that would leave `remaining ≤ ~0` (not just `< used`), ignores the Prusament parser's fabricated 1000g default (new `weight_is_default` flag in [external_parsers.py](inventory-hub/external_parsers.py)), and resolves effective tare/initial with a `>0` "has" guard via `_pm_first_pos` so Spoolman's `spool_weight=0.0` default (152/258 dev spools) falls through to filament/vendor instead of proposing a spurious `0g→Ng` diff; (c) overlay closes only the applied section so a spool needing BOTH temp + weight fixes isn't forced into a second scan. Tests: [test_prusament_scan.py](inventory-hub/tests/test_prusament_scan.py) = 25 (all green) — adds archived-skip, default-1000-skip, 0-tare-fallthrough, would-empty block, 3 endpoint re-validation cases, and a parser-length-type regression. **Live-verified end-to-end** on a throwaway spool pointed at a real Prusament URL (PETG Ultramarine Blue, product 17637): scan → live prusament.com fetch → overlay (Total 900→1020g, Tare —→278g, Remaining 600→720g) → confirm → apply, with Spoolman recomputed correctly (used 300 **preserved**, remaining 720, spool NOT archived); plus the idempotent no-op path on a re-scan and on real spool #196. **Adjacent pre-existing bug found + fixed during the demo:** the Prusament parser emitted `prusament_length_m` as an int, 400-ing the silent metadata backfill ("Value is not a string") on every matched scan — now stringified in [external_parsers.py](inventory-hub/external_parsers.py) (it's a Spoolman *text* extra) with a regression test.]_
  _[CODE-PATH NOTES 2026-05-25 (superseded by the implementation above) — handler `inv_details.js:promptEditSlicerProfile`-adjacent or wizard `wizardScanSpoolRow` at [inv_wizard.js:2236](inventory-hub/static/js/modules/inv_wizard.js#L2236) is where the spool-side `/api/external/search?source=prusament` call already lives — for the Add path. Open questions before implementing: **(1)** Which surface — global top-bar scan, the spool-search input on the offcanvas, the Edit Spool wizard? **(2)** "Current weight ≠ data-card weight → legacy spool" tolerance — exact-match required, or ±N grams? **(3)** Initial-weight field semantics — when the scan brings a new `initial_weight` value, should it overwrite the existing initial_weight, AND should `remaining_weight` be recomputed (`new_initial - used`) or left as-is? **(4)** Sibling-wipe risk — needs to go through `compute_dirty_extras` per the CLAUDE.md write-surface conventions, NOT raw PATCH. Same pattern as the L319 sweep-unused fix from 2026-05-25.]_

* **[2026-06-05 — Feature-Buglist cleanup sweep: ~50 completed items consolidated]** — Housekeeping pass (NO code change). Moved every fully-shipped / resolved item out of `Feature-Buglist.md` into this archive so the active buglist shows only outstanding work. Items that are done-but-with-a-caveat — a ⚠️ prod-action still pending or an active watch/repro note (Core One MMU prod replication, L28 frontend-lockup repro guidance, L37 `locations.json` monitor) — were intentionally LEFT in the buglist per Derek’s call, as was the Config epic (L18). The full **verbatim** text of each removed item is preserved below, grouped by its original buglist section; most also have a narrative session summary elsewhere in this archive, so this block is the searchable per-item record. The working-groups index (`docs/agent_docs/working-groups.md`) was re-synced in the same pass (stale DONE rows pruned, `(buglist line N)` references corrected).

  **Reconciliation note — L122.** Its buglist marker still read "PARTIAL 2026-05-14" (scan-side only), but the manage-modal slot-assign side (`_confirmActivePrintAssign`) was migrated to `mountOverlay` on 2026-05-25 (commit `07b1ed4` — see the 2026-05-20→25 trio/sweep entry below). **L122 is fully DONE**; the stale PARTIAL text is preserved verbatim below for the record.

  **— From "New and Unsorted Features/Bugs":**

*(L4 — `[PRIORITY] Test-sweep flake stabilization` — DONE 2026-05-12 via Group 14. Sweep: 809 passed / 0 failed / 0 errors / 15 skipped. See `completed-archive.md`.)*

*(L6 — Details modal pops up after editing from global search — DONE 2026-05-14 via Group 10 Session A (item 10.11). `openEditWizard` and `openCloneWizard` now only set a return-id when a spool/filament details modal is actually visible at launch. See `completed-archive.md`.)*

*(L17 — `[RECURRING] Modal / overlay Z-ordering` — DONE 2026-05-11 via Group 15. `window.mountOverlay()` is now the canonical helper; 5 inline overlays migrated; symptom checklist + z-index ladder documented in CLAUDE.md "Project Conventions". See `completed-archive.md`.)*

*(✅ DONE 2026-05-30 on `feature/scan-match-pipeline` — the still-open half of the PARTIAL note below shipped: an inline ✏️ toggle on the Filament Details modal (`promptEditSampleStatus`) now edits `sample_printed` independently. Resolves OQ1 (inline toggle on the Details badge); OQ2 kept as-is — toggling to false does NOT flip needs_label_print. Regression coverage: `test_filament_sample_label_status.py::test_filament_sample_status_inline_toggle_round_trip`.)*
* Looks like scanns that confirm a filament label aren't also confirming that the sample was printed. This are to be confirmed in a pair I believe. Also, seems to be no way to edit filament sample status?
  _[PARTIAL 2026-05-25 — pairing half shipped: FIL: label-confirm scan in [app.py:1930](inventory-hub/app.py#L1930) now sets both `needs_label_print=false` AND `sample_printed=true` in a single update. The Activity Log line already advertised "Label & Sample Verified"; the code now matches. Regression coverage in `test_print_queue.py::test_identify_scan_barcode_filament` (extended assertion) + 2 new tests `_pairs_sample_when_previously_unset` and `_skips_sample_when_already_verified`. **Still open: dedicated UI to edit sample_printed independently.** The wizard already surfaces `sample_printed` as a checkbox (per `test_wizard_surfaces_label_and_sample_flags.py`) and the Filament Details modal shows the current status badge (`#fil-detail-sample-status`), but there's no inline toggle/edit affordance on the Details modal itself. Open questions: **(1)** Inline toggle on the Details modal badge, OR open Edit Filament with sample_printed pre-focused? **(2)** Should toggling sample_printed to false ALSO flip needs_label_print=true (the reverse pairing — "no sample means need to reprint")? Lean: no, those are separate concerns.]_

*(Box-bounding on Printer Status — "status keyed off attached dryerboxes, not actual print heads" — DONE 2026-06-01 on `feature/core1-status-box-bounding`. Occupancy now keys off the toolhead LOCATION (new `spoolman_api.bucket_spools_by_location`, 6→1 fetch) instead of dryer-box `slot_targets`; loaded-but-unbound toolheads render as spool + 🔗 hint. FilaBridge auto-deduct confirmed already toolhead-driven. Full write-up in `completed-archive.md`.)*

*(`_prune_locations_backups` import-time NameError — def lived ~900 lines below its module-import callers, so L347's startup prune never ran (`.bak` files accumulated). Moved the def above the callers — DONE 2026-06-01. Full write-up in `completed-archive.md`.)*

*(Dashboard Printer Status / spool-card badge clicks did nothing — L206 regression: `state.allLocations` was never primed on the bare dashboard (pulse only fetched locations when the manager table was visible) so `openManage` silently bailed. Primed at startup + refresh on locations-changed — DONE 2026-06-01. Also turned 7 mis-attributed E2E failures green. Full write-up in `completed-archive.md`.)*

* Changing a spools location from a toolhead, or a non toolhead attached dryerbox or slot, should unload the filament from the tool head on assignment to another location.
  _[→ GROUP 20.1 (PROPOSED 2026-06-03) — bundled with L206 + lines 37/43 into the Toolhead Binding / Unbind-Propagation cluster (`docs/agent_docs/tasks/20-toolhead-binding-unbind-cluster.md`). HIGH-risk eject/unbind subsystem; the FilaBridge unmap is already in place (L204) so this needs live repro to find which of the 3 drifting binding stores stays stale — NOT a blind patch. Triaged but intentionally not touched in the autonomous sweep.]_

* If a spool in a single slot drybox (PM (Polymaker), possibly others) is assigned to a toolhead, the dryerbox that the spool lives in should be assigned to the toolhead as a location, and removed when the the spool is unloaded/ejected from the toolhead. If a spool is ejected from a box, and that box is attached to a toolhead, the box should be removed from the toolhead. This should only apply to single slot boxes. This will help with the Core1's misisng box attachment notification, but the core one by designe doesn't always have a dryer box attached, and can take spools that are not in a dryerbox.
  _[→ GROUP 20.2 (PROPOSED 2026-06-03) — NEW feature in the Toolhead Binding cluster (`docs/agent_docs/tasks/20-toolhead-binding-unbind-cluster.md`). Restrict to `Max Spools <= 1` boxes; lean toward reusing `slot_targets` (no schema change) and hooking the same assign/eject points as 20.1 so the binding stays consistent. Triaged, not yet built.]_

* Need to do something about the fact that if a toolhead has multiple slots assigned to it for a dryer box, that new spool assignments don't automatically take over the current toolhead's assigned spool. **Decision: (c) Leave it to the user via Quick-Swap.** Intent is ambiguous — a new spool landing in a shared box may not mean the user wants to switch the active toolhead assignment (e.g., staging spools for later). Quick-Swap is the deliberate, explicit action for swapping. No auto-switch or auto-prompt. _[RESOLVED — no code change needed.]_

*(L32 — `PRINTER:<id>` sentinel for printer-pool slot binding — DONE 2026-05-13 via Group 9.2. All four sub-items complete; Quick-Swap grid now renders a "🏭 Printer Pool" banner row for sentinel slots with deposit-flow handoff. See `completed-archive.md`.)*

*(L37/L39 — Default-Unassigned + location selector "Flow" cleanup — DONE 2026-05-14 via Group 10 Session A (items 10.3 + 10.2). Wizard combobox now shows full list on focus, highlights current selection, and placeholder advertises Unassigned default. See `completed-archive.md`.)*

*(L40 — Legacy QR code scans triggering the help overlay — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `shortcuts_registry.js` capture-phase `?` handler now detects an in-flight scan (non-empty `state.scanBuffer` + `state.scanStartTime` within 500ms) and lets `?` flow through to the scan accumulator instead of popping the overlay. Regression coverage in `test_quickswap_ui_e2e.py::test_shortcuts_overlay_question_mark_mid_scan_does_not_trigger`.)*

*(L42 — Version number stale — DONE 2026-05-14, ENHANCED 2026-05-15 via `feature/buglist-sweep-2026-05-14`. Round 1: replaced hardcoded `VERSION = "v154.26..."` constant with a source-file mtime stamp. Round 2: added git commit resolution via `inventory-hub/_gen_build_info.py` — a standalone helper that walks up for `.git`, parses `HEAD` + `logs/HEAD` directly (no git binary required), and writes `inventory-hub/.build_info` with the format `<short_sha>|<unix_ts>`. `app.py` runs the helper on startup AND reads the file; falls back to mtime if .git is unreachable. Frontend now renders `commit <sha> • YYYY-MM-DD HH:MM` in user-local time, with mtime fallback. `.build_info` is gitignored — for auto-update per commit install the post-commit hook via `bash setup-and-rebuild/install_build_info_hook.sh` (per-clone, idempotent); for prod, run `python inventory-hub/_gen_build_info.py` before `docker build` so the file gets baked into the image. Regression coverage in `test_build_version_badge.py` (3 tests — accepted-format match, legacy `v154.x` literal gone, commit-attrs present when .git reachable).)*

*(L122 — Active-print confirm overlay blocked/cancelled/hidden — PARTIAL 2026-05-14 via `feature/buglist-sweep-2026-05-14`. The scan-side `_confirmActivePrintScan` in `inv_cmd.js` was rolled by hand (createElement + appendChild + document keydown) and could end up blocked behind a sibling modal — the exact symptom Derek reported. Migrated to `window.mountOverlay({ tier: 'confirm' })` so it inherits the canonical 20100 z-index, focus-guard, and Escape-via-onEscape discipline. Regression coverage in `test_active_print_scan_overlay.py` (2 tests — mount + z-index assertion, Escape cancels). Existing `test_doassign_buffer_safety.py` active-print suite (7 tests for the assign-side overlay in `inv_loc_mgr.js`) still green — that overlay still uses the hand-rolled pattern but is exercised through a different code path; migrating it is a natural follow-up but no symptom yet maps to it specifically. _[NEEDS REPRO — Derek should confirm the next time the symptom occurs whether it was on the scan path (now fixed) or the manage-modal slot-assign path (still hand-rolled). If the latter, the same mountOverlay migration applies to `_confirmActivePrintAssign` in inv_loc_mgr.js:~1110.]_ _[UNCOUPLED FROM L28 2026-05-19 — L28's 2026-05-18 repro was diagnosed as a polling-flood / socket-buffer issue, not an active-print overlay path. L122 stays open against the manage-modal slot-assign path (`_confirmActivePrintAssign`); waiting on a clear repro that specifically hits that code path before migrating, since the scan-side migration was the only symptom Derek had concrete evidence for.]_)*

*(L124 — Toolhead scan assigns ALL buffer spools — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `processScan` in `inv_cmd.js:251` now inspects the scanned location's `Type` / `Max Spools` against `state.allLocations`. For single-occupancy targets (Tool Head / MMU Slot / No MMU Direct Load / `Max Spools <= 1`), only the topmost held spool is passed to `performContextAssign`; the rest stay in the buffer. Toast surfaces the partial outcome ("Toolhead holds 1 spool — assigning #N; M stays in buffer"). `performContextAssign` now accepts an explicit `spoolIdsOverride` subset and only drops the actually-moved spools from `state.heldSpools` on success. Multi-spool destinations (Dryer Box, Shelf, etc.) retain the original bulk-assign behavior. Regression coverage in `test_toolhead_scan_single_spool.py` — one test asserts the toolhead-single-spool branch, one asserts the multi-spool dryer-box branch still sends the full buffer.)*

*(L130 — Location search box should show all locations after selection — DONE 2026-05-14 via Group 10 Session A (item 10.10). Same `wizardBindCombobox` fix as 10.2; mirror behavior added to `promptEditLocation` Force-Location dialog. See `completed-archive.md`.)*

*(L128 — "Already verified" Activity Log spam — REVERTED 2026-05-15 via `feature/buglist-sweep-2026-05-14`. The 2026-05-14 first iteration replaced the log line with a 1.5s toast on every scan; Derek's verdict was "the toast is probably worse than just adding to activity log" — the toast is more visually disruptive than a one-line append, AND blocks reading of any prior toast. Activity Log entry is back; the `label_already_verified` response field is preserved for future surfaces that want the signal, but the frontend no longer toasts. Better mitigation for blind-scan volume is the log-pause indicator (L286 — click to pause, no accidental hover-pauses anymore).)*

*(L130 — Force-location should clear deployed status — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `perform_smart_move` GENERIC branch in `logic.py:620` now `pop()`s `physical_source` / `physical_source_slot` when the target is NOT a toolhead. (Previously only the DRYER MOVE branch cleared them; the GENERIC branch — which handles Force-Location → Room/Cart/Shelf — left stale ghost extras intact, so `spoolman_api.search_inventory.is_deployed` kept flagging the spool as still on a toolhead because its ghost source was a toolhead.) The Force-Location UI in `inv_details.js:promptEditLocation` already filters toolheads out of the destination list, so this fix covers every reachable force-location target. Regression coverage in `test_deployed_flag_preservation.py::test_force_move_to_room_clears_ghost_trail`.)*

* If possible, set certain text fields to only prompt with auto fill on some (perhaps none) fields. I think this might be a setible somewhere in the code to prevent a list of previously used values for showing up. Most of the time, this is just getting in the way for me. _[DONE 2026-05-12 (Group 8.4 / `feature/keyboard-nav-polish`) — Quick-Weigh suppression from Group 13 extended to 14 more inputs across 6 files: location ID/name, manual spool ID, wizard search/external/color, edit-filament hex/external query, global search/color, FilaBridge recovery delta. Free-form notes (`editfil-comment`, `vendoredit-comment`, `wiz-spool-comment`) deliberately left as default — history may help there. Regression coverage in `test_keyboard_nav_polish.py::test_autofill_suppressed_on_internal_inputs` and `test_freeform_comments_keep_browser_autofill`.]_

*(L138 — at-a-glance Printer Status widget — DONE 2026-05-13 via Group 9.3. Dashboard widget with stylized per-printer schematic (color-tinted toolhead blocks + remaining-weight bars + low-stock cue), reorderable via ▲▼ arrows, collapsible to a single inline chip strip. See `completed-archive.md`.)*

*(L140 — Printer Status widget hides Core One on prod — DONE 2026-05-16 via `feature/buglist-sweep-2026-05-14`. Root cause was `inv_printer_status.js:_aggregate()` filtering out any printer whose toolheads had zero bound dryer-box `slot_targets` — `if (!bound.length) return;`. Derek's prod environment has bindings for XL (`LR-MDB-1 → XL-1/2/3`, `LR-MDB-2 → XL-4/5`) but Core One runs dryer-boxless by design (no need yet), so it never met the filter. The dryer-box binding was the wrong gating criteria. Fix: render every printer in `printer_map`. Unbound toolheads now show a `🔗 no bound slot` placeholder tile (dashed yellow border, both in the expanded row and the collapsed compact chip) so the printer is visible AND the user gets a clear hint that nothing's wired up yet. Bound toolheads continue to render with their current contents. Test renamed + flipped: `test_widget_shows_unbound_printers_with_placeholder` replaces the old `test_widget_hidden_when_no_bound_toolheads` (which was asserting the bug, not the desired behavior).)*

*(Derek aside 2026-05-16: Core One MMU slots `CORE1-M1` through `CORE1-M5` are unused now that he's not planning to use the MMU anymore. Recommend removing those `MMU Slot` rows from `locations.json` to clean up the printer-status widget render. **Add to backlog: L140 follow-up — remove unused CORE1 MMU rows + decide whether `CORE1-M0` should be renamed from "No MMU Direct Load" to simply "Tool Head"** so it reads consistently with the XL toolheads.)*

*(L142 — Add an Unknown location for physically-lost spools — DONE 2026-05-15 via `feature/buglist-sweep-2026-05-14` (Group 18.1, first half). Virtual `UNKNOWN` row injected into `/api/locations` with `Type: "Unknown"` and Name `❓ Unknown (Physically Lost)`. Derek's visual-treatment choices: icon `❓`; sort position **bottom** of the list; spool-card badge is **yellow caution** (`bg-warning text-dark`). Yellow band applied at three surfaces so the visual is consistent everywhere: (1) Location Manager TABLE row type-badge (inv_core.js, the bigger list view), (2) Location Manager MANAGE-MODAL title bar (inv_loc_mgr.js, when you click into the UNKNOWN row), (3) spool-card location badge (ui_builder.js, both search + buffer modes), (4) Spool Details modal location pill (inv_details.js — previously blue; now yellow with `❓ Unknown` text and a click-through to the Manage modal for UNKNOWN). Force-Location dropdown surfaces it (not filtered out — Type='Unknown' isn't in the exclusion list). `logic.resolve_scan` accepts bare `UNKNOWN` as a valid location target. `perform_smart_move`'s GENERIC branch clears `physical_source` ghost trail on non-toolhead destinations (L130 lineage). Most spools landing here will be ghost rows from the old spreadsheet system that got used up without a proper update — the yellow badge makes them easy to spot for cleanup. Regression coverage in `test_unknown_location_bucket.py` (4 tests).)*

*(L144 — Filament sample + label-confirmed status on Display modal — DONE 2026-05-14, EDIT-SIDE 2026-05-15 via `feature/buglist-sweep-2026-05-14` (Group 17.1 both halves). Display side: two new fact-card rows on the Filament Details modal — "🎨 Swatch Printed" (reads `extra.sample_printed` → ✅ Yes / No / unknown) and "🏷️ Label Confirmed" (reads `extra.needs_label_print` tri-state → ✅ Confirmed / 🖨️ Needs print / unknown). Edit side (Derek's pick: option b — surface via existing dynamic-field rendering): both flags were already registered in `setup_fields.py:filament_standards` as boolean fields; only `needs_label_print` was in the wizard's hide list (line 1369). Un-hidden, so both now surface as native checkbox inputs in the wizard's Filament Extras section (`#wiz-fil-dynamic-extra-fields`) when editing a filament. Regression coverage in `test_filament_sample_label_status.py` (display) + `test_wizard_surfaces_label_and_sample_flags.py` (edit). Spool-level `needs_label_print` stays filtered out on the spool tab — it's system-managed by the create-new flow and shouldn't be hand-toggled.)*

*(L146 — Filament-modal "Queue all active spools" should NOT auto-open the Print Queue modal — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14` (Group 17.2). Handler in `inv_details.js:413` no longer calls `window.openQueueModal()` after success; instead toasts "Queued N labels — open Print Queue to review" (4 s). Users can keep queuing labels from other filaments/spools without dismissing the panel each time. Regression coverage in `test_filament_queue_all_no_auto_open.py`.)*

*(L148 — Queue-label button on just-created spool — DONE 2026-05-14, REVISED 2026-05-15 via `feature/buglist-sweep-2026-05-14` (Group 17.3). After a successful create-wizard run, `inv_wizard.js` renders a "🖨️ #N" chip per `created_spools[]` id in the dedicated `#wiz-postcreate-actions` row in the modal footer (alongside Cancel + Create Inventory). The 2026-05-15 revision moved chips out of `#wiz-status-msg` because the 3-second post-success "Make a change to add more..." auto-prompt was overwriting them before the user could click. Clicking the chip enqueues the label via `window.addToQueue` and disables itself to visually mark it as done. `wizardReset()` clears the row so a fresh wizard open doesn't show stale chips. Regression coverage in `test_wizard_queue_label_chips.py`.)*

*(L150 — Add-new affordance in duplicate Legacy ID picker — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14` (Group 17.4). `duplicate_picker.js` now renders an "➕ Add new spool" button when the candidate list carries a `filament_id` (always true for ambiguous-legacy-id scans). Clicking routes through the existing `window.openNewSpoolFromFilamentWizard(filamentId)` flow so the user lands in the wizard with the parent filament pre-selected. The scan is aborted (legacy id is still ambiguous until the new spool gets its own ID:NNN label). Null-guard hides the button when filament metadata is absent. Regression coverage in `test_duplicate_picker_add_new.py` (2 tests — happy path + null-guard).)*

*(L152 — Surface empty-spool weight on Filament Details modal — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14` (Group 17.5, recommendation B). New "⚖️ Empty Spool: <N>g [↩ filament / ↩ vendor / not set]" fact-card row added to `modals_details.html` and populated by `inv_details.js` using the canonical `window.resolveEmptySpoolWeightSource` cascade from `weight_utils.js`. Inheritance badge tells the user where the value came from (filament's own value vs vendor inheritance) so they don't need to round-trip through the Edit modal. Regression coverage in `test_filament_empty_spool_weight_row.py`.)*

*(L154 — Audit mode refinement — DONE 2026-05-15/16 via `feature/buglist-sweep-2026-05-14` (Group 18.2 Part A + Part B visual panel + label improvement). On audit `CMD:DONE`, every spool expected at the audited location but not scan-confirmed is moved to the virtual `UNKNOWN` bucket with `extra.fcc_pre_audit_location` set as a recovery breadcrumb. Per-spool Activity Log entry + the end-of-audit Missing/Extra summary both use `#N <display label>` (brand material color) instead of bare IDs. `CMD:CANCEL` skips the auto-park (user explicitly bailing).

  **Part B — Visual Audit Panel (Derek 2026-05-15: "more visual than just relying on activity log alone"):** New `mountOverlay`-based panel auto-opens when audit becomes active (toggleAudit / CMD:AUDIT). Shows the location being audited + a tile grid for every expected spool (color swatch, brand+material+name, weight, slot) with per-tile status: ✅ scanned (green border + bg), ⬜ not scanned (gray), ⚠️ rogue (yellow). Live stats header: `<found>/<total>` found, missing, rogue. Polls `/api/audit_session` every 2s while open so tiles tick as scans come in. Closes automatically on CMD:DONE/CMD:CANCEL. "Hide" button dismisses without ending the audit. Activity Log still fires every message — the panel is additive, not a replacement (Derek 2026-05-15: "I still like the idea of having the activity log reference too").

  New backend endpoint `GET /api/audit_session` returns the enriched payload (per-spool display label, color hex, multi_color_hexes for gradient swatches, remaining weight, slot) so the frontend doesn't do per-id Spoolman lookups itself. Cheap when no audit active (returns `{active:false}` immediately).

  `fcc_pre_audit_location` added to `SYSTEM_MANAGED_EXTRAS` AND to `REQUIRED_SPOOL_EXTRAS` so app startup auto-registers the field on any Spoolman that's missing it (fixes the 2026-05-16 prod break where the field wasn't registered and the auto-park PATCH 400'd with "Unknown extra field fcc_pre_audit_location"). Also added to `setup-and-rebuild/setup_fields.py` for the canonical full-rebuild path.

  **Audit exit UX corrections (Derek 2026-05-16):**
  - The AUDIT deck-button toggle now sends `CMD:CANCEL` (not `CMD:DONE`) when bailing out, so an accidental second click doesn't force-move unscanned spools to UNKNOWN. The destructive commit path is the explicit "✅ Done & Auto-Park" button in the panel.
  - The visual panel surfaces both actions as click-targets AND scannable QR codes: "✅ Done & Auto-Park" (CMD:DONE) and "❌ Cancel Audit" (CMD:CANCEL). Previously the panel only had a "Hide" button — the user could dismiss the panel but couldn't end the audit without re-clicking the deck button (which used to be the auto-park trap).
  - New `CMD:CANCEL` scan-command parser in `logic.py:resolve_scan` (the audit code already handled `cmd == 'cancel'` but nothing produced that cmd value). Registered in `shortcuts_registry.js` so the `?` help reference covers it.

  **Idle-timeout watchdog (Derek 2026-05-16):** an audit session left "active" with no human behind the wheel (browser tab closed mid-audit, page refresh, server restart cycles during dev — Derek confirmed this happened during a recent session) used to haunt the dashboard indefinitely; the panel would auto-open on every subsequent load. Added `state.AUDIT_IDLE_TIMEOUT_SECONDS = 30 * 60` + a `last_activity_ts` field on `AUDIT_SESSION` (updated on every audit-mode scan via `api_identify_scan`) + `_check_audit_idle_timeout()` helper called from `/api/logs` (5s heartbeat) and `/api/audit_session` (direct queries). When the session is stale beyond 30 min, the watchdog force-cancels and writes a yellow Activity Log entry explaining what happened so the user notices on the next refresh. Sessions missing `last_activity_ts` entirely (pre-watchdog state migration) get the timer seeded to "now" instead of cancelled.

  Regression coverage: `test_audit_auto_park_unknown.py` (12 tests — including new `test_audit_idle_timeout_auto_cancels_stale_session`, `test_audit_idle_timeout_leaves_fresh_session_alone`, `test_audit_idle_timeout_seeds_missing_timestamp` for the watchdog + the earlier `test_resolve_scan_recognizes_cmd_cancel` and `test_required_spool_extras_includes_fcc_pre_audit_location` regression guards), `test_audit_session_endpoint.py` (3 tests), `test_audit_visual_panel.py` (1 e2e). _[Parent/child rollup so scanning all of a cart's shelves counts toward auditing the cart itself is still a future enhancement on top of this — would need to expand the audit session to span multiple locations.]_)*

*(✅ DONE 2026-05-30 on `feature/scan-match-pipeline` — scanning a Prusament spool QR now backfills the matched filament's BLANK nozzle/bed temps (OQ3 → fill-empties only, per the lean); differing temps are surfaced for confirmation ONLY when the filament has no active spools (OQ4 → silent fill + a matched-overlay diff); match is by the spool's stored `product_url` via top-bar `identify_scan` (OQ1). New spools onboard through the pre-filled Add wizard. Tests in `test_prusament_scan.py`. NOTE: the line-220 item below (edit an EXISTING spool, preserving used-weight) is a distinct, more nuanced case NOT covered here.)*

*(L206 — Polling architecture reduction — DONE 2026-05-19 via `feature/l28-frontend-lockup-ghost-eject` (same branch as L28). **Bulk-pulse endpoint**: new `/api/dashboard_pulse?include=...&manage_id=...` with optional `refresh_spool_ids` POST body assembles `logs`, `status`, `locations`, `buffer`, `manage`, `printer_status`, and `spools_refresh` in a single response. Backend parallelizes printer_status per-printer via ThreadPoolExecutor (capped at 8). **Frontend migration**: each polling function's render path extracted (`_renderLogsPayload`, `_renderLocationsPayload`, `_renderManagePayload`, `_renderSpoolsRefreshPayload`, `refreshPrinterStatusWidgetFromAggregate`) so the new `_dashboardPulseTick` can fan one response out to every renderer. The standalone `setInterval(updateLogState, 2500)` in `scripts.html` (an even-more-frequent duplicate of the 5s heartbeat that contributed to the L28 polling-flood) removed; `startSmartSync` owns cold-start prime + heartbeat now. **Adaptive cadence**: 5s when active, 15s after 60s of no input, 30s when `document.hidden`. Activity bumped by keydown/mousedown/pointerdown/wheel/touchstart + visibilitychange. Net effect: ~3 req/sec sustained → ~0.2 req/sec active, ~0.067 req/sec idle, ~0.033 req/sec hidden. Regression coverage in `test_dashboard_pulse_api.py` (13 backend tests) + `test_dashboard_pulse_frontend.py` (6 E2E tests covering heartbeat URL, adaptive cadence buckets, status-dot repaint, and the `inventory:sync-pulse` source tag). **Stretch goals** (file separately if pursued): SSE for logs+status push (~2 hrs, long-lived connection has TrueNAS reverse-proxy implications); HTTP/2 termination via Caddy (~half day); ETag/If-Modified-Since on logs and locations (~1 hr per endpoint).)*

  **— From "Modals & Add Inventory Wizard":**

*(L192 — Maintain multi-spool creation ability — DONE 2026-05-14 via Group 10 Session A (item 10.7). No code change required; verified via existing `test_wizard_per_spool_scan_*` suite that the qty-driven row sync still works after the Session A changes. See `completed-archive.md`.)*

*(L162 — Consolidate duplicate purchase link fields — DONE 2026-05-14 via Group 10 Session B (item 10.4). Single spool-tab `wiz-spool-purchase_url` with smart-fallback placeholder advertising the inherited filament URL; `wizardReset` now also clears `input[type="url"]` (the "doesn't clear between usages" bug). See `completed-archive.md`.)*

*(L163 — Slicer profile auto-add to current filament — DONE 2026-05-14 via Group 10 Session B (item 10.5). New `window.wizardOnNewChoiceAdded` hook fires after the schema refresh and selects the freshly-added value on the wizard's working filament. Mirrors the existing `inv_details.js:promptEditSlicerProfile` pattern. See `completed-archive.md`.)*

*(L164 — Spoolman field ordering bug — DONE 2026-05-14 via Group 10 Session B (item 10.6). Backend `FIELD_ORDER` constant + `_enrich_field_order` in `app.py` stamps each field dict with a canonical order index; the wizard's pre-existing `.sort((a,b) => (a.order||0) - (b.order||0))` now actually works. Unknown keys pin to 9999 (end). See `completed-archive.md`.)*

*(L165 — SweetAlert2 nested modal audit — DONE 2026-05-14 via Group 10 Session B (item 10.8). All three `Swal.fire` sites in `inv_wizard.js` (unsaved-changes confirm, field-sync picker, add-new-choice prompt) migrated to `window.mountOverlay()`. Closes the `.cmd-deck` scrollbar-shift symptom on the unsaved-changes prompt. See `completed-archive.md`.)*

  **— From "Search, Display & Filtering":**

*(L209 — Search by remaining weight — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `min_weight` was already wired up (offcanvas-search + `search_inventory`). Added the symmetric `max_weight` filter — new input next to Min g in `offcanvas_search.html`, propagated through `inv_search.js` (trigger list, executeSearch, clear-all, has-state checks) and `app.py:/api/search` into `spoolman_api.search_inventory`. Regression coverage in `test_search_max_weight.py` (3 tests — both backend bounds + frontend input wiring).)*

  **— From "Quick-Swap Enhancements":**

*(L208 — Denser `SpoolCardBuilder` cards inside the Quick-Swap grid — DONE 2026-05-13 via Group 9.1. New `'quickswap'` variant + adopted in `inv_quickswap.js`; action footer integrates Details/Edit/Queue/Eject directly from each bound slot. See `completed-archive.md`.)*

*(L216 — Slot-render-order arrows inconsistent — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. Switched to one arrow per button, both pointing in the visual direction of slot numbering: `Left → Right` / `Right → Left`. Same `→` glyph on both labels removes the prior asymmetry.)*

*(L218 — Shortcuts overlay click-through — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. Added `#fcc-shortcuts-overlay-backdrop` sibling at z-index 10000 (just below the panel's 10001) with `onclick="toggleShortcutsOverlay()"`. `toggleShortcutsOverlay` shows/hides both elements together. Regression coverage in `test_quickswap_ui_e2e.py::test_shortcuts_overlay_backdrop_dismisses_overlay`. Existing button + `?`-key toggle tests still pass.)*

  **— From "Location Management & Scanning":**

*(L251 — Buffer-scan assign-all-at-once — VERIFIED + CLOSED 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `test_toolhead_scan_single_spool.py::test_multispool_dryer_box_scan_still_sends_full_buffer` directly asserts that scanning a multi-spool destination (dryer box / shelf / cart) sends the entire buffer in a single `/api/smart_move` POST. The new L124 toolhead branch is the only single-spool carve-out; everything else preserves the legacy bulk-assign behavior. No new repros needed.)*

  **— From "Print Queue, Labels & Filament Usage":**

*(Group 3 — Print Queue & Label Management — completed 2026-04-28; see `completed-archive.md` for resolved items.)*

  **— From "Testing":**

*(L263 `test_visual_quickswap_confirm_overlay` baseline mismatch — DONE 2026-05-12 via Group 14.4 — baseline re-captured at the new Group-15 mountOverlay shape. See `completed-archive.md`.)*

*(L264 `test_details_modal_interactions` offcanvas-backdrop intercept — DONE 2026-05-12 via Group 14.3 + 14.6 follow-up. Defensive teardown promoted to `conftest.reset_dom_state_js`; page-wide `.fcc-card-action-btn` scoped to `#offcanvasSearch`. See `completed-archive.md`.)*

*(Group 16 testing-hardening items — parallel `tests/` directory consolidated, `_open_manage` helper promoted to `open_manage_modal` conftest fixture (10 files migrated), `reset_dom_state_js` adopted in 6 more e2e test files, Windows pip/pytest interpreter mismatch documented — DONE 2026-05-12. See `completed-archive.md`.)*

  **— From "UI & Theming":**

* Theres a little animation and modal that appears when you add a new Slicer Profile in the Add/edit enventory wizzard. Its so nifty I want this used in other places. (I'm not sure if this is a sweetalert2 thing, or if we implemented ourselves.)
  _[ANSWER 2026-05-25 — it's our own `window.mountOverlay()` + `handle.setContent()` pattern, NOT SweetAlert2. The "niftyness" isn't a discrete CSS animation; it's the absence of bad behavior: a single persistent overlay swaps its inner HTML between stages (input → fuzzy-match suggestion → final confirm) instead of dismissing one Swal and opening another. No backdrop flicker, no focus loss, no Bootstrap fade — it just feels smooth. Reference impl: [inv_wizard.js:wizardPromptNewChoice](inventory-hub/static/js/modules/inv_wizard.js#L1919) calls [wizardSwapNewChoiceToSuggestion](inventory-hub/static/js/modules/inv_wizard.js#L1766) and [wizardSwapNewChoiceToConfirm](inventory-hub/static/js/modules/inv_wizard.js#L1792) which each do `handle.setContent(newPanelHtml)`. **To reuse anywhere**: `const handle = window.mountOverlay({...})` for stage 1, then `handle.setContent(stage2Html)` to transition. The `data-stage` attr on the overlay root is the convention for tracking which panel is currently shown. **Candidates for the same pattern**: any multi-step confirm flow (Force Location escape-confirm already uses it; Force Unassign + bulk-archive prompts could). If you want a literal CSS transition between stage swaps, that'd be a separate ask — `setContent` currently does an instant DOM replace.]_

  **— From "App Flow, Architecture & Database":**

*(L286 — Auto-refresh pause button — DONE 2026-05-14, REVISED 2026-05-15 via `feature/buglist-sweep-2026-05-14`. Click-to-toggle the `#log-status` indicator is the ONLY pause path. The hover-mouseenter/leave pause that lived on `#live-logs` was removed in the follow-up because it caused accidental pauses Derek didn't realize were active — exactly the frustration the buglist item described. Server-side `RECENT_LOGS` is bounded at 50, so resume backfills with whatever ticked while paused. Regression coverage in `test_log_sticky_pause.py` (2 tests — click-toggle + assert hover handlers gone).)*

*(L293 — Python server auto-reload on code changes — DONE 2026-05-14 via `feature/buglist-sweep-2026-05-14`. `app.run(...)` now reads `FCC_DEV` env var; when set to `1` / `true` / `yes` / `on`, passes `use_reloader=True` so werkzeug watches files and auto-restarts on edit. Defaults to off so the TrueNAS prod image keeps its current single-process behavior — no reload churn or behavior changes in prod unless the env var is explicitly set. To enable on the dev container, add `FCC_DEV=1` to the env section of the dev compose/spec (or `docker exec` with `-e FCC_DEV=1` for ad-hoc).)*

  **— From "Filabridge Error Recovery":**

*(L324 — Filabridge ↔ Spoolman reconcile utility — DONE 2026-05-16 via `feature/buglist-sweep-2026-05-14`. Shipped as the first inhabitant of a new Config / Admin modal scaffold (the ⚙️ button now sits next to `?` in the nav bar). Per Derek's "having both options would be nice" — both resolution paths are surfaced per-mismatch row:
  - **Trust Spoolman** → clears the stale FilaBridge toolhead entry via `_fb_write(printer, th, 0)`. Spoolman's location wins.
  - **Trust FilaBridge** → writes Spoolman `location = <fb_location>` for the affected spool. System-managed extras (container_slot, physical_source, etc.) are preserved — this is a "rewrite Spoolman to match the truth FB already accepted" op, not a smart_move.

  Two new endpoints: `GET /api/filabridge/reconcile` (scans, returns mismatches with both views) and `POST /api/filabridge/reconcile/apply` (resolves one row). UI: `configModal` opens via the ⚙️ button, presents a table of mismatches with action buttons per row + a per-row spool display label. After applying any fix the scan auto-reruns so the table reflects the current state.

  Config UI scaffold (Derek 2026-05-16: "I've been wanting to add a configuration button and system this would be a nice way to start that up for future feature use"). The modal is structured as a series of Bootstrap `.card` panels — adding future sections (user preferences, choice cleanup launcher, etc.) is a matter of dropping another card into `modals_config.html` and adding its JS to `inv_config.js`. The existing card pattern is the contract.

  Build Info section added as the second card: surfaces the resolved commit SHA + timestamps from the L42 round 2 stamp. Read-only.

  Regression coverage: `test_filabridge_reconcile.py` (8 backend tests — clean/mismatch/orphan-position/zero-spool detection + both apply paths + payload validation) and `test_config_modal_smoke.py` (2 e2e — modal opens, scan renders results).)*

---

* **[2026-06-05 — L271 Group 20 (toolhead binding cluster) + Phase 5 (Edit-UI, shelf nesting, custom Types)]** — Group 20 on `feature/toolhead-binding-cluster` (→ `main` `9650106`, **deployed + verified on prod 2026-06-05**); L271 Phase 5 Edit-UI slice on `feature/l271-phase-5` (→ `main` release 2026-06-05). The whole L271 epic stays open for the remaining Phase-5 prefix-retirement slice (see the L271 item still in `Feature-Buglist.md`).

  **Group 20 — toolhead binding/unbind cluster (dissolved the 33/37/43 + L206 bug class).** **20.1 = VERIFIED FIXED, no code change** (cumulative L204 + Phase 3.5/4 closed it; live diagnostic on archived #240 + non-archived #225 — direct eject, auto-deploy cycle, cross-toolhead reassign all CLEAN: eject/reassign always unmaps FilaBridge + clears Spoolman loc + ghost trail). **20.2 = single-slot dryer-box auto-attach/detach** (`c7c9847`): a `Max Spools<=1` box auto-binds to a toolhead on assign / clears on eject via `slot_targets` (`locations_db.attach_single_slot_box_to_toolhead`/`detach_single_slot_boxes_from_toolhead` + hooks in `perform_smart_move` PRINTER MOVE + `perform_smart_eject`; lifecycle-driven, multi-slot untouched); 9 unit tests + live-verified (#150 PM-DB-1→XL-2). **Behavior change: a manual single-slot Feeds binding now auto-clears on eject.** **20.3 = full toolhead-delete cascade** (`daa31dc`): `logic.perform_toolhead_delete_cascade` — direct spool→UNASSIGNED, ghost→un-deploy-keep-box (fixed a LATENT bug where the old blanket `location=""` cascade yanked ghost spools out of their boxes), FilaBridge unmap, dryer `slot_targets` cleared, pruned from the Printer-row `toolheads[]`, active-print-gated + Activity-Log breadcrumb; 8 tests; wired into `/api/locations` DELETE. Resolves the buglist "propagate toolhead removal to Spoolman locations" item. **Deployed code-only (no startup migration); prod verified zero data impact** (locations.json/config.json byte-stable, 60 rows, toolheads intact).

  **L271 Phase 5 — Edit-UI + shelf nesting + custom Types** (`5413eb8` impl → `daba457` review-fixes → `e7ccea3` rename/custom-types). The hierarchy used to be *implicit in the LocationID*; Phase 5 makes `parent_id` explicit/visible/editable and adds the missing shelf-nesting levels. **(a) Explicit Parent selector + ancestry breadcrumb** in the Edit Location modal (`#edit-parent` optgroup-by-kind, excludes self+descendants; `#edit-parent-breadcrumb`); `saveLocation` sends `parent_id` (Auto→omit/derive-preserve, Top-level→null, else id) + surfaces save errors; backend `api_save_location` validates an explicit `parent_id` (must be a real row or a PM/PJ/TST pseudo-prefix; rejects self + descendant cycles via `is_descendant(strict=True)`; rejects duplicate LocationID; canonicalizes the stored value) → 400; fixed the latent `openAddModal` Type-non-reset bug. **(b) Data-driven Type dropdown + custom types:** `#edit-type` is JS-populated (`_populateTypeSelect`) from `_LOC_BUILTIN_TYPES` + custom types (localStorage `fcc.locMgr.customTypes` + any Type already on a row), plus a free-text `#edit-type-custom` "add new type" input (wins over the dropdown, persisted) — editing a custom-typed row no longer drops its Type; synthetic Virtual/Unknown denied, Sliding Drawer is a built-in. Added `Printer` + `No MMU Direct Load` (fixes silent Type-rewrite). **(c) Real shelf nesting — Room → Wall Shelf → Row → Section:** `locations_db.migrate_shelf_grouping_rows_if_needed` synthesizes the intermediate **Wall Shelf** + **Row** grouping rows (structural, no spools, excluded from every spool picker) so the flat `CR-WLN-R1-SC1` sections nest properly, and RETYPES the leaf sections `Shelf → Section` (the spool-holding leaf) — generic (derives ids+Names from each section's segments/Name, typo-robust), self-contained (a fixpoint for the 3.5 immediate-parent pass), idempotent, respects operator overrides, collision-guarded (won't nest under a pre-existing non-grouping row at a Wall/Row id), alias-aware (folds the pre-rename `Wall`/`Shelf` rows). Startup-wired after Phase 3.5 with a `pre-wall-row-synthesis-migration-{stamp}.bak`. **Decisions (Derek, AskUserQuestion):** new Wall Shelf/Row/Section Types (not reuse-Shelf); Printer + No-MMU both added; Group 20 released first. **17-agent adversarial review → 9 confirmed → all fixed** (collision guard, strict cycle-walk vs dangling dashed FK, dup-id reject, canonicalize, all-picker grouping exclusion, Row-name fallback). **Custom-type edit/rename/delete deferred** → backlog item in `Feature-Buglist.md` (natural home = the Config system L9). Tests: `tests/test_l271_phase5.py` (26 unit + 5 live integration) + updated migration/canary suites; 341 in the consolidated locations regression green; visual baseline unchanged (tree scrolls inside `max-h-70vh`). Live-verified on dev (Room→Wall Shelf→Row→Section, "24 Total" rollup, Parent selector + breadcrumb + custom Type input render). **Prod release 2026-06-05** — startup migration creates the Wall Shelf/Row rows + retypes the 8 prod sections → Section; the prod `CR-WLN-R2-SC3` "Section 1" Name typo fixed in the same deploy. **REMAINING L271 (still in buglist):** Phase 5 prefix-retirement (delete `derive_parent_id_from_prefix` + the `resolve_parent` fallback, zero-`split('-')[0]`, write-time orphan rejection).

* **[2026-06-03 — Buglist sweep: L22 + 4 fixes + weight Mixed-precision]** — Branch `feature/buglist-sweep-2026-06-03`. Five well-scoped buglist items shipped with regression tests, all verified against the live container. (Same sweep also: triaged the toolhead binding cluster into proposed **Group 20** and authored the full **L271** phased plan — both tracked separately, not archived here.)

  **L22 — Filament attributes on BOTH detail modals.** Read-only 🏷️ Attributes chip row now renders on the Spool Details modal (`#spool-detail-attributes`, inherited from the parent filament's `extra.filament_attributes`) AND the Filament Details modal (`#fil-detail-attributes`) — previously the attributes only existed as editable chips in the Edit Filament form. Shared `parseFilamentAttributes` (JSON-array / quoted-string / bare-string tolerant) + `renderReadonlyAttributeChips` helpers in `inv_details.js`; generic `.fcc-attr-chip` style in `modals_details.html`. Row self-hides when empty (gotcha: the outer `<p>` must NOT carry Bootstrap's `d-flex`, whose `display:flex !important` defeats the inline `display:none` toggle). Tests: `test_detail_modal_attributes.py` (4).

  **Buffer cards refresh instantly on local move/assign/eject (buglist line 35).** Cards lagged ~5s (the `dashboard_pulse` cadence) after a local mutation. The immediate-refresh path existed (`inventory:sync-pulse` non-dashboard → `liveRefreshBuffer`) and the details/weigh-out/wizard surfaces fired it, but the scan/assign/eject paths in `inv_cmd.js` + `inv_loc_mgr.js` never signalled the buffer. Added an `inventory:locations-changed → liveRefreshBuffer()` listener (no-op on empty buffer + in-flight guarded); `performContextAssign` + `doEject` now dispatch that canonical event (→ fetchLocations + printer-status + buffer refresh) instead of calling `fetchLocations` directly. Remote changes (FilaBridge auto-deduct, other tabs) still surface on the next pulse — inherent. Tests: `test_buffer_immediate_refresh.py` (2).

  **Legacy-ID scan falls back to the filament when no spools exist (buglist line 39).** `find_spools_by_legacy_id` returns `[]` when the matched filament has no spools → the URL/`range=` branch and the explicit `LEGACY:`/`LEG:`/`OLD:` branch both dead-ended at "unknown error". Added the pure-number branch's Priority-4 filament fallback (`find_filament_by_legacy_id` → `{'type':'filament','id':fid}`) to both branches in `logic.resolve_scan`; flows through existing filament handling (`app.py:2275`, `inv_cmd.js:615` → `openFilamentDetails`), so a spool no longer needs to exist. Tests: `test_legacy_id_filament_fallback.py` (5). *(Diagnostic-only sub-question — why the example spool's weight was 0 + archived — left open, needs that spool's id + log trail.)*

  **FilaBridge status dot was constantly RED even when active (buglist line 45).** Root cause confirmed live: the `/api/logs` liveness probe hit FilaBridge's `/api/status`, which polls every printer's live PrusaLink state and takes ~5.8s — past the old `timeout=3` — so it raised on EVERY heartbeat → `fb_ok=False` → dot pinned red despite FilaBridge being up (`/api/print-errors` returned 200 instantly on the same host). It also added ~6s latency to every `dashboard_pulse`. Fix: probe the lightweight `/api/print-errors` (fetched right after anyway) and reuse the one response for both the dot + error poll; coerced `{"errors":null}` → `[]` (latent `for err in None` TypeError, masked by the bare `except`). Verified `False → True` live. Tests: `test_filabridge_status_dot.py` (3). DIFFERENT bug from the L20 ON-HOLD flicker.

  **Weight-remaining Mixed-precision display (buglist line 51).** Derek's scale reads whole grams, so the 0.x is inherent measurement variance from the fractional empty-spool tare; the reported "~1g off" was INCONSISTENT rounding (the wizard rounded `used`/`remaining` to whole AND persisted that, while the Details modal showed 1 decimal — so they stopped reconciling). A 72-site surface map (workflow) confirmed the glance tier was already whole (`Math.round` on cards/printer-status) and the detail tier already 1-decimal (`weight_entry.js` `fmtG`); only the wizard `.toFixed(0)` + weigh-out `Math.round` were off. Decision: **Mixed precision**, data stored exact. New shared `window.fmtGramsGlance` (whole) + `window.fmtGramsPrecise` (≤1 decimal, trailing `.0` trimmed, null/'' → blank, real 0 → "0") in `weight_utils.js`; wizard calc fields + edit-load prefill + Spool Details `used`/`remaining` + weigh-out current/preview routed through `fmtGramsPrecise`; glance card path left untouched. `used + remaining` now reconciles to `initial`, no surprise 1g jumps. Tests: `test_weight_display_precision.py` (4).

* **[2026-06-02 — Group 19: Dev-Env Reset & E2E Test-Debt Triage]** — Branch `feature/dev-env-reset-test-debt`. Two halves: a `reset-dev` automation that restores the shared, mutating dev backend to a reproducible baseline before an E2E sweep, plus the four known TEST-DEBT fixes that would fail even in a pristine env. All four test fixes verified green against the live container (43 passed / 1 skipped across the touched files); reset-dev read/compare logic proven live + covered by 11 hermetic unit tests.

  **19.1 — `reset-dev` script** (`setup-and-rebuild/reset_dev.py` + `reset-dev.ps1`/`.sh` host wrappers). Runs on the host (must `docker restart inventory_hub` + rewrite the bind-mounted locations.json; reaches dev Spoolman directly). Modes: default **non-destructive restore** (locations.json ← seed; PATCH only *drifted* spool/filament/vendor fields back to seed values — whole-`extra` overwrite, the inverse of the user-edit `compute_dirty_extras` merge rule; `docker restart`), `--prune` (also DELETE sweep-created entities absent from the seed, FK-safe spool→filament→vendor), `--capture` (snapshot current dev → seeds), `--dry-run`, `--no-restart/-locations/-spoolman`. **Decisions (Derek 2026-06-02): seed = current dev; reset mode = non-destructive default, prune behind a flag.** Committed seed pair captured read-only: `setup-and-rebuild/seeds/spoolman-dev-seed.json` (40 vendors / 177 filaments / 238 spools, de-nested FKs) + `locations-seed.json` (53 locations). Pytest-PLA test junk (47 filaments + 42 spools accumulated from past `test_wizard_manual_creation` runs) curated out of the seed file **locally** — no shared-dev mutation; the destructive cleanup of dev (`--prune`) is **agent-invoked, NOT human-invoked** — Derek confirmed 2026-06-02 he'll never run the script or `--prune` himself ("don't even know how to anyway"), so Claude runs it on his behalf with explicit per-session OK (an agent mass-delete was also correctly blocked by the auto-mode classifier, consistent with the non-destructive-default decision). The junk stays in dev until the next Claude-run prune. **Idempotent**: `--dry-run --prune` immediately after `--capture` → 0 drift across all 455 real entities, locations unchanged, 47+42 junk flagged for prune. **Limitation**: Spoolman assigns ids on create, so an entity a sweep *deleted* can't be restored with its original id — such entities are reported, not silently recreated (recover via a Spoolman backup). In practice sweeps corrupt location/extra/archived/weight on existing spools — all fully PATCH-restorable. Logic locked down by `tests/test_reset_dev.py` (11 hermetic unit tests on `_drifted_payload` / `_values_equal`: location/archived/extra/weight drift, whole-extra overwrite dropping sweep-added keys, filament_attributes restore, float tolerance, no-drift idempotency, restore-only-listed-fields). **Pytest integration (follow-up 2026-06-02, after Derek confirmed he won't run the script manually)**: an OPT-IN session-autouse fixture in `inventory-hub/tests/conftest.py` — `pytest --reset-dev` (or `RESET_DEV_BEFORE_SWEEP=1`) runs the non-destructive restore at session start + health-waits for the container; `pytest --reset-dev-prune` adds the destructive delete; a flagless run is a strict no-op (deliberately not unconditional autouse — auto-resetting before every single-test run would docker-restart the container + yank dev state). Live-validated end-to-end with Derek's OK (real `docker restart inventory_hub` + 0-drift restore + 11 tests green); the no-flag no-op path proven too.

  **19.1a — seed-readiness investigation**: `data/locations.json.example` is a **6-location fresh-install stub, NOT a test baseline** — spools reference 40 distinct locations, so restoring from the stub would orphan ~170 spools. The test baseline needed a new coordinated seed *pair* (Spoolman dump + matching 53-location locations.json), captured from current dev. The `.example` stays as the fresh-install bootstrap. Full findings + the reset-dev design + an acceptance-test procedure in `docs/agent_docs/tasks/19-seed-readiness-and-failure-buckets.md`.

  **19.1b — failure-bucket framework**: split the 2026-05-30 sweep's 77 environmental failures into **DATA-caused** (manage-modal won't open, quickswap/locmgr/returns, buffer-card refresh, weigh-out/archive — a `reset-dev --prune` fixes these; measured, not eliminated, by the script — the empirical re-bucket is the script's human-run acceptance test since it re-contaminates shared dev) vs **TEST-DEBT** (the four below). Documented analytically rather than via a full contaminating re-sweep.

  **19.2 — `test_wizard_escape_warns_when_dirty`** (`tests/test_ui_structural.py`): asserted the dead `.swal2-container`; the dirty-close guard migrated to `window.mountOverlay()` (`#fcc-wiz-unsaved-changes`) in Group 10.8. Rewrote it to assert the overlay + its Keep-Editing/Discard buttons and that no Swal renders. **Kept** (not deleted as redundant) because it uniquely drives the real **Escape-key** trigger of `hide.bs.modal`, whereas the migration suite triggers via programmatic `m.hide()`.

  **19.3 — `test_structural_global_modals` text-truncate false-positive** (`tests/test_ui_structural.py`): added a `text-truncate` skip to `execute_global_scanner`'s whitelist (Bootstrap's `text-truncate` is *designed* to clip with an ellipsis). Removing that mask surfaced a previously-hidden sibling false-positive — `.fcc-wiz-section-toggle`, the wizard's collapsible section headers, whose title + summary children clip via hand-rolled `overflow:hidden;text-overflow:ellipsis` (same designed-to-clip class, just without the Bootstrap utility class); a few px of flex sub-pixel rounding on an element built to clip is not a layout bug. Whitelisted it too, consistent with the existing `wizard-step` entry.

  **19.4 — wizard collapsed-panel tests** (`tests/test_wizard_e2e.py::test_wizard_manual_creation`, `tests/test_wizard_vendor_edit_button.py::test_vendor_created_does_not_clobber_user_typed_empty_weight`): `#wiz-fil-density` / `#wiz-fil-empty_weight` live in `#wiz-fil-physical-panel`, which `wizardApplyCollapseDefaults('create')` collapses on open → fields "not visible" for `.fill()`. Expand the panel first (the same idiom already used for the color panel). (`debug_console.py` also fills these but isn't a collected pytest test; `test_wizard_queue_label_chips.py` uses JS `setVal` which bypasses visibility — neither needed a change.)

  **19.5 — material-split tests' choices dependency** (`tests/test_wizard_per_spool_scan_e2e.py`): `splitMaterialAndAttributes` only recognizes attribute tokens present in the canonical `filament_attributes` *choices* (from `/api/external/fields`, which mirrors ambient dev config). Added a reusable `_route_fields_with_attrs()` helper that fetches the real schema and merges the required attrs into the choices via `route.fetch()` (keeping the rest of the schema real so the chip container still renders) — hermetic regardless of dev state. Applied to **both** dependent e2e tests: the named `test_per_spool_scan_splits_material_into_base_plus_attribute_chips` (chip render) **and** `test_backfill_silent_patch_fires_on_autoswitch` (set-union — surfaced during verification as the same class; the buglist note said "tests", plural). Blocks `/api/external/fields/add_choice` as belt-and-suspenders so the hermetic test can't mutate dev config. The material-split *unit* tests (`test_wizard_per_spool_scan_unit.py`) were already hermetic (they pass the known-attrs list explicitly). **Side-finding — FIXED 2026-06-02 (follow-up commit)**: `test_wizard_manual_creation` was itself the contamination source — it creates a real filament + spool every run (its genuine end-to-end value) and used to leave them behind, which is where all 47 Pytest-PLA junk filaments came from. Now self-cleaning: it captures `filament_id` + `created_spools` from the wizard create response and deletes them in a `finally` (spool first, FK order; fail-quiet; runs on assertion failure too). Verified net-zero — dev filament/spool counts identical before and after a run. Mirrors `test_vendor_create_end_to_end.py`'s cleanup pattern; deletes go straight to dev Spoolman via the `dev_spoolman_url` fixture.

* **[2026-06-01/02 — Core One Printer Status: box-bounding fix + 3 adjacent fixes]** — Branch `feature/core1-status-box-bounding`, started via `/startnewtask` on Derek's standing "printer status not showing Core One data correctly" bug and grew into a 4-fix cluster. 9 tracked files + 1 new test file; 19+ new tests.

  **Box-bounding on Printer Status (the original ask)** — Symptom (Derek, buglist line 20): "the status seems to key off attached dryerboxes, and not actual print heads." Root cause (adversarially verified via a 6-agent diagnosis workflow): `_pulse_section_printer_status.fetch_for_printer()` gated a toolhead's `item` (loaded-spool contents) behind `is_bound`, and `is_bound` derives PURELY from dryer-box `slot_targets` (`locations_db.get_bindings_for_machine`). So a directly-fed toolhead with no dryer box — Core One's prod setup — rendered empty even with a spool loaded. Dev masked it because CORE1-M0 happened to have a box bound. **Fix**: occupancy now keys off the toolhead LOCATION. New `spoolman_api.bucket_spools_by_location(loc_ids, spools=None)` resolves every toolhead's occupancy in ONE Spoolman fetch (vs the old N-per-bound-toolhead fan-out — a 6→1 perf win) by replaying the same `location` + `physical_source` ghost predicate via an extracted `_build_location_match` helper (shared with the refactored `get_spools_at_location_detailed`, zero behavioral drift). `unbound` stays a pure binding hint that only drives the "🔗 no bound slot" affordance. Frontend `inv_printer_status.js` (expanded tile, compact chip, AND the `_aggregate` cold-load fallback) now renders a loaded-but-unbound toolhead as **spool + dashed accent + 🔗 hint** (Derek's pick) instead of a bare placeholder; guard flipped from `if (th.unbound)` → `if (th.unbound && !it)`; fingerprint now includes the unbound flag so a bind/unbind toggle re-renders. Proven non-destructively by simulating the dryer-box-less config in-memory (CORE1-M0 #150 surfaces with `unbound=True`). Tests: `test_printer_status_box_bounding.py` (8 unit — bucket semantics + ghost, the ungate, `unbound`-independent-of-occupancy, perf 1-fetch guard) + strengthened L140 `test_widget_shows_unbound_printers_with_placeholder` + new `test_occupied_unbound_toolhead_shows_spool` (injection E2E). Widget contrast guard still green. **Confirmed in passing** (Derek's specific worry): the FilaBridge auto-deduct path is ALSO toolhead-driven, NOT box-dependent — it resolves via `_resolve_active_locs_for_printer`(printer_map) + `get_spools_at_location`(toolhead location/physical_source), zero `slot_targets` references; a print on a dryer-box-bypassed toolhead still deducts correctly. The `state=null` Core One badge is a dev-only artifact (printer IP unreachable from the dev container; creds resolve fine) — prod shows real state; out of scope.

  **`_prune_locations_backups` definition-ordering NameError** — The startup backup-prune calls ran at MODULE-IMPORT time but the function was defined ~900 lines lower (app.py:1019), so every boot threw `NameError: name '_prune_locations_backups' is not defined` → caught → logged "backup prune skipped". Net: L347's startup pruning never ran, `locations.json.*.bak` accumulated unbounded. **Fix**: moved `MAX_LOCATIONS_BACKUPS` + `_prune_locations_backups` to just after `app = Flask(__name__)`, above the import-time callers. Verified `app._prune_locations_backups()` runs clean on import.

  **Dashboard Printer Status badges (and spool-card location badges) — clicking did nothing** (Derek 2026-06-01). Regression from the L206 polling rework: `openManage(id)` looks the id up in `state.allLocations` and silently returns if missing; `state.allLocations` is only populated by `_renderLocationsPayload`, which the adaptive `dashboard_pulse` includes ONLY when the Location Manager table is visible (`inv_core.js`). On the bare dashboard the table is hidden → `allLocations` stayed `[]` → every dashboard `openManage()` bailed with no error. Pre-L206 the unconditional `fetchLocations` poll kept it primed — hence "used to work." **Fix**: prime `allLocations` via `fetchLocations()` at startup + refresh on `inventory:locations-changed`; turned the silent bail into a `console.warn`. Guard `test_dashboard_primes_all_locations_for_openmanage`. **Notable**: this fix turned 7 widget/contrast E2E tests GREEN that had been mis-attributed (in the same session) to "shared-dev contamination" — they were catching this real bug. (Lesson saved to memory: a baseline stash A/B proves "not my change," NOT "not a real bug.")

  **Core One MMU rows cleanup** (Derek aside 2026-05-16, surfaced by the box-bounding fix making the 5 unused MMU rows render as 🔗 clutter). Removed `CORE1-M1`..`CORE1-M5` (empty + unreferenced — verified) from BOTH `config.json:printer_map` (guarded `PUT /api/printer_map`) AND `locations.json` (5× `DELETE /api/locations`) — the synthesizer re-injects any toolhead still in printer_map, so both had to go. Retyped `CORE1-M0`: Type `No MMU Direct Load` → `Tool Head`, Name → `🦝 Core One Upgraded Tool Head`. Verified safe first: `Tool Head` is a first-class member of every toolhead-type set, CORE1-M0 already carried every key the `Tool Head` schema needs (integrity test stays green), and the deduct path keys on the `-M0` suffix not the Type. Tracked seed `data/locations.json.example` updated to match. **⚠️ PROD ACTION NEEDED**: live dev data/config only — replicate on prod (Location Manager: delete CORE1-M1..M5, edit CORE1-M0 → Tool Head; Config toolhead editor: drop CORE1-M1..M5).

  **Repro-blocked / deferred**: L25 (Print-status weight not updating on print-finish) and L19 (in-progress printing modal QR codes on the right) both need a live DevTools capture during the event — left open in `Feature-Buglist.md`.

* **[2026-05-25 to 2026-05-28 — Multi-Item Buglist Sweep + Phase 1B + L347/L318 Hardening]** — Branch `feature/buglist-sweep-2026-05-25`, **20 commits**, started as a "knock out as many standalone items as possible" sweep and grew through 7 rounds into a substantial hardening pass covering one structural refactor (Phase 1B of the Location Manager redesign), one developer-experience fix Derek caught in real-time (Jinja2 template cache), and three "data integrity" tickets that required full preview-before-commit safety flows.

  **L15 — Wizard collapsed Color section spacing** (commit `d4dafd2`) — Added `ms-2` to the trailing hex / count tag in `inv_wizard.js:wiz-fil-color-panel` so the color name and the count tag no longer read as cramped. One-line CSS fix.

  **L21 — Sort filament search by spool count** (commit `4037422` + dev-mode fix `d186c87`) — New "Sort" dropdown in `offcanvas_search.html` with filament-only options ("Most spools" / "Fewest spools"). Backend already populated `spools_count` on filament results; sort just orders by it ahead of the default color/id heuristic. `executeSearch` empty-state guard relaxed so sort-alone (no filter) triggers a search. 2 new unit tests in `test_search_api.py` (`test_search_filaments_sort_by_spool_count`, `test_search_unknown_sort_falls_through`); endpoint-call assertion extended to include the new `sort` kwarg + a previously-missed `max_weight` kwarg. **Dev-mode follow-up 2026-05-28**: Derek caught the dropdown not appearing in the running container even though the commit was in HEAD. Root cause: with `debug=False` (production-safe), Flask doesn't auto-reload Jinja2 templates — `use_reloader=True` from L293 only re-execs on `.py` edits. Set `app.config['TEMPLATES_AUTO_RELOAD'] = True` inside the existing FCC_DEV gate; dev containers now pick up template edits on the next request, prod stays on the production-safe default.

  **L23 — Cart-2 labels constantly re-appearing in print queue** (commit `b2b8881`) — Root cause was a race between `persistQueue` (POST after user-Clear) and an in-flight `loadQueue` GET that returned pre-Clear data and overwrote the cleared local queue. Loop re-armed every 2s, so the queue felt like it kept "re-adding" items the user just removed. Fix: stamp `lastPersistAt` on every write; each `loadQueue` captures its issue time and drops responses whose request pre-dates the most recent local mutation. Re-seed-on-empty path (server-restart case) preserved.

  **L122 — Confirm-change modal blocked / cancelled / hidden during print** (commit `07b1ed4`) — Finished the PARTIAL from 2026-05-14. Scan-side `_confirmActivePrintScan` migrated then; manage-modal slot-assign `_confirmActivePrintAssign` was still hand-rolled (`createElement + appendChild + document keydown`). Migrated to `window.mountOverlay({ tier: 'confirm', host: manageModal, onEscape })` mirroring the `_confirmActivePrintScan` reference impl exactly (Escape via `onEscape`, focus trap via keyHandler, QR-confirm pair via `attachConfirmQRs`). Closes the L122 symptom AND defuses the L361 PRINTING-state flake risk surface — the hand-rolled overlay was suspected of intercepting toolhead-tile clicks.

  **L204 — Box slot empty doesn't propagate to FilaBridge** (commit `2ba63b5`) — Five paths could leave FilaBridge pinned to a spool Spoolman had already moved off a toolhead. The unmap was gated on `current_location in printer_map` — if Spoolman's location had drifted to a dryerbox or room, the unmap was skipped. Fixed in `logic.py`: `perform_smart_eject` else-branch, `perform_force_unassign` else-branch, `perform_smart_move` DRYER MOVE branch, `perform_smart_move` GENERIC MOVE branch — all now probe FilaBridge via `_fb_spool_location` and unmap there regardless of Spoolman's view. 2 new regression tests in `test_filabridge_move_ordering.py` (`test_l204_dryer_move_unmaps_filabridge_when_spool_was_on_toolhead`, `test_l204_eject_falls_back_to_filabridge_lookup_when_spoolman_lags`); existing 5 still pass.

  **L243 — Slicer Profile "nifty animation" reuse** (commit `98082cb`) — Investigated. It's `window.mountOverlay()` + `handle.setContent()`, NOT a CSS animation. A single persistent overlay swaps its inner HTML between stages (input → fuzzy-suggestion → confirm) instead of dismissing one Swal and opening another. The "niftiness" is the absence of bad behavior (no backdrop flicker, no focus loss, no Bootstrap fade). Documented the reuse pattern + reference impls in the buglist so the next time it comes up the answer is one search away. No code change needed.

  **L318 — Restore Spoolman field sort order** (backend `ff5db0e` + UI `bec98f6` + safety `569ea19`) — Spoolman doesn't preserve canonical sort order; every `order` value drifts to `0` after edits and Spoolman's own UI shows extras in mutation order. New `POST /api/spoolman/restore_field_order` walks every Spoolman field on filament + spool, looks up the canonical FIELD_ORDER index, and POSTs the field back with the right `order` value. Since `POST /field/{entity}/{key}` is an upsert that defaults omitted properties, the endpoint echoes `name` / `field_type` / `choices` / `multi_choice` / `unit` / `default_value` from the GET back on the POST so the only thing that ever changes is `order`. Idempotent — fields already in position skip the round-trip; unknown keys pass through untouched. **Safety preview** (Derek 2026-05-28: "make sure that whatever implementation you have come up with doesn't break or lose data"): endpoint accepts `?dry_run=true` to PREVIEW without writing; the UI defaults to dry-run on first click and renders a Bootstrap table with one row per moving field showing `from_order` → `to_order`. Only the explicit "Apply N changes" button on the preview commits. **UI**: "🔢 Restore Spoolman Field Order" card in the Admin / Config modal (right above Build Info) — two-step preview / apply flow. **Regression coverage**: `test_restore_field_order.py` (7 tests covering idempotent skip, drift-corrects with echoed props, unknown-key pass-through, surfaces 4xx errors, dry-run never POSTs, apply matches dry-run plan, no-drift reports zero).

  **L347 — Log cleanup routine** (commits `d94a5d1` + `e2dffee` + `eab86f8`) — Two unbounded state files capped: (1) `filabridge_error_snapshots.json` via `_evict_old_fb_snapshots()` (MAX_FB_ERROR_SNAPSHOTS=100, insertion-order eviction — Python 3.7+ dict preserves order); (2) `locations.json.pre-*.bak` migration backups via `_prune_locations_backups()` (MAX_LOCATIONS_BACKUPS=5, mtime-based eviction). Pruning is wired into both backup write sites AND a one-shot startup prune so accumulated backups from previous boots get trimmed even when no migration fires this run. **Safety on snapshot eviction** (Derek 2026-05-28: "Unless our code actively does something with the error codes we don't have to worry about external"): audited every snapshot consumer. `/api/fb_recovery_spools` already falls through to live `get_spools_at_location_detailed` when the requested error_id isn't found (try/except + conditional), so an evicted ID just degrades to current state instead of erroring. `/api/fb_aggressive_parse` and `/api/fb_manual_recovery` never read the snapshot at all — error_id is used only as an ack token. Pinned the fall-through with 2 new regression tests + an inline design-intent comment for the next maintainer. **Other logs already bounded**: `hub.log` rotates via `RotatingFileHandler('hub.log', maxBytes=1000000, backupCount=5)`; `state.RECENT_LOGS` capped at 50 entries in memory. Coverage in `test_filabridge_recovery.py` (6 new tests across the three areas).

  **L361 — Test flakes when a printer is in active PRINTING state** (commit `b7ff2b3`, sealed by L122 migration above) — Root cause: per-element click listeners re-attached on every Printer-Status widget `_render`. During active PRINTING, the widget re-rendered between Playwright's actionability check and the click, dropping clicks onto orphaned listeners. Fix: event delegation on the stable widget root in `inv_printer_status.js`. Handler survives arbitrary body re-renders. New regression test `test_l361_toolhead_click_survives_rerender` forces 3 full re-renders before clicking. Combined with the L122 migration (above) — both the proximate cause AND the suspected upstream cause are now closed.

  **L17 partial — Filament label-confirm pairs sample_printed** (commit `7b12f33`) — Pairing half only. FIL: barcode scan in `app.py:1936` now sets both `needs_label_print=false` AND `sample_printed=true` in a single update (Activity Log already advertised "Label & Sample Verified"; code now matches the message). Early-exit path (already-verified label) intentionally doesn't touch sample_printed so re-scanning doesn't reset a value the user may have cleared. 1 extended test + 2 new tests in `test_print_queue.py`. UI-editing half of L17 stays open with annotation — kept in buglist.

  **Phase 1B — Location Manager redesign** (commit `aeeaa09`) — First consumer migration off the prefix-split heuristic to the parent_id abstraction shipped in Phase 1A. `/api/locations` room-occupancy synthesizer now reads each row's explicit `parent_id` via `locations_db.resolve_parent`, falling back to legacy `split('-')[0]` only when no parent_id is present. Behavior-preserving for the dominant case (Phase 1A migration backfilled parent_id from prefix on every row). Unlocks: PM/PJ boxes that have been explicitly re-parented to a real room via parent_id will now contribute to that room's count — pre-fix they fell into the PM/PJ exclusion and got silently dropped. 93 existing tests in the locations/parent/synthesizer/dryer-bindings suite still pass. Part of multi-phase L271 — phase tracker stays in `Feature-Buglist.md`.

  **Annotations only (kept in buglist, no code shipped)**: L25 (Print Status weight) — code-path mapped, needs DevTools repro. L198 (Prusament temp scan updates existing filaments) — 4 open UX questions documented. L200 (Prusament data-link spool scan updates existing fields) — 4 open questions + sibling-wipe risk flagged. L351 (Spool product data text field) — audit shows it's likely already done via existing `product_url`/`purchase_url` extras; flagged for confirmation. L352 (Background refresh in Location Manager) — audit shows dashboard_pulse heartbeat already drives live refresh across all major surfaces.

  Total **20 commits**, **~13 buglist items closed** with code, **5 items annotated** for follow-up, **~20 new tests** across 6 test files. Working tree clean at branch tip.

* **[2026-05-20 to 2026-05-25 — L202/L56/L58 Trio + Sibling-Wipe Class-Bug Fix]** — Branch `feature/buglist-trio-2026-05-20`, **11 commits**, started as a three-item standalone sweep (L202 Import collapse glitch, L56 Core One state widget, L58 filament-attributes bulk-editor) and grew into a deeper data-safety incident around the `extra`-dict PATCH pattern.

  **L202 — Import-from-External collapse sliver** (commit `902de80`) — Bootstrap's 350 ms `.collapsing` height animation was leaving the top child of the panel (the Source `<select>`) visible inside the animating window. Snap-collapse CSS in `global.css` (`#editfil-import-panel.collapsing { transition: none !important; height: 0 !important }`) + `overflow: hidden` on the panel itself. Regression coverage in `test_edit_filament_external_import.py::test_l202_collapse_leaves_no_sliver`.

  **L56 — Printer-state badge for dryer-box-less printers** (commit `0740f61`) — `_pulse_section_printer_status` now joins a direct `prusalink_api.get_printer_state` probe into the per-printer payload alongside the toolhead list. New `PRINTING / PAUSED / IDLE / OFFLINE` chip renders under each printer name in the Printer Status widget. Ticks live for Derek's Core One direct-feed setup where toolhead weight only updates after FilaBridge auto-deduct (which itself requires the dryer-box-mediated mapping). Also patched a latent dual-path bug: legacy sync-pulse-triggered `_aggregate()` was wiping the badge between bulk pulses; `refresh()` now merges cached state into fan-out rows. Regression coverage in `test_printer_status_widget.py` (2 new cases — payload includes state + badge survives sync-pulse cycle).

  **L58 — Filament-attributes manager UI** (commits `8666fa0` + `9be2ff2` + `7de6620` + `bdfa961`) — New Admin/Config → 🏷️ Filament Attributes panel restructured into two cleanly-delineated sub-cards:
    - **🛠 Choices Manager** (blue): per-choice usage chips with bright red ✕ buttons (schema-level remove with usage-aware confirm), green "＋ Add new tag" affordance, "🧹 Sweep unused" button that opens a styled `mountOverlay` dialog letting the user uncheck individual tags they want to keep before committing.
    - **📋 Per-Filament Bulk Editor** (yellow): free-text filter bar (id/vendor/material/name/attribute), select-all on filtered rows, bulk Add/Remove tag against selection.

    New backend endpoints: `GET /api/filament_attributes/report`, `POST /api/filament_attributes/bulk_set` (set-semantics merge), `POST /api/filament_attributes/add_choice`, `POST /api/filament_attributes/remove_choice` (DELETE+POST schema recreate with full-extras snapshot/restore), `POST /api/filament_attributes/sweep_unused` (two-step preview-then-commit with optional `choices: [subset]`). Tag icon fixed: bare `🏷` (text-style, inherits color) → `🏷️` (FE0F variant selector, emoji-style, distinct color). `inv_config.js?v=1.0` cache-bust → `?v=1.5` so the new bundle actually loads on Derek's browser.

  **L58 follow-on: FLAG_CHOICES drain** (commit `764d0e8`) — Derek footgun avoidance. `FILAMENT_ATTRIBUTES_FLAG_CHOICES` was auto-promoting any choice with zero usage into the boot-time delete set. Re-adding a legit-but-rare choice (e.g. "For Infill") without tagging at least one filament before next restart silently re-stripped it. Drained to empty `frozenset()`. DELETE_CHOICES (true typos / dupes) still runs at boot — those values are unambiguously bogus. The two FLAG_CHOICES tests in `test_filament_attributes_auto_cleanup.py` monkeypatch a non-empty set in to keep the algorithm under coverage in case it's re-enabled.

  **🚨 Sibling-wipe class-bug** (commits `1127a21` + `07c9470`) — The big one. Three call sites (`sweep_unused`, `remove_choice`, `ensure_filament_attributes_cleaned`) were sending `requests.patch(.../api/v1/filament/<id>, json={"extra": {"filament_attributes": ...}})` after the DELETE+POST schema recreate. **Spoolman's PATCH on `extra` REPLACES the whole sub-document** — same gotcha CLAUDE.md flags for every other write surface. So every "restored" filament lost every sibling extra: `product_url`, `purchase_url`, `original_color`, `sample_printed`, `slicer_profile`, `sheet_link`, `nozzle_temp_max`, `bed_temp_max`, `spool_temp`, `price_total`. Smoking gun: prod 2026-05-19 19:41 UTC boot log line `🧹 Filament Attributes cleaned: removed [...] (112 filaments restored)` matches the count of filaments now in the attrs-only group. Fix in all three sites: snapshot the FULL extras dict per filament and PATCH the whole thing back with `filament_attributes` adjusted. Spoolman's replace-on-PATCH now preserves siblings because we send them in the payload. Regression coverage in `test_sweep_preserves_sibling_extras` (fails before fix, passes after). New meta-test `test_no_direct_extra_patch_in_production_code` scans every production .py file and fails CI if any `requests.patch` to `/api/v1/(filament|spool|vendor)` includes `"extra"` in its json — explicit `# noqa: spoolman-extra-patch` marker required for the two legit full-dict bypass sites. **Prod blast radius**: cross-referenced all 6 rotated hub.log files (68,605 lines, 2026-02-16 → today) — only ONE confirmed data-loss filament (`#122 Prusament Black (Carbon Fiber)` lost `bed_temp_max="120"` and `nozzle_temp_max="295"`). The other 108 attrs-only filaments had zero logged `extra.*` edits ever → likely never carried siblings to lose. **Prod restored**: `/api/update_filament` POST through prod's existing merge-safe helper put `#122`'s temps back; `filament_attributes` preserved by the merge. Prod was on commit `cfe00ed7` at the time of the wipe — won't recur on next boot because all DELETE_CHOICES and FLAG_CHOICES values are already gone from the schema (early-return at `spoolman_api.py:881-882`).

  **Sweep overlay UX** (commit `636bbba`) — Replaced browser `confirm()` with a `mountOverlay`-tier `confirm` styled dialog. Per-tag checkboxes (default all-checked), Select-all/Select-none, sweep-only-selected commit. Backend accepts optional `choices: [subset]` parameter, intersected server-side with the freshly-computed unused list (stale-client guard). Also fixed 9 places using Bootstrap's `.text-muted` (#6c757d sits at ~1.4:1 against the dark panel backgrounds — same WCAG-AA pitfall pinned in `inv_printer_status.js` / `test_contrast_guard`) → explicit `style="color:rgba(255,255,255,0.65-0.7)"`.

  **Test count**: 19 L58 + auto-cleanup + meta-test tests pass. Pre-existing flakes (6 tests in `test_printer_status_widget` + `test_contrast_guard`) that depend on a printer being IDLE-not-PRINTING during the sweep stay open in the buglist — confirmed not introduced by this branch (fail on `dev` parent too when Core One is actively printing).

* **[2026-05-14 to 2026-05-19 — Autonomous Buglist Sweep + Groups 17 + 18 + Config UI Scaffold]** — Branch `feature/buglist-sweep-2026-05-14`, **40 commits**, ~25 buglist items resolved across 6 days of mixed autonomous + interactive work. Started as a "keep yourself busy" sweep of doable standalone items; absorbed Groups 17 (Details / Queue / Wizard Polish) and 18 (Unknown Location + Audit Refinement) wholesale; finished with the L324 FilaBridge reconcile utility + a new Config / Admin modal scaffold for future config-system work. The buglist's inline `*(LNN — ... — DONE 2026-05-1X via feature/buglist-sweep-2026-05-14 ...)*` markers stay in place as searchable per-item references; the summary below groups them by surface for "what changed this branch" reading order.

  **Scan + overlay layer** — L40 (legacy QR `?` mid-stream → defer-then-cancel handler in `shortcuts_registry.js`; round 2 also covers QR payloads that START with `?` via a 120 ms timer that cancels on any subsequent character), L218 (`#fcc-shortcuts-overlay-backdrop` sibling at z-index 10000 catches outside clicks instead of falling through to whatever modal sat underneath), L26 follow-up (wizard now closes Spool/Filament details siblings via `window.hideAllDetailsModals` — addresses the 2026-04-29 lock-up race), L122 PARTIAL (scan-side `_confirmActivePrintScan` migrated to `mountOverlay({ tier: 'confirm' })`; manage-modal `_confirmActivePrintAssign` stays hand-rolled pending repro).

  **Version badge (L42)** — Hardcoded `VERSION = "v154.26..."` → mtime stamp → commit-SHA stamp via new `inventory-hub/_gen_build_info.py` standalone helper that parses `.git/HEAD` + `logs/HEAD` directly (no `git` binary required). App startup writes `inventory-hub/.build_info`; render re-reads on every dashboard load so post-commit hook updates show up without a docker restart. `setup-and-rebuild/install_build_info_hook.sh` installs a per-clone post-commit hook (tries `python` → `python3` → `py` to handle the Windows Microsoft-Store-shim trap). Removed `{{ version }}` from the `<title>` so it doesn't clutter the browser tab.

  **Buffer + scan logic** — L124 (toolhead scan with multi-spool buffer now assigns ONLY the topmost spool; `performContextAssign` accepts a `spoolIdsOverride` subset), L128 (revised: kept the "already verified" line in the Activity Log after the toast iteration turned out noisier), L130 (`perform_smart_move`'s GENERIC branch clears `physical_source` ghost trail when target isn't a toolhead — fixes "still shows as deployed" after a force-relocate), L251 (verified + closed via the new `test_multispool_dryer_box_scan_still_sends_full_buffer` regression).

  **Filament Details + Edit (Group 17)** — 17.1 both halves: ⚖️ Empty Spool + 🎨 Swatch Printed + 🏷️ Label Confirmed fact-card rows on Details modal; un-hid `needs_label_print` from the wizard's hide list so both flags surface as boolean checkboxes when editing a filament. 17.2: "Queue all active spools" no longer auto-opens the queue modal. 17.3: `🖨️ #N` queue-label chips render in the wizard's `#wiz-postcreate-actions` row in the modal footer (revised — chips previously lived in `#wiz-status-msg` and got clobbered by the 3 s "Make a change..." auto-prompt). 17.4: ➕ Add-new affordance in the legacy-ID duplicate picker, routed through `openNewSpoolFromFilamentWizard`. 17.5: empty-spool weight row uses `resolveEmptySpoolWeightSource` cascade so the inheritance badge says where the value came from.

  **Search (L209)** — Added `max_weight` filter (symmetric complement to existing `min_weight`); the two inputs share a single input-group with explicit `Min` / `Max` labels because the prior side-by-side `⚖️` boxes looked identical at a glance.

  **Activity Log (L286 + F/L184)** — Click-to-toggle pause on `#log-status` indicator; removed the hover-mouseenter/leave pause that was causing accidental pauses Derek didn't realize were active. New corner pill (`#fcc-log-pill`, bottom-right above the search FAB) flashes "N new" when unseen entries arrive; click opens a `mountOverlay`-based compact log overlay. Positioned to clear both the cmd-deck WEIGH QR and the search FAB on dashboard pages via `body:has(.cmd-deck)`.

  **Layout polish** — L216 (Feeds-editor slot-order radio buttons now use one consistent `→` glyph per button instead of mismatched arrows), gear-icon centering for the new ⚙️ Config button (flex-center + zero horizontal padding because the emoji em-box is left-biased).

  **Locations / Printer Status (L140, Group 18.1)** — L140: `inv_printer_status.js:_aggregate()` no longer filters out printers with zero bound dryer-box `slot_targets`; Core One now shows up on prod with `🔗 no bound slot` placeholders on its unbound toolheads instead of being mysteriously absent. Group 18.1 — virtual `UNKNOWN` location bucket: pinned at the bottom of `/api/locations`, yellow caution badge consistently applied at four surfaces (LocMgr table row type-badge, manage-modal title bar, spool-card location badge in search + buffer modes, Spool Details modal location pill — was rendering blue, now yellow `❓ Unknown` with click-through). Force-Location dropdown surfaces it; `logic.resolve_scan` accepts bare `UNKNOWN`. Skip-and-replace logic for any pre-existing on-disk `UNKNOWN` row Derek created manually.

  **Audit refinement (L154 / Group 18.2 A + B)** — Part A: on `CMD:DONE`, every spool expected at the audited location but not scan-confirmed gets moved to `UNKNOWN` with `extra.fcc_pre_audit_location` planted as a recovery breadcrumb. `CMD:CANCEL` skips the auto-park entirely. Per-spool log entry + end-of-audit Missing/Extra summary both use `#N <brand material color>` display labels (bare IDs were unactionable). Part B: new `mountOverlay`-based visual panel auto-opens with the audit, polls `/api/audit_session` every 2 s (hash-skip on unchanged payload so it doesn't redraw idle), shows tile grid with ✅ scanned / ⬜ not scanned / ⚠️ rogue states. Two explicit action buttons + scannable QR codes: "✅ Done & Auto-Park" (CMD:DONE) and "❌ Cancel Audit" (CMD:CANCEL). AUDIT deck-button toggle now sends `CMD:CANCEL` on bail-out (was `CMD:DONE`, which silently auto-parked on accidental second click). Multiple supporting fixes: `fcc_pre_audit_location` added to `REQUIRED_SPOOL_EXTRAS` (self-heal at startup; fixes the 2026-05-16 prod 400 error), `CMD:CANCEL` parser added to `logic.resolve_scan`, generateSafeQR called by bare name (not `window.generateSafeQR` — typo bug that silently dropped the QR generation), `#focus-guard` z-index bumped to 30000 so it always wins against the mountOverlay tiers. 30-minute idle-timeout watchdog (`_check_audit_idle_timeout()`) auto-cancels abandoned sessions on the next `/api/logs` heartbeat — covers the closed-tab / crashed-browser / mid-audit-restart case where the panel used to keep auto-opening forever.

  **Config / Admin modal scaffold (L324)** — New ⚙️ button in the nav bar opens `configModal` (Bootstrap modal-xl, scrollable). First inhabitant: FilaBridge ↔ Spoolman Reconcile tool. `GET /api/filabridge/reconcile` walks FB's `/status`, cross-checks every non-zero toolhead mapping against Spoolman's per-spool `location`, returns mismatches with both views. `POST /api/filabridge/reconcile/apply` resolves one row with action `trust_spoolman` (clears FilaBridge) or `trust_filabridge` (rewrites Spoolman, preserving system-managed extras). Second card: read-only Build Info (commit SHA + timestamps from the L42 stamp). Card-pattern scaffold so future sections drop in without UI rework.

  **Filament Attributes auto-cleanup (L271)** — `setup-and-rebuild/migrate_filament_attributes.py` is the generalizable standalone tool for cleaning choice-type fields (DELETE_CHOICES + FLAG_CHOICES sets at the top are the only constants to edit). Round 2 (Derek 2026-05-19: "can this just run on its own first run") — the same logic now lives in `spoolman_api.ensure_filament_attributes_cleaned()` and is called from app startup right after `ensure_required_extras()`. Idempotent (one field-def GET + early-return when nothing to clean); first boot on a dirty Spoolman runs the cleanup with FLAG_CHOICES safety net (keep on any usage, auto-promote on zero). Defensive: zero-filaments-returned skips (transient state guard); network errors swallowed (never crashes startup); Activity Log entry on actual changes.

  **Dev infrastructure** — L293 (FCC_DEV env var opts in to werkzeug auto-reload; prod unchanged by default), test-isolation hardening (`reset_dom_state_js` + DOMContentLoaded waits added to new e2e tests).

  **B + H investigation note (L28 + L122)** — Updated the buglist's "🔬 NEXT REPRO — capture before refresh" block with explicit DevTools capture instructions. Derek's 2026-05-18 8:35 PM repro added to the open log (force-removed an extra filament from a 2/1 dryer box; spool was incorrectly marked deployed to Core1-M0 when FilaBridge was correct; has screenshots to dig out).

  Total test coverage added: **~80 new tests across ~25 new test files**, plus several existing test files extended. Major new files: `test_shortcuts_overlay_question_mark_at_start_of_scan_does_not_trigger`, `test_build_version_badge.py` (3), `test_filament_sample_label_status.py`, `test_filament_empty_spool_weight_row.py`, `test_filament_queue_all_no_auto_open.py`, `test_wizard_queue_label_chips.py`, `test_duplicate_picker_add_new.py` (2), `test_search_max_weight.py` (3), `test_toolhead_scan_single_spool.py` (2), `test_unknown_location_bucket.py` (4), `test_audit_auto_park_unknown.py` (12 inc. idle watchdog), `test_audit_session_endpoint.py` (3), `test_audit_visual_panel.py`, `test_filabridge_reconcile.py` (8), `test_config_modal_smoke.py` (2), `test_log_sticky_pause.py` (2), `test_filament_attributes_auto_cleanup.py` (7), `test_wizard_closes_details_siblings.py` (2), `test_active_print_scan_overlay.py` (2), `test_wizard_surfaces_label_and_sample_flags.py`.


* **[2026-05-14 Group 10 Session C — Wizard UX Overhaul (10.1 Aggressive Cleanup Pass)]** — Branch `feature/wizard-cleanup-aggressive`. The final remaining Group 10 item (10.1, Buglist L41 "wizard is too complicated…clunky…needs modernization") shipped across **9 commits and 6 user-feedback rounds**, completing Group 10 in full. The wizard's 3-step linear form was wrapped in **7 Bootstrap `.collapse` panels** (Color, Physical Specs, Print Temperatures, Custom Filament Attributes, Weight & Scale, Pricing & Metadata, Custom Spool Attributes) — every panel defaults collapsed in both create and edit modes, with always-visible summary chips on each toggle that show populated values at a glance. The chips use `getFilamentStyle` / `makeSwatchHtml` (same as the printer-status toolhead pattern) so multi-color filament swatches render as gradients. All locked-in field IDs and combobox DOM contracts preserved; placeholder copy unchanged.

  - **Foundation (commits e2a657c, 444f98d, b7dfe9b)** — Three-commit incremental rollout: (1) `.fcc-wiz-section-toggle` CSS scaffolding alone (no behavior change, baseline still green); (2) HTML restructure wrapping Step 2/3 field clusters in `.collapse` panels (all default `.show` so existing tests still pass — contract check); (3) JS `wizardApplyCollapseDefaults(context)` + per-section summarizers + collapse-state defaults wired into all four entry points (`openWizardModal`, `openEditWizard`, `openCloneWizard`, `openNewSpoolFromFilamentWizard`). Bulk-qty bar moved out of the Step 3 `<h5>` into the always-flat "Quantity & Location" row; `qtyBox.parentElement.style.display` traversal in `wizardReset` / edit-mode still works (new wrapper preserved `d-flex` class).

  - **Round 1 (f8495b5) — 7 feedback items**: (1) `original_color` extra relocated into the Color panel via `data-extra-key` stamping in `wizardGenerateFieldHTML` + `wizardRelocateOriginalColorField` post-render hook; (2) `shore_hardness` extra gated by material — only renders when material starts with TPU/TPE/TPC/FLEX (`wizardApplyShoreHardnessGate`, fires on material input/change/blur); (3) keyboard focus indicator: inset 2px cyan ring on `:focus-visible`; (4) multi-color direction field wrapped in labeled `#wiz-fil-color-direction-wrapper` with "(only with 2+ colors above)" hint; (5) Prusament-prefixed labels renamed to generic "Manufacturing Date" / "Length (m)" — `ensure_extra_field` extended to PATCH existing fields' name when canonical drifts, log confirmed `'Prusament Manufacturing Date' → 'Manufacturing Date'` at startup; (6) auto-scroll on `shown.bs.collapse` so the just-expanded panel lands at viewport top; (7) compact value-summary chips on each toggle (color: swatch + name + hex; temps: "Nozzle 215–240° · Bed 60–80°"; weight: "920g left · 80g used · net 1000g · tare 250g"; metadata: price/archived/link/comment; extras: "N fields set").

  - **Round 2 (db28f76) — 6 feedback items**: (1) `wizardRelocateOriginalColorField` clears `slot.innerHTML = ''` before append (was piling up on re-open); (2) `wizardApplyCollapseDefaults` switched to direct class manipulation (no Bootstrap animations) + `modalBody.scrollTop = 0` reset so edit mode no longer cascades through 5+ animated expansions; (3) smart-scroll on user expansion only fires when panel overflows the modal viewport (compares `getBoundingClientRect()`); (4) section title styling bumped — 1rem (was 0.85rem), cyan `#00d4ff` to match wizard headers, weight 600; (5) summary chip styling brighter `#d0d0d0`, 0.9rem, `border-left` separator; (6) **Shift+E / Shift+C** keyboard shortcuts for expand-all / collapse-all, modal-scoped capture-phase keydown, registered with `window.registerShortcut` under "Wizard" scope.

  - **Round 3 (eae9d63) — 6 feedback items**: (1) wizard shortcuts visible in `?` overlay — root cause was script load order (`inv_wizard.js` line 12, `shortcuts_registry.js` line 34); fix polls for `window.registerShortcut` (100ms × 50 ticks) before registering; (2) help-overlay Escape no longer bubbles to close the wizard underneath — handler moved to capture-phase with `stopImmediatePropagation`; (3) Enter/Space on focused toggle expands it — explicit keydown handler added since Bootstrap's default didn't fire reliably; (4) modal frame jumps eliminated — removed `modal-dialog-centered` and pinned dialog to `margin-top: 1.5rem` via `.fcc-modal-wizard-dialog`; (5) chips always render (dropped the "hide when expanded" gate, removed `▸` prefix); (6) Physical Specs got a summarizer; `wizardReset` restores the HTML factory defaults (`#wiz-fil-diameter = 1.75`, `#wiz-fil-density = 1.24`) that the bulk `input[type="number"]` wipe was clearing.

  - **Round 4 (57a61fc) — 2 feedback items**: (1) Enter/Space double-fire eliminated — keydown handler tracks `lastKbAt` per button; capture-phase click listener drops any native (`isTrusted`) click arriving within 300ms of a synthetic one; (2) edit mode no longer auto-expands populated panels — `hasAnyValue`-based smart-expand dropped entirely, so both create and edit start fully collapsed. Three of my own tests (`test_edit_with_price_auto_expands_metadata_panel` etc.) rewritten to assert "panel stays collapsed AND chip reflects the populated value"; `test_wizard_auto_prefill_empty_weight.py` got a `_expand_weight_panel()` helper that opens the Weight & Scale panel before badge-visibility checks.

  - **Round 5 (5b77b9e) — 2 feedback items**: (1) edit-mode "everything open then collapse" flash eliminated — `wizardReset` now strips stale `.show` / `.collapsing` / `aria-expanded` from all optional panels synchronously before `modal.show()`, so previous-session expand state (especially from Shift+E) doesn't bleed into the new session; (2) Physical Specs keyboard flicker fixed — switched from `btn.click()` (which routes through Bootstrap's data-bs-toggle handler) to `bootstrap.Collapse.getOrCreateInstance(panel).toggle()` directly, bypassing the click handler entirely. Even if a native click leaks through, Bootstrap's `_isTransitioning` lock blocks the redundant toggle. Debounce bumped 300→400ms.

  - **Round 6 (0221036) — 2 feedback items**: (1) Physical Specs demoted from `'always-open'` to `'optional'` for consistency — chip shows `1.75mm Ø · 1.24 g/cm³` at a glance, user expands only to deviate from defaults; (2) Escape now works immediately after editing from a spool/filament details modal — `shown.bs.modal` listener force-focuses the first focusable element inside `wizardModal`, overriding Bootstrap's focus management which had been leaving focus on the launcher button in the underlying modal. Probe confirmed: focus parent flips from `spoolModal` to `wizardModal`, single Escape dismisses only the wizard.

  - **Visual baselines** — deferred per user direction: capture only after final-look approval to avoid baseline churn during the polish rounds.

  - **Test impact** — all 30 wizard tests pass. The 1 occasional `test_wizard_manual_creation` failure is the pre-existing first-test session-warmup flake (passes in isolation; verified to fail on clean `dev` too). Three smart-expand tests rewritten to match new collapse-everywhere policy; `test_wizard_collapse_ux.py`'s `ALWAYS_OPEN_PANELS` list emptied (Physical Specs moved into `OPTIONAL_PANELS`).

* **[2026-05-14 Group 10 Session B — Wizard UX Overhaul (Field-Level Fixes, Validation, Nested-Swal Audit)]** — Branch `feature/wizard-session-b`. Five items shipped (10.4, 10.5, 10.6, 10.8, 10.9); Group 10 remains `PARTIAL` because 10.1 (aggressive progressive-disclosure cleanup) deferred to its own future branch per Derek's choice — the layout overhaul will get its own visual-baseline recapture cycle. New tests across **5 new test files / 36 new tests, all passing** ([test_wizard_field_order.py](inventory-hub/tests/test_wizard_field_order.py), [test_wizard_overlay_migration.py](inventory-hub/tests/test_wizard_overlay_migration.py), [test_choice_validation_unit.py](inventory-hub/tests/test_choice_validation_unit.py), [test_wizard_add_choice_guards_e2e.py](inventory-hub/tests/test_wizard_add_choice_guards_e2e.py), [test_wizard_slicer_autoassign_e2e.py](inventory-hub/tests/test_wizard_slicer_autoassign_e2e.py), [test_wizard_purchase_link_e2e.py](inventory-hub/tests/test_wizard_purchase_link_e2e.py)). Broader wizard sweep stayed clean — the 4 `test_wizard_auto_prefill_empty_weight` failures + 2 `test_wizard_per_spool_scan_e2e` failures were verified to fail on the clean `dev` baseline too (pre-existing flakes; not caused by Session B).

  - **10.6 — Stable wizard extras ordering** — Spoolman's `/api/v1/field/{entity}` response has no `order` key, so the wizard's existing `.sort((a,b) => (a.order||0) - (b.order||0))` ([inv_wizard.js:840-859](inventory-hub/static/js/modules/inv_wizard.js)) was a no-op and fields shifted whenever Spoolman re-emitted its array (e.g. after `update_extra_field_choices` was called by an add_choice POST). Fix: `FIELD_ORDER` dict in [app.py](inventory-hub/app.py) keyed by `'filament'`/`'spool'` plus a `_enrich_field_order(entity, fields)` helper that stamps each field dict with `order = FIELD_ORDER[entity].index(key)` (unknown keys pin to `FIELD_ORDER_UNKNOWN = 9999`). `api_external_fields` calls the helper for both entity types before returning. Wizard side requires zero changes — the existing sort step just starts working. **Deviation from plan**: `FIELD_ORDER` lives in `app.py` (next to the consumer), not `setup_fields.py` — the latter is a one-shot setup script in `setup-and-rebuild/` and isn't imported at runtime. Test guard in [test_wizard_field_order.py:test_field_order_covers_all_setup_fields_py_definitions](inventory-hub/tests/test_wizard_field_order.py) hardcodes the expected key sets so drift between `setup_fields.py` and `FIELD_ORDER` is caught.

  - **10.8 — Nested Swal → mountOverlay** — All three `Swal.fire` call sites in `inv_wizard.js` migrated to `window.mountOverlay()`:
    - **Unsaved-changes confirm** (was the cmd-deck scrollbar-shift offender): `hide.bs.modal` listener now mounts `#fcc-wiz-unsaved-changes` with `host: wizEl` so the overlay auto-cleans on host modal close. Bootstrap's `.modal-open` + SweetAlert's `.swal2-shown` body-class scrollbar comp no longer fights, and the `.cmd-deck`'s `margin-top:auto` no longer jitters when the prompt opens.
    - **`wizardPromptFieldSync`** — was a Swal with `input: 'select'`; now a `mountOverlay` with a native `<select>`, Enter-to-commit, dark-theme styling matched to the wizard.
    - **`wizardPromptNewChoice`** — was a Swal with `input: 'text'` + `preConfirm` fetch + `Swal.showValidationMessage`; now a `mountOverlay` with inline validation message, live canonical preview, and **stateful in-place content swaps** (`data-stage="input" → "suggestion" → "confirm"`) rather than nested overlays. Nested-overlay Escape race avoided: capture-phase Escape listeners on `document` fire in registration order (oldest wins), so a nested CONFIRM-tier overlay couldn't reliably reclaim Escape from its STANDARD-tier parent — content swaps keep the workflow inside a single overlay element. Regression-guard test in [test_wizard_overlay_migration.py:test_no_more_swal_fire_in_wizard_js](inventory-hub/tests/test_wizard_overlay_migration.py) fails loudly if any future change reintroduces a non-comment `Swal.fire` in the wizard JS.

  - **10.9 — Add-new-attribute validation guards** — New shared module [choice_validation.js](inventory-hub/static/js/modules/choice_validation.js) exposing `window.normalizeChoice`, `window.levenshtein` (inline O(m·n) DP, ≤30 LOC, no new deps), and `window.validateNewChoice(raw, existingChoices)` returning `{ok, canonical, error?, suggestion?}`. Five guards: (1) min length ≥3 chars, (2) leading/trailing punctuation rejection (`; , : /`), (3) fuzzy/prefix/normalized-key match suggestion (case-insensitive equality → suggest existing; `_normKey` matching catches `Carbon-Fiber` ↔ `Carbon Fiber`; prefix-of catches `Tran` → `Transparent`; Levenshtein ≤2 catches single-char typos), (4) two-step confirm overlay before committing a brand-new value, (5) live canonical preview ("Stored as: …") rendered below the input as the user types. Hooked into THREE entry points: the `wizardPromptNewChoice` overlay (with the content-swap stages from 10.8), `wizardAddMultiChoiceChip`'s silent blur-commit path (rejects with toast hint instead of UI overlay since the user isn't expecting a dialog on blur), and `inv_details.js:addAttrChip` for parity with the details-modal Edit Filament path. Module registered in [scripts.html](inventory-hub/templates/components/scripts.html) right after `overlay_mount.js`. Unit tests in [test_choice_validation_unit.py](inventory-hub/tests/test_choice_validation_unit.py) (20 tests covering normalize, Levenshtein, and every validate branch); E2E coverage in [test_wizard_add_choice_guards_e2e.py](inventory-hub/tests/test_wizard_add_choice_guards_e2e.py) (6 tests: single-char rejection, leading-punctuation rejection, fuzzy suggestion stage, two-step confirm, cancel-on-confirm-step preserves no-POST contract, live canonical preview).

  - **10.5 — Slicer profile auto-assign in wizard** — New `window.wizardOnNewChoiceAdded(apiEntity, key, value, info)` hook fires from `wizardPromptNewChoice`'s post-submit path AFTER `wizardFetchExtraFields()` re-renders the field schema (chained off the returned promise so the new `<option>` exists before we try to pick it). The hook walks to `wiz_${entityPrefix}_ef_${key}` and, if it's a `<select>`, sets `value` + dispatches a bubbling `change` so the wizard's dirty-tracker fires. Also handles the "Use existing" suggestion branch (`wasNew: false`) so the suggested existing value gets selected too, not just newly-added ones. Mirrors the existing pattern in [inv_details.js:promptEditSlicerProfile](inventory-hub/static/js/modules/inv_details.js) on the details-modal side. E2E test in [test_wizard_slicer_autoassign_e2e.py](inventory-hub/tests/test_wizard_slicer_autoassign_e2e.py): stubs add_choice + the schema refresh so the test doesn't pollute Derek's dev Spoolman with throwaway slicer profile names, then asserts the new option exists AND is pre-selected AND `wizardState.isDirty === true`.

  - **10.4 — Purchase link consolidation + clear-bug fix** — `purchase_url` removed from the wizard's filament-tab dynamic extras (added to the existing skip list at [inv_wizard.js:855](inventory-hub/static/js/modules/inv_wizard.js)); user now edits the spool-tab `wiz-spool-purchase_url` exclusively. New `wizardApplyPurchaseLinkFallback(spoolUrl, filamentUrl)` helper implements smart fallback: spool's own URL fills the input + default placeholder; if no spool override but filament has a URL, input stays empty and placeholder advertises `<truncated-filament-url> (inherited from filament)` so the user sees what details-modal will fall back to without inadvertently upgrading the inherited link to a spool-level override on save. Clone and edit paths both routed through the helper. Import path now writes the parser-scraped URL to the spool input (visible to user pre-save) instead of the now-removed filament-tab field. Hint copy in [modals_wizard.html](inventory-hub/templates/components/modals_wizard.html) updated to `(Optional — falls back to the filament's link if left blank)`. Clear-bug root cause: `wizardReset`'s selector at [inv_wizard.js:200](inventory-hub/static/js/modules/inv_wizard.js) was `input[type="text"], input[type="number"]` — `wiz-spool-purchase_url` is `type="url"`, so the reset missed it and the prior session's URL bled into the next wizard open. Fixed by extending the selector to include `input[type="url"]` AND explicitly resetting the placeholder to the static default (so the inherited-from-filament ghost text doesn't bleed either). E2E coverage in [test_wizard_purchase_link_e2e.py](inventory-hub/tests/test_wizard_purchase_link_e2e.py): asserts filament tab no longer renders the duplicate, exercises all three fallback branches plus URL truncation, verifies `wizardReset` clears both value and placeholder, and confirms the save payload only includes `purchase_url` when the user actually typed.

  - **10.7 — Multi-spool creation regression guard** — Re-verified (no code change). Session A's existing `test_wizard_per_spool_scan_*` suite still produces N spools after Session B's combobox / validation / overlay-migration changes. The 2 pre-existing flakes in that suite (`test_per_spool_scan_splits_material_into_base_plus_attribute_chips` and `test_backfill_silent_patch_fires_on_autoswitch`) were confirmed via `git stash`-and-rerun baseline to fail on clean `dev` too — not caused by Session B.

  - **Deferred to Session C (or its own branch)** — 10.1 (aggressive progressive-disclosure cleanup) — Derek explicitly chose "Aggressive — full progressive disclosure across all 3 steps" during planning. Substantial layout overhaul touching `modals_wizard.html` step structure plus a CSS pass for `<details>`/`<summary>` styling plus a visual-baseline recapture at 1600×1300. Recommended next move: start `feature/wizard-cleanup-aggressive` once `feature/wizard-session-b` lands on `dev`. Spec preserved in [10-wizard-ux-overhaul.md](docs/agent_docs/tasks/10-wizard-ux-overhaul.md).

* **[2026-05-14 Group 10 Session A — Wizard UX Overhaul (Flow & Defaults)]** — Branch `feature/wizard-ux-overhaul`. Five items shipped (10.2, 10.3, 10.7, 10.10, 10.11); Group 10 moved from `READY` → `PARTIAL`. Session B remains (10.1, 10.4, 10.5, 10.6, 10.8, 10.9) for a future branch — items still annotated in [10-wizard-ux-overhaul.md](docs/agent_docs/tasks/10-wizard-ux-overhaul.md). New tests in [test_wizard_group10_session_a.py](inventory-hub/tests/test_wizard_group10_session_a.py) — 6/6 passing; broader wizard sweep stayed at 112 passed with only pre-existing flakes remaining (verified via `git stash`-and-rerun baseline).

  - **10.2 + 10.10 — Location combobox UX** — `wizardBindCombobox` ([inv_wizard.js:285](inventory-hub/static/js/modules/inv_wizard.js#L285)) now renders the FULL options list on focus (and on click-while-already-focused, which the old code missed because `mousedown.preventDefault` keeps focus on the input after picking an item) instead of treating the previously-selected label as a filter query. The currently-selected row gets the `.active bg-primary` class on focus-render and scrolls into view so Enter re-confirms it without re-picking. Typing still switches to filtered render; clearing the input restores the full list. Mirror fix in `promptEditLocation` Force-Location dialog ([inv_details.js:801](inventory-hub/static/js/modules/inv_details.js#L801)) — on search-input refocus, the filter is cleared and all items become visible again so a second pick after typing isn't blocked by the prior query.

  - **10.3 — Default Unassigned for new spools** — Two complementary touches: defensive empty-location coercion in `api_create_inventory_wizard` ([app.py](inventory-hub/app.py)) so any future caller that drops the `location` field falls through to `''` (which `spoolman_api.create_spool`'s existing UNASSIGNED coerce already maps to an unassigned spool); plus placeholder copy now reads `Unassigned (default)` on both [modals_wizard.html](inventory-hub/templates/components/modals_wizard.html) and the `wizardBindCombobox` placeholder option ([inv_wizard.js](inventory-hub/static/js/modules/inv_wizard.js)) so users see they don't have to pick anything. Dropdown item label `-- Unassigned --` left intact (referenced by `test_wizard_ux_polish.py` and `test_manual_loc_override_e2e.py`).

  - **10.7 — Multi-spool creation regression guard** — No code change required. Confirmed via the existing `test_wizard_per_spool_scan_e2e.py` + `test_wizard_per_spool_scan_unit.py` suite that the qty-driven `wizardState.spoolRows` sync still produces N spools after the combobox / cancel-restore changes. Both pre-existing flakes in `test_wizard_per_spool_scan_e2e.py` (`test_per_spool_scan_splits_material_into_base_plus_attribute_chips` and `test_backfill_silent_patch_fires_on_autoswitch`) were verified to fail on the dev baseline too — not caused by Session A.

  - **10.10 — Show full list after selection (other location search boxes)** — Covered together with 10.2 above; same `wizardBindCombobox` fix benefits every location combobox the wizard hosts. `promptEditLocation` mirror covers the Force-Location surface.

  - **10.11 — Wizard cancel restores correct launch surface** — Both `openEditWizard` ([inv_wizard.js:2110](inventory-hub/static/js/modules/inv_wizard.js#L2110)) and `openCloneWizard` ([inv_wizard.js:1957](inventory-hub/static/js/modules/inv_wizard.js#L1957)) previously used `if (filModal.show) { setFilamentReturn } else { setSpoolReturn unconditionally }` — when launched from the search FAB or Location Manager (no details modal open at all), the `else` branch still set `returnToSpoolId`, so cancel popped a spurious spool details modal. Fix introduces an explicit `else if (spoolModal.show)` arm; when neither details modal is visible the launcher is a non-details surface (dashboard FAB, Location Manager Edit, ui_builder grid cards, global search Edit), and both return-ids stay null so the `hidden.bs.modal` listener closes silently. Preserved: launches from spool / filament details modals still pop the source modal back on cancel.

  - **Bottom-bar shift symptom** — Surfaced during Derek's review: opening the unsaved-changes Swal at [inv_wizard.js:92](inventory-hub/static/js/modules/inv_wizard.js#L92) shifts the `.cmd-deck` bottom bar down because Bootstrap's `.modal-open` and SweetAlert's `.swal2-shown` both add body-level scrollbar compensation independently and the flex column at [global.css:128](inventory-hub/static/css/global.css#L128) (which uses `margin-top:auto` to pin the deck) follows the body's effective-height change. Not a regression from Session A; documented in 10.8's task entry as a known symptom that goes away when the Swal migrates to `mountOverlay`.

* **[2026-05-13 Group 11 — External Parsers & Prusament Cleanup]** — Branch `feature/external-parsers-cleanup`. All three sub-items shipped; touched-area sweep stayed green: **40 passed / 0 failed** across `test_filament_merge` (7 new), `test_external_parsers` (8), `test_delete_helpers` (11), and `test_overlay_mount_helper` (14).

  - **11.1 — Wizard "Import from External" audit** — Added a module-level docstring to [external_parsers.py](inventory-hub/external_parsers.py) enumerating the plugin contract, every parser's current status (spoolman ✅ keyword search via Spoolman's aggregated `/api/v1/external/filament`; prusament ✅ URL-driven and powers the Step-3 per-spool scan; amazon ⚠️ requires both `SCRAPER_API_KEY` and `beautifulsoup4` — silently no-ops without them; 3dfp ⚠️ scraper exists but unverified; open_filament 🔲 stub returning []), a "Currently supported external sources" checklist, a "Gaps tracked for future work" list (open-filament-database, Open Print Tags, Onlyspoolz, purchase emails, generic vendor pages), and a 5-step runbook for adding new parsers. The wizard dropdown in [modals_wizard.html](inventory-hub/templates/components/modals_wizard.html) now shows an explicit Prusament URL option (was previously only reachable via auto-detect on URL paste), labels Amazon as "(needs API key)", and relabels 3DFP as "(untested)" — both still selectable but no longer pretending to be in active rotation. A `title=` tooltip on the picker notes that URL paste is auto-routed regardless of the dropdown choice.

  - **11.2 — Merge duplicate filaments UI** — Closes the gap left by the 2026-04-26 duplicate-prevention work (tier-1 product-id matcher + duplicate-picker UI), which prevents *new* duplicates but never gave Derek a way to clean up the ones already in the DB. New endpoint `POST /api/filament/<src_fid>/merge_into/<dst_fid>` in [app.py](inventory-hub/app.py) re-parents every child spool (including archived, queried directly via Spoolman's `?allow_archived=true` since `get_spools_for_filament` only returns active spools) onto the target via `update_spool_or_raise(sid, {"filament_id": dst_fid})` — per-spool error capture, aborts before deleting the source if any single re-parent rejects so partial state stays recoverable, activity-logs both the success path (`🔗 Merged Filament #src → #dst (N spools re-parented; source deleted)`) and every per-spool rejection. Source-delete-failure-after-successful-reparents path surfaces `LAST_SPOOLMAN_ERROR` in the response body so the toast can show the real reason. Frontend entry point: new `🔗 Merge into another filament…` item in the gear dropdown on the Filament Details modal ([modals_details.html](inventory-hub/templates/components/modals_details.html)), sibling to the existing Delete. `_showMergeFilamentFlow` and `_showMergeConfirmStep` in [inv_details.js](inventory-hub/static/js/modules/inv_details.js) drive the two-step UX: Step 1 is a target picker overlay with a datalist of every filament (sorted by ID desc so newer canonical filaments bubble up; `#42 — Brand Material Color` labels; direct `#42` numeric entry also accepted) and Step 2 is a type-`MERGE` confirm overlay showing source label, target label, and spool count. Both overlays route through `window.mountOverlay()` per project convention (`host: filamentModalEl` for cascade close, `tier: 'standard'` then `'confirm'` so they stack correctly, `initialFocus` on the input, Enter-to-submit on both inputs), surface Spoolman error bodies on rejection at 7-second toast duration, and emit `inventory:sync-pulse` on success so every dependent surface refreshes. The two-step pattern uses sequential overlay lifecycles (Step 1 fully cleans up before Step 2 mounts) instead of nested overlays.

  - **11.3 — External import source documentation** — Folded into 11.1's module docstring rather than adding a separate doc surface. The "Currently supported external sources" and "Gaps tracked for future work" sections enumerate what's in the box today (Spoolman aggregated DB, Prusament per-spool URLs, conditional Amazon, untested 3DFP) and what's outstanding (open-filament-database, Open Print Tags NFC, Onlyspoolz, purchase emails, generic vendor pages). The runbook block tells a future maintainer exactly which five surfaces a new parser has to touch: parser subclass, PARSERS registry, `wizardSearchExternal` auto-detect branch, dropdown `<option>` in `modals_wizard.html`, and coverage in `test_external_parsers.py`.

  - **Test coverage** — New [test_filament_merge.py](inventory-hub/tests/test_filament_merge.py) (7 tests, all mocked Spoolman): same-source-and-target rejection (400), missing source / missing target (404), happy path verifies every child re-parented onto target then source deleted, partial re-parent failure aborts before source-delete (critical atomicity check — partial state stays recoverable), source-delete-failure-after-full-reparent surfaces Spoolman error in response body, and zero-children edge case still cleanly deletes the orphan filament.

* **[2026-05-13 Group 9 — Quick-Swap Grid Enhancements & Printer Status Widget]** — Branch `feature/quickswap-grid`. All three sub-items shipped plus several iterative polish rounds based on Derek's review feedback. Group 9 surface stayed green: **101 passed / 0 failed** across `test_printer_status_widget` (6), `test_quickswap_visual` (4), `test_quickswap_ui_e2e` (9), `test_contrast_guard` (8), `test_quickswap_deposit_and_header` (4), `test_quickswap_printer_pool` (4), `test_quickswap_api` (11), and `test_dryer_bindings` (56).

  - **9.1 — Denser `'quickswap'` `SpoolCardBuilder` variant** — Filled toolhead-bound slots in the Quick-Swap grid now render via a new `'quickswap'` variant on `SpoolCardBuilder` ([ui_builder.js](inventory-hub/static/js/modules/ui_builder.js)) with a real filament card: color frame, brand/material, name, weight, and a tight action footer (🔍 details / ✏️ edit / 🖨️ print queue / ⏏️ eject). The outer card is a focusable `<div>` with `tabindex=0` + `.fcc-qs-slot` class + all `data-box`/`data-slot`/`data-toolhead` attributes preserved, so existing keyboard nav, Playwright selectors, and `quickSwapTap` handler keep working unchanged. The original empty-slot and buffer-deposit affordances stay as the lightweight buttons they are today (they aren't spools). Visual baselines refreshed.

  - **9.2 — Printer Pool banner row** — Backend extended: `locations_db.get_bindings_for_machine` now returns a `printer_pool: [{box, slot}, ...]` field listing every dryer-box slot bound to the `PRINTER:<id>` sentinel that affiliates with the requested printer (was being silently dropped before). Frontend ([inv_quickswap.js](inventory-hub/static/js/modules/inv_quickswap.js)) renders a dedicated **🏭 Printer Pool** section after the toolhead-bound rows, distinct yellow/amber styling vs. the cyan toolhead chips. Pool slots are interactive: filled → opens spool details; empty + buffered spool → green deposit button (uses the same `LOC:BOX:SLOT:N` scan path so auto-deploy/Activity Log/FilaBridge sync all run exactly once); empty + no buffer → inert disabled button. Closes the four-step rollout begun 2026-04-23. Tests: `test_reverse_lookup_printer_pool_aggregation` + 4 Playwright tests in new `test_quickswap_printer_pool.py`.

  - **9.3 — Printer Status widget** — New dashboard widget ([inv_printer_status.js](inventory-hub/static/js/modules/inv_printer_status.js), mounted at the top of `dashboard.html`). Aggregates every printer with at least one bound toolhead and renders one row per printer with a stylized schematic — color-tinted toolhead blocks (full multi-color gradient frames via `getFilamentStyle`) with horizontal remaining-weight bars colored by stock level (cyan healthy → amber `<100g` → red `≤0g`). Click any toolhead block opens Quick-Swap focused on it. Auto-refreshes on `inventory:sync-pulse` with a fingerprint-based skip so unchanged state doesn't repaint. Per Derek's iterative review, the widget gained: ▲▼ reorder arrows next to each printer name (order persisted in `localStorage` under `fcc.printerStatus.order`); collapsible to a single inline chip strip in the header (one row, swatch + ID + weight per toolhead, vertical dividers between printer groups, in user's chosen printer order); horizontal divider between printer rows in expanded view; "Loading printer status…" skeleton in the dashboard markup so the layout space is reserved from the first paint (no sudden pop-in); auto-hides when no printers have bound toolheads. Visual baseline + 6 Playwright tests in new `test_printer_status_widget.py`. **Project Color Loadout integration explicitly out of scope** — V1 keeps the schematic generic so the future Loadout work can swap in printer photos / overlays without changing the underlying aggregation surface.

  - **Multi-color filament rendering bug fix** — Discovered during Derek's review: the new variants and widget swatches all used `String(color).split(',')[0]` which silently dropped every component except the first for multi-color filaments (Transition Spools, Chameleons). Same bug class as ones squashed before in the existing card paths. Added `makeSwatchHtml(color, direction, opts)` helper in [inv_core.js](inventory-hub/static/js/modules/inv_core.js) that routes through `getFilamentStyle` (the canonical gradient logic), exposed `window.getFilamentStyle` (it had never been exposed before), and migrated all 6 swatch sites — the `'quickswap'` variant chip swatch, the `'printer-status'` variant chip swatch, both buffer-deposit chip swatches in `inv_quickswap.js` (toolhead branch + pool branch), and the printer-status widget's toolhead block body background and mini-chip swatch. Coextruded / coaxial / CSV color strings now render as proper gradients everywhere.

  - **Contrast hardening** — Three `text-muted` Bootstrap usages on dark widget surfaces (~1.4:1 contrast against the dark bg) replaced with explicit `rgba(255,255,255,0.65)` light gray that meets WCAG AA. Pinned in [test_contrast_guard.py](inventory-hub/tests/test_contrast_guard.py) — added two new tests covering the Printer Status widget in both expanded and collapsed states so this exact recurring footgun can't slip back in there.

  - **Polish round** — Q-key visual focus indicator: the keyboard nav handler was correctly adding `.kb-active` to focused cards but the new variant `<div>` wasn't a Bootstrap button so there was no `:focus` ring; added explicit `outline: 3px solid #0dcaf0` + glow CSS so pressing `Q` produces an obvious cyan focus indicator. Q-key now also `scrollIntoView({behavior:'smooth'})`s the Quick-Swap section before focusing — previously the focus glow could appear off-screen when the section sat below the fold. Card seam removed: the `'quickswap'` variant's action footer originally rendered as a darker stripe due to a separate background over the parent's color frame; restructured to a single continuous gradient with just a thin horizontal divider between content and actions. Toolhead-block dark surround removed (`.fcc-ps-th` background → transparent, `padding: 0`) so the colored body fills the block edge-to-edge for maximum filament-color presence. Out-of-scope items surfaced during review filed back to `Feature-Buglist.md` (slot-order radio arrow inconsistency in the Feeds editor, shortcuts overlay click-through dismissing background modals).

  - **Test stabilization** — Six 3s/5s/6s `to_be_visible` and `wait_for_function` timeouts on async-render-dependent waits across `test_quickswap_deposit_and_header`, `test_quickswap_ui_e2e`, `test_contrast_guard`, and `test_printer_status_widget` bumped to 8s/12s. Same pattern as Group 14 stabilization — async render chain (`/api/printer_map` → `/api/machine/.../toolhead_slots` → per-box `/api/get_contents` → `grid.innerHTML`) can exceed the prior tight timeouts under sweep load.

* **[2026-05-12 Group 16 — Testing Hardening & Helper Consolidation]** — Branch `feature/testing-hardening-helpers` (19 commits). Wraps the post-Group-14 hygiene follow-ups so the next sweep-order shift can't re-introduce the same class of flakes. Full sweep stayed green throughout: **858 passed / 18 skipped / 0 failed**, up from 804 before the consolidation.

  - **16.1 — Parallel project-root `tests/` directory consolidated** (commit `bdbc22d`). The repo-root `tests/` dir wasn't in `pytest.ini`'s `testpaths`, so its 28 tests had been silently rotting outside the regression sweep. Audit + dispositions:
    - **Moved** with unique coverage (renamed for clarity): `test_state.py` → `test_state_log_colors.py` (add_log_entry color CSS); `test_logic.py` → `test_logic_undo.py` (perform_smart_move UNDO_STACK + buffer origin recording); `test_locations_db.py` → `test_locations_db_migration.py` (CSV→JSON migration); `test_filabridge_recovery.py` (kept name — no conflict); `test_frontend.py` → `test_frontend_source_presence.py` (grep-style frontend wiring guards).
    - **Deleted** as redundant: `test_spoolman_api.py` — `sanitize_outbound_data` is fully covered post-ALEX-FIX by `test_spoolman_serialization.py`. The stale `test_sanitize_outbound_data_json_strings` test reflected the pre-ALEX-FIX contract.
    - All moves drop the `sys.path.insert(..., '../inventory-hub')` shim (the inventory-hub conftest already puts it on sys.path) and fix relative paths to be local to the new location. **+26 tests now in the sweep.**

  - **16.2 — `open_manage_modal` factory fixture in conftest** (11 commits: `f6997fe` adds the fixture + 10 per-file migrations). The duplicated `_open_manage(page, base_url, loc_id)` helper lived in **10 e2e files** (5 more than the task spec called out — newer arrivals followed the same pattern). All migrated to a single fixture:
    - **Fixture (conftest.py):** `open_manage_modal(page, base_url)` — factory yielding a callable. Bakes in the canonical readiness wait Group 14.4 originated for `test_quickswap_visual`: `state.allLocations.length > 0` (the silent no-op condition `window.openManage` falls into when locations haven't loaded yet). Tests now call `open_manage_modal(loc_id)` and get the readiness wait for free.
    - **Migrated files** (1 commit each, bisect-clean): `test_feeds_section_visual.py`, `test_bind_slot_picker.py`, `test_contrast_guard.py`, `test_loc_mgr_bindings_ui_e2e.py`, `test_quickswap_visual.py`, `test_quickswap_ui_e2e.py`, `test_quickswap_deposit_and_header.py`, `test_return_and_breadcrumb.py`, `test_return_overlay_and_refresh.py`, `test_return_text_and_overlay_close.py`.

  - **16.3 — `reset_dom_state_js` adopted in 6 more e2e files** (6 commits, 1 per file): `test_filament_new_spool_e2e.py`, `test_search_deployed_filter.py`, `test_search_e2e.py` (all 8 tests), `test_modal_focus_trap.py` (via the `_wait_app_ready` / `_wait_details_ready` helpers — passed through to keep callers clean), `test_ui_structural.py` (all 12 tests), `test_visual_baseline.py` (via the `_goto_dashboard` helper). Defensive cross-test pollution teardown is now in 10 e2e files total — every e2e file that opens a Bootstrap modal or offcanvas during setup.

  - **16.4 — Windows pip/pytest interpreter mismatch documented** (commit `51794ed`). Added a note to `CLAUDE.md`'s Testing section: bare `pip install <pkg>` resolves to `D:\Programming\Languages\Python\Python311\python.exe` on Derek's machine, but `pytest` runs under `C:\Python314\python.exe`. Canonical install: `"C:/Python314/python.exe" -m pip install <pkg>`. Bit the Group 14.5 BS4 install; future-Derek won't repeat the trap.

* **[2026-05-12 L37 — locations.json corruption hardening (1)+(2)]** — Branch `feature/locations-json-write-hardening` (1 commit: 6259f10). Two-piece hardening for the recurring "valid content + duplicate tail" `locations.json` corruption (first seen 2026-04-28, recurred 2026-05-11). Closes candidate hardening items (1) and (2) from the L37 buglist entry; (3) explicit truncate and (4) named Docker volume deferred pending whether the verify-after-write tripwire surfaces in prod hub.log.

  - **(2) Per-call unique temp filename.** `_write_locations_atomic` now uses `tempfile.NamedTemporaryFile(dir=DATA_DIR, prefix='locations.', suffix='.tmp', delete=False)` instead of a fixed `JSON_FILE + ".tmp"`. Concurrent Flask threads can no longer collide on the `.tmp` path. Test coverage: `test_save_locations_atomicity.py::test_temp_filename_is_unique_per_call` + `_concurrent_writes_do_not_corrupt_final_file` (12-thread barrier).

  - **(1) Post-write read-back-and-verify.** After every successful `os.replace`, `_verify_locations_file` immediately re-opens + `json.loads`es the target. On parse failure: critical log (`locations.json verify-after-write FAILED at <path>: <error> — file prefix: <first 4KB>`) + one retry. A still-failing retry emits a second critical line and returns without raising — caller already got "success" control flow. Test coverage: `test_save_locations_atomicity.py::test_verify_after_write_logs_critical_on_corruption` + `_no_retry_when_clean` + `_retry_failure_logs_critical`.

  - **Existing `test_tmp_file_is_in_same_directory_as_target` updated** to spy on `NamedTemporaryFile` instead of `builtins.open` (new code doesn't go through `builtins.open` for the temp file).

  Files: [inventory-hub/locations_db.py](inventory-hub/locations_db.py), [tests/test_save_locations_atomicity.py](inventory-hub/tests/test_save_locations_atomicity.py) (new), [tests/test_locations_atomic_write.py](inventory-hub/tests/test_locations_atomic_write.py). Sweep delta vs. Stage 2 (804/0/0): **809 passed (+5), 0 failed, 0 errors, 15 skipped**.

  **Deferred (still candidates if recurrence happens):** (3) explicit `os.truncate` belt-and-suspenders after replace, (4) move `data/` from host bind-mount to a named Docker volume.

* **[2026-05-12 Group 14 — Test-Sweep Flake Stabilization]** — Branch `feature/test-sweep-flake-stabilization` (11 commits: 00e18d1 14.5, ca2bd1b 14.4 panel + 4 baselines, 70e70f4 14.3, eb3261c 14.6, 9089f63 14.1, 776f4b5 14.2, 3cf548e 14.4 follow-up, 58c9086 conftest consolidate, 6a4380f ±1px tolerance + offcanvas scope, 7566d64 details/delete card scope, 20ec2d4 quickswap-visual state.allLocations wait). Closes the [PRIORITY] L4 test-sweep flake item — all 18 originally-failing tests resolved. Sweep delta vs. 2026-05-11 baseline (772/12/6): **804 passed (+32), 0 failed (-12), 0 errors (-6), 15 skipped**. Per-item:

  - **14.5 — `test_amazon_parser_matching`** — Added `beautifulsoup4>=4.12` to [requirements-dev.txt](requirements-dev.txt). Pinned on host so the parser test exercises the BS4 path; prod image stays lean (Dockerfile still doesn't ship bs4 — `external_parsers.py` already guards the import).

  - **14.4 — Visual baselines × 4** — Recaptured `quickswap-confirm-overlay.png`, `quickswap-grid-default.png`, `quickswap-grid-kb-active.png`, `shortcuts-overlay-default.png`. Plus Quick-Swap confirm overlay polish: panel constrained to `max-width:540px` and the Yes/No buttons re-laid into 133px/123px columns mirroring the QR row's flex so each sits centered under its QR (sub-pixel alignment per `getBoundingClientRect` measurement). `clean_buffer` fixture pinned on both quickswap-grid visual tests — leftover buffer state makes the empty-slot row swap to "Deposit from buffer" affordance and adds ~22px to the section height. `state.allLocations.length > 0` wait_for_function added to test_quickswap_visual's `_open_manage` to fix a separate sweep flake where openManage no-ops if locations haven't loaded.

  - **14.3 — `test_details_modal_interactions` offcanvas-backdrop click intercept** — Defensive cross-test pollution teardown promoted to `conftest.RESET_DOM_STATE_JS` exposed via `reset_dom_state_js` fixture (session-scoped, returns the JS string). Hides any `.offcanvas.show` / `.modal.show` via Bootstrap, drops orphan `.offcanvas-backdrop` / `.modal-backdrop` elements, removes mountOverlay-mounted overlays, closes any open SweetAlert, resets every `<select>` to `selectedIndex=0` + blurs it, blurs `activeElement`. Page-wide `.fcc-card-action-btn[title='View Details']` selector additionally scoped to `#offcanvasSearch` so `.first` can't pick a dashboard buffer card behind the backdrop.

  - **14.6 — `test_force_location_keyboard_e2e` × 7 + `test_manual_loc_override_e2e` × 2** — All 9 errored in the sweep, all passed in isolation. Same `reset_dom_state_js` fixture applied to both `force_location_modal` fixtures. Page-wide `.fcc-spool-card.first` also scoped to `#offcanvasSearch` (same dashboard-card-behind-offcanvas root cause as 14.3).

  - **14.1 — `test_delete_ui_e2e` × 5** — Same `reset_dom_state_js` applied to `_open_first_spool_modal` helper. Same `.fcc-card-action-btn` scope-to-offcanvas fix as 14.3.

  - **14.2 — `test_deposit_confirm_overlay_names_the_spool_and_toolhead` + `test_quickswap_refreshes_manage_view_after_yes`** — Real timing issues, not pollution. The first wasn't waiting long enough for `showConfirmOverlay`'s 3s active-print probe (`_probeWithTimeout` was a 3s ceiling in dev with no reachable PrusaLink, not 1s as the test assumed) — bumped overlay-visible timeout from 2s to 6s. The second used a flat 1200ms sleep for refreshAfterMove that wasn't enough — replaced with a `wait_for_function` poll (8s cap) on a planted sentinel disappearing.

  - **Cross-cutting infra**: `conftest.RESET_DOM_STATE_JS` is the single source of truth for cross-test DOM-pollution teardown. `snapshot` fixture now allows ±1 px size mismatch in either dimension before bailing — sub-pixel font rendering drifts between isolation and sweep contexts and the 1% per-pixel tolerance is the right comparison anyway. Both visible to the rest of the test base for adoption.

  Files: [tests/conftest.py](inventory-hub/tests/conftest.py), [tests/test_ui_details_modal_e2e.py](inventory-hub/tests/test_ui_details_modal_e2e.py), [tests/test_force_location_keyboard_e2e.py](inventory-hub/tests/test_force_location_keyboard_e2e.py), [tests/test_manual_loc_override_e2e.py](inventory-hub/tests/test_manual_loc_override_e2e.py), [tests/test_delete_ui_e2e.py](inventory-hub/tests/test_delete_ui_e2e.py), [tests/test_quickswap_deposit_and_header.py](inventory-hub/tests/test_quickswap_deposit_and_header.py), [tests/test_return_overlay_and_refresh.py](inventory-hub/tests/test_return_overlay_and_refresh.py), [tests/test_quickswap_visual.py](inventory-hub/tests/test_quickswap_visual.py), [inventory-hub/static/js/modules/inv_quickswap.js](inventory-hub/static/js/modules/inv_quickswap.js), [requirements-dev.txt](requirements-dev.txt), plus four re-captured PNG baselines under `tests/__screenshots__/chromium-1600x1300/`.

* **[2026-05-11 Group 15 — Canonical Overlay-Mount Helper]** — Branch `feature/canonical-overlay-mount` (7 commits: 63a3104 foundation, 2cf002e weight_entry, 089214d weight_utils missing-tare, d7cc2ae duplicate_picker, a9dd656 inv_quickswap, fc2cc9e force-location escape-confirm, baaaa2e gitignore). Closes the L17 [RECURRING] Z-ordering item. Path A scope: 4 real inline overlays + 1 special case (force-location escape-confirm nested in Swal) migrated; the 4 listed-but-non-real surfaces deferred per spec.

  - **15.1 — `window.mountOverlay()` foundation.** New [static/js/modules/overlay_mount.js](inventory-hub/static/js/modules/overlay_mount.js) (loaded first in [scripts.html](inventory-hub/templates/components/scripts.html)) owns: `document.body` mount target (lesson from 13.1's reverted modal-subtree mount), `OVERLAY_Z` ladder (STANDARD=20000 / CONFIRM=20100, toast layer stays at 11000 intentionally below), capture-phase `focusin` guard that `stopImmediatePropagation`s events targeting the overlay subtree (defeats Bootstrap's `_enforceFocus` without subtree-mounting), Escape contract via `stopImmediatePropagation` so sibling capture-phase listeners on `document` don't double-fire, host-close cascade on `hidden.bs.modal` / `hidden.bs.offcanvas`, occlusion of underlying selects via `pointer-events:none` with restore on cleanup, idempotent cleanup, and an id-keyed registry so re-mount with the same id tears down the prior handle first. 14 Playwright tests in [tests/test_overlay_mount_helper.py](inventory-hub/tests/test_overlay_mount_helper.py) cover each behavior.

  - **15.2 — Migrate 5 inline overlay surfaces.**
    - `<WeightEntry>` ([weight_entry.js](inventory-hub/static/js/modules/weight_entry.js)) — stripped manual cssText + focusGuard + Escape branch in favor of `mountOverlay({focusGuard:true, initialFocus, onEscape})`. The existing `test_value_input_accepts_keystrokes_inside_bootstrap_modal` regression test pins that the focus-guard still neutralizes `_enforceFocus`.
    - Missing-tare prompt ([weight_utils.js](inventory-hub/static/js/modules/weight_utils.js)) — auto-promotes to `tier:'confirm'` (z-index 20100) when WeightEntry is already open above it.
    - Duplicate picker ([duplicate_picker.js](inventory-hub/static/js/modules/duplicate_picker.js)) — routed through `mountOverlay`; per-overlay button lookups via `ov.querySelector` instead of `document.getElementById`.
    - Quick-Swap confirm ([inv_quickswap.js](inventory-hub/static/js/modules/inv_quickswap.js)) — pre-rendered `#fcc-quickswap-confirm-overlay` template in [modals_loc_mgr.html](inventory-hub/templates/components/modals_loc_mgr.html) removed; mounted dynamically with the same IDs preserved for existing selectors. `inv_loc_mgr.js`'s manage-modal Escape carve-out updated to detect any `[data-overlay-mount="1"]` element (covers every present and future mountOverlay surface generically). Two existing tests adjusted for the new full-screen backdrop: `test_confirm_overlay_dismisses_when_modal_closes_via_x` now invokes `bootstrap.Modal.hide()` programmatically (the backdrop correctly intercepts the X-button click — a real user can't physically reach it with a confirm open); `test_showconfirmoverlay_prepends_banner_when_printer_active` smoke-asserts `window.closeQuickswapConfirm` is exposed (the static DOM node is gone with dynamic mounting).
    - Force-location escape-confirm ([inv_details.js](inventory-hub/static/js/modules/inv_details.js)) — special case nested inside a Swal popup. Now mounts via `mountOverlay({tier:'confirm'})` (sits above the Swal's z-index 15000 backdrop). Outer window-capture Escape handler retained to TRIGGER the confirm overlay on first Escape, but bails early when the confirm is already up so mountOverlay's `onEscape` can close it cleanly (avoids the double-fire that would close-and-reopen).

  - **15.3 — Documentation.** [CLAUDE.md](CLAUDE.md) "Project Conventions" rewritten — "No nested Swal" subsection replaced with "Inline overlays MUST route through `mountOverlay`" rule, z-index ladder + focus-guard + Escape contract + host-cascade documented, plus the Z-order incident symptom checklist as a code-review rubric.

  **Deferred (Path A intentional scope reduction):** The 4 surfaces from the original 9-item list that aren't currently inline DOM overlays — delete-confirm (doesn't exist yet), force-eject confirm (doesn't exist yet), edit-slicer-profile pencil (Swal.fire), Vendor edit modal (Bootstrap Modal). When delete-confirm and force-eject get built, the CLAUDE.md rule mandates they use `mountOverlay`. The Swal-based and Bootstrap-modal-based surfaces have their own working focus management and aren't the bug surface — leave them as-is.

* **[2026-05-11 Group 13 — Recent Bugfixes: Weight + Dryer Display + LOC Search + Bind Desync + Eject]** — Branch `feature/recent-bugfixes-weight-dryer` (5 commits: 983d368 Phase A, b9ea64a Phase B, 319f0f7 Phase C, 89c6f39 13.1 follow-up + autocomplete-off + buglist Z-order entry, f46a0f9 docs). Closes all 9 group items.

  - **13.1 (L135 + L150)** — Quick-Weigh value input was uneditable when opened from Location Manager filament cards (Bootstrap modal's `_enforceFocus` yanked focus off the overlay's input on every keystroke; arrow keys worked because the overlay re-focused on mode switch). First fix mounted the overlay inside the open `.modal.show` subtree, which defeated the focus-trap but caused a Z-order regression (overlay rendered behind sibling modal chrome). Final fix: overlay always mounts at `document.body` (z-index 20000 wins cleanly) plus a capture-phase `focusin` guard that `stopImmediatePropagation`s events targeting our overlay before Bootstrap's listener can see them. Files: `inventory-hub/static/js/modules/weight_entry.js`. 3 new tests cover mount target, keystroke passthrough (with a stand-in focusin trap), and the autocomplete-off attributes.

  - **13.5 (L152)** — Dropped "(ALEX cap)" parenthetical from the high-cap clamp warning in `inventory-hub/static/js/modules/weight_entry.js` (Quick-Weigh preview area). String-only change; covered by the existing `test_clamp_high_warns_in_preview` assertion which doesn't pin the parenthetical.

  - **13.7 (L148)** — Gross-mode Skip path. Added opt-in `allowSkip` parameter to `promptMissingEmptyWeight` in `inventory-hub/static/js/modules/weight_utils.js`; when true, renders a third "Skip — save as Used Weight" button between Cancel and Save. The `<WeightEntry>` overlay passes `allowSkip: true` only in Gross mode; on Skip, downgrades the submission to Net (`used = initial − net`) for that submission only — no tare persisted to the spool. Other callers (post-archive weight prompt, etc.) keep their existing Cancel + Save behavior. Sentinel `window.PROMPT_SKIP_TARE = 'skip'` exposed for the caller to detect. 2 new tests cover the Skip path (mode downgrade verified, no tare written) and the gating (no Skip button when `allowSkip` omitted).

  - **13.9 (L152)** — Quick-Weigh default-mode preference persisted in `localStorage` under `fcc.weighEntry.defaultMode` (values: `gross` / `net` / `additive` / `set_used`). Stored value wins over the caller's `defaultMode` option; corrupt/unknown values fall through to the caller default. "Set as default" link rendered next to the Mode tabs; clicking persists current mode with a brief `✓ <mode> is default` confirmation flash. `D` shortcut inside the overlay does the same (registered via `window.registerShortcut` so the `?` help overlay picks it up under a new "Weigh / Quick-Weigh Overlay" scope). Documented in `CLAUDE.md` under a new "User preferences (pre-Config-system)" subsection so the eventual Config-system design (still pending) can migrate the key cleanly. 4 new tests cover stored-wins-over-caller, corrupt-falls-back, click-persists, and D-shortcut-persists.

  - **13.2 (L137)** — Ghost spool after auto-eject. `perform_smart_eject` in `inventory-hub/logic.py` now detects when the saved-home-slot is already claimed by another spool (direct OR ghost from a different spool) and lands the returning spool unslotted instead of creating a 2-in-1-slot collision. Activity Log entry: `🛑 Slot collision: <loc> slot <n> already claimed by Spool #<other_id> (ghost|direct); landing #<self_id> unslotted instead.` 2 new tests in `test_buffer_ejection_logic.py` cover the collision path and the self-ghost-doesn't-count guard.

  - **13.3 (L142)** — Overflow / collision items in Dryer Box `renderGrid`. JS-only fix in `inventory-hub/static/js/modules/inv_loc_mgr.js`: items with `slot > max` or whose slot is already claimed by another item now route to the Unslotted section instead of vanishing into `state.currentGrid` at an index the render loop never reaches. Box header `(X/Y)` reflects total visible cards including overflow. Pre-fix: 5 spools in a 4-slot box showed only 4; the 5th was silently lost. `renderGrid` is IIFE-scoped so no direct unit test was practical; deferred to manual verify (done in prod).

  - **13.6 Part A (L154)** — Toolhead-direct ghost binding. `perform_smart_move`'s PRINTER MOVE branch now synthesizes `physical_source` + `physical_source_slot` from the destination toolhead's reverse-binding when the spool's current source is empty (UNASSIGNED / buffer / room). New helper `logic._find_box_slot_feeding_toolhead(toolhead_id, loc_list)` does the reverse-lookup via `extra.slot_targets`. Symmetric with the existing slot → toolhead auto-deploy chain; preserves a real existing source so Return-to-Slot still works after a multi-hop deploy. 3 new tests cover synthesize-when-source-unknown, preserve-real-source, and no-synthesis-when-toolhead-unbound.

  - **13.6 Part B (L154)** — Auto-archive slot binding cleanup. `_auto_archive_on_empty` in `inventory-hub/spoolman_api.py` now clears `container_slot`, `physical_source`, and `physical_source_slot` when the auto-archive path moves a spool to UNASSIGNED at 0g. The `fcc_pre_archive_location` breadcrumb is still planted so `_auto_unarchive_on_refill` can restore the location on refill; only the slot binding is dropped. Pre-fix the dryer box's slot view kept rendering an archived spool that FilaBridge had already released. 5 new tests guard the slot-cleanup-on-archive contract; 2 pre-existing breadcrumb-skip tests updated to reflect the always-clears-extras behavior.

  - **13.4 (L144)** — LOC value search in the Add/Edit Wizard. `wizardFetchLocations` in `inventory-hub/static/js/modules/inv_wizard.js` now emits dropdown labels as `${Name} (${LocationID})`, mirroring the force-location modal's existing convention. `wizardBindCombobox`'s label-substring filter then matches typed LOC values like `LR-MDB-1` or `PM-DB-XL-L`. Both `wizardComboboxSet` callsites updated to emit the same label shape when restoring a saved value programmatically. Other location pickers (force-location modal, feeds-combo in LocMgr, Quick-Swap) were already correct.

  - **13.8 Part A (L150)** — PrusaLink active-print classification narrowed. `prusalink_api.get_printer_state` in `inventory-hub/prusalink_api.py` now only flags `is_active=True` for the `PRINTING` / `PAUSING` / `RESUMING` states. `PAUSED`, `BUSY` (heating/homing/prep), `OPERATIONAL`, `IDLE`, `FINISHED`, `STOPPED`, `ATTENTION`, `READY` all read as not-active so eject / smart-move / quick-swap don't block during the filament-swap prep flow Derek hit. Legacy `/api/printer` endpoint's `paused` flag no longer counts; only `printing`. 14 new parametrized tests cover all 11 states across both v1 and legacy endpoints.

  - **13.8 Part B (L150)** — Duplicate eject confirm modal that did nothing on Yes. `doEject` in `inventory-hub/static/js/modules/inv_loc_mgr.js` now distinguishes the active-print confirm (from `confirm_type === 'active_print'`) from the room-unassign confirm and routes each to its own retry flag (`confirm_active_print` vs `confirmed_unassign`). Pre-fix clicking Yes on the active-print modal retried with `confirmed_unassign=true` but `confirm_active_print=false`, so the backend re-flagged the same state and the modal popped up again — appearing to "do nothing" on Yes.

  - **Bonus follow-up — `autocomplete="off"` on Quick-Weigh value input.** Browser auto-suggest dropdowns of previous `+25` / `-50` deltas were cluttering the Quick-Weigh input. The dynamic overlay didn't inherit the `autocomplete="off"` convention used on the template-based modals (Edit Filament etc., 10+ inputs have it). Added `autocomplete` + `autocorrect` + `autocapitalize` + `spellcheck="false"` to the value input in `inventory-hub/static/js/modules/weight_entry.js`. Regression test guards the attributes. Note: `spellcheck="false"` is correct here since the input is numeric — other free-text fields elsewhere remain unchanged. Feature-Buglist entry kept open as PARTIAL since other dynamic-overlay inputs still need a sweep.

  - **Bonus — recurring `data/locations.json` corruption recurred + repaired.** During Phase C the dev container's `/api/locations` started returning `{error: "locations_corrupt"}` with the same "valid content + duplicate tail" fingerprint that commit `8430d81` (Group 4) originally fixed. Confirmed every Python writer still goes through atomic `save_locations_list`; leading hypothesis is a Docker Desktop on Windows bind-mount filesystem mediation quirk where `os.replace` truncation doesn't always propagate to the host NTFS view. Repaired in-place by truncating to the first balanced `]` from the right (41 stray bytes dropped, 57 records intact, pre-repair backup at `data/locations.json.pre-13.x-repair-*.bak`). Full investigation notes + 4 candidate hardening approaches (read-back-verify, unique temp filename per call, post-write explicit truncate, move runtime state to named volume) documented as a new entry in Feature-Buglist.md.

  - **Bonus — recurring modal/overlay Z-ordering bug class documented.** Derek's 13.1 Z-order regression made it concrete: every few weeks a new modal-on-modal / overlay-on-modal flow lands its top layer below the host modal or backdrop. Added a top-of-Feature-Buglist entry with a symptom checklist and a proposed canonical `mountOverlay()` helper design (single entry point in `static/js/modules/overlay_mount.js`, z-index ladder 20000-29999 above Bootstrap's 1055, focus-guard pattern, host-modal cleanup hook). Worth scheduling as its own design session.

  - **Test coverage — 31 new tests added; all 23 weight overlay tests, 51 Phase C tests (printer_state + active_print_backend + locations_integrity + locations_atomic_write), and 144 Phase B-related backend tests pass.** Full sweep (post-locations.json repair): 772 passed / 12 failed / 6 errors, none of the failures/errors trace to Group 13 changes (verified by isolated re-runs). The 18 remaining flakes are bundled into a new top-of-buglist priority entry for delegation to a sweep-stabilization agent.

* **[2026-05-11 Group 6 — Edit Modal: New Panels + Legacy Add-New-Vendor Cleanup]** — Branch `feature/edit-modal-new-panels`. Closes both group items, plus a follow-up cleanup that retired the wizard's legacy add-new-vendor toggle in favor of the same Vendor Edit modal.

  - **6.1 (L10)** — External-metadata import panel for the Edit Filament modal.
    - New "📥 Import from External" collapsible section at the bottom of the Advanced tab in `modals_details.html`. Source dropdown (Spoolman / Prusament / Amazon / 3DFP) + URL/keyword input with URL auto-detect + Parse button.
    - Reuses the existing `GET /api/external/search` endpoint and `external_parsers.py` parser chain — no new backend code. The per-field diff preview reuses `window.computeFilamentBackfillDiff` from `inv_wizard.js:2647` directly: silent fills (current value empty) default-checked, mismatches (would overwrite) default-unchecked, `color_hex` disabled with "(not yet supported)" note.
    - Apply Selected writes values into the matching `#editfil-*` form inputs only — does NOT POST. User clicks the modal's main Save button to persist via the existing `/api/update_filament` path, so all the extras-merge + `LAST_SPOOLMAN_ERROR` contracts from CLAUDE.md are inherited automatically.

  - **6.2 (L31)** — Manufacturer/Vendor Edit Modal. Scope expanded from the original V1 (name + comment + website only) to **every editable Spoolman-native field plus our extras** per follow-up requests.
    - Fields: name (native), comment ("Notes", native), external_id (native), empty_spool_weight (native, validated non-negative client-side), website (new `extra.website` extras field). Read-only "Registered <date>" footer derived from Spoolman's `registered` timestamp.
    - Backend: new `update_vendor` + `update_vendor_or_raise` in `spoolman_api.py` mirroring the spool/filament pair (LAST_SPOOLMAN_ERROR + partial-extras-merge), new `PATCH /api/vendors/<id>` route in `app.py:314` with activity-log entry on success and 7s error-toast surface on rejection. `POST /api/vendors` extended to accept the full `{data: {...}}` payload (with legacy `{name}` short-form kept for back-compat with the Edit Filament inline "+Create vendor" hint). `create_vendor` now populates `LAST_SPOOLMAN_ERROR` on rejection (was a silent-fail before).
    - Setup: first vendor-extras registration in the codebase — `vendor.website` registered via `setup_fields.py` AND mirrored into `REQUIRED_VENDOR_EXTRAS` in `spoolman_api.py:ensure_required_extras` so app-startup self-heal covers prod instances that haven't re-run the script.
    - Entry points: pencil next to brand on Filament Details modal (uses `dataset.vendorId` stash on `fil-detail-vendor`), pencil next to vendor combobox in the Edit Filament modal (shown only when an existing vendor is selected), AND ✏️ next to vendor combobox in the Add/Edit Wizard. All three reach the same `window.openVendorEditModal(id)`.
    - Events: `vendor:updated` listener refreshes brand text on Filament Details and the info-pill in Edit Filament without requiring a full re-fetch. `vendor:created` is a new event for the create-mode follow-up below.

  - **Follow-up: legacy add-new-vendor flow retired (cleanup).** The wizard previously had a ✏️ button that toggled the vendor combobox into a free-text "type new vendor name" input; submit then put that name into `extra.external_vendor_name` and the backend created the vendor as a side-effect of `/api/create_inventory_wizard` (and `/api/edit_spool_wizard`). The "shortcut" of typing a new name directly in the search box silently dropped the vendor on submit — confirmed during the cleanup investigation. Replaced with:
    - `window.openVendorCreateModal(prefill)` opens the same Vendor Edit modal in create mode (empty `vendoredit-id` flips `vendorEditSave` to POST instead of PATCH, title flips to "➕ Add Manufacturer", focus auto-lands on the name field).
    - Wizard ➕ button pre-fills the modal's name with whatever's typed in `wiz-fil-vendor-search` so a user mid-typing doesn't have to retype.
    - After save, the new `vendor:created` event fires; a wizard-side listener refetches `wizardFetchVendors`, auto-selects the new vendor via `wizardComboboxSet`, AND pushes the vendor's `empty_spool_weight` through the cascade into `wiz-fil-empty_weight` (which `wizardSetupFieldSync` mirrors to `wiz-spool-empty_weight`). User-typed values are never clobbered.
    - Removed entirely: `wizardToggleVendorMode` function, `wiz-fil-vendor-new` text input element, both `extras.external_vendor_name` handlers in `app.py` (`api_create_inventory_wizard` + `api_edit_spool_wizard`), and the auto-toggle paths inside external-import / clone-from-existing-filament fill flows (they now leave the parsed vendor name in the search input so the user can pick a close match or click ➕).

  - **Regression caught + fixed during the cleanup.** Initial implementation had `wizardComboboxSet` dispatching a `bubbles: true` change event on the hidden id input. The wizard's modal-level dirty-flag listener picked that up during programmatic clone/edit init and flipped `wizardState.isDirty = true`, which made Cancel pop the unsaved-changes Swal instead of closing (caught by `test_clone_e2e.py::test_clone_spool_button`). Fixed by switching the dispatch to `bubbles: false` — direct listeners (the ✏️ visibility hook) still fire, but the wizard-wrapper listener doesn't see it. The reset-on-open dispatch was updated to match for consistency.

  - **Empty-weight cascade verification.** Confirmed the Spool > Filament > Vendor cascade walks all three levels through the update flows. The helper (`resolveEmptySpoolWeight` in `weight_utils.js`) was already unit-tested in 9 cases (`test_empty_spool_weight_priority.py`); this work added integration coverage for the Clone Spool wizard (`openCloneWizard`) and Edit Spool wizard (`openEditWizard`) flows in 7 new tests, including the vendor-fallback case ("if spool and filament both blank, fall back to manufacturer"). Plus a real-Spoolman E2E that exercises vendor empty_weight → filament + spool persistence through the wizard submit so any "value populated in UI but dropped on the wire" bug would surface.

  - **Write-surface inventory updated.** New row in `CLAUDE.md` for `PATCH /api/vendors/<id>` documenting the extras-merge guarantee + activity-log + 7s error-toast contract.

  - **Test coverage.** Five new spec files (34 new tests, all green):
    - `test_vendor_edit_modal.py` (10) — DOM presence, hydration from existing vendor, save payload shape (PATCH), empty-weight validation (non-negative + null-on-clear), error banner surfacing, both pencil entry points
    - `test_edit_filament_external_import.py` (6) — source dropdown switching, URL auto-detect, per-field checkbox toggling, Apply Selected writes form values without POSTing
    - `test_wizard_vendor_edit_button.py` (8) — split ✏️/➕ render, ✏️ visibility tied to vendor selection, ➕ pre-fills create modal from search input, vendor:created auto-selects in wizard, empty_spool_weight propagation through cascade, user-typed values not clobbered, legacy `wizardToggleVendorMode` confirmed removed
    - `test_vendor_create_end_to_end.py` (3) — real-Spoolman E2E: full vendor create → list → filament create → vendor link verification; vendor empty_weight → filament + spool persistence through wizard submit; legacy `external_vendor_name` path confirmed gone
    - `test_empty_weight_vendor_fallback_update_flows.py` (7) — Spool > Filament > Vendor cascade through Clone + Edit wizard flows, including 0-as-unset and missing-key edge cases

* **[2026-05-08 Group 5 — Edit Filament & Details Modal: Small Additions]** — Branch `feature/edit-modal-small-adds` (1 commit). Closes both group items + a follow-up alignment polish.
  - **5.1 (L44)** — Slicer profile surface. The `slicer_profile` extra has been a registered Spoolman `choice` field (`setup_fields.py:346`, `multi=False`) since the inventory-hub rewrite, but only the Add/Edit Wizard could write it — never visible at-a-glance and not editable from anywhere else. Two surfaces added:
    - **Filament details modal** — new "🎚️ Slicer Profile: <value> ✏️" fact card (`modals_details.html` filament-modal body). Pencil opens a new `window.promptEditSlicerProfile` overlay modeled on `promptEditLocation` (Swal `target` keyed to `#filamentModal` so it stacks inside the Bootstrap modal — same convention, not a nested Swal). Picker offers a search box, the registered choice list, and a separate "Or add a new profile" input. On confirm: read current filament from `/api/filaments/<id>`, build a fully-merged `extra` dict (siblings preserved per the CLAUDE.md write-surface contract — Spoolman replaces the whole `extra` on PATCH), POST to `/api/update_filament`, and fire-and-forget `add_choice` registration when the user typed a new name. The fact card refreshes on success via `openFilamentDetails(filamentId, true)` silent re-render. Error toast on save failure (7s, per the standard).
    - **Edit Filament modal** — new "Slicer Profile" dropdown in the Advanced tab, populated from the same `/api/external/fields` call that already feeds `filament_attributes` chips. Empty `(none)` option + registered choices + `+ Add new…` sentinel that unhides a free-text input. Save handler diff'd in alongside the other dirty-extras checks; new-choice registration POST mirrors the `attrPendingNew` pattern.
  - **5.2 (L54)** — Min/Max temp display. Filament details modal now shows nozzle/bed Min and Max side-by-side (was Min only); spool details modal gained a brand-new temperature section sourced from `d.filament.*` (was no temp display at all). Min stays on the native `settings_extruder_temp` / `settings_bed_temp`; Max reads `extra.nozzle_temp_max` / `extra.bed_temp_max` (both already in `JSON_STRING_FIELDS` since the 2026-04-27 expansion). Missing values render as `--`. After a quick alignment polish in the same session, both rows use a 5-column CSS grid (`max-content` columns) so the label, `Min:` heading, min value, `Max:` heading, and max value all line up vertically across the Nozzle and Bed rows — the user noted the original side-by-side layout was hard to scan because emoji-label widths drifted between rows.
  - **Refactor** — `unquoteExtra` lifted from a local closure inside `_editfilOpenModal` to module scope at the top of `inv_details.js`, so `openSpoolDetails`, `openFilamentDetails`, the existing edit modal, and the new pencil overlay all share one definition.
  - **Write-surface inventory updated.** New row added to `CLAUDE.md`'s spool/filament write-surface table for `inv_details.js:promptEditSlicerProfile` documenting the client-side extras-merge contract (read-merge-write to avoid wiping siblings) — keeps future maintainers aware that this is the same partial-extras hazard documented for the wizard.
  - **Test coverage.** 9 new Playwright tests appended to `tests/test_filament_edit_button.py` (file now 62 → 71 tests, all green):
    - 4 tests for the Edit-modal dropdown: populate from current value, save changes the value, "+ Add new…" sentinel writes payload + fires `add_choice`, picking `(none)` clears the field via the empty-string-deletes-key branch of the existing merge.
    - 5 tests for the details-modal pencil overlay: fact card renders the value (and stashes raw on dataset for the prompt round-trip), `--` rendered when missing, picking a different existing choice sends merged extras (siblings preserved through `__sliceUpdatePayload`), typing a new name fires both `update_filament` AND `add_choice`, picking `(none)` clears the field while keeping siblings intact. Shared stub `_stub_filament_details_for_slicer` mirrors the `_stub_editfil_fetches` pattern but covers `/api/filament_details`, `/api/filaments/<id>`, `/api/external/fields`, `/api/update_filament`, and `/api/external/fields/add_choice`.
    - The shared `_stub_editfil_fetches` field-schema mock gained a `slicer_profile` choice entry alongside `filament_attributes` so the populator path runs without breaking any existing test.
  - **Regression coverage.** Full sweep: 704 / 720 pass (15 skipped, 4 failed, 9 errored). All 13 non-pass results reproduced as pre-existing or order-dependent flakes that pass on isolated re-run — the same `test_force_location_keyboard_e2e` / `test_manual_loc_override_e2e` / `test_ui_details_modal_e2e` ordering flakes that have been documented in prior sessions. Visual baselines: 11/11 pass — the new markup stayed inside the 1% pixel tolerance even after the CSS-grid alignment polish.

* **[2026-05-07 Group 7 — Testing Housekeeping]** — Branch `feature/testing-housekeeping`. Closes both group items.
  - **7.1 (L153 / L235)** — `inventory-hub/tests/test_return_and_breadcrumb.py:54` — `fake_move(target, spools, target_slot=None, origin=None)` now accepts `**kwargs`, absorbing `confirm_active_print` (added to `perform_smart_move` during the 4/25 locations refactor) and any future signature additions. The 3 affected unit tests pass.
  - **7.2 (L152 / L234)** — Cleared `inventory-hub/` of dead probe scripts and migrated 5 test files to shared `conftest.py` fixtures.
    - **Deleted 15 root-level `inventory-hub/test_*.py` files** that were `print()`-only manual probes with no asserts and no importers: 8 Amazon scraper experiments (now covered by `tests/test_external_parsers.py`), 5 wizard manual probes (covered by `tests/test_wizard.py` + `test_wizard_e2e.py`), and 2 Spoolman multi-color revert probes (covered by `tests/test_spoolman_live.py`). `.dockerignore` already excluded these from the prod image; the rule remains as a guard against future accidental stragglers.
    - **Migrated 5 test files** from local constants/helpers to shared fixtures:
      - `test_active_print_live_probe.py` — `BASE_URL` constant → `api_base_url` fixture
      - `test_backfill_spool_weights.py` — local `SPOOLMAN`/`_sm_ok()` → `dev_spoolman_url` + `require_spoolman` + `require_server`; renamed local `throwaway_filament` → `backfill_test_filament` to avoid silently shadowing the conftest fixture of the same name
      - `test_large_spool_weight_tracking.py` — same fixture migration; redundant `sys.path.insert` removed (conftest does it)
      - `test_archive_unarchive_e2e.py` — same fixture migration; `_get_spool`/`_patch_spool` helpers now take `spoolman_url` as an arg instead of baking in the constant
      - `test_weight_setting_dispatches_events.py` — inline `SPOOLMAN` constant → `dev_spoolman_url` fixture; manual `/api/identify_scan` POST → `scan` fixture
    - The conftest.py docstring's note that `test_backfill_spool_weights.py` and `test_large_spool_weight_tracking.py` could be migrated to the shared throwaway fixtures "as a follow-up" is now resolved (kept as local domain-specific fixtures because each needs controlled spool weights the generic conftest fixture can't provide, but they now consume `require_spoolman` + `dev_spoolman_url` instead of the inline constant + helper).
  - Full suite (server up): 705 passed, 15 skipped, 3 failed in 10:43. None of the 6 touched files appear in the failure list. The 3 failures are pre-existing and unrelated: a visual baseline size mismatch (`test_quickswap_visual`), an offcanvas-backdrop test-isolation flake (`test_ui_details_modal_e2e`), and the known BeautifulSoup4-missing-in-Docker issue (`test_amazon_parser_matching`, already on buglist On-Hold). The first two have been added to the Testing section of `Feature-Buglist.md` for follow-up.

* **[2026-04-28 Group 2 — Buffer Cards & Main-Menu Refresh]** — Branch `feature/buffer-cards-refresh` (1 commit). Closes all 3 group items: buffer-card location staleness (L24 + L40, root cause same code), and search FAB blocking the WEIGH QR on smaller / scaled viewports (L36).
  - **2.1 + 2.2 (L24 + L40)** — Buffer-card staleness root-caused to one missing line in `inv_cmd.js::liveRefreshBuffer`. The function polls `/api/spools/refresh` every sync-pulse and diffs against `state.heldSpools`, but its diff check + in-place mutation only covered `display`, `color`, `color_direction`, `remaining_weight`, `details`, and `archived` — even though the backend response (`logic.py::get_live_spools_data`) carries `location`, `is_ghost`, `slot`, `deployed_to` as well. So a backend-driven location move (Location Manager, Quick-Swap, force-unassign, auto-archive-on-empty) would correctly update the archived flag and weight on the card, but leave the location badge stale until the user navigated away and back. Fix: extend the diff check + the mutation to include all four location fields. The L24 PARTIAL note from 2026-04-23 already had the dispatch chain working; this filled the consumer-side gap.
  - **2.3 (L36)** — `.fcc-fab-search` was anchored at `bottom: 30px; right: 30px` (65×65px circle), which sat on top of the rightmost `.cmd-deck` button (the WEIGH QR) on shorter / Windows-scaled viewports. Added a `body:has(.cmd-deck) .fcc-fab-search` rule that lifts the FAB to `bottom: 180px` only on pages that include the dashboard's bottom-pinned cmd-deck. Other pages keep the standard bottom-right anchor.
  - **Test coverage.** 12 new tests across 2 files, all green:
    - `test_buffer_card_refresh_propagates_location.py` (9): static asserts that `liveRefreshBuffer`'s diff clause + mutation block both reference each of the four location fields, plus a backend-contract guard that `get_live_spools_data` keeps returning all four keys.
    - `test_fab_no_overlap_with_cmd_deck.py` (3): bounding-box overlap checks at the canonical 1600×1300 dev viewport, a short 1280×700 viewport that reproduced the original failure, and a tall 1920×1600 to confirm the fix isn't viewport-height-dependent.
  - Targeted regression sweep across all suites that touch the changed files: **83 / 83 pass** (1 graceful skip), plus all **7 visual baselines** passed (FAB shift didn't bust any snapshots).

* **[2026-04-28 Group 4 — Archive / Delete / Cleanup Lifecycle]** — Branch `feature/archive-delete-lifecycle` (1 commit). Closes 3 of 4 group items + the L3/L18 reframing already documented below. Item 4.4 (filament-attributes choice-list cleanup) deferred — see active-backlog audit entry for confirmed-safe-to-delete list and migration template.
  - **4.1 (formerly L3)** — Filament-archive non-feature retired. Spoolman silently drops `archived: true` on filaments and exposes no UI for it; per Derek 2026-04-28 it's a non-feature for FCC. Removed unreachable auto-unarchive branch from `app.py:api_create_inventory_wizard`, deleted the always-skipped `test_wizard_ux_polish.py::test_create_spool_auto_unarchives_parent_filament`, pruned the dead reference from the test module docstring.
  - **4.2 (re-scoped from L18)** — Spool auto-unarchive on weight refill. The original L18 entry was about *spool*-level recovery, written ambiguously. New `spoolman_api._auto_unarchive_on_refill` mirrors the existing `_auto_archive_on_empty` shape: when an archived spool's `remaining_weight` goes back above 0, it un-archives automatically. Pre-archive location breadcrumb (`extra.fcc_pre_archive_location`) planted on the auto-archive path and consumed on refill — restores the spool to its original location when available, leaves at UNASSIGNED otherwise. New field added to `SYSTEM_MANAGED_EXTRAS` so user-driven write surfaces (wizard, vendor edit) cannot clobber the breadcrumb. Field registered in `setup-and-rebuild/setup_fields.py`. Symmetric activity-log entry on every auto-unarchive: `📤 Auto-unarchived Spool #N (weight refilled) — restored to <loc>`.
  - **4.3 (formerly L56)** — Delete spools and filaments via UI. New backend: `spoolman_api.delete_spool` / `delete_filament` (both surface `LAST_SPOOLMAN_ERROR` on failure per the symmetry contract); `get_spools_for_filament` for cascade preview. New `DELETE /api/spool/<id>` and `DELETE /api/filament/<id>` endpoints — the filament endpoint cascades through child spools, returns per-spool error list on partial failure, refuses to delete the parent if any child failed (Spoolman would reject anyway, partial state stays recoverable). New buried gear-icon dropdown menu in the spool/filament details modals → "🗑️ Delete this spool/filament…" → inline overlay (no nested Swal per CLAUDE.md). Two-step confirm: Step 1 broad warning (cascade count shown for filaments), Step 2 type-the-id (or `CONFIRM` for cascading filament delete). Bootstrap modal `data-bs-keyboard="false"` on both modals so the overlay's Escape handler doesn't race Bootstrap's; custom Escape-to-close re-implemented in JS for the underlying modals when no overlay is showing. Activity log entries on every delete (success + failure); error toasts use 7s duration per the standard.
  - **4.4 (L160 → reworded L222)** — DEFERRED. Audit completed: 5 dead choices confirmed safe to delete (`Carbon-Fiber`, `Tran`, `Transparent; High-Speed`, `Wood`, `F`), 1 keep (`+` for PLA+), 2 needing further investigation. Spoolman blocks per-choice removal via POST (`400 "Cannot remove existing choices"`), so a true cleanup requires the snapshot-restore migration pattern — too heavy for purely cosmetic dropdown cleanup. Active-backlog entry updated with full audit context and migration template.
  - **Bonus — locations.json corruption + atomic-write hardening.** Discovered during the broader regression sweep: `inventory-hub/data/locations.json` had a duplicate `}\n]` tail past the proper closing bracket (likely from a non-atomic write that got interrupted or raced by a previous agent session). Dev file truncated at the proper end, backup at `data/locations.json.bak-pre-fix`. Prod confirmed clean by Derek's PowerShell check. Root cause patched: `locations_db.save_locations_list` now uses temp-file + `fsync` + `os.replace` for atomic-or-nothing writes — `with open(JSON_FILE, 'w')` was the prior smoking gun (truncates immediately, leaves a half-written file if anything goes wrong). Also restored XL-5's missing `Device Identifier` / `Device Type` / `Location` / `Order` fields (likely destroyed by the same prior agent blunder); now matches XL-1 through XL-4 structurally — printer always had 5 toolheads.
  - **Test coverage.** 50 new tests across 5 new files, all green:
    - `test_archive_unarchive_helpers.py` (25): pure-helper edge cases for `_auto_archive_on_empty` (breadcrumb planting, caller-intent-wins, no-op cases) + `_auto_unarchive_on_refill` (refill triggers, breadcrumb consumption, defensive missing-existing handling) + `update_spool` wiring tests with mocked HTTP.
    - `test_archive_unarchive_e2e.py` (2): full round-trip against live container + Spoolman — drain spool → auto-archive with breadcrumb → refill → auto-unarchive with location restored; breadcrumb-less path leaves spool at UNASSIGNED.
    - `test_delete_helpers.py` (11): `delete_spool` / `delete_filament` symmetry + LAST_SPOOLMAN_ERROR surfacing on every failure path; `get_spools_for_filament` filtering + tolerant-of-malformed-entries.
    - `test_delete_ui_e2e.py` (6): gear dropdown exposes Delete with text-danger styling, Step 1 warning + Cancel returns to modal cleanly, Step 2 type-to-confirm rejects wrong input with inline error, Escape closes overlay only (modal stays open), modal close mid-flow doesn't strand overlay state, filament cascade prompt shows correct child count.
    - `test_locations_atomic_write.py` (6): success replaces file exactly, shorter new content doesn't leave old tail, failed write leaves original intact (atomic-or-nothing), no `.tmp` turds left behind, empty-list no-op, temp file lives in same directory as target.
  - Targeted regression sweep across all suites that touch the changed files: **251 / 252 pass** (1 known pre-existing flake `test_location_combobox_enter_selects` that reproduces on `dev`, documented in prior Phase-1 archive entry). All 13 `test_locations_json_integrity.py` tests pass after the file repair + XL-5 backfill.

* **[2026-04-28 Filament archive — won't fix]** — Closed `[IMPORTANT] Spoolman filament archiving is broken` (formerly buglist L3). Spoolman silently drops the `archived` flag on filaments and exposes no UI for it. Per Derek 2026-04-28: filament-level archiving is a non-feature for FCC; "0 spools" lookups already covered by search. Removed the unreachable auto-unarchive branch from `app.py:api_create_inventory_wizard` and deleted the always-skipped `test_wizard_ux_polish.py::test_create_spool_auto_unarchives_parent_filament`. The original L18 entry — which was actually about *spool*-level recovery, written ambiguously — was reworded in place: "Adding weight back to an archived spool should auto-unarchive the spool" (Group 4 item 4.2). Spool archive remains fully supported by Spoolman.

* **[2026-04-28 Group 3 — Print Queue & Label Management]** — Branch `feature/print-queue-labels` (1 commit, 626 insertions / 47 deletions across 10 files). Closes all 7 items in Group 3 (`docs/agent_docs/tasks/03-print-queue-labels.md`).
  - **3.7 (priority — recently moved into the group)** — "filament 229 didn't get its label validated" — two compounding bugs: (1) Wizard never initialized `extra.needs_label_print` on new spool/filament creation, so brand-new records had a null/missing flag; fix at [app.py:364](inventory-hub/app.py#L364) `api_create_inventory_wizard` defaults `needs_label_print=true` on both new filament and new spool extras (edit-mode goes through `api_edit_spool_wizard` and never touches the flag, preserving existing state). (2) Verify-on-scan check at [app.py:1351](inventory-hub/app.py#L1351) (spool) and [app.py:1409](inventory-hub/app.py#L1409) (filament) was too strict — only flipped flag when raw value was True; treated null/missing the same as False. Now uses tri-state semantics: `already_verified iff raw_flag is False`, so True AND null both fire the verify on a valid `ID:NNN`/`FIL:NN` scan; only an explicit False short-circuits. Pytest coverage in `test_print_queue.py` covers wizard auto-flag, scan-verify true/null/false paths, and negative cases (legacy barcode, source='keyboard'). **Note carried forward**: items without externalID may not be getting the flag set via some non-wizard path — investigate as separate bug if it recurs.
  - **3.6** — Disambiguation picker for ambiguous legacy barcodes. Backend: new `spoolman_api.find_spools_by_legacy_id` returns the full candidate list (sorted non-empty first); `find_spool_by_legacy_id` is now a thin shim. New `_resolve_legacy_spool_lookup` helper in [logic.py](inventory-hub/logic.py) returns `{type: 'ambiguous', candidates: [...]}` when 2+ spools share a legacy id; wired into all four legacy-lookup call sites in `resolve_scan` (LEGACY:/LEG:/OLD: prefix, URL parsing with `range=N`, pure-number priority-3 fallback). `api_identify_scan` passes the ambiguous payload through to the frontend. Frontend: new [duplicate_picker.js](inventory-hub/static/js/modules/duplicate_picker.js) renders an inline overlay (no nested Swal per CLAUDE.md) with three actions — "✓ Use selected" re-routes through `processScan('ID:NNN','barcode')` so the normal spool flow runs (label-verify, buffer add, etc.); "🖨️ Print new label" queues a fresh ID:NNN label via `addToQueue` so the legacy id stops being ambiguous; Cancel aborts. Single-match scans are unchanged. Tests covering all four scan paths live in `test_print_queue.py`.
  - **3.1** — Adding a duplicate label to the Cart Queue now toasts info-style "Label already in queue" (4s) at [inv_queue.js:60](inventory-hub/static/js/modules/inv_queue.js#L60) instead of the prior warning-style "⚠️ Already in Queue".
  - **3.3** — Filament sample cards now render the 🖨️ Add-to-Print-Queue button. The `!isFil &&` gate at [ui_builder.js:102](inventory-hub/static/js/modules/ui_builder.js#L102) (search-mode card-builder) was removed; `addToQueue` already supported `{type:'filament', id}` and the `/api/print_queue/set_flag` filament branch at [app.py:2113](inventory-hub/app.py#L2113) flips `needs_label_print=true` on the filament extra → item appears in Backlog. The other three card modes (`loc_list`, `loc_grid`, `buffer`) are spool-only contexts and were left unchanged.
  - **3.4 + 3.2** — "Spoolman Reprint / label_printed native fields" and "Some values in Print Queue are being set to yes" closed after a direct probe of the running Spoolman API (`curl http://192.168.1.29:7913/api/v1/spool/229` and `/api/v1/filament/<id>`) confirmed this Spoolman version does not expose native `label_printed` (spool) or `Reprint` (filament) top-level fields. We rely exclusively on the `extra.needs_label_print` boolean. The "yes vs null" the user observed was Spoolman's display of our extra (stored as JSON-string `"true"` when set, absent when null/missing). FCC writes only `true`/`false`. Backlog determination logic unchanged — `extra.needs_label_print === true` is the single source of truth. If a future Spoolman upgrade adds native fields, mirror them in addition to the extra without changing the Backlog query.
  - **3.5** — "Refresh ticks clearing the print queue / search button broke" closed as NOT REPRODUCIBLE after code review confirmed both surfaces are guarded: Cart Queue at [inv_queue.js:230-247](inventory-hub/static/js/modules/inv_queue.js#L230-L247) hash-compares against server state and only re-renders when data changes; Backlog at [inv_backlog.js:240-242](inventory-hub/static/js/modules/inv_backlog.js#L240-L242) does the same and re-applies checkbox state at [inv_backlog.js:253-255,280](inventory-hub/static/js/modules/inv_backlog.js#L253-L255). Filter/sort dropdowns are read from the DOM each fetch so they persist naturally. Re-open with concrete repro (which surface, which control) if it recurs.
  - 14 new pytest assertions in `inventory-hub/tests/test_print_queue.py` (16 existing → 23 total): wizard auto-flag for new spool, new filament, and explicit-false respect; scan-verify tri-state coverage (true clears, null clears, false short-circuits — both Python bool False and stringified 'false'/'False'); filament-branch null-flag clearing; legacy-barcode negative test; ambiguous resolve_scan for LEGACY: prefix, single-match passthrough, pure-number fallback; `api_identify_scan` passes ambiguous payload to the frontend. All 258 unit-level tests pass; pre-existing failures (`test_locations_json_integrity`, `test_amazon_parser_matching`) confirmed unaffected via stash-and-rerun.

* **[2026-04-27 Weight Entry Unified Component — Phase 2]** — Branch `feature/weight-entry-unified-component` (7 commits). Closes the weight-entry unification effort started in Phase 1: every active weight surface now routes through the same Phase-2 math (`computeUsedWeight()`, `parseAdditiveInput()` in `static/js/modules/weight_utils.js`) and shares either `<EmptyWeightField>` (empty_spool_weight setters) or `<WeightEntry>` (used_weight setters).
  - **L38** — Gross-weight workflow now works everywhere it's needed. Quick-Weigh's new `<WeightEntry>` overlay has a "Gross" mode tab that auto-subtracts the resolved empty-spool tare; bulk weigh-out gained a modal-level Mode picker that does the same for batch weighing. The original main-menu QR weight bug ("Total weight then overwrites the current weight without taking into account the spool's empty weight") is resolved — switching to Gross mode in either surface produces a live preview of `used_weight = initial - (gross - empty_tare)` before submit.
  - **L48 (full)** — All fragmented weight-update paths now share one math core. New `<EmptyWeightField>` component (`static/js/modules/empty_weight_field.js`) owns the inheritance badge + auto-clear-on-input + optional copy-vendor-button affordance, adopted in the wizard Spool tab and the Edit Filament Specs tab. New `<WeightEntry>` component (`static/js/modules/weight_entry.js`) renders an inline overlay with 4 modes (Gross / Net / Additive / Set Used), live preview panel, ALEX-cap warnings, and a `+/-` prefix parser; adopted in Quick-Weigh as a full overlay and in bulk weigh-out + FilaBridge manual recovery as a modal-level mode picker driving per-row previews. Legacy `#quickWeighModal` Bootstrap markup retired. Backend contract unchanged — saveSpoolWeight + /api/spool/update + /api/fb_manual_recovery preserved.
  - 14 new Playwright component tests in `inventory-hub/tests/test_weight_entry_overlay.py` cover overlay open / mode switching / preview math / submit-cancel-Escape / arrow-key mode swap / Gross+missing-tare prompt flow. 23 new math tests in `tests/test_compute_used_weight.py` cover all four modes' clamp behavior + the additive parser. 211 weight-related tests pass overall.
  - `docs/agent_docs/phase-2-after/` mirrors `phase-2-baseline/` with the same six surface screenshots captured against this branch — diff side-by-side for PR review.
  - All 15 must-preserve features from the task file remained working: auto-archive on remaining ≤ 0, force-unassign chain, Quick-Weigh +/- syntax, ALEX cap, `update_spool_or_raise` contract, backfill button, vendor-copy button, wizard sync toggle, `inventory:sync-pulse` / `inventory:buffer-updated` events, FilaBridge DELTA vs weigh-out ABSOLUTE semantics, Activity Log strategy logging, `LAST_SPOOLMAN_ERROR` surfacing.
  - **Buglist item**: `[Phase 2 — Weight Unification] Build a unified <WeightEntry> component reused by every weight-entry surface` — moved to archive during groups refresh (missed during original cleanup pass).

* **[2026-04-27 Weight Handling Unification — Phase 1]** — Branch `feature/weight-unification`. Foundation pass for the larger weight-entry unification effort; Phase 2 (the unified `<WeightEntry>` component) is tracked separately in `Feature-Buglist.md` and on `feature/weight-entry-unified-component`.
  - **L34** — Wizard now auto-prefills `wiz-spool-empty_weight` from the parent filament/vendor cascade on open. New "↩ from filament/vendor" badge appears next to the field when the value is inherited and clears the moment the user types. Covers the new-spool-from-filament, clone, and edit-spool flows. Files: `inventory-hub/static/js/modules/inv_wizard.js`, `inventory-hub/templates/components/modals_wizard.html`.
  - **L46** — Post-archive empty-weight Swal prompt (`showArchiveEmptyWeightPrompt`) now binds Enter via a `didOpen` keydown handler. preConfirm validation still gates blank/negative input. File: `inventory-hub/static/js/modules/inv_details.js`.
  - **L48 (partial → foundation only)** — Extracted `resolveEmptySpoolWeight` and a new `resolveEmptySpoolWeightSource` (returns `{value, source}` so UI can badge inheritance) into `inventory-hub/static/js/modules/weight_utils.js`. This is the foundation Phase 2 will build the unified component on top of. Full unification + the deferred L38 ("gross-weight workflow") roll into Phase 2.
  - 6 new Playwright tests (4 wizard auto-prefill + 2 archive-prompt Enter-key) live in `tests/test_wizard_auto_prefill_empty_weight.py` and `tests/test_archive_empty_weight_prompt.py`. All 34 weight-suite tests + 104 wizard-suite tests pass on this branch (1 pre-existing failure on `test_location_combobox_enter_selects` reproduces on `dev` and is unrelated).
  - `CLAUDE.md` gained a "Weight-entry surfaces" subsection flagging the fragmentation hot-spot and pointing forward to Phase 2.

* **[2026-04-27 — All 8 [CRITICAL · PRIORITY] extras-clobber + silent-fail items resolved]** — Branch `feature/critical-extras-clobber-silent-fail-fixes` (824 insertions / 85 deletions across 14 files). Single coherent attack on the architectural root of the 2026-04-26 / 04-27 prod incidents.
  - **Real Spoolman integration tests** — new `pytest.mark.integration` marker in `pytest.ini`, `--run-integration` opt-in flag (default off so unit runs stay fast), `dev_spoolman_url` + `require_spoolman` session fixtures, `throwaway_filament` / `throwaway_spool` / `throwaway_spool_with_extras` factories with auto-cleanup, orphan-sweep autouse hook. 10 pinned scenarios in `test_spoolman_integration.py`: partial-PATCH preserves siblings, text-type numeric-string round-trip survival, label-scan flips `needs_label_print` without wiping siblings (spool + filament sides), slot-assignment with all 13 JSON_STRING_FIELDS loaded, `LAST_SPOOLMAN_ERROR` populated on rejection, wizard-edit on slotted spool preserves `container_slot`, `setup_fields.py` is idempotent and non-destructive. **All 9 integration tests pass against real dev Spoolman + the running Flask container.**
  - **Silent-fail audit + symmetry fix** — `update_spool` now populates `LAST_SPOOLMAN_ERROR` symmetrically with `update_filament` (the asymmetry that left the 2026-04-27 outage undiagnosed for hours). Added `update_spool_or_raise` / `update_filament_or_raise` variants that raise a new `SpoolmanRejection` exception. Audited and repaired every fire-and-forget caller in `app.py` (12 sites: wizard auto-unarchive, edit_spool_wizard save, manage_contents set_meta, locations cascade unassign, label-confirm spool + filament paths, print queue mark_printed + set_flag both sides, filabridge auto-deduct + manual + auto-recover, weight setter at line 2659) and `logic.py` (7 sites in `perform_smart_move` / `perform_smart_eject` / `perform_force_unassign`). Every Spoolman rejection now writes the actual error body to the activity log with ERROR severity (`ff4444`). Standardized error JSON shape for endpoints (`{success: false, msg, error}`); frontend toasts in `inv_backlog.js` + `inv_queue.js` show errors with ≥7 s duration per CLAUDE.md.
  - **Spool label-confirmation regression (Items 3 + 6)** — Refactored both spool-side (`app.py` `ID:<n>` branch) and filament-side (`FIL:<n>` branch) label-confirm paths to surface `LAST_SPOOLMAN_ERROR` on failure, and added an "ℹ️ Spool/Filament #N already verified" info entry on every scan whose flag was already cleared. Users now always get acknowledgement on a label scan — silent success was the symptom that made Item 3 untrackable.
  - **Wizard slot-loss fix (Item 4)** — Added `SYSTEM_MANAGED_EXTRAS = frozenset({"container_slot", "physical_source", "physical_source_slot"})` to `spoolman_api.py`. New `compute_dirty_extras(existing, requested, system_managed=...)` helper unconditionally strips system-managed keys before diffing. `api_edit_spool_wizard` migrated to use the helper — wizard CANNOT clobber slot assignments regardless of what the JS sends. Stripped keys logged as a warning so we can spot misbehaving frontends.
  - **Deployment-triggered slot-loss fix (Item 5)** — Smoking gun confirmed: `setup-and-rebuild/setup_fields.py:178` called `create_field("spool", "container_slot", ..., force_reset=True)` on every deploy. Spoolman wipes the value of an extra field when its schema is deleted, so every deployment silently zeroed `container_slot` on every spool that wasn't actively deployed at deploy-time. Removed the steady-state `force_reset=True`. Added guarded `migrate_container_slot_to_text()` — runs the destructive rebuild ONLY when current type ≠ `text`, snapshots every spool's value first, restores via individual PATCHes after the rebuild. Idempotent on subsequent deploys (no-op when type is already `text`). Added `error_check=True` parameter to `create_field` / `delete_field` for high-stakes setup calls. **Verified end-to-end against real Spoolman**: throwaway spool with `container_slot="PROD-DEPLOY-1"` survived a full setup_fields run.
  - **Unify update logic (Item 7)** — `compute_dirty_extras` is the proof-of-concept helper for the unified write surface. New "Spool / Filament write surfaces" section in `CLAUDE.md` documents every existing caller (file:line, current state, conventions) so future contributors funnel through the same pattern.
  - **`setup_fields.py` non-destructive handling (Item 8)** — Bundled with Item 5 fix above. `error_check=True` opt-in for steady-state setup calls; existing choice-field merging preserved.
  - **Test coverage** — 29 new unit tests (`test_spoolman_api_symmetry.py` 18, `test_wizard_extras_clobber_guard.py` 4, `test_setup_fields_idempotent.py` 7) + 10 integration tests + 1 upgraded existing test. Final scoreboard: **74 passed, 1 graceful skip** across all changed-code tests; zero regressions in pre-existing passing tests.

* **[2026-04-27 URGENT prod fix: text-type extras with numeric-string values]** — Branch `fix/sanitize-text-extras-no-strip-quotes`. Production-breaking bug surfaced after the prior session's merge work shipped: spools/filaments carrying any text-type extra whose value happened to look like a number (`"330"`, `"225"`, `"60"`) silently broke every subsequent update. `sanitize_outbound_data`'s generic `json.loads` round-trip parsed `"330"` as int 330 and left it unwrapped on the wire; Spoolman's text-type validator rejected with 400 (`Value is not a string`). Symptoms in user testing: slot/toolhead assignment scans didn't update slots, label-confirmed notifications never fired, force-moves no-oped, wizard backfill PATCHes against filaments with `nozzle_temp_max` set 400'd silently. Fix: expand `JSON_STRING_FIELDS` to cover every known text-type extra holding numeric-looking values (`nozzle_temp_max`, `bed_temp_max`, `prusament_length_m`, `price_total`, `drying_temp`, `drying_time`, `flush_multiplier`) plus defensively the free-form text ones (`prusament_manufacturing_date`, `sheet_link`, `slicer_profile`). Made the `JSON_STRING_FIELDS` branch idempotent so JS callers that already pre-wrap don't get double-wrapped (would have leaked literal quote chars on read-back). New regression test `test_sanitize_does_not_strip_quotes_on_numeric_string_text_extras` pins both behaviors. Two follow-ups promoted to top-of-buglist as `[CRITICAL · PRIORITY]`: (1) real Spoolman integration tests with `pytest.mark.integration` — would have caught this on CI before users; (2) silent-fail audit on every `update_spool`/`update_filament` caller — failures must surface, not return None into the void.

* **[2026-04-26 Per-Spool Prusament Scan — Hardening Pass]** — Multiple branches over a single session, addressing the bugs that surfaced once the original feature hit real-world testing. Most of these came from in-browser testing rather than the test suite, which exposed a real gap (see Testing/Infrastructure note at the bottom).
    - **Tier-1 product-id matcher** (`feature/prusament-attribute-split` → `fix/matcher-prefers-tagged-product-url`) — added `_extractPrusamentProductId` helper plus a Tier-1 disambiguator in `findFilamentMatches`. When multiple basic-gate matches tie, the matcher prefers candidates whose stored `extra.product_url` shares the same Prusament `/spool/<id>/` as the scan. Same product, different physical box = strongest possible identity signal. Symptom case: spools 252/253 (PC Blend Carbon Fiber) landing on filament 121 instead of canonical 122. Returned array now carries a `.definitive` property; ambiguous multi-matches with no Tier-1 winner trigger the duplicate picker rather than gambling on lowest-id.
    - **Duplicate picker reinstated** — `wizardRenderDuplicatePicker` shows a small alert under Step 2 with each candidate filament + `[Use selected]` and a prominent yellow `[+ Create new filament]` button. User explicitly asked: "fall back to letting the user decide if we can't after all the matching attempts." Picker only fires when the matcher returns `definitive: false`; clear single-match and Tier-1 cases stay silent.
    - **Token-aware material match** — `_materialMatches` splits both sides on whitespace/punct and accepts when the smaller token set is a subset of the larger. `PC` → `PC Blend Carbon Fiber` matches; `PE` still won't pull from `PETG` (word-boundary aware via the regex tokenizer).
    - **Filament base/attribute split on import** — `splitMaterialAndAttributes(rawMaterial, knownAttrs)` greedy longest-first matches against the canonical Spoolman `filament_attributes` choices list. Prusament's "PC Blend Carbon Fiber" → base "PC" (lands in native `material`) + attrs ["Blend", "Carbon Fiber"] (chips on `extra.filament_attributes`). Lets Spoolman's flat material filter still group all PC variants. Applied to both per-spool scan AND legacy "Import from External" button (same shared `applyFilamentFieldsFromTemplate` path).
    - **Rapid-scan race fixed** — hardware QR scanners send Enter at the end of each box; the next box's stream arrives within milliseconds. The eager focus advance now runs BEFORE the fetch begins so the scanner has a landing target. Prior version waited until the fetch resolved and lost characters (logs showed mangled `//prusament.com/...` URLs missing the scheme). Plus: row badge updates are now targeted via `wizardRenderSpoolRowBadge(idx)` instead of full-container re-render, so a status flip on row 1 doesn't destroy the input element a user is mid-typing into on row 2.
    - **Quick-clear UX** (`feature/quick-clear-failed-scan`) — small × button between URL input and badge (visible when row has any value), AND the red error badge itself is clickable. Both fire `wizardScanSpoolRow(idx, '')` to reset. Bigger target for blind-scanning workflows where focus may already be elsewhere.
    - **Spoolman PATCH-extras MERGE fix** (`fix/preserve-extras-on-spoolman-patch`) — **critical data-loss bug**. Spoolman's PATCH on `extra` REPLACES the entire dict instead of merging. A partial update like `{extra: {nozzle_temp_max: '"225"'}}` therefore wiped every other extra on the record. Symptom case: filament 157 lost `product_url`, `purchase_url`, `original_color` after a single "Use Scanned" click on max-temps. Fix: `update_filament` and `update_spool` now fetch the existing record's RAW extras (Spoolman wire form, not parse_inbound_unwrapped), pre-sanitize the caller's incoming extras, and merge so siblings ride through. New helpers `_get_raw_extras` + `_merge_extras_with_existing`. Filament 157's lost data restored via the now-safe endpoint.
    - **Original-color quote-cruft fix** (`fix/original-color-quotes`) — `original_color` is in `JSON_STRING_FIELDS`, so `sanitize_outbound_data` wraps it via `json.dumps`. The backfill code was ALSO pre-wrapping in JS, double-wrapping each scan and adding another layer every time. Fix: send raw, let sanitize do the canonical wrap once. Records already corrupted with N layers are auto-cleaned by the strict canonical-form check.
    - **Activity log shows from→to** (`fix/activity-log-always-show-diff`) — `_format_filament_edit_log` snapshots the existing record before the PATCH and emits `field: old → new` per requested field, marking unchanged fields explicitly rather than collapsing into a useless bare-key summary.
    - **product_url + purchase_url silent backfill on existing filaments** — first encounter of a Prusament filament tags it with the canonical store + per-spool URLs so the Tier-1 matcher works on future scans. Doesn't overwrite user-customized URLs.
    - **NFC hint direction fix + always-overwrite original_color + Step-2 Import-from-External runs matcher** — small follow-ups all in same area.
    - **Test isolation fix** (`fix/tests-leaking-patches-to-real-spoolman`) — earlier E2E tests mocked `/api/filaments` with real filament IDs (42, 122, 157, 192) but didn't mock `/api/update_filament`. Backfill PATCHes leaked through to real Spoolman and corrupted those records with test fixture defaults (color_hex `111111`, weight `998`, etc.). Three layers of safety added: autouse fixture intercepts `/api/update_filament` for every test in the directory, fixture IDs renamed to absurdly-high stand-ins (99042, 99122, etc.), and the `_prusament_result()` default `color_hex` changed from `111111` to `FAFAFA` so any future leak is instantly recognizable in activity logs.
    - **Tests** — across all branches: ~30 new unit tests, ~10 new E2E tests. Final wizard sweep landed at 91 passed / 1 skipped (the unrelated pre-existing Spoolman archive bug). Caveat: the merge-extras data-loss bug ONLY manifested against real Spoolman, not the mocks — surfaced the gap that prompted the buglist `[Testing/Infrastructure]` entry to add Spoolman-touching integration tests.
    - **Recovery** — filament 157's lost `product_url` / `purchase_url` / `original_color` restored via the now-safe `/api/update_filament` endpoint; the previously-set `nozzle_temp_max` + `bed_temp_max` survived the merge. User has been refreshing dev's Spoolman by copy-pasting prod's DB to keep test state clean.
    - **Honest postmortem** — most of these bugs should have been caught by automated tests but weren't, because the test suite mocks Spoolman responses entirely. The mocks always echo back what we send, so server-side behaviors (PATCH-replaces-extras, type-validator strictness on text-extras, unknown-extra-key 400s) are invisible to the suite. The buglist entry on Spoolman integration tests is the path forward.

* **[2026-04-26 Per-Spool Prusament QR Scanning in the Add-Inventory Wizard]** — Branch `feature/prusament-spool-scan`. Step 3 of the wizard now exposes one optional Prusament URL row per spool (count follows `wiz-spool-qty`). A successful scan populates that spool's `initial_weight`, `spool_weight`, `extra.prusament_manufacturing_date`, `extra.prusament_length_m`, and `product_url` without touching the row's neighbors. Rows left blank still create spools using the Step-3 default fields. Failed scans turn red and block submit until cleared — no silent fallback to defaults.
    - **Backend** — `api_create_inventory_wizard` accepts a new optional `spool_overrides: list[dict]` alongside the existing `spool_data` + `quantity`. When present it drives the spool count and shallow-merges per index, with `extra` deep-merged so wizard-wide flags (`needs_label_print`, `purchase_url`, etc.) survive. Existing `quantity`-only callers see no behavior change. New failure mode: when spool data was requested but every create failed, the parent endpoint now returns `success: false` with an explanatory message instead of misleading "Success!". `REQUIRED_SPOOL_EXTRAS = [prusament_manufacturing_date, prusament_length_m]` self-heals at startup via `ensure_required_extras()` mirroring the existing filament-side pattern.
    - **Duplicate-filament prevention** — first successful scan in `manual` mode runs `findFilamentMatches(temp)` against the cached `wizardState.allFilaments`. Match rule is fuzzy on color: exact `color_name` field equality, OR parsed color appears anywhere in stored `name`, OR stored `color_name` appears in parsed color (the third only when stored color_name is non-empty — the empty-string substring trap was a real bug we hit). Multiple matches sort by id ascending; the wizard silently auto-switches to the oldest (canonical) filament via `wizardSelectType('existing', true)` + `wizardExistingSelected()`. No picker, no clicks, no chance to accidentally create another duplicate.
    - **Mode coverage** — works whether the wizard is opened fresh (`openWizardModal`, manual mode) or pre-bound to an existing filament (`openNewSpoolFromFilamentWizard` from the Filament Details "+ New Spool" button or Location Manager's `manualAddSpool`). When pre-bound, scans never touch Step 2; every scan only feeds spool-level overrides.
    - **Repeat-submit lockout** — after a successful (non-edit) create, the wizard sets `wizardState.lockedAfterSuccess = true`, resets quantity to 1, clears the per-spool URL rows, and disables submit. The dirty-listener (input/change anywhere in the modal) lifts the lock and re-arms submit on the next user edit. `wizardValidateSubmit` and `wizardUpdateSubmitGate` honor the flag so row-resync side-effects can't silently re-enable. `wizardReset` clears the flag so a fresh wizard open isn't pre-locked.
    - **UX nits** — Spoolman text-type extras must arrive as JSON-quoted strings (`"\"330\""`); `extractSpoolFieldsFromTemplate` wraps both extras in literal quotes, matching the `nozzle_temp_max` pattern at inv_wizard.js:1493. After a successful row scan, focus hops to the next still-empty row so blind-scanning multiple boxes is hands-free.
    - **Tests** — 11 backend (`test_wizard.py` adds: `test_spool_overrides_new_filament`, `test_spool_overrides_existing_filament`, `test_required_spool_extras_includes_prusament_fields`, `test_ensure_required_extras_registers_spool_side`, `test_spool_overrides_partial_row_uses_defaults`, `test_spool_overrides_extras_deep_merge_preserves_wizard_extras`, `test_wizard_returns_failure_when_all_spools_fail`). 6 E2E in new `test_wizard_per_spool_scan_e2e.py` (golden path + Step-2 fill, auto-switch on existing-filament match, fuzzy color match for `Silver (Pearl Mouse)` ↔ `Pearl Mouse` scan, lowest-id wins on multi-match, post-success lock blocks repeat submit until edit, parser failure blocks submit). 16 JS-unit-style tests in new `test_wizard_per_spool_scan_unit.py` driven through `page.evaluate` covering every branch of `findFilamentMatches`, `extractSpoolFieldsFromTemplate`, `wizardResetSpoolRows`, and the lock-survives-resync regression. Full wizard sweep stayed green at 27 passed / 1 skipped (skipped is the unrelated pre-existing Spoolman archive bug).
    - **Real-world bugs caught + fixed during in-browser testing** — (1) Spoolman 400'd every spool create with `Unknown extra field prusament_manufacturing_date` because spool-side extras weren't auto-registered; (2) Spoolman then 400'd with `Value is not a string` for `prusament_length_m` because `String(348)` parses back as int through `sanitize_outbound_data`'s `json.loads`; (3) the matcher silently mismatched filament 157 (`Silver (Pearl Mouse)`, no `color_name` field) against the parser's `color_name="Pearl Mouse"`; (4) when junk duplicates existed in the DB the picker fired and the user dismissed it, creating yet another duplicate; (5) post-success the button re-enabled after 3 seconds with the same data still in the form, allowing repeat submits. All five fixed in this branch.

* **[2026-04-25 Locations Schema Refactor — Phase 1A: additive plumbing]** — Branch `feature/locations-parent-id-phase-1a`. First step of the multi-phase Location Manager refactor (the CRITICAL DESIGN entry under 📍 Location Management & Scanning, which blocks Project Color Loadout). Deliberately the smallest possible safe step: add the new field, write the migration, ship green. **No consumer reads `parent_id` yet** — this commit only prepares the data. Phases 1B/2/2.5/3/4/5 are tracked as a checklist inside the parent entry so the rest of the journey doesn't drift.
    - **New `parent_id` schema field** on every row in `data/locations.json`. Either an uppercase parent LocationID (e.g. `LR-MDB-1.parent_id = "LR"`, `CORE1-M0.parent_id = "CORE1"`) or `null` for top-level rows (Rooms with no `-` in the LocationID). Allowed to reference synthesized printer prefixes that don't yet exist on disk — Phase 3 will persist those.
    - **Three new functions in `locations_db.py`** — `derive_parent_id_from_prefix(loc_id)` (pure prefix utility), `resolve_parent(row_or_id, loc_list=None)` (prefers explicit `parent_id`, falls back to prefix derivation; `loc_list` accepted for forward-compat with Phase-3 FK validation), and `migrate_parent_ids_if_needed(loc_list)` (idempotent backfill mirroring the `migrate_feeder_map_if_needed` shape).
    - **Startup migration in `app.py`** sits right after the existing feeder_map migration block, uses the same timestamped pre-write backup pattern (`locations.json.pre-parent-id-migration-<stamp>.bak`), and short-circuits on second boot because `'parent_id' in row` is the migration's idempotence signal.
    - **Tests** — 16 new unit tests in `test_dryer_bindings.py` covering helper edge cases (uppercase normalization, explicit-None preservation, empty/string args, non-list input, non-dict rows, save→load round-trip), idempotence, and synthesized-printer-parent backfill. 3 new contract tests in `test_locations_json_integrity.py` that run the migration in-memory against the real file and assert every row has a string-or-None `parent_id` whose value matches the prefix-derived expectation. Full suite remains green; the 4 failures + 9 errors observed during verification are all pre-existing (confirmed by re-running them on a `git stash`-ed clean tree).
    - **Live verification on dev** — restarted `inventory_hub` Docker container; migration logged backfill for all 55 rows + the `📦` backup line + the `💾 Phase-1A migration complete` summary; on-disk file gained `parent_id` on every row; spot-checks (`LR-MDB-1` → `"LR"`, `CORE1-M0` → `"CORE1"`, `CR` → `null`, `LR` → `null`, `PM-DB-1` → `"PM"`) all match. Second restart was a clean no-op. `/api/locations`, `/api/printer_map`, `/api/get_contents` all return identical-shape 200 responses post-migration.
    - **New buglist entry filed**: `test_return_and_breadcrumb.py::test_return_prefers_physical_source_over_first_binding` was failing before this branch with `TypeError: fake_move() got an unexpected keyword argument 'confirm_active_print'`. Logged under 🧪 Testing for follow-up. Pre-existing, not a regression.


* **[2026-04-23 PM Buglist Sweep + Regression Cleanup]** — Autonomous sweep on `feature/buglist-sweep-2026-04-23-pm`. Closed 4 backlog items, plus a follow-up regression-fix sub-sweep when the user reported issues during live testing.
    - **Wizard max-temp parity** — Added `wiz-fil-nozzle_temp_max` / `wiz-fil-bed_temp_max` Min/Max-paired inputs in the Add Inventory Wizard so newly-created filaments record both temps (Edit Filament Specs parity). Wired into payload builder, external-metadata autofill, and existing-filament populate. Prusament parser extended to emit `he_max`/`hb_max`. _Regression discovered + fixed:_ raw numeric strings like `"245"` were rejected by Spoolman because `sanitize_outbound_data`'s `json.loads` parsed them as integers; wrapped values in literal quote bytes to match `inv_details.js:1617` Edit-Filament idiom. _Second regression:_ duplicate dynamic input `#wiz_fil_ef_nozzle_temp_max` was racing the static input on save (clearing the static one didn't actually clear the field) — added both keys to the wizard's legacy-field skip-list at `inv_wizard.js:648`. Tests in `test_wizard_fields.py`.
    - **Spool-weight backfill tool** — New `POST /api/backfill_spool_weights/<fid>` endpoint resolves filament → vendor inheritance and PATCHes every spool under that filament whose `spool_weight` is null/≤0 (archived included). UI button on Filament Details modal shows only when there's work to do AND an inheritable value exists. Tests in `test_backfill_spool_weights.py`.
    - **MMU M0/M1 alias dedup** — PrusaLink probe found `/api/v1/info.mmu` exposes the MMU-attached flag (hardware-static, useful as the "preferred alias" signal). New `prusalink_api.get_printer_mmu_flag()` + `app._resolve_active_locs_for_printer()` re-orders printer_map candidates so the preferred alias (`-M1` when mmu=True, `-M0` when mmu=False, insertion order when unknown) is iterated first. Both gcode-usage deduction loops (`/api/filabridge_error` + `_auto_recover_task`) now track processed positions and only deduct from the FIRST alias per position that has a spool — guaranteeing each gcode usage slice applies exactly once. Tests in `test_mmu_alias_dedup.py`.
    - **>1kg spool weight tracking** — Test-coverage-only ask: confirmed no silent 1kg cap, partial deductions persist verbatim, over-deduction clamps at initial_weight, successive deductions accumulate correctly. Five cases in `test_large_spool_weight_tracking.py`, all green on dev Spoolman.
    - **PRINTER:`<id>` sentinel slot_targets (Phase 1)** — Backend (validator + auto-deploy guard) and Location Manager combobox shipped. `slot_targets` accepts `"PRINTER:<id>"` for printer-pool staging slots that don't bind to a specific toolhead. Phase 2 (Quick-Swap grid "Printer Pool" banner row) deferred. Tests in `test_dryer_bindings.py`, `test_loc_mgr_bindings_ui_e2e.py`.
    - **Buffer-card refresh event guards (PARTIAL)** — Seven static asserts that each interactive weight-setting path still dispatches `inventory:sync-pulse`/`inventory:locations-changed` + one E2E spy. In `test_weight_setting_dispatches_events.py`. Synchronous push for backend-only writes (filabridge auto-deduct, server auto-archive) deferred — would need SSE/WS.
    - **Regression sub-sweep (live testing)** — User reported "Filament database update rejected" on temp save and "Location list broken after PRINTER:<id> assignment". Root causes: (1) wizard sending unquoted numeric extras, fixed by quoting + skip-list; (2) `data/locations.json` corruption in the XL-3 entry — pre-existing manual-edit fragment that `load_locations_list` was silently swallowing. Repaired the file, made `load_locations_list` raise `LocationsCorruptError` on `JSONDecodeError` (CRITICAL log + `/api/locations` returns structured 500), and added 10 integrity guards in `test_locations_json_integrity.py` (valid JSON / non-empty / row schema / no dup LocationIDs / per-Type minimum keys / unbalanced quotes / save-load roundtrip / no-blank-Type-on-parents / every-printer_map-prefix-has-a-parent).
    - **Printer-parent synthesizer hardening** — User then reported "🦝 Core One Upgraded shows under Unassigned virtual storage." Two contributing causes: (a) `XL` parent row had `Type: ""` and the synthesizer skipped it; (b) `CORE1` had no parent row AND no spool was deployed, so `room_occupancy["CORE1"]` was empty and synthesis didn't fire. Fixed by seeding the synthesizer from BOTH `room_occupancy` AND every `printer_map` prefix, treating blank-Type rows as needing synthesis (upgrade in place), and pulling the friendly Name from `printer_map.printer_name`. Repaired the data file with proper `CORE1`/`XL` parent rows.
    - **Branch:** `feature/buglist-sweep-2026-04-23-pm` — 9 commits, 60+ new/updated tests across the affected suites.


    - **Edit Filament — vendor/color follow-up (Wave 0)** — `openEditFilamentForm` now renders a Vendor `<select>` (loaded from `/api/vendors`) and a paired color picker + hex input. Dirty-diff handles both fields and validates hex format.
    - **Edit Filament Wave 1 — Spoolman defaults + multi-color** — added diameter, filament net weight, price, external_id/legacy ID, and a multi-color picker matching the wizard's pattern (+ button, direction selector appears at 2+ colors, `multi_color_hexes` CSV round-trips).
    - **Edit Filament Wave 2 — searchable fields + Advanced** — searchable Material via `<input list>` + `/api/materials`; searchable Vendor with inline "+ NEW" creation path that POSTs `/api/vendors` (new `api_create_vendor` endpoint) then PATCHes the filament with the returned id; collapsible Advanced section holds Original Color, Filament Attributes, Product URL, Purchase URL, Sheet Link, Legacy/External ID. Extras merge is safe (preConfirm clones and only overrides user-touched keys).
    - **Edit Filament Wave 3 — Bootstrap tabbed modal** — promoted from SweetAlert to `#editFilamentModal` with four tabs (Basic / Colors / Specs / Advanced).
    - **Edit Filament Wave 4 — polish + max-temp + Add mode** — stable modal size across tabs (min-height on tab-content); native `settings_extruder_temp` / `settings_bed_temp` now documented as min/recommended, with new `extra.nozzle_temp_max` / `extra.bed_temp_max` for the high side (closes the "track high temperatures" buglist item); color reorder with up/down buttons; Filament Attributes chip-picker; "+ NEW" badge on Material; Add mode via `window.openAddFilamentForm()` + new `/api/create_filament` endpoint.
    - **Edit Filament Wave 5 — custom comboboxes + UX** — fixed a pre-existing SyntaxError (dangling `};` in `modals_filabridge_recovery.html`) that was breaking ALL page JS; softer active-tab styling; custom combobox dropdowns for Material/Vendor with keyboard-nav + "+ Create" hint; Escape scoped to dropdowns; Nozzle/Bed repositioned; Copy-from-vendor (⇩) for Empty Spool Wt; improved `.text-muted` contrast; Spoolman error passthrough via `spoolman_api.LAST_SPOOLMAN_ERROR`; `setup_fields.py` gained `nozzle_temp_max`, `bed_temp_max`, `multi_color_direction` extras.
    - **Edit Filament Wave 6 — color fix + layout feedback** — color save was failing with "Cannot specify both color_hex and multi_color_hexes" (Spoolman rejects both set); fixed by emitting `multi_color_hexes` exclusively for 2+ colors and excluding `color_hex`; Nozzle/Bed moved back to Specs; Original Color + Filament Attributes moved to Basic; hex input Enter/Tab commits to `#rrggbb`; Price placeholder visibility fixed; "Use Vendor" button now has visible text + cyan background; vendor info pill (`ⓘ 165g empty · 2 extras`); Escape-with-unsaved-changes prompt; active-print pre-flight walks slot bindings in `perform_smart_move`; new `test_edit_filament_visual.py` snapshots all 4 tabs.
    - **Edit Filament Wave 7 — three real bugs surfaced post-Wave 6** — (a) Spoolman demanded `multi_color_direction` ("Multi-color filament must have multi_color_direction set"); confirmed via live 0.23.1 schema that it IS a native top-level field; reverted Wave 5 extras-routing mistake, direction is now always sent top-level with `multi_color_hexes`, defaulting to `'longitudinal'`. (b) Max-temp save failed with "Unknown extra field nozzle_temp_max"; added `spoolman_api.ensure_required_extras()` that runs at app startup and idempotently registers required extras so prod self-heals without a manual `setup_fields.py` run. (c) Buffer item disappeared on cancel-swap-confirm flows — `handleSlotInteraction` was eagerly shifting the held spool before `doAssign` POSTed; fixed by deferring mutation to `_doAssignFinalize`'s success branch, with `options.swapDisplaced` threaded through `doAssign` → `_confirmActivePrintAssign` → `_doAssignFinalize`.
    - **Edit Filament Wave 8 — keyboard UX + focus trap** — lost keyboard Enter-to-accept restored by routing Enter to the currently-focused button instead of blindly mapping Enter→proceed (fixes "Cancel still assigns" regression); Tab/Shift+Tab now cycles only between the overlay's Yes/No buttons (focus trap) so tabs no longer navigate outside the active window.
    - **Auto-assign to first free slot** — `perform_smart_move` now picks the lowest-numbered free slot for single-spool moves into Max-Spools>1 containers. Bulk moves intentionally skip the auto-pick. Covered by `test_auto_slot_pick.py`.
    - **`test_manual_loc_override_e2e` rewritten** — migrated from the old `<select>`-driven flow to the new `.swal-loc-item[data-id=""]` click pattern. Added a keyboard-path variant (ArrowDown + Enter). xfail marker removed; both tests pass green.
    - **Warn if spool reassignment during active print** — `prusalink_api.get_printer_state()` best-effort probes `/api/v1/status` then legacy `/api/printer`; new `/api/printer_state/<toolhead_id>` endpoint; `doAssign` in `inv_loc_mgr.js` shows an inline confirm overlay on PRINTING/PAUSED/BUSY; `inv_quickswap.js` prepends a warning banner inside the existing confirm overlay. Fail-open on network hiccup. **Follow-up**: probe moved into `showConfirmOverlay` itself so every caller (quick-swap, deposit, return-to-slot, empty-return) picks it up; probe fires async so the overlay opens instantly and the banner slots in when the response lands. Backend enforcement: `perform_smart_move` / `perform_smart_eject` now accept a `confirm_active_print` kwarg and bail with `requires_confirm` unless the UI has explicitly confirmed, including when the target is a Dryer Box slot bound to an active toolhead via `extra.slot_targets`. Covered by `test_printer_state_api.py`, `test_active_print_warning_e2e.py`, `test_active_print_backend_enforcement.py`.
    - **Active-print confirm overlays now scan-confirmable** — new `window.attachConfirmQRs()` helper mounts a polished QR pair (~70px each, white-on-tinted-warning-border tiles with "📷 SCAN TO CONFIRM" / "📷 SCAN TO CANCEL" labels) inline below Yes/No. Each session gets a unique id (`CMD:CONFIRM:<sid>` / `CMD:CANCEL:<sid>`) so a stale printed QR can't fire into the wrong dialog. Wired into Location Manager `doAssign`, scan-based `_confirmActivePrintScan`, and the Quick-Swap `showConfirmOverlay` (only when the active-print banner is up). `processScan` intercepts `CMD:CONFIRM` / `CMD:CANCEL` at the top before normal dispatch. Covered by `test_confirm_qr.py` + `test_confirm_qr_visual.py`.
    - **Search by deployment status** — `/api/search` accepts `deployed` query param (`deployed` | `undeployed` | `` / `any`). A spool counts as deployed when its Spoolman location is in `printer_map` OR it carries a ghost `extra.physical_source` pointing at a toolhead. Frontend: small select dropdown in the search offcanvas next to Min Weight. Reset button also clears this filter. Covered by `test_search_deployed_filter.py`.
    - **Slot render direction LTR/RTL toggle** — per-dryer-box `extra.slot_order` (`'ltr'` | `'rtl'`), new `/api/dryer_box/<id>/slot_order` GET/PUT endpoints, radio toggle in the Location Manager feeds editor. `renderGrid` reverses iteration when rtl. Grid re-renders immediately after Save Feeds (busts `state.lastLocRenderHash`). Covered by `test_slot_order_api.py` + `test_slot_order_ui_e2e.py`.
    - **Confirmed-label ✅ indicator on spool cards** — `format_spool_display` emits a normalized `needs_label_print` bool; `SpoolCardBuilder.buildCard` renders a small green ✅ next to the 🖨️ Add-to-Queue button when the flag is explicitly `false`. The 🖨️ button is untouched; the ✅ is purely a status indicator. Covered by `test_label_confirmed_indicator.py`.
    - **Buffer-shift safety** — `handleSlotInteraction` no longer eagerly mutates buffer before `doAssign` POSTs; mutation is deferred to the success branch of `_doAssignFinalize`, with `swapDisplaced` threaded through the confirm chain so the displaced spool lands in the buffer only on success. Covered by `test_doassign_buffer_safety.py` (11 tests).
    - **Remaining gaps / follow-ups** left as NEEDS INPUT / NEEDS REPRO entries in the active buglist: wizard-side max-temp parity, external-metadata import panel (Prusament / open-filament-database), backfill tool for historical `spool_weight=0` rows. (feature/buglist-sweep-2026-04-23, 2026-04-23)


* **[2026-04-22 Buglist Sweep]** — Closed 10 backlog items on `feature/buglist-sweep-2026-04-22`. 48 new tests added (15 serialization unit tests, 10 Playwright priority-resolver tests, 7 filament-edit tests, 8 archive-prompt tests, plus backend/logic unit tests).
    - **Manufacturer text casing** — `format_spool_display` and `search_inventory` no longer `.title()`-case vendor names; CC3D stays CC3D, eSUN stays eSUN, etc. (`spoolman_api.py`)
    - **Auto-Deploy activity log brand + color** — Auto-deploy entries now read `⚡ Auto-deployed Spool #42 — Polymaker PLA (Crimson Red) → TOOLHEAD …`, sourced via new `logic._spool_brand_color_suffix()` helper that no-ops on Spoolman fetch errors.
    - **Empty Spool Weight priority (Manufacturer > Filament > Spool)** — New `window.resolveEmptySpoolWeight({spoolWt, filamentWt, vendor})` helper walks the inheritance chain (null/undefined/0/'' = unset). Wired into wizard filament-load, spool-load, clone, and save paths so a vendor-level weight flows down instead of surfacing as blank.
    - **`text-pop` audit** — Added `text-pop` to two card-text rows that sat next to siblings already using it (`ui_builder.js` text-line-3 on slot-info-gold; `inv_loc_mgr.js` nav-text-main on "READY TO SLOT").
    - **Spool ↔ Filament card gradient parity** — Filament branch of `search_inventory` no longer truncated `multi_color_hexes` via `.split(',')[0]`; now passes the full CSV so filament cards render tri-color gradients identical to their spool siblings. Default `color_direction` now `'longitudinal'` to match the spool branch.
    - **Playwright on Docker question** — Closed without code change: CLAUDE.md already documents the intentional host-install pattern (Docker image stays slim, `requirements-dev.txt` + `playwright install chromium` on host, `.dockerignore` excludes `tests/`).
    - **Filament Edit button (MVP)** — New "✏️ Edit Filament" on the Filament Details modal footer. Opens a Swal form with name/material/empty-spool-weight/density/nozzle/bed temps/notes; the empty-weight input surfaces the vendor's `empty_spool_weight` as a placeholder hint. Saves via new thin `/api/update_filament` endpoint. preConfirm returns a dirty-diff so no-op saves are never POSTed. (Follow-ups kept in active backlog: backfill tool for legacy `spool_weight=0` rows; vendor + color_hex editing.)
    - **Auto-archive on weight-0** — `spoolman_api.update_spool` now injects `archived: True` + `location: ''` whenever a weight-touching payload would leave `remaining_weight <= 0`. New `_auto_archive_on_empty()` helper respects caller intent (explicit `archived: False` or explicit `location` are preserved). Activity Log fires on the transition. Single pre-fetch of the spool shared between the used_weight cap and the auto-archive check, so net effect is one fewer Spoolman GET per update.
    - **Archive-empty-weight prompt (dedicated modal, option B)** — After an auto-archive, if the filament has no empty-spool-weight anywhere along Spool → Filament → Vendor, a Swal modal (`window.showArchiveEmptyWeightPrompt(spoolId, filamentId)`) pops from the weigh-out finalize path with title, explanation, weight input, and `[Save] / [Later] / [Cancel]`. Save writes to `/api/update_filament`; future spools of the filament auto-inherit the value via the priority resolver.
    - **`/api/spool/update` response shape** — Now returns `auto_archived`, `needs_empty_weight_prompt`, and `filament_id` so any caller (not just weigh-out) can trigger the archive-empty-weight prompt.


* **[2026-04-22 Manage-Modal Default Pickup + Buffer Race + Slot-Actions Modal Removal]** — three coordinated UX/bug items on `feature/manage-default-pickup-and-buffer-race-fix`. (1) **Default action for unslotted-item card body = Pick Up** (Feature-Buglist line 51). Was `openSpoolDetails(id)`, now `ejectSpool(id, locId, true)` at [inventory-hub/static/js/modules/ui_builder.js:113-114](inventory-hub/static/js/modules/ui_builder.js#L113-L114) inside the `'loc_list'` mode branch. The 🔍 Details affordance stays on its icon button ([ui_builder.js:120](inventory-hub/static/js/modules/ui_builder.js#L120)). `'loc_list'` mode is used only by `renderUnslotted()` at [inv_loc_mgr.js:895](inventory-hub/static/js/modules/inv_loc_mgr.js#L895), so the change is precisely scoped. (2) **Buffer race: manual-entry IDs getting wiped on refresh** (Feature-Buglist line 16). Root cause: `persistBuffer()` at [inv_cmd.js:61](inventory-hub/static/js/modules/inv_cmd.js#L61) was silently skipped when `isBufferSyncing===true`, then `loadBuffer().then` at [inv_cmd.js:373-387](inventory-hub/static/js/modules/inv_cmd.js#L373-L387) overwrote `state.heldSpools` with stale server state — any user add (manual *or* barcode) inside the ~500 ms window around the 2 s heartbeat was lost. Fix: `renderBuffer` now bumps `window.lastLocalBufferChange = Date.now()` and queues `window.pendingPersist = true` when a user mutation lands during a sync; `loadBuffer` skips its overwrite when `localAge < 3000 ms` and flushes any queued persist on resolve/catch. A `window.suppressBufferDirty` sentinel wraps the one server-driven `renderBuffer` call inside `loadBuffer` so it isn't mis-classified as a local mutation. Protects both barcode and keyboard sources uniformly. (3) **Redundant Slot Actions modal removed** (Feature-Buglist line 55). The `promptAction` modal launched by `handleSlotInteraction` when clicking a filled loc_grid slot with the buffer empty used to offer Pick/Eject/Details — all three already exist as explicit action buttons on the slot card itself. [inv_loc_mgr.js:939-946](inventory-hub/static/js/modules/inv_loc_mgr.js#L939-L946) now performs Pick Up directly, mirroring (1). The Swap/Overwrite/Cancel chooser at [inv_loc_mgr.js:910-931](inventory-hub/static/js/modules/inv_loc_mgr.js#L910-L931) is preserved — it's a genuine destructive choice. Note: `promptAction` generated scannable `CMD:MODAL:N` QR codes for hands-free action selection ([inv_core.js:512](inventory-hub/static/js/modules/inv_core.js#L512)); that advanced-scanner affordance can come back later as a localStorage or Config-button toggle if it ends up being missed. **Tests added** ([test_unslotted_default_action.py](inventory-hub/tests/test_unslotted_default_action.py) × 2, [test_buffer_race_protection.py](inventory-hub/tests/test_buffer_race_protection.py) × 4, [test_slot_action_modal_removed.py](inventory-hub/tests/test_slot_action_modal_removed.py) × 2). Full suite: 231 passed / 7 skipped / 3 pre-existing unrelated failures (Amazon parser — BS4 blocker from buglist On-Hold, + 2 cross-file `openManage` flakes that pass in isolation, same pattern as the 2026-04-21 session). **Investigation dropped:** buglist line 3 (Spoolman filament archival) was initially the target; investigation confirmed our code intentionally tries `update_filament({archived: False})` but Spoolman 0.23.1 is the latest release, no release has ever supported filament archival, and no upstream issue or PR exists — option (a) "upgrade" is a dead end. Left entry in place; future fix path is option (b) custom `extra` field workaround, or file upstream. (feature/manage-default-pickup-and-buffer-race-fix, 2026-04-22)

* **[2026-04-22 UI Regression Cleanup]** — three items from the `[IMPORTANT]` active-backlog queue, shipped on `feature/ui-regression-cleanup` (merged commit `9c06dd2`). (1) **Search offcanvas click-intercept** — Bootstrap's `.offcanvas` hides itself via `visibility: hidden`, which does not block pointer events, so `#global-search-material` inside the hidden subtree kept grabbing clicks on spool cards behind it after the offcanvas was closed. Fixed in [inventory-hub/static/css/global.css](inventory-hub/static/css/global.css#L829-L831) with `.fcc-offcanvas-search:not(.show) { pointer-events: none; }`. This single CSS rule unblocked `test_ui_details_modal_e2e::test_details_modal_interactions` and 6 of 7 tests in `test_force_location_keyboard_e2e.py`. (2) **`.navbar-brand-group` 3 px vertical overflow** — the container had no layout rules and inherited Bootstrap defaults, so `.navbar-brand` at `font-size: 1.5rem` + `.version-tag` badge padding produced 39 px inside a 36 px client box. Added a flex rule at [global.css:44-49](inventory-hub/static/css/global.css#L44-L49) with `display: flex; align-items: center; gap: .5rem; height: 100%;` — now `scrollHeight === clientHeight` and `test_structural_global_dashboard` + `test_structural_global_modals` pass. (3) **Cascading Force-Location Escape fix** — with the offcanvas regression gone, one remaining failure surfaced: `test_force_location_escape_toggles_overlay`. SweetAlert2's focus-trap stops keyboard-event propagation between window-capture and document-capture whenever focus lands on a button inside `.swal2-popup`, so the bubble-phase popup listener at [inv_details.js:577](inventory-hub/static/js/modules/inv_details.js#L577) never saw the second Escape. Moved the listener to `window.addEventListener('keydown', …, true)` with a cleanup hook in `willClose`; all 7 force-location tests now pass. Regression test added in [test_search_e2e.py](inventory-hub/tests/test_search_e2e.py) covering the closed-offcanvas pointer-events invariant, the hit-test on `#global-search-material`'s own coordinates, and the reopen round-trip. Full suite: 224 passed / 7 skipped / 2 pre-existing unrelated failures (Amazon parser — BS4 not installed, documented on-hold; `test_ui_details_modal_e2e` order-dependent flake that passes in isolation). **Dryer-box initial-load flicker zombie-entry removed** — item was investigated this session (two consecutive `/api/get_contents?id=<db>` calls are byte-identical; initial open renders all cards complete on first paint; 5-s tick's `state.lastLocRenderHash` guard short-circuits; closing/reopening produces byte-identical HTML) and then discovered to have been fixed two weeks earlier by commit `9e96adf` on 2026-04-08 ("fix: resolve location manager spool sync UI pop-in and recalculation race conditions") — the fix is already archived at line 53 of this file. The backlog entry had simply never been struck when the fix shipped. Removed from [Feature-Buglist.md](Feature-Buglist.md). (feature/ui-regression-cleanup, 9c06dd2, 2026-04-22)

* **[2026-04-22 Filabridge Move-Ordering Fix]** — Root-cause fix for the filabridge desync class that started with the 2026-04-22 incident (#240 moved Core1-M0 → LR-MDB-1:SLOT:3 bound to a toolhead; Spoolman updated, filabridge didn't). Filabridge enforces a one-spool-one-toolhead invariant and was silently 409'ing map calls whenever the spool was still mapped to its origin toolhead — the bare `except: pass` at `logic.py:233` hid every rejection, so the desync was invisible until the UI froze. **Two-commit fix on `feature/filabridge-move-ordering`:** (1) `acf309c` added `_fb_write()` helper (checks response, returns `(ok, detail)`, never swallows), `_toolhead_of()` location-to-toolhead resolver, and a new `origin_toolhead` kwarg threaded through `perform_smart_move`'s auto-deploy recursion so Phase 2 knows the spool's pre-phase-1 toolhead after Spoolman has been rewritten to the dryer box. Printer branch now unmaps origin before Spoolman write + destination map, and aborts the map if the unmap fails. Removed `suppress_fb_unmap` from `perform_smart_eject` entirely — it was a footgun that only existed to paper over the ordering bug. All 6 existing filabridge POST sites (smart_move / eject / force_unassign / undo x3) routed through `_fb_write()`. Result dict gains `filabridge_ok` / `filabridge_detail` / `filabridge_outcomes`, auto-surfaced by all 5 API endpoints; new `window.maybeWarnFilabridge()` in `inv_cmd.js` raises a 7 s warning toast on failure. (2) `efa15dd` added `_fb_spool_location()` — a pre-flight `GET /api/status` query that becomes the authoritative origin, because Spoolman's `location` can lag filabridge (stale desyncs, manual DB edits, prior failed moves). Live-verified: spool #213 had Spoolman at CORE1-M0 while filabridge still held it on XL toolhead 2; the Spoolman-only snapshot short-circuited as origin==destination, but the filabridge query correctly identified XL-2 and unmapped it before the CORE1 map. **Tests:** 5 new ordering tests in `test_filabridge_move_ordering.py` (cross-toolhead, two-step dryer-slot semantics, reject-aborts-map, filabridge-authoritative, same-toolhead no-op); updated `test_universal_fallback.py` to drop the obsolete `suppress_fb_unmap=True` assertion. Full suite: 224+ passed, 5 failures all pre-existing (offcanvas-intercept x3, navbar overflow x2, BS4-missing x1). **Backlog item added:** "Filabridge ↔ Spoolman reconcile utility" under Filabridge Error Recovery — `_fb_spool_location()` prevents new desyncs but doesn't heal existing ghost mappings. (feature/filabridge-move-ordering, acf309c + efa15dd, 4/22/2026)

* **[2026-04-22 Wizard UX Polish Bundle]** — five coordinated Add/Edit Inventory Wizard improvements on `feature/wizard-ux-polish`. (1) **Spool/Filament ID badges** in the modal title + "— Working on 🧬 Filament #Y" / "🧵 Spool #X" context chips appended to the Step 2 and Step 3 section headers, all toggled via a new `wizardSetContextLabel({ action, spoolId, filamentId })` helper. Add-mode stays clean (no badges). (2) **Vendor picker** rewritten from `<select>` into a keyboard-searchable combobox (text input + hidden vendor_id + filtered dropdown). Toggle-to-new-vendor still works. (3) **Location picker** gets the same combobox treatment; hidden `#wiz-spool-location` preserves the LocationID so all downstream readers are untouched. Both comboboxes reuse a new `wizardBindCombobox({ searchId, hiddenId, dropdownId, items, placeholder })` helper styled after the existing material autocomplete. (4) **Auto-unarchive parent filament** added to `/api/create_inventory_wizard` — when a spool is added to a pre-existing filament flagged as archived, the endpoint un-archives it automatically and logs to Activity Log. (Blocked end-to-end by Spoolman not exposing filament archival — see the new `[IMPORTANT]` entry in the active buglist.) (5) **Nested `Swal.fire()` at `wizardPromptNewChoice` killed** — replaced the "Added!" success dialog with a 3 s `showToast('success')` so the parent wizard Swal no longer gets clobbered. Companion test file `inventory-hub/tests/test_wizard_ux_polish.py` locks in the behaviour (4 passing + 1 skipped on Spoolman limitation). Test-suite housekeeping along the way: `test_weigh_out_e2e` now depends on the `clean_buffer` fixture (fixes stale-buffer flake), quickswap visual baselines refreshed (+22 px from the earlier contrast-guard CSS round). (feature/wizard-ux-polish, 4/22/2026)

* **[Master Feature] Dryer Box ↔ Machine Assignment & Quick-Swap UI — fully shipped to main** — 3 phases + 8 milestones + 11 feedback rounds + contrast-guard round + runtime-state refactor + universal auto-deploy refactor. See `inventory-hub/app.py`, `inventory-hub/logic.py` (perform_smart_move auto_deploy chain), `inventory-hub/locations_db.py` (slot_targets schema + `data/` migration), `inventory-hub/static/js/modules/inv_quickswap.js`, `inv_loc_mgr.js` (Feeds editor + breadcrumb + Escape-through-stack), `shortcuts_registry.js`, `static/css/global.css` (contrast guard). Test coverage: 100+ tests across unit / API-path / UI-path / visual including `test_contrast_guard.py` (WCAG AA guard). Notable follow-ups this round: (a) runtime state moved under `inventory-hub/data/` and gitignored to stop pull-overwrites on prod; (b) `.githooks/pre-commit` backstop; (c) auto-deploy centralized in `perform_smart_move` so every caller (not just scan) deploys to bound toolheads + notifies Filabridge. Final merged commits: feature M0–M7 (193f8a4…4794ddc), M7 cleanup (9168c29), feedback rounds 1–11 (965035a, a3c89d1, 3f89cb5, a308388, 5fe1524, d2b1ec2, 08ee5be, 4f0791d, 575180b, 02896a5, 07c95de, 1caff3f), contrast guard (394c3c7), untrack+data-refactor (0180ac6, 47c51d5). Merged to main: 7e0ebe9, 53f9058. (main, 4/21/2026)
* **Keyboard Shortcuts Reference:** Shipped the `?` keyboard-shortcut overlay driven by `static/js/modules/shortcuts_registry.js`. Overlay lists every app-wide shortcut plus a dedicated **Scan Commands** scope enumerating `FIL:<id>`, `LOC:<id>`, `LOC:<id>:SLOT:<n>`, `ID:<n>`, `LEG:<n>`, and every `CMD:*` variant with their meanings. `?` toggles from anywhere outside text inputs; `Esc` closes. Extendable via `window.registerShortcut({id, scope, keys, description})` so any future shortcut added elsewhere auto-lists itself.
* **Scanning / Barcode Parsing:** Hardened location-barcode parsing and generation. `logic.resolve_scan()` now matches `LOC:<id>:SLOT:<n>` FIRST via a non-greedy regex at `logic.py:15-23` before any bare-`LOC:` or legacy fallbacks, eliminating the "legacy location label scanned" error on deploy QR codes. Slot QR codes generated from the UI use the `LOC:` prefix exclusively. Regression-locked via `tests/test_slot_qr_scan_api_e2e.py`, `tests/test_assignment_logic.py`.
* **Location Systems / Toolhead View:** Shipped the Quick-Swap grid on the toolhead view. For any toolhead (Tool Head / MMU Slot / No MMU Direct Load) or virtual printer (`Type="Printer"`), the grid aggregates every (box, slot) across all Dryer Boxes whose `extra.slot_targets[slot]` points at this toolhead, grouped by source box with per-button spool swatch, material, weight. Tap or keyboard-navigate (`Q` to focus, arrows to move, Enter to confirm) to load the slot's spool into the toolhead — no scan needed. Deposit mode (empty slot + buffered spool) reuses the same button with a distinct style. Reverse flow ("↩️ Return to Slot") sends the loaded spool back to its `physical_source` slot. See `inv_quickswap.js`.
* **[Master Feature] Dryer Box ↔ Machine Assignment & Quick-Swap UI** — 3 phases, 8 milestones (M0–M7). Slot-QR scans now auto-load the buffered spool into the target toolhead/MMU slot (fixes the "Slot Based QR codes…" backlog bug). Per-slot Dryer Box → Toolhead bindings stored under `extra.slot_targets` in `locations.json`, editable via a new "🔗 Slot → Toolhead Feeds" section in Location Manager. Quick-Swap grid on the toolhead view aggregates slots from all boxes feeding that toolhead — tap or keyboard-navigate + Enter to swap; no scan needed. Supports split boxes (multiple boxes per printer), partial use (unassigned slots), and scales to indxx-style high-toolhead-count printers. New `?` keyboard shortcut reference overlay driven by `shortcuts_registry.js`. Full test suite: 67+ new tests across unit, API-path E2E, UI-path E2E, and visual snapshots at 1600×1300, plus a PIL-backed visual regression harness. Fixed three of four pre-existing known-failing tests (grid button count, weigh-out emoji, search_api min_weight). Legacy `config.json:feeder_map` retired with back-compat startup migration. (feature/dryer-box-machine-quickswap, M0:193f8a4 M1:e02864d M2:aa7dc89 M3:e3fe3d0 M4:6903084 M5:5f2047c M6:4794ddc M7:pending, 4/20/2026)

* **[2026-04-21 Audit Cleanup]** — Swept Feature-Buglist.md against current code to surface items already resolved by the dryer-box-quickswap merge and earlier wizard fixes. Promoted nine entries to this archive:
    - **Dryer-box scan auto-ejects prior occupant**: scanning a new spool into an occupied dryer-box slot now clears the existing spool's `container_slot` before assigning the new one via `spoolman_api.update_spool()` with a full extra-merge (`logic.py:193-202`).
    - **Ejecting from the Command Center buffer clears deployed flag**: `perform_smart_eject` clears `container_slot`, `physical_source`, and `physical_source_slot`; `deployed_to` is computed dynamically from `is_ghost`. Validated by `tests/test_deployed_flag_preservation.py`.
    - **Smart eject fires on occupied toolhead (CoreOne+)**: `perform_smart_move` at `logic.py:152-157` explicitly checks `is_printer or is_toolhead` and calls `perform_smart_eject(rid, suppress_fb_unmap=True)` on the prior occupant before deploying the new spool; FilaBridge unmapping handled at `app.py:276-279`.
    - **Edit → close → unwanted Details popup**: the wizard's `hidden.bs.modal` listener now only re-opens Details when `wizardState.returnToFilamentId`/`returnToSpoolId` are set. `openEditWizard`/`openCloneWizard` at `inv_wizard.js:1511`/`:1395` set and clear the flags, so editing from a non-Details context no longer pops Details. (Sub-bug "Display modal on Display modal" at `inv_details.js:380-398` remains open — kept in the active buglist.)
    - **Eject button on unslotted items actually removes**: `window.ejectSpool` → `window.doEject` → `/api/manage_contents` (action `'remove'`) → `perform_smart_eject`, which explicitly zeroes `container_slot` before the Spoolman PATCH. No more flash-and-disappear.
    - **Buffer cards show current location**: `ui_builder.js:277-282` renders a location badge on dashboard buffer cards (location for undeployed, `deployed_to` for ghost items) with click-to-open-manage.
    - **Server reboots no longer reset dryer slot numbers**: slot assignments persist as `slot_targets` in `extra` on each Dryer Box in `locations.json`. Startup migration at `app.py:16-26` carries any legacy `feeder_map` entries forward on first boot.
    - **`CR-MDB-1:SLOT:4` parses as slot, not location**: `logic.py:15-23` checks the `LOC:(.+?):SLOT:(\d+)` pattern (non-greedy) before the bare-location fallback; locked in by `tests/test_slot_qr_scan_api_e2e.py` and `test_assignment_logic.py`.
    - **Advanced Search accepts a Filament selection**: `wizardOpenGlobalSearch()` at `inv_wizard.js:1015-1070` now handles `type === 'filament'` callbacks (fetches full filament details and populates the wizard); previously only handled spools.
    - Noted-in-place (not archived): one remaining nested `Swal.fire()` at `inv_wizard.js:895` — addressed in the Wizard UX Polish bundle on `feature/wizard-ux-polish`.

* **Nested Swal.fire() in wizardPromptNewChoice killed** — replaced the "Added!" success dialog inside the add-new-custom-field `.then()` chain (formerly `inv_wizard.js:895`) with a 3-second green `showToast()`. SweetAlert2 can't stack modals, so this fixed the last known place where a nested Swal would clobber its parent. The top-level "No nested Swal.fire()" project convention (active backlog 🗂️ Modals section) remains as a standing rule. (feature/wizard-ux-polish, 4/22/2026)

* Force location modal keyboard navigation — Arrow keys to navigate, Enter to select, Escape with inline confirmation overlay, auto-focus on search input. Added Playwright E2E tests. (feature/force-location-keyboard-nav, a76455a, 4/16/2026)

* **Escape Key Unsaved Warning** — Added dirty-state tracking and a SweetAlert2 confirmation dialog to the Add/Edit Inventory wizard. Pressing Escape (or clicking X/Cancel) with unsaved form data now prompts "Discard & Close" vs "Keep Editing" instead of silently discarding. Covers text inputs, selects, checkboxes, and chip tag additions/removals. Dirty flag resets on successful save so rapid-add workflow is unaffected. Two new Playwright tests added to `test_ui_structural.py`.
* **Double archive Badge** — Removed duplicate "📦 ARCHIVED" badge from the color name line. Kept the Row 3 (weight area) badge and updated its style to match the preferred look (`px-1` + `text-pop` shadow). Extended `test_structural_archived_badges` in `test_ui_structural.py` to assert the badge never appears inside `.text-line-3` or `.fcc-card-title`.
* **System/UX:** Finalized the FilaBridge Error Recovery architecture. Implemented fully-automated background polling utilizing intelligent HTTP `Range` caching with robust RAM fallbacks (handling natively forced HTTP 200 payload blobs). Added freeze-frame JSON spool snapshotting mapping strict Error IDs across multi-printer setups instantly. Injected real-time parsing progress trackers, rigorous math breakdown deductions straight into High-Contrast red error UI logs (`[X at start -> Y remaining]`). Constructed manual local-PC `FileReader` upload fallback into the recovery modal. Defaulted configuration to `True` unconditionally.
* **Modals/Wizard:** Added spool-level "Price Paid ($)" field to the Add/Edit Inventory Wizard (Step 3), wired to Spoolman's native `spool.price` attribute. Pre-fills during Clone and Edit operations.
* **Modals/Wizard:** Added spool-level "🔗 Purchase Link" field to the Add/Edit Inventory Wizard (Step 3). Registered `purchase_url` as a new spool extra field in Spoolman and wired it into create/clone/edit flows with proper JSON string sanitization.
* **Bug Fix:** Fixed Clone Spool silently dropping `spool_weight` (empty weight) due to a field name mismatch. The wizard JS sent `empty_weight` but Spoolman's API expects `spool_weight`. Renamed all references throughout the codebase and UI labels from "Empty Weight" to "Spool Weight" to match Spoolman terminology. Added filament-level fallback during clone when spool-level weight is null.
* **Modals/UX:** Implemented automatic detail modal re-open after wizard close. Spool and Filament detail modals now intelligently re-appear when the Edit/Clone/New Spool wizard is dismissed, detecting context (spool vs filament) to return to the correct parent view.
* **Location Systems:** Synchronized the offline P-Touch label generator (`generate_location_labels.py`) with the core Web API structure. The offline generator now correctly includes explicitly sanitized `Cleaned_Name` strings and accurate `Slot` matrix identifiers to support hardware printers lacking Unicode compatibility.
* **Location Systems:** Unified standard CSV output filenames globally; both explicit Web API generation routes and offline Python utility pipelines now consistently target the `slots_to_print.csv` artifact simultaneously, eliminating confusion.
* **Modals / UX:** For the Location Override Modal, completely replaced the native \`<datalist>\` implementation with a custom SweetAlert2 scrollable, live-filtering dropdown. This safely limits bounding constraints and allows instantaneous search visibility without restricting usability for exceptionally long location lists.
* **UI / Fast Flow:** Added a dedicated 'Drop from Buffer' (`❌`) action directly into the primary top-center "Ready to Slot" buffer carousel card within the Location Manager modal. This guarantees users can seamlessly thrash unwanted spools dynamically while inspecting locations without forcing an exit.
* **UI/UX:** Fixed the FilaBridge status light blinking erroneously by expanding the background polling API timeout thresholds.
* **UI/UX:** Fixed the legacy location label scan toast disappearing too quickly by extending its duration and appending the raw barcode string directly into the persistent activity log entry.
* **UI/UX (Quick Weigh):** Fixed gray-on-gray low-contrast text across the Quick Weigh, Filament Details, and Spool Details modals. Upgraded Bootstrap components from `.text-muted` to `.text-light` universally.
* **Modals:** Replaced the Quick Weigh SweetAlert2 overlay with a completely native, non-conflicting Bootstrap Modal, resolving strict focus-trapping behaviors that prevented text input inside nested views.
* **Feature:** Enhanced the Quick Weigh Adjustment field to natively parse mathematical operators. Standard inputs strictly deduct mathematically for smooth usage tracking, while explicit prefixes (e.g., `+50`) automatically convert into additive weight models. Bound explicitly to the `<Enter>` key listener for zero-click saving.
* **Bug Fix:** Fixed the Search Engine and Main Command Center Buffer desynchronizing dynamically from Spool weight updates. The QuickWeigh backend sequence natively intercepts the Spool ID modifications and injects a local payload pulse to universally patch and rapidly re-render background/shadow DOM states behind modals *without* initiating a Spoolman network query.
* **UI/UX:** Fixed Search Cards and Display Cards displaying 'Generic PLA' by correcting backend Spoolman API data serialization. Refactored the unified card layout structure to hierarchically stack the Filament Material (with smart attributes), Brand, and Color Name distinctly below the location badge, separating them from the top identifier row mathematically and improving contrast readability.
* **Location/Ghost:** Fixed Ejecting an item from a toolhead/MMU slot defaulting to the root printer location by implementing "Universal Fallbacks". Spools now inherently track their exact physical source (including sub-slots) globally across the entire Location Manager, and gracefully revert directly back there when ejected from an active consumer.
* **Filabridge Sync:** Engineered explicitly mirrored API sequences (`spool_id: 0`) between Spoolman Ejections and FilaBridge Toolhead Mappings. Pressing Eject or Force Unassign now securely zeroes out the FilaBridge mapping on PrusaLink.
* **Feature:** Built "Swap Protection" logic within the Ejection architecture. If a user manually replaces an active Spool Mid-Print, the `Eject` protocol dynamically suppresses the FilaBridge `spool_id: 0` signal, enabling smooth tracking continuity while cleanly overwriting the slot assignment with the replacement Spool.
* **Location Systems:** Hardened location scanner validation logic. Scanners and QR readers now fully utilize the `perform_smart_move` universal mappings, completely eliminating assignment drift where barcodes were "failing to assign filaments to correct locations."
* **Bug Fix:** Fixed explicit edge-case where active spools retained database location artifacts (ghost data) inside the location manager after forced ejections. They now correctly clear `physical_source` caches and fall to Unassigned.
* **UI/Location Manager**: Eliminated frontend data pop-in latency for Dryerboxes by matching the initial load data payload structure directly to the tick data array, ensuring immediate synchronized rendering.
* **UI/Location Manager**: Fixed header occupancy race conditions where assigning a new spool wouldn't correctly recalculate the active header counts until the backend tick fired. Header counts are now mathmatically calculated in real-time on the client side during events.
* **Bug Fix:** Fixed "Deposit here" barcode for adding items to a location from the buffer throwing an error when scanning by explicitly generating the barcode with a `LOC:` prefix.
* **System:** Fixed the WakeLock persistence logic systematically failing on laptops by rewiring the API acquisition out of exponential visibility-change loops and anchoring it correctly to the primary DOMContentLoaded pulse.
* Filaments no longer stay in there dryerboxes when assigned to a printer head. (Deployed State)
* **Bug Fix:** Fixed the `Edit Location` override button on the Spool Details modal appearing unresponsive by elevating the SweetAlert2 popup's z-index to `15000!important` and bounding the `.swal2-container` directly to the `spoolModal` target, circumventing Bootstrap 5 modal stacking overlaps and focus-traps dynamically introduced by the `inv_core.js` layering engine.
* **Bug Fix:** Fixed spools incorrectly spawning a sticky Yellow `Deployed` ghost copy inside their container box when ejected from the Location Manager. Adjusted the `perform_smart_eject` unassign logic to explicitly wipe the `physical_source` and `physical_source_slot` legacy fields during a forced ejection, preventing the fallback logic from falsely categorizing the item as deployed to a room.
* **Location Systems:** Engineered structural fallback hierarchies for Spools inside physical Rooms. Transient boxes (like MDB-1) seamlessly group spools into their parent Virtual Room boundaries during smart assignment workflows, preventing items from dropping into `Unassigned/Loose` buffers unnecessarily. 
* **Location UI:** Engineered explicit grouping sub-headers inside the interactive `Location Manager` grid panels. Directly segregates and sorts internal physical sub-containers dynamically behind colorful bounded boxes nested perfectly beneath any globally targeted Room Parent.
* **Location Configuration:** Injected dynamic root sorting capabilities to strictly prioritize native Printer allocations to the pinnacle of Location Tables. Included newly customizable assignments for Root Room systems and explicitly segmented `Virtual` (Unassigned) badges graphically to eliminate structural context confusion for Transient Carts and Rooms.
* **UI/UX:** Improved readability for the status line, Location type, and amount of spools (1/x) with specific high-contrast color indicators for fully packed boxes.
* **Buffer Sync:** Fixed loading spools into buffer from filament definition failing to load all advanced spool context data (like location/status) into the card correctly natively.
* **Location/Ghost:** Fixed Ejecting an item from a toolhead/MMU slot carelessly wiping the slot tracking data. The slot correctly unsets the 'deployed' label natively but preserves legacy container_slot assignments consistently.
* **Modals:** Added a "➕ New Spool" button natively into the Filament Details popup to directly open a pre-filled Add Inventory Wizard. Added seamless UI fallbacks to intelligently propagate Filament `purchase_url` and `product_url` data down into the Spool Card details whenever the spool itself lacks explicitly tracked links.
* **System/UI:** Implemented full Multi-Color Filament support in the Add Inventory Wizard. Added robust handling for Spoolman 0.19.1+ schema mapping of mutually exclusive solid `color_hex` vs `multi_color_hexes` and `color_direction` attributes, and stabilized background REST API synchronization cascades across the global Dashboard and Search grids to instantly redraw complex slice-gradients without flashing DOM structures.
* **Bug Fix:** Fixed explicit `null` data-type bypass errors causing invisible 422 rejections over Spoolman Database syncs during bulk Spool Cloning routines. Concurrently refactored Wizard Mode State router to dynamically accept a `skipSearch` bypass flag to natively prevent the existing filament dropdown from asynchronously overwriting injected Filament selections.
* **Feature:** Built a comprehensive FilaBridge Error Recovery workflow. Added a secure PrusaLink API background worker to aggressively resolve, parse weight payloads, and acknowledge unhandled errors automatically. Injected a fallback "💊 Fix" inline recovery UI directly onto FilaBridge warning logs to allow immediate manual weight adjustments over active toolhead sockets.
* **System/UI:** Overhauled Spool Card coloring logic to natively support 4+ colors on a single swatch. Dynamically rendering 'coaxial' filaments as segmented conic pie-gradients while allowing 'longitudinal' elements to blend evenly without crashing the styling engine.
* **Feature:** Overhauled the Location List UI ("Glow-up"), separating Type and Status styling into dedicated sortable columns capable of dynamic ascending/descending alignment numerically.
* **Feature:** Handled Unassigned Spools natively appending them as a virtual catching location at the solid top sorting position of the Location Manager list.
* **Feature:** Built a universal Location QR Overlay Modal and quick-access header scanner block embedded into the Location List actions for immediate physical deployment label mapping.
* **System/UI:** Standardized legacy location assignments globally by ensuring proper backend `LOC:` validations with new frontend Toast scanning safeguards tied explicitly to `/api/log_event` recording.
* **Bug Fix:** Fixed explicit data-type validation in the Bulk Weigh-Out modal. Corrected a JavaScript input listener that fed a parsed string into an integer-based `Set`, which caused saved updates to instantly visually reappear on the DOM.
* **Bug Fix:** Hardened the API barcode parsing constraints to protect against legacy integer IDs triggering the new automatic label tracking flags. Ensured only strict `ID:` and `FIL:` prefix patterns are permitted to zero-out the `needs_label_print` status within Spoolman.
* **UI/UX:** Fixed dark text visibility and input box geometric clipping issues on the new Bulk Weigh-Out Modal by adapting the elements to standard dark-mode layout parameters.
* **Feature:** Built the Bulk Weigh-Out modal specifically for rapid successive spool measuring operations, directly binding to the native `state.heldSpools` cache buffer with complete integration back to the active Spoolman scale database.
* **Feature:** Refactored the global keyboard scanner listener script to buffer barcode inputs asynchronously. This intercepts characters independently of the aggressive 5-second backend `state.processing` DOM freeze blocks, saving them and dispatching the total string afterward instead of blindly rejecting scans mid-fire.
* **Bug Fix:** Fixed the "Failed to load location Data" exception occurring on the Location Manager when loading boxes containing ghost (deployed) spools. The crash was triggered by an undeclared `shadowColor` variable inside the Ghost Card UI string templating branch left over from the glossy artifact removal, which has now been correctly mapped to `styles.frame`.
* **Bug Fix:** Fixed the `r is not defined` JavaScript exception occurring during Spool Details rendering. The `getFilamentStyle` color processing matrix was failing to unpack hex variables for solid base colors during the prior layout refactor, causing the details fetch sequence to completely crash. Fixed by properly destructuring RGB digits sequentially, and additionally hardened the detail swatch renderer to intentionally bypass glossy card gradients for solid spools.
* **Feature:** Added a seamless click-through feature on the Location label inside the Spool Details modal: clicking any assigned standard location badge now instantly jumps into the Location Manager context while automatically hiding the details modal.
* **Feature:** Upgraded the Add Inventory Wizard's `MaterialType` input into a dynamic auto-complete dropdown. It parses the Spoolman database for existing material strings, filters live on keystroke, and supports Up/Down/Enter keyboard navigation to mirror the Filament Attributes standard.
* **UI/UX:** Fixed the borders on solid/black filaments losing contrast against dark UI backgrounds. Refactored `inv_core.js` physically lighting models to globally standardize the "fancy button" UI component dynamic across Solid, Gradient, and Black filament components, rigorously protected by E2E Playwright DOM structural tests.
* ⚖️ **The "Weigh-Out" Protocol / Manual Usage Input**: Added a specific "+ Add Printed Wt (g)" field into the wizard modal to manually log filament usage delta from the printer screen, gracefully adjusting the Spoolman database when filabridge auto-logging fails or times out.
* **Bug Fix:** Fixed the `Location` update in the Spool Details modal throwing a 400 Bad Request API error when set to `Unassigned` by correcting a schema typo (`physical_source_sub` to `physical_source_slot`) in the backend force unassign logic.
* **Feature:** Standardized the UI background refresh "no-wiggle" hashing logic globally. Implemented strict state caching in the Filament Details, Spool Details, Location Manager, and Live Logs to prevent silent 5-second pulses from destructively overwriting complex DOM components and dropping user button clicks.
* **Feature Implemented:** Added ability to manually edit and assign location data directly on a spool, including a force unassign Ghost mechanism.
* **Bug Fix:** Fixed explicit `test_clone_e2e.py` Chromium failure by correcting the Playwright button locator to match the rendered `[🐑 Clone]` markup instead of a legacy string.
* **Bug Fix:** Cleaned up subpixel rendering artifacts in `offcanvas_search.html` layout that were previously tripping the global UI scanner by adding a targeted CSS flex bypass filter for Chromium `justify-content-between` boundaries.
* **Feature:** Standardized the 'Archived' badge to `text-bg-danger` (Red) globally and implemented structural Playwright tests to strictly enforce this layout convention.
* **Bug Fix:** Fixed explicit empty spool weight updates silently failing to clear the Spoolman DB value. Refactored the Add Inventory Wizard payload processor to ensure empty fields securely emit `null` objects for proper attribute resets via the Spool API endpoint.
* **Feature:** Enhanced the Add Inventory Wizard to natively read, display, and serialize `settings_extruder_temp` and `settings_bed_temp` strings dynamically onto newly initialized Filaments, bypassing fallback metadata payloads.
* **[UI Testing]** Implemented comprehensive Playwright E2E structural testing across all UI elements globally to detect layout regressions.
* **Bug Fix:** Fixed `DIV#.fcc-card-action-btn` internal content structurally overflowing its strict dimensions.
* **Bug Fix:** Fixed missing inset `box-shadow` on Spool/Filament Cards that are assigned a gradient style.
* **Bug Fix:** Fixed empty `.text-white` elements rendering fully collapsed/squished on the DOM by improving JS scanner logic.
* **[UI Standardization]** Converted hard-coded inline JS/HTML `style="..."` brute-force stylings across the entire application into standardized global `fcc-spool-card` CSS configurations.
* **UI/UX:** Stabilized Spool Card rendering logic for "Deployed/Ghost" states, restoring complex multi-layered gradient backgrounds natively protected against background live-sync loops across the Buffer UI.
* **Modals:** Implemented a robust global window/modal management system in `inv_core.js` to mathematically handle dynamic z-index stacking, backdrop layering, and body scrolling when multiple modals are open concurrently. Bound auto-collapse triggers to the Search off-canvas menu to reduce redundant clicks when launching subsequent dialogs.
* **UI/UX:** Location label for deployed items in the search window is too long and breaks the layout by pushing action buttons out of the card. Refactored Spool Card layout to move Location Data to a new dedicated row.
* **UI/UX:** Extra long filament/spool names cause the Add Inventory Wizard buttons to become skewed with cancel above Create Inventory. Text data and UI buttons should not be a part of the same frame.
* **System/UI:** Removed legacy task "combine creation of spool and filament" from backlog since it was inherently solved by the Add Inventory Wizard's monolithic payload structure. Included minor cleanup of duplicate DOM ID in modal structural HTML.
* **Feature:** Added dynamic Spool counts `🧵 X Spool(s)` natively into the Filament Card UI template to easily identify stock limits without opening menus. Added seamless backing API loop integration to fetch counts dynamically.
* **Feature:** Built a robust native "Edit Spool" modal into the Add Inventory Wizard system, circumventing the need to enter Spoolman web UI to adjust core physical characteristics or identifiers on existing inventory.
* **Feature:** Designed and implemented a bi-directional "Remaining Wt (g)" fields synchronizer in the Wizard Editor to allow users to directly zero-out Filabridge-depleted spools without performing reverse math, natively protected by pre-flight validation preventing Spoolman DB initialization weight violation crashes.
* **Bug Fix:** Isolated and diagnosed TrueNAS dataset permission issues creating `ReadOnly` lockups on Dev Spoolman that resulted in continuous 500 Internal Server Error returns for single-field Boolean patch attempts.
* **Bug Fix:** Fixed the Spool Archive toggle throwing a 500 "Failed to gracefully update Spool" error by implementing a backend dictionary diff in `api_edit_spool_wizard` to drop unmodified values, circumventing Spoolman 0.19.1 internal crashes caused by redundant string rewrites on extra fields and weight schemas.
* **Feature:** Added the ability to directly toggle the `Archived` status of a spool from within the "Edit Spool" modal interface. Bound the state to both the edit wizard submission payload and injected a synchronized dynamic badge into the "Spool Details" viewing modal.
* **Bug Fix:** Bound a missing `showToast()` confirmation feedback trigger natively onto the `+ Queue Label` action inside the Filament Details popup to verify database insertion.
* **Bug Fix:** Fixed remaining weight on spool cards rendering as excessively long unrounded decimal strings (e.g. `987.654321987g`). Rounded at the source in `spoolman_api.py` `search_inventory()` and added a defensive `Math.round()` guard on the fallback path in `ui_builder.js`.
* **UI/UX:** Clone button on a spool didn't properly load in the spool/filament to be duplicated into the existing filament section of the add inventory wizard.
* **UI/UX:** Fix Location Manager UI Dryer box slot locations not correctly displaying all filament information.
* **Modals:** Add spool button on the list of spools in the filament details modal that links you to the details of the spool.
* **Modals:** Added "Send to Print Queue" button for individual spools inside the Filament Details popup.
* **Bug Fix:** Live: Names are not working correctly on spools, shows "" instead of the name. (Fixed in `inv_loc_mgr.js`)
* **Bug Fix:** Live: Cannot create spools manually. (Fixed JSON serialization in `spoolman_api.py`)
* **Feature:** Support clone feature for Spools, to auto populate an existing spool. (Completed Epic 2)
* **Feature:** Figure out a way to fill in density for filaments. (Completed Epic 2)
* **Database:** Fix `container_slot` serialization error causing Spoolman database update rejections (`400 - Invalid extra field for key container_slot: Value is not a string`).
* **Location:** Added color coding to the badges on the location (Location Type).
* **Location:** Added color indicate of dryerbox/tool fullness (1/1 Green, 2/1 Red, < Max White).
* **Location:** Ejecting from slot is not showing as being ejected by setting slot to empty.
* **Location:** Trash button, which I think does the same thing as ejecting, also doesn't seem to work here, for spools that are assigned to the location, but doesn't have a slot attached to them.
* **Location:** Refactored Location DB to use JSON config instead of CSV, added native sync for Spoolman Location API, and built automated tests.
* **Logging:** Fix ghost `Label Verified` log spam when Spoolman API explicitly rejected backward backlog reductions.
* **Command Center:** Fixed Undo Buffer Restoration to correctly recall spools to the buffer and reversed Toolhead Ejection cascades.
* **Command Center:** Eject should auto disable if we move off of the command screen to any other screen or modal.
* **Command Center:** Fix backend logging to not repeat non-useful data in the logs.
* **Print Queue:** Fixed Print Queue displaying "No Loc" for all spools.
* **Live Activity:** Fixed swatch circles in the "Live Activity" Dashboard log pane to accurately display CSS conic gradients mathematically divided for multi-color filament spools.
* **Live Activity:** Implemented background polling for Filabridge `GET /api/print-errors` to expose `gcode` parsing errors in the Live Activity log.
* **System:** Engineered a native CSS physical shield inside `scripts.html` to consume hardware `mousedown`/`click` wake-up hits natively when the Scanner Pauses.

* **UI/UX:** Unify Filament/Spool UI Cards into a universal `SpoolCardBuilder` engine to guarantee all Command Center sections (Search, Location, Buffer) share the exact same structural HTML layout and style. 
* **UI/UX:** Fixed Location Manager footer action buttons squishing into each other and clipping their colorful backgrounds by properly wrapping the flexbox grid and padding the text elements.
* **UI/UX:** Addressed the "Black filaments need a better way to show border" bug by mathematically injecting a dark metallic/carbon diagonal gradient for perfectly dark spools in `getFilamentStyle`.
* **UI/UX:** Fixed the floating point display issue for weight metrics (`33.860000000014g`) by stripping backend calculations and handling clean structural `Math.round` execution securely inside the browser via the new card builder logic.
* **Bug Fix:** Fixed the Spool Edit modal bug where opening it over the Location Manager caused the editing dialog to pop up behind the backdrop, blocking interaction.
* **UI/UX:** Globally swapped the generic spool emoji (🧵) for the specific DNA vector (🧬) when referring to Filament roots across the Multi-Spool Queue Modal and UI Builders.
* **UI/UX:** Added a dedicated "Refresh" button directly natively integrated into the Search Offcanvas to easily pull immediate REST queries without clearing query states.
* **UI/UX:** Contextually renamed the "TRASH" interface button to "EJECT" inside Location Manager cards dynamically whenever the target spool does not hold a specific structural slot assignment, unifying command terminology.
* **UI/UX:** Injected `position: sticky; top: 0;` natively to all globally generated dark-table headers across the application so that structural dataset columns never vanish when scrolling a deep component list.
* **UI/UX:** Globally replaced the dark-grey-on-dark-grey Bootstrap modal close button arrays with the `.btn-close-white` variant, pushing the `X` into a high-contrast visual priority above the system backdrops.
* **UI/UX:** Explicitly unbound the `Tab` sequence bypass from the Filament Attributes multiselect tag engine loop; users can now safely form-tab to the next wizard field logically, while `Enter` retains explicit commit control logic.
* **Bug Fix:** Fixed Filament Attributes being completely non-functional — selections couldn't be made, items couldn't be added to the list, and the Add button had no effect.
* **UI/UX:** Relabeled "STEP 1: MATERIAL SELECTION" to "STEP 1: CREATION METHOD" across the Inventory Wizard structural HTML to clarify that you are determining *how* a spool is sourced rather than strictly mapping internal materials.
* **UI/UX:** Bound a global green `showToast()` confirmation feedback trigger natively onto the `+ Queue Label` action scripts within both the Filament and Spool view contexts to verify database insertion.
* **UI/UX:** Injected dynamic `item.archived` checks broadly into the universal `SpoolCardBuilder` string processor, appending a highly-visible red `[📦 ARCHIVED]` tag right onto the item name element directly dynamically so hidden obsolete artifacts are blatantly identifiable across all Location/Buffer/Search panes.
* **Bug Fix:** Fixed the Search UI completely ignoring the 'In Stock' toggle state by dynamically appending `?allow_archived=true` to the native Spoolman REST queries whenever the toggle is deactivated, bypassing Spoolman's default hidden state payload.
* **Bug Fix:** Fixed explicitly typed Command Center scan payloads dropping the archive identifier. Fixed a multi-layer disconnect where `/api/identify_scan` failed to map the JSON key, AND `inv_cmd.js` aggressively stripped unmapped keys from the `state.heldSpools` cache arrays before passing the object to the card builder.
* **Bug Fix:** Fixed explicit `[📦 ARCHIVED]` display omission across Search and Buffer UI. Resolved a secondary backend logic loop in `spoolman_api.py` dropping the archived dictionary flag during search generation, and concurrently softened Javascript boolean checks in `ui_builder.js` to ensure payload string conversions render correctly.
* **Feature:** Implemented off-canvas Search dashboard to easily find spools without relying on Spoolman UI, including robust color-based search filtering.
* **Feature:** Engineered complete Print Queue UI system, including modals to list/sort unprinted labels (`spoolman_reprint` flags) and batch print exports to P-Touch CSV interface.
* **Feature:** Added a toggle to display inactive/archived spools in the filament details modal's spool list.
* **Feature:** Bound the native scale/weight readout text dynamically onto SpoolCards to instantly launch the `QuickWeigh` adjustment modal when clicked, speeding up manual corrections.
