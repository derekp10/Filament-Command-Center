# Working Groups — Batched Task Sessions

> Tasks from `Feature-Buglist.md` grouped by shared code surfaces for efficient batch execution.
> Each group has a detailed task file in `docs/agent_docs/tasks/`.

## Session Order (Recommended)

| # | Group | Items | Est. Effort | Status | Task File |
|---|-------|-------|-------------|--------|-----------|
| 1 | 🔵 Weight Handling Unification — Phase 1 | 3 | ~3–4 hrs | `DONE` 2026-04-27 | [01-weight-unification.md](tasks/01-weight-unification.md) |
| 2 | 🟢 Buffer Cards & Main-Menu Refresh | 3 | ~1.5–2 hrs | `DONE` 2026-04-28 | [02-buffer-cards-refresh.md](tasks/02-buffer-cards-refresh.md) |
| 3 | 🔴 Print Queue & Label Management | 7 | ~2.5–3.5 hrs | `DONE` 2026-04-28 | [03-print-queue-labels.md](tasks/03-print-queue-labels.md) |
| 4 | 🔶 Archive / Delete / Cleanup Lifecycle | 4 | ~3 hrs | `PARTIAL` 2026-04-28 (4.4 deferred) | [04-archive-delete-lifecycle.md](tasks/04-archive-delete-lifecycle.md) |
| 5 | 🟡 Edit Filament & Details Modal (small adds) | 2 | ~3 hrs | `DONE` 2026-05-08 | [05-edit-modal-small.md](tasks/05-edit-modal-small.md) |
| 6 | 🟡 Edit Filament & Details Modal (new panels) | 2 | ~3 hrs | `DONE` 2026-05-11 | [06-edit-modal-large.md](tasks/06-edit-modal-large.md) |
| 7 | 🧪 Testing Housekeeping | 2 | ~1–1.5 hrs | `DONE` 2026-05-07 | [07-testing-housekeeping.md](tasks/07-testing-housekeeping.md) |
| 8 | 🟣 Keyboard Navigation & Dialog Polish | 4 | ~3.5 hrs | `DONE` 2026-05-12 (8.1 partial — 12 button-only modals deferred as Bootstrap-default-acceptable) | [08-keyboard-nav-polish.md](tasks/08-keyboard-nav-polish.md) |
| 9 | 🟢 Quick-Swap Grid Enhancements & Printer Status Widget | 3 | ~4–5 hrs | `DONE` 2026-05-13 | [09-quickswap-grid.md](tasks/09-quickswap-grid.md) |
| 10 | 🟠 Add/Edit Wizard UX Overhaul | 11 | ~6.5–8 hrs | `DONE` 2026-05-14 (Sessions A+B+C — 10.1 progressive-disclosure cleanup shipped on `feature/wizard-cleanup-aggressive` across 9 commits / 6 feedback rounds; merged to dev + main) | [10-wizard-ux-overhaul.md](tasks/10-wizard-ux-overhaul.md) |
| 11 | ⚫ External Parsers & Prusament Cleanup | 3 | ~3 hrs | `DONE` 2026-05-13 | [11-external-parsers.md](tasks/11-external-parsers.md) |
| 12 | 🔵 Weight Entry Unified Component — Phase 2 | 2 | ~6–10 hrs | `DONE` 2026-04-27 | [12-weight-entry-component.md](tasks/12-weight-entry-component.md) |
| 13 | 🐛 Recent Bugfixes (Weight + Dryer Display + LOC Search + Bind Desync + Eject) | 9 | ~6–8 hrs | `DONE` 2026-05-11 | [13-recent-bugfixes.md](tasks/13-recent-bugfixes.md) |
| 14 | 🧪 Test-Sweep Flake Stabilization | 6 | ~3–5 hrs | `DONE` 2026-05-12 | [14-test-sweep-flake-stabilization.md](tasks/14-test-sweep-flake-stabilization.md) |
| 15 | 🪟 Canonical Overlay-Mount Helper | 3 | ~5–7 hrs | `DONE` 2026-05-11 (Path A — 5 inline overlays migrated; 4 non-inline surfaces deferred) | [15-canonical-overlay-mount.md](tasks/15-canonical-overlay-mount.md) |
| 16 | 🧰 Testing Hardening & Helper Consolidation | 4 | ~3–4 hrs | `DONE` 2026-05-12 | [16-testing-hardening.md](tasks/16-testing-hardening.md) |
| 17 | 🪟 Details Modal, Queue & Wizard End-State Polish | 5 | ~3–4 hrs | `DONE` 2026-05-14/15 (all 5 items shipped on `feature/buglist-sweep-2026-05-14`; 17.1 edit-toggle side completed 2026-05-15 by un-hiding `needs_label_print` from the wizard's hide list) | [17-details-queue-wizard-polish.md](tasks/17-details-queue-wizard-polish.md) |
| 18 | 📍 Unknown Location + Audit Mode Refinement | 2 | ~4–6 hrs | `DONE` 2026-05-15/16 (18.1 Unknown bucket + 18.2 Part A auto-park + Part B visual panel + idle watchdog all shipped on `feature/buglist-sweep-2026-05-14`; parent/child rollup deferred as future enhancement on top of the multi-location audit) | [18-unknown-location-audit.md](tasks/18-unknown-location-audit.md) |
| 19 | 🧪 Dev-Env Reset & E2E Test-Debt Triage | 5 | ~4–6 hrs | `DONE` 2026-06-02 (`feature/dev-env-reset-test-debt` — reset_dev.py + seed pair + 11 unit tests; 19.2–19.5 test-debt all green; 19.1a/b triage doc. Destructive `--prune` left for Derek to invoke per the non-destructive-default decision) | [19-dev-env-reset-test-debt.md](tasks/19-dev-env-reset-test-debt.md) · [triage notes](tasks/19-seed-readiness-and-failure-buckets.md) |

## Items NOT Grouped (Solo, Deferred, or On Hold)

These items remain in `Feature-Buglist.md` but aren't part of a batch session:

| Item | Reason |
|------|--------|
| **NEW** Filament-attribute add broken in wizard (buglist 2026-05-30) | No `+` button to add a new attribute in the add/edit wizard; the attribute modal lingers too long / ignores Escape; Escape on a toast should dismiss it; the Config-modal path works but is "clunky." **Config-attribute portion feeds L18 (Config system) — captured in the L18 design notes.** Touches the wizard, `inv_config.js`, and the toast layer. |
| **NEW** Weight-remaining rounding error (buglist 2026-05-30) | Remaining-weight display is off by ≥1 g on BOTH the Spool Display modal and the add/remove wizard. Standalone bugfix on the weight compute path (`weight_utils.js` / `<WeightEntry>`). |
| `locations.json` corruption recurring (L25) | `PARTIAL` 2026-05-12 — Hardening (1) per-call unique temp filename + (2) verify-after-write tripwire shipped on `feature/locations-json-write-hardening`. Monitor prod hub.log for `verify-after-write FAILED` critical lines. (3) explicit truncate + (4) Docker named volume deferred pending recurrence signal. |
| Screen blanking / wake lock (L17) | ON HOLD — OS-level issue |
| Config system (L18) | **DONE (Phases 1–4) 2026-06-01** on `feature/config-system` — safe atomic writer + `GET`/`PUT /api/config` + schema-driven Settings card (Phase 1); editable connection settings + masked/updatable `SCRAPER_API_KEY` + Spoolman/FilaBridge host decoupling (Phase 2); guarded toolhead (`printer_map`) editor — fails closed, blocks referenced removals (Phase 3); config import/export with redacted secrets + dry-run diff (Phase 4). Each phase adversarially reviewed (21+18+24+17 agents); 76 unit+E2E tests. Full design + phased plan in [tasks/L18-config-system-design.md](tasks/L18-config-system-design.md). **Phase 5 (OPTIONAL / future, fill-time):** action-tool co-location — refactor the config-modal action cards (Reconcile / Filament-Attributes / Restore-Field-Order) into per-section hosts so the "clunky attributes" manager sits beside its prefs (UI-organization refactor, NO endpoint logic change). Overlaps the config-modal portion of the standalone "Filament-attribute add broken in wizard" bug above; the wizard `+`-button / modal-timing / toast-escape parts of that bug remain a separate wizard/UX task. |
| Filabridge status light (L20) | ON HOLD — all-software (FilaBridge process ↔ FCC `/api/status` polling). Defer until correlated logs from both sides + a clear repro of flicker-vs-solid behavior are available. |
| FIL:58 label scan (L23) | ON HOLD — needs physical label |
| Display modal on Display modal (L26) | `PARTIAL` 2026-05-12 + FOLLOW-UP 2026-05-14 — details↔details and wizard↔details stacking now both close siblings via `window.hideAllDetailsModals`. Retest the L28 freeze with same scenario. |
| Frontend lock-up (L28) | `DONE` 2026-05-19 on `feature/l28-frontend-lockup-ghost-eject` — root cause was polling-flood / socket-buffer exhaustion, **not** modal-on-modal as initially suspected. The 2026-05-18 DevTools console showed `net::ERR_NO_BUFFER_SPACE` cascading + uncaught `updateLogState` rejections. Added in-flight guards + `.catch()`/`.finally()` to 5 polling functions (`updateLogState`, `fetchLocations`, `loadBuffer`, `liveRefreshBuffer`, `refreshManageView`). Printer Status `_aggregate` already had one. Regression coverage in `test_polling_inflight_guards.py` (6 tests). Steady-state load remains high — see new L29 polling-reduction follow-up. |
| Unknown crash after auto-deduct (L69) | ON HOLD — needs repro; server logs suggest container restart mid-session |
| Printer Status: Core1 state-read badge (L56) | `DONE` 2026-05-25 on `feature/buglist-trio-2026-05-20`. `_pulse_section_printer_status` now joins `prusalink_api.get_printer_state` per-printer; PRINTING/PAUSED/IDLE/OFFLINE chip rendered. Latent dual-path bug (sync-pulse wiping badge between bulk pulses) also fixed via `refresh()` merging cached state. Full write-up in `completed-archive.md`. |
| Filament-attributes manager UI (L58) | `DONE` 2026-05-25 on `feature/buglist-trio-2026-05-20`. New Admin/Config → 🏷️ Filament Attributes panel with Choices Manager (add/remove/sweep-unused with styled `mountOverlay` confirm) + Per-Filament Bulk Editor (filter bar, bulk add/remove against selection). 4 new backend endpoints. Surfaced + fixed a `extra`-PATCH sibling-wipe across 3 production write surfaces (snapshot-restore now full-dict); meta-test `test_no_direct_extra_patch_in_production_code` guards CI. Full write-up in `completed-archive.md`. |
| **NEW** Make Activity Log more ubiquitous (L238) | Persistent mini-log widget OR "N new events" pill OR modal-aware docking so the log is always visible during modal-heavy workflows. Once log is always-visible, toast durations can drop and toasts can become purely "happened now" flashes. Candidate approaches stack — pick one or all. (Partial progress: corner `#fcc-log-pill` "N new" flash + click-to-open compact log overlay shipped under L286 follow-on — but the in-modal docking ask is still open.) |
| Prusa metrics tooling research (L161) | Research / future inspiration — `prusa_exporter` + Prusa-Firmware-Buddy metrics docs. Could feed 9.3 (Printer Status widget), filabridge reconcile, or richer state probing. Not actionable as a discrete task. |
| Prusament temp scans should update existing filaments (L198) | `NEEDS DECISIONS` 2026-05-25 — backend Prusament parser at `external_parsers.py:125-216` already returns `settings_extruder_temp`, `settings_bed_temp`, `extra.nozzle_temp_max`, `extra.bed_temp_max`. Match logic exists at `inv_wizard.js:findFilamentMatches` and dedupes via Prusament product id. **4 UX questions for Derek before implementing**: (1) Which trigger surface — top-bar scan, Spool details "Spool data link" QR, Edit Filament > Import-from-External, or all three? (2) Multi-match policy — lowest-id only, all matches, or duplicate picker? (3) Backfill policy — overwrite OR fill-empties (lean: fill-empties, matches "values should stay the same")? (4) Surface mode — silent toast OR Edit Filament with diff pre-applied? |
| Prusament data-link spool scan should update existing fields (L200) | `NEEDS DECISIONS` 2026-05-25 — handler location identified: `wizardScanSpoolRow` at `inv_wizard.js:2236` already calls `/api/external/search?source=prusament` for the Add path. **4 UX questions for Derek before implementing**: (1) Which surface — global top-bar scan, offcanvas spool-search, Edit Spool wizard? (2) Weight-mismatch tolerance for "legacy used" detection — exact-match or ±N grams? (3) Initial-weight semantics — overwrite existing initial_weight, AND recompute remaining_weight (`new_initial - used`) or leave as-is? (4) **Sibling-wipe risk** — MUST go through `compute_dirty_extras` per CLAUDE.md write-surface conventions, not raw PATCH. Same pattern as the L319 sweep-unused fix from 2026-05-25. |
| Edit Filament > Import from External collapse glitch (L202) | `DONE` 2026-05-25 on `feature/buglist-trio-2026-05-20`. Snap-collapse CSS on `#editfil-import-panel.collapsing` + `overflow: hidden` removes the Bootstrap-animation sliver. Regression coverage in `test_edit_filament_external_import.py::test_l202_collapse_leaves_no_sliver`. |
| Box slot → empty doesn't propagate to filabridge (L204) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commit `2ba63b5`). Whenever a spool leaves a toolhead, the toolhead's FilaBridge mapping is now explicitly cleared via `_fb_write(printer, th, 0)`. Full write-up in `completed-archive.md`. |
| Color section: name/count cramped in collapsed wizard view (L15) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commit `d4dafd2`). Wizard CSS gap fix between color name and color count in the rolled-up Color collapsed section. Full write-up in `completed-archive.md`. |
| Filament label-confirm doesn't also confirm sample-printed + no UI for sample status (L17) | `DONE` 2026-05-30 (UI half) on `feature/scan-match-pipeline` (commit `7154f12 feat(L17): editable Swatch Printed toggle + app-wide Swal pass`). Pairing half shipped 2026-05-25 (label-confirm scan now also sets `sample_printed=true`). UI half: inline ✏️ pencil toggle next to the Swatch Printed badge on Filament Details modal (`promptEditSampleStatus`) — dark custom-list dialog with keyboard nav (capture-phase + `stopImmediatePropagation` to beat Swal's button nav), sibling-preserving extras merge → `/api/update_filament`, server-side bool→"true"/"false" normalization. OQ1 resolved (inline toggle). OQ2 kept as-is — toggling sample_printed=false does NOT flip needs_label_print=true (separate concerns). Regression coverage `test_filament_sample_label_status.py::test_filament_sample_status_inline_toggle_round_trip`. App-wide Swal pass piggy-backed (heightAuto:false + dark widget overrides). |
| **NEW** In-progress printing modal: QR codes appear on right side (L19) | Sometimes the in-progress printing update modal renders its QR codes on the right side instead of the expected position. Seemed to coincide with a background toast — possibly unrelated. Needs repro. |
| Verify L56 Printer Status box-bounding fix (buglist line 20) | `DONE` 2026-06-01 on `feature/core1-status-box-bounding` (commit `618957b`). Confirmed Derek's continuing symptom WAS a different code path — toolhead occupancy was gated on dryer-box `slot_targets` instead of the toolhead location itself, so direct-fed toolheads (Core One's prod setup) rendered empty even with a loaded spool. Fixed via new `spoolman_api.bucket_spools_by_location` (6→1 Spoolman fetch perf win); loaded-but-unbound toolheads now render as spool + 🔗 hint. FilaBridge auto-deduct confirmed already toolhead-driven. 8 unit + 2 e2e tests. Full write-up in `completed-archive.md`. |
| **NEW** Surface filament attributes on Spool Details modal (buglist line 22) | Derek 2026-05-28: "Assigned filament attributes should appear in the spool detail modal as well as the filament modal." — Filament attributes (the `extra.filament_attributes` chip list) currently render only on the Filament Details modal. Replicate the chip row on the Spool Details modal (read-only, inherited from parent filament). Touches `modals_details.html` + `inv_details.js`. |
| **NEW** Rename inventory hub → Filament Command Center (buglist line 25) | Derek 2026-05-31: "Rename inventory hub to Filament Command Center." — Repo-wide rename. Scope: `inventory-hub/` directory + every internal import / path reference; Docker container name (`inventory_hub` in `docker-compose.yml`), `Dockerfile`, `.dockerignore`; HTML `<title>` / nav-brand / page headings; `setup-and-rebuild/` references; test helpers / fixtures that mention "inventory-hub"; CLAUDE.md project conventions section; any external links Derek bookmarked (mark as breaking change). Sequencing: (1) inventory path rename + grep-and-replace, (2) Docker compose + container name (downtime), (3) verify TrueNAS prod compose can pick up the renamed image, (4) update CLAUDE.md + docs. Plan first — this is a large blast radius. |
| **NEW** Toolhead reassignment should unload filament from prior toolhead (buglist line 33) | Derek 2026-06-02: "Changing a spool's location from a toolhead, or a non-toolhead-attached dryerbox or slot, should unload the filament from the toolhead on assignment to another location." — Possible NEW symptom of the existing "Box-slot ↔ toolhead spool-switching inconsistency (L206)" — the eject/reassign path doesn't fully clear the prior toolhead bind. Could cluster with L37 + L43 as a Toolhead-binding/unbind-propagation group (potential Group 20). |
| **NEW** Buffer card updates slow (~5s pulse-rate latency) (buglist line 35) | Derek 2026-06-02: "Updates to the Main menu buffer spool cards are slow, taking (around 5 seconds) to update. This matches our pulling/sync rate, but changes in data to something in the active buffer should display almost instantly." — Local mutations to buffer items should bypass the 5s/15s/30s adaptive cadence and fire `inventory:sync-pulse` immediately (or repaint the affected card directly from the local state). Standalone bug; touches the buffer-mutation path + `_renderSpoolsRefreshPayload`. |
| **NEW** Single-slot dryer-box auto-attach to toolhead (buglist line 37) | Derek 2026-06-02: "If a spool in a single slot drybox (PM Polymaker, possibly others) is assigned to a toolhead, the dryerbox that the spool lives in should be assigned to the toolhead as a location, and removed when the spool is unloaded/ejected. ... This should only apply to single-slot boxes. ... Core One by design doesn't always have a dryer box attached." — New auto-binding feature; helps the Core One missing-box notification for printers that aren't Core One. Restrict to `Max Spools <= 1` boxes only. Sibling of L33 + L43 — potential Group 20. |
| **NEW** Legacy ID scan fails when no active spools exist for the filament (buglist line 39) | Derek 2026-06-02 (with sheet link example): legacy-id scan throws "unknown error" because no active spools exist for that filament — should fall back to surfacing the filament itself (legacy id can link to a filament too). Side question: why was the spool's weight set to 0 and archived? — diagnostic only. Standalone bugfix on the legacy-id resolution path; touches the scan handler + `resolve_scan` filament fallback. |
| **NEW** Filament-attribute cleanup still firing on prod ("removed ['Wood']") (buglist line 41) | Derek 2026-06-02: "I keep seeing this line in the activity log on prod '🧹 Filament Attributes cleaned: removed [\"Wood\"] (180 filaments restored)' I thought we disabled/fixed this." — Possible regression of L319 / L58 sibling-wipe fix. Expected: `ensure_filament_attributes_cleaned()` early-returns on subsequent boots after the first cleanup. Hypotheses to investigate: (a) early-return predicate fails on prod (e.g. field-definition GET returns transient shape); (b) Spoolman re-adds Wood via some unknown path (filament import? wizard?); (c) the schema DELETE+POST recreate is rejected silently so Wood comes back. Needs prod log inspection + reproduce. |
| **NEW** Propagate toolhead removal to Spoolman locations (buglist line 43) | Derek 2026-06-02: "I was recently restructuring prod to match dev after the changes to handling the Core One, realised that some of the locations still remained even after the MMU locations were removed in location manager. I think the whole process could be a little more robust or user friendly." — Surface follow-through to the Core One MMU cleanup: when toolhead rows are deleted via Location Manager, any spools whose `location` still references the deleted toolhead need to be migrated (UNASSIGNED? UNKNOWN? prompt user?). Sibling of L271 Phase 4 (`printer_map` into locations.json) and L33/L37 — potential Group 20. |
| **NEW** Filabridge indicator light constantly red even when active (buglist line 45) | Derek 2026-06-02: "Filabridge indicator light is constantly red, even though it's active. Needs to be looked into." — DIFFERENT symptom from existing L20 ON HOLD entry ("blinking erratically"). Constantly red suggests the status read is returning a hard-failure code path (vs L20's transient flicker). Standalone investigation; touches `/api/status` rendering of FilaBridge state + `inv_status.js` (or wherever the indicator dot lives). |
| Search filaments by most-spools-available (L21) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commit `4037422`). Backend sort axis + offcanvas search hookup so filament search results can be ranked by spool count. Full write-up in `completed-archive.md`. |
| Computer-room cart 2 labels constantly reappear in print queue (L23) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commit `b2b8881`). Stale `loadQueue` responses that pre-date a local mutation are now dropped instead of overwriting the just-mutated queue. Full write-up in `completed-archive.md`. |
| Print Status not updating weight on print-finish (L25) | `NEEDS REPRO` 2026-05-25 — code-path mapped: `_pulse_section_printer_status` at `app.py:4056` reads `spoolman_api.get_spools_at_location_detailed(tid)` per toolhead on every dashboard pulse and bumps the widget fingerprint via rounded grams, so the widget SHOULD pick up the new weight within one poll cycle. **Suspect adaptive-cadence lag** — when the printer transitions from PRINTING→IDLE/FINISHED, the cadence drops to 15s-30s buckets and the filabridge auto-deduct lag becomes visible. **Possible fix**: one-shot fast-poll for ~30s after a PRINTING→non-PRINTING transition. Repro with DevTools: confirm dashboard_pulse is actually firing post-print, confirm Spoolman serves post-deduct weight, confirm `inventory:sync-pulse` dispatch. L361 event-delegation fix is unrelated. |
| Polling architecture reduction (L206) | `DONE` 2026-05-19 on `feature/l28-frontend-lockup-ghost-eject` (shared with L28). Bulk `/api/dashboard_pulse` endpoint + adaptive cadence (5s active / 15s idle / 30s hidden). ~3 req/sec → ~0.2 req/sec active. Regression coverage in `test_dashboard_pulse_api.py` (13) + `test_dashboard_pulse_frontend.py` (6). |
| Box-slot ↔ toolhead spool-switching inconsistency (L206) | **NEEDS INVESTIGATION** — continuing-bug observation 2026-05-12: eject doesn't fully clear toolhead bind when box slot is bound to a toolhead. Despite Group 13.6 (toolhead-first assign + auto-archive cleanup), the eject path still leaves stale state. 10 min of Activity Log capture included as raw data for diagnosis. Likely a new flavor of the same architectural gap — different code path than 13.6 covered. L204 is the narrower sibling. |
| Refactor strip cards in Location Manager (L241) | UI/Theming standalone — merge horizontal layout with modern grid card features |
| High-Contrast Pop everywhere (L242) | Large UI/Theming pass — White Text + Heavy Black Shadow + adaptive variants per color |
| Slicer Profile animation reuse (L243) | `ANSWERED` 2026-05-25 — it's our own `window.mountOverlay()` + `handle.setContent()` pattern, NOT SweetAlert2. The "niftyness" is the absence of bad behavior: a single persistent overlay swaps inner HTML between stages instead of dismissing one Swal and opening another. Reference impl `inv_wizard.js:wizardPromptNewChoice` → `wizardSwapNewChoiceToSuggestion` / `wizardSwapNewChoiceToConfirm`. Reuse: `const handle = window.mountOverlay({...}); handle.setContent(stage2Html)`. |
| Help button per modal (L250) | Standalone — modal-specific help affordance |
| Legacy ID assignment tool (L252) | Standalone — pair existing Spoolman IDs to physical legacy spools (bulk-import case) |
| Location Manager redesign (L271) | IN PROGRESS — Phase 1A (parent_id additive) shipped 2026-04-25. **Phase 1B shipped 2026-05-25** on `feature/buglist-sweep-2026-05-25` (commit `aeeaa09 refactor(Phase 1B): use resolve_parent in /api/locations synthesizer`). Phases 2 / 2.5 / 3 / 4 / 5 separately tracked in `Feature-Buglist.md` phase tracker. |
| Bulk Moves (L298) | Blocked by Location Manager Phase 3+ |
| Shapeshifting QR codes (L299) | Small UX win — extend shapeshift pattern to more places (Audit button, etc.) |
| Location Manager cross-browser sync (L302) | ON HOLD — needs SSE/WS transport |
| Mobile mode (L315) | Large standalone architectural effort |
| Prusament QR codes as labels (future, blocked by L315) | Derek 2026-05-30 (buglist line 38) — use Prusament manufacturer spool QR (`prusament.com/spool/<id>`) in place of (or alongside) FCC's custom QR labels to load/operate spools. The scan pipeline already recognizes these URLs and matches them to the in-inventory spool (shipped on `feature/scan-match-pipeline`). **Blockers**: (1) Prusament spools are *reusable* — after a re-spool/refill the QR still resolves to the ORIGINAL filament Prusa sold, so it may NOT match what's physically on the roll. Today bounded by fill-blanks-only + matched-overlay name display + conflict prompt; would need a real guard if we lean into this. (2) One bench barcode scanner doesn't read the spool QR reliably — a phone camera in the mobile-first version (L315) likely would. Revisit with L315. |
| Dashboard modularization (L316) | Large standalone refactor |
| Make as much configurable (L317) | Folds into Config system (L18) — see scaffold note |
| Spoolman extras sort order restore (L318) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commits `ff5db0e` + `bec98f6` + `569ea19`). Backend endpoint to restore Spoolman field sort order + Config modal UI card + safety preview (dry-run + per-field diff before any write). Full write-up in `completed-archive.md`. |
| Clean up filament attributes (L319) | `PARTIAL` 2026-05-16 + AUTO-RUN 2026-05-19 — script shipped at `setup-and-rebuild/migrate_filament_attributes.py` + auto-cleanup at startup via `spoolman_api.ensure_filament_attributes_cleaned()`. `For Infill` + `Matte Pro` pending Derek's prod check before promotion to DELETE_CHOICES. |
| Continue supporting Spoolman Import from External (L341) | Tracks gap list of unimplemented sources (open-filament-database, Prusament spool-specific, Open Print Tags) |
| Dev Spoolman/filabridge environment (L345) | Standalone — set up isolated dev versions. **Note**: L345's "automate dev-env reset for clean E2E runs" follow-up shipped 2026-06-02 via Group 19.1 (`setup-and-rebuild/reset_dev.py` + seed pair). The base L345 ask (separate isolated dev Spoolman / FilaBridge instances vs the current shared dev) remains open. |
| Log cleanup routine (L347) | `DONE` 2026-05-25 on `feature/buglist-sweep-2026-05-25` (commits `d94a5d1` + `eab86f8` + `e2dffee`). `filabridge_error_snapshots.json` capped at 100 entries + pin recovery fallback when snapshot evicted/missing + pre-migration `.bak` files pruned. Full write-up in `completed-archive.md`. |
| Dynamic setup code refactor (L348) | Standalone — keep brand-new-install path working |
| Continue Spoolman vendor→filament→spool cascade (L349) | Continuing |
| Configure extras propagation Filament↔Spool (L350) | Standalone — relates to Config system |
| Spool product data text field (L351) | `LIKELY ALREADY DONE` 2026-05-25 audit (commit `365bacc` annotation) — per-spool extras `product_url` and `purchase_url` already exist on the Spool record (per `FIELD_ORDER["spool"]` in `app.py:553`). Prusament parser auto-populates them on scan (mapping in `inv_wizard.js:2407`). Spool Details modal surfaces both fields with edit (`inv_details.js:1200,1912`). **Confirm with Derek** whether the ask is for a separate third field. If yes, file what it should hold + where it should surface. |
| Background refresh in Location Manager (L352) | `LARGELY DONE` 2026-05-25 audit (commit `070d830` annotation) — the L206 `dashboard_pulse` adaptive heartbeat (5s active / 15s idle / 30s hidden) already drives live refresh across: Location Manager (via `manage_id` + `_renderManagePayload`), Printer Status widget (`refreshPrinterStatusWidgetFromAggregate`), Buffer cards (`refresh_spool_ids` + `_renderSpoolsRefreshPayload`), Spool/Filament Details modals (silent re-fetch on `inventory:sync-pulse` at `inv_details.js:584`), Backlog modal (own poll at `inv_backlog.js:27`). **Intentionally exempt**: Add/Edit Wizard (would clobber typed values), Print Queue modal (user is reviewing). File follow-ups against specific missing surfaces. |
| Project Color Loadout (L391) | Blocked by Location Manager Phase 3 |
| Core One MMU rows cleanup (Derek aside) | `DONE` 2026-06-01 on `feature/core1-status-box-bounding`. Removed unused `CORE1-M1`–`CORE1-M5` from printer_map + locations.json; retyped `CORE1-M0` `No MMU Direct Load` → `Tool Head` (Name `🦝 Core One Upgraded Tool Head`). Surfaced alongside the box-bounding fix (the rows were rendering as 🔗 placeholder clutter). Seed `locations.json.example` updated. ⚠️ Prod replication still needed. See Feature-Buglist.md for the full write-up. |
| All "On Hold" section items | ON HOLD |

**Note on test flakes:** The `test_quickswap_visual` baseline mismatch, `test_ui_details_modal_e2e` offcanvas-backdrop flake, and `test_amazon_parser_matching` BS4-missing issue were all closed by Group 14 (14.4 / 14.3 / 14.5 respectively). Detailed write-up in `completed-archive.md`. Post-Group-14 follow-ups (parallel `tests/` directory, `_open_manage` consolidation, `reset_dom_state_js` audit, Windows pip footgun) are bundled into Group 16.

## How to Use

**With Claude Code:**
```
/project:work-group 1          # Start Group 1 (Weight Unification)
/project:work-group weight     # Same — matches by keyword
/project:finish-group           # When done — commit, archive, update status
```

**With Gemini / Antigravity:**
- Point at the specific task file: "Read `docs/agent_docs/tasks/01-weight-unification.md` and implement it"
- Or use the `/startnewtask` workflow which now knows about these groups
