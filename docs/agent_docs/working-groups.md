# Working Groups — Batched Task Sessions

> Tasks from `Feature-Buglist.md` grouped by shared code surfaces for efficient batch execution.
> Each group has a detailed task file in `docs/agent_docs/tasks/`.

> **2026-06-05 cleanup:** All 20 working groups are complete. `Feature-Buglist.md` was decluttered — ~50 fully-done items were moved to `completed-archive.md` (verbatim consolidation block at the top of that file). The **Items NOT Grouped** table below was pruned to outstanding items only; archived rows were removed, and a few `(buglist line N)` pointers were re-synced to the new line numbers.
>
> **2026-06-10 refresh:** Slot-assign latency flipped to `DONE` (A+B shipped to `main`, `e3b9481`; 14.6s→6.5s). Two new standalone items added — the **🌉 FilaBridge absorption epic** (the remaining ~4s latency tail + the dual-writer desync class fold into it) and the **Escape-cancels-dropdown-not-modal** sweep. No batch groups changed; all 20 remain `DONE`.
>
> **2026-06-12 refresh:** New **Group 21** filed (4 brand-new bugs at the top of the buglist — buffer-card badge overlap, post-eject QR re-render, manual-assign auto-eject, queue-keeps-details-modal-open). **FilaBridge epic** updated — Phase 0 + Phase 1/Scope 1 + the whole cancelled-print partial-deduct **shipped + released to `dev`+`main`**; Scope 2 cutover checklist (gate = L271 Phase 4 credential relocation) is the remaining work. **L271 Phase 4 status CORRECTED** against the live tree: the toolheads[] structure cutover shipped (`a5b1ee6`), but `config.json:printer_map` survives as a PRIME-ONLY boot seed (not deleted) and the **credential-relocation gate + Phase 5 prefix-retirement are both still open** — the prior "only Phase 5 remains" was wrong. **L161** annotated with the metrics NO-GO. No batch groups 1–20 changed.
>
> **2026-06-13 refresh:** Buglist gained 8 new items at the top. Two new groups: **Group 22** (FilaBridge Phase-2 detection & deduct-attribution follow-ups — 3/5/7, field-observed running the new monitor live) and **Group 23** (Edit-Filament import/scrape & label-invalidation — 11/15/17). **Group 21** grew to 6 (added 21.5 force-location double-click + 21.6 buffer-return-after-scan). **FilaBridge epic + L271 rows updated** — the Scope-2 cutover actually advanced on `feature/filabridge-phase2-creds-gate`: creds relocated off FilaBridge (`43bb56d`+`a26468c`, = the L271 Phase 4 gate, now BUILT/pending-merge), bgcode-aware recovery parser (`da58e89`), and FCC-owns-FINISHED-completions deployed DARK (`c1b0f5e`); remaining = the atomic dark→live switch + retire/decommission. No batch groups 1–20 changed.
>
> **2026-06-13 refresh (later):** Cutover advanced again. **Group 22 → `PARTIAL`** — `54f271c` "detection-layer hardening" shipped 22.1 (instant-ack + adaptive poll) + 22.2 (ambiguous-IDLE review), both on `feature/filabridge-phase2-creds-gate` (unmerged); only 22.3 (spool-swap apportionment) remains. **FilaBridge epic step (e) "retire the surface" now IN PROGRESS** (`5758454` L324 reconcile + `9a18499` `/print-errors` poll retired). **3 new standalone items** filed (printer-states-in-Printer-Status, detail-modal button standardization, archived-spool empty-weight type-in/combine/propagate). No batch groups 1–20 changed.
>
> **2026-06-13 refresh (cutover COMPLETE):** The 🌉 **FilaBridge Phase-2 cutover SHIPPED end-to-end and is verified on prod** (main `92e4d74`, prod `92e4d749`; FilaBridge stopped/decommissioned). The **FilaBridge epic** + the two ✅-FIXED detection items (cancel instant-ack + completion power-cycle = Group 22.1/22.2) were MOVED from `Feature-Buglist.md` to `completed-archive.md` (top block) — this also cleared the stale unchecked `[ ]` Scope-2 checklist the group system was flagging. **Group 22 → still `PARTIAL`** (22.1/22.2 released to prod; only 22.3 spool-swap apportionment remains open). Two open standalone buglist items survive: 22.3 + a low-pri cosmetic FB-artifact cleanup. No batch groups 1–20 changed.
>
> **2026-06-13 refresh (post-finish-task tidy):** Building on Derek's finish-task. Filed the **vestigial-FB-artifact cleanup** as its own NOT-Grouped row; **tidied the FilaBridge epic row** (removed the now-contradictory `⏳`/`🔨` in-progress checklist markers — the cutover is COMPLETE; full per-phase detail lives in `completed-archive.md`); folded Derek's new **"show printer state in the collapsed Printer Status bar"** ask into the existing Printer-Status-state standalone item. No batch groups changed.
>
> **2026-06-14 refresh:** One genuinely new buglist item since the cutover. **Group 22 → grew to 4 items** — added **22.4 (deduct-path code-review follow-ups)** = the 6 polish/robustness items deferred from two local max-effort reviews of the now-merged recoverable-unresolved-deduct work (`d8e0db9`/`b23d0b2`); folded INTO Group 22 (Derek's call) because it edits the same deduct surface as 22.3 (`_apply_usage_to_printer` / `deduct_completed_print` / `_confirm_no_spool_review` / `deduct_cancelled_print`). Group 22 stays `PARTIAL` (22.1/22.2 prod; 22.3 + 22.4 open). Also noted on the Group 22 row + task file: the **recoverable no-spool/progress-unknown deduct foundation is now prod-verified** (`main`/`3d88d70`), so 22.3's recommended start is option (c) minimum-viable on solid ground. **Added a NOT-Grouped row** for the pre-existing flaky E2E `test_wizard_group10_session_a::test_edit_wizard_cancel_from_spool_details_reopens_details` (found 2026-06-07, never tracked; low-pri test-infra). No batch groups 1–21 / 23 changed.
>
> **2026-06-14 refresh (Chat 1 of 2 — 22.4 + 22.3c built):** Group 22 is a **2-chat project**. **Chat 1** (this session, `feature/group-22-deduct-followups`, committed `e522d99`) shipped **22.4 (all 6 polish items) + 22.3 option (c) minimum-viable** (a `spool_changed` manual-split review when a toolhead's spool changes mid-print, start-spool snapshot threaded through live edge / restart / deferred-fetch). 183 deduct+adjacent tests green; zero regressions (the full-sweep's 25 fails A/B-confirmed pre-existing). Each feature was design→adversarial-verify→implement→**adversarial diff-review** via multi-agent workflows; the diff review found + fixed one material bug (deferred-completion retry bypassed swap detection) + test gaps. **Chat 2 = 22.3(b)** full automatic per-segment apportionment (the M600 / coded-pause multi-filament case — Derek's "in-progress deductions"), fully scoped in the task file §22.3(b) with research findings + an experiment-capture checklist (Derek is generating real M600 data). **Group 22 status: 22.1/22.2 prod ✅, 22.4 ✅ (chat 1, `e522d99`), 22.3(c) ✅ (chat 1, `e522d99`), 22.3(b) ⏳ (chat 2).** No batch groups 1–21 / 23 changed.
>
> **2026-06-14 refresh (verification pass):** Re-ran `/refresh-groups`; the docs were already in sync — the only buglist delta since the prior refresh (the 22.3(c)-DONE annotation on the mid-print-swap item) was committed WITH the code in `e522d99`, so nothing needed regrouping. Corrected the stale **UNCOMMITTED** wording (the Chat-1 work IS committed, `e522d99`). **Verified 22.4 + 22.3(c) are genuinely done** (Derek asked to confirm, not just trust the DONE marker): an 8-agent adversarial source check returned *implemented / high confidence* on every claim — `select_deduct_targets` dedupe+skip, the `_record_applied_deduct` drift fix, orphan-shortfall scoping, 2dp alignment, single MMU probe, progress_unknown threading (TOCTOU correctly **deferred-as-latent**, not falsely claimed), the `_snapshot_active_spools`→`_route_completion_to_review` spool_changed routing, and the deferred-retry-bypass fix. Tests green: **49 new** (`test_deduct_followups_22_4` + `test_spool_swap_22_3`) + **176 deduct-domain backend**, **0 deduct regressions** (the 2 wider-run fails are pre-existing E2E flakes, incl. the one already tracked NOT-Grouped). Minor non-blocking polish surfaced (no per-row ambiguous cue in `cancel_review.js`; two component sub-warnings still 1dp vs the 2dp aggregate) — not blockers; now documented in the Group 22 task file under **"22.4 — residual polish"** for the next chat to optionally sweep alongside 22.3(b).
>
> **2026-06-14 refresh (Chat 2 merged + 2 new buglist items):** Group 22 **Chat 2 foundation shipped + MERGED to dev** (`28c484f` → `8592964`; `feature/group-22-deduct-followups` deleted) — 22.3(b) swap-event capture (a) + threading + degrade-to-review (now catches **A→B→A**, which the start-vs-end diff missed) + the `parse_color_change_segments` (b) segmentation primitive + the 2 residual 22.4 nits (per-row ambiguous badge in `cancel_review.js` + 1dp→2dp sub-warnings). 29 new tests, 233 deduct/parser green, 0 regressions. **Group 22 stays OPEN** — the ONE remaining piece (validated automatic grams-split) is **DEFERRED, blocked on DATA** (a real multi-swap / active print to ground-truth the apportioned grams + confirm `;COLOR_CHANGE` survives the bgcode decode); Derek's steering insight: make it genuinely **swap-aware — a pause ≠ a filament swap** (could be magnets/inserts/hardware), tie segmentation to actual filament events. **Two new NOT-Grouped rows filed:** (1) **drop the round-trip `⏱️` perf-trace logs** from the Activity Log (the L3 latency probe — issue fixed + FilaBridge gone; `logic.py:373` + `app.py:3058`); (2) **sub-location add workflow redesign** (adding shelf/row/section is clunky — the L271 *data* model is done, this is the UX layer). No batch groups 1–21 / 23 changed.
>
> **2026-06-15 refresh:** **Group 21 → `DONE`** (all 6 shipped + merged `dcfcb3c`/`04af99f`; row already flipped). **11 new ungrouped buglist items triaged via a 7-agent grounding workflow** (each verified against real code, not guessed). **Group 23 grew 3→6** (+23.4 blanking a product_url/any edit-filament extra won't save — root cause backend `_merge_extras_with_existing` omit-means-keep, needs a delete-sentinel, **higher-risk**; +23.5 product-link "filled" badge — wizard metadata panel is purchase-only; +23.6 Prusament-scan persists the scanned URL to the matched spool's `product_url`). **Two new groups:** **Group 24** Weight Transparency & 1000g-Default Correctness (F manual-weight before→after log + K hard-1000g audit/make-variable-by-size) and **Group 25** Wizard UX — Print Affordances & Tab-Confirm (H print-existing/print-all + I combobox Tab-to-confirm). **Four new standalone rows:** movable activity-log pill (fab_drag generalization, 21.1 sibling), label-CSV export robustness (likely a filename-rename not a P-touch lock), `TOOLHEAD_TYPES` double-def dedup, undo-button overhaul (rich label already computed + thrown away; weight-undo doesn't exist). Grounding findings + file:line in each row/task file. No batch groups 1–20 / 22 changed.
>
> **2026-06-15 refresh (Option-B cleanup sweep SHIPPED):** Four standalone NOT-Grouped rows flipped to `✅ DONE` on `feature/standalone-cleanup-sweep-2026-06-15` (`19a4a5e`): **undo-button log-readability**, **movable activity-log pill** (shared `draggable_pill.js` engine), **`TOOLHEAD_TYPES` dedup** (pure-code, no prod step), and **drop the round-trip `⏱️` perf-trace logs** (FULL retire — `perf_trace.py` deleted, probe-cache gating re-wired to a thread-local depth guard). Each was grounded → implemented → adversarially diff-reviewed (perf-trace retire returned ZERO findings) → tested (14 unit + 12 Playwright + 225 backend green). One new **Testing** row filed: the pre-existing **doassign buffer-safety** E2E flake (A/B-confirmed NOT caused by the sweep). Committed on the feature branch; not yet merged to `dev`.
>
> **2026-06-15 refresh (Option-B verify + doassign flake row):** Re-ran `/refresh-groups`; the docs were already in sync — the sweep's doc commit (`6078cf1`) had already flipped the 4 standalone rows to ✅ DONE + added the note above, and `24a4cd6` archived those 4 items to `completed-archive.md`. The Option-B sweep is now **merged to `dev`** (`b2a4be4`; the prior note's "not yet merged" is superseded). Sole gap closed this pass: filed a NOT-Grouped row for the new **`test_doassign_buffer_safety`** E2E flake (parity with the wizard-cancel flake row — both pre-existing test-infra, no prod impact). No groups changed. (The 4 DONE standalone rows are kept marked-in-place per Derek; a future bulk cleanup can prune them like 2026-06-05 did.)

> **2026-06-18 refresh (second Option-B standalone sweep — 4 items SHIPPED on `feature/option-b-standalone-sweep-2026-06-16`, `fd5ac89`):** Four NOT-Grouped rows flipped to ✅ DONE: **Escape-cancels-dropdown** (full sweep — feeds combo, wizard location/material/multi-choice comboboxes, bind-slot-picker; the Escape branch now `stopPropagation`s and is handled ahead of the empty-list guard so it closes only the dropdown, never the modal), **collapsed printer-state chip** (shared `_stateMeta()` → a compact `.fcc-ps-state-chip` per printer in the collapsed strip), **detail-modal footer standardization** (one canonical order across the spool + filament footers), and **label-CSV robustness** (atomic temp+fsync+replace, never-silent Activity Log + 7s toast that names the file actually held open; the configured-filename-vs-mode-filename realignment + the `api_print_location_label` friendly-lock-message stay open pending Derek's P-touch repro). Each grounded → implemented → 4-lens adversarial review → review-fixes applied → tested (9 backend/static + 8 new Playwright). A 4-lens adversarial review caught 2 missed comboboxes (wizard material + multi-choice) **confirmed live** + the feeds empty-list edge — all fixed. **Also fixed the pre-existing `test_bind_slot_picker` reds** (stale `config_loader.load_config` mock → `get_active_printer_map` after the L271-Phase-4 cutover, plus poll-not-fixed-sleep hardening on 3 flaky live tests) — new DONE row below; 14/14 stable across 2 runs. Not yet merged to `dev`.
>
> **2026-06-29 refresh:** Re-ran `/refresh-groups`; the docs were already reconciled by the sweep's doc commit (`ca3df70` flipped the 4 rows + filed the bind-picker test-debt + added the note above) and the sweep is now **merged to `dev`** (`97a6402`; the prior note's "not yet merged" is superseded). `14ac268` (finish-task) archived the completed items. **One stale row synced:** `14ac268` narrowed the label-CSV item — the **filename-mismatch is RESOLVED** (Derek's 2 P-touch files, dev- + live-linked, confirmed no mismatch, so no realignment is needed), main bug DONE + archived; only the `api_print_location_label` friendly-lock-message residual stays open (low-pri). Updated the label-CSV NOT-Grouped row to match the buglist. No new items, no group status changes (22 `PARTIAL`; 23/24/25 `TODO`).
>
> **2026-06-29 refresh (Group 23 SHIPPED):** **Group 23 → `DONE`** (`feature/group-23-edit-filament-import-scrape`, commits `359e97f` + follow-up `1766573`, merged to `dev`). All 6 items + a label-scope follow-up; grounded (6-agent) → 17-agent adversarial diff-review (8 confirmed fixes) → focused follow-up review (clean). 185+ targeted tests green, 0 regressions; 16 new backend tests. The 6 buglist items were archived to `completed-archive.md`. **⚠️ One new convention to absorb at the next CLAUDE.md touch:** 23.4 introduced the extras **delete-sentinel** (`window.FCC_DELETE_EXTRA` / `spoolman_api.DELETE_EXTRA_SENTINEL`) — omitting an extra key still means "keep"; send the sentinel to DELETE. Remaining `TODO`: 24, 25 (22 `PARTIAL`).
>
> **2026-06-29 refresh (Group 23 cleanup double-check):** Derek asked to verify the last group's cleanup. **Result: correct, with one bookkeeping gap fixed.** The 6 items were archived cleanly (zero buglist remnants, nothing over-removed), the row + archive block + CLAUDE.md conventions are all in place — but the **Group 23 task file still said `Status: TODO`** (`40c815a` flipped the row + archived items but missed the task-file banner; Group 21's was flipped on completion). Flipped it to ✅ DONE. **Also deep-verified the work** (Derek's call): **82/82** Group-23 backend+E2E tests green + a **6-agent adversarial source check = `implemented`/high-confidence on all 6 items** (23.1–23.6), no over-claims. **Two minor follow-ups surfaced for Derek (not blockers; NOT filed — the buglist is his):** (a) 23.3's spool-label flag WIZARD `edit_spool` reuse path omits `original_color` (a Color-name-only edit via the wizard won't flag spool labels; the primary edit-filament path does); (b) several FE behaviors (23.1 apply-map, 23.2 anti-leak, 23.5 product chip, 23.6 URL write) are verified by code-read but not test-pinned (the 23.4/23.3 backend invariants ARE pinned, 16 cases). Detail in the Group 23 task file. No group status changes.
>
> **2026-06-29 refresh (Group 23 verification follow-ups filed):** Per Derek, filed the two follow-ups the cleanup verification surfaced. **(1)** the 23.3 wizard `edit_spool` `original_color` gap → **Group 25 grew 2→3** (new 25.3; same `inv_wizard.js` surface). **(2)** the Group-23 FE test-debt (23.1 apply-map, 23.2 anti-leak, 23.5 product chip, 23.6 URL write — verified-by-read, not test-pinned) → new standalone **NOT-Grouped row**. Both also added to `Feature-Buglist.md` (at Derek's explicit direction, overriding the refresh default). No status changes (22 `PARTIAL`; 24/25 `TODO`).
>
> **2026-07-01 (Group 24 SHIPPED):** **Group 24 → `DONE`** (`feature/group-24-weight-transparency-1000g` → `dev`). Both items: **24.F** shared `_log_manual_weight_change` before➔after log across both manual funnels (`api_spool_update` + `api_edit_spool_wizard`), **24.K** `weight_is_default` honored on the wizard create-path + Amazon/3DFP parsers + the edit-filament import preview + printer-status bar scaled to `initial_weight`. Grounded (5-agent) → 22-agent adversarial diff-review (4 of 18 findings applied — incl. a missing wizard-edit funnel and the edit-import sibling leak the task file hadn't scoped) → full `RUN_INTEGRATION=1` sweep 1759 passed / 21 **pre-existing** reds (zero Group-24 regressions; `locations.json` held 55→55 — the config-save wiper fix works). The 6 completed items were archived; the 21 pre-existing reds were catalogued as a NEW standalone **`## 🧪 Testing` cleanup row** in `Feature-Buglist.md` (per Derek's ask — clean them up so a red sweep means something). Remaining `TODO`: **25** (22 `PARTIAL`).
>
> **2026-07-01 (Group 25 SHIPPED):** **Group 25 → `DONE`** (`feature/group-25-wizard-ux-print-tab` → `dev`). All 3 items, pure-frontend `inv_wizard.js`: **25.I** `Tab` confirms the highlighted combobox item (shared location+vendor); **25.3** extracted `wizardBuildFilamentLabelDiff` + added the missing `original_color` diff (wizard Color-name edits now flag spool labels via the shared `_maybePromptLabelReprint`); **25.H** extracted `wizardRenderPostSaveQueueActions` — "Queue All" for multi-create + an edit-mode queue chip. 11-agent adversarial review found + fixed 1 real bug (Tab pass-through fired a bubbling `change` → spurious `isDirty` flip → false "Unsaved Changes"; guarded on actual-change, Enter too) + 2 pinning tests. 13 new tests + 34 green regression, 0 regressions. The 3 items were archived. **All 25 batch groups are now `DONE`; only Group 22 remains `PARTIAL`** (22.3(b) automatic grams-split, blocked on Derek's multi-swap print data). Open non-batch work: the pre-existing red-test cleanup row + the standalone/epic items (Rename→FCC, Mobile L315, Bulk Moves L298, sub-location add redesign, etc.).
>
> **2026-07-01 refresh (Group 24+25 cleanup verified; Group 26 filed):** Verified both just-shipped groups cleaned up correctly — session-order rows + **task files** flipped to DONE (the Group-23 task-file-miss lesson was applied), the 5 items archived with **zero buglist remnants**, both `completed-archive.md` blocks present. **🎉 All 25 original batch groups DONE; only Group 22 stays `PARTIAL`** (22.3(b) automatic grams-split, blocked on Derek's multi-swap print data). Filed the new **Group 26 — Test-Infra Debt Cleanup** (Derek's call: consolidate) from the 21-pre-existing-reds catalog (6 buckets) Group 24's sweep surfaced, **folding in** the 2 intermittent flakes (wizard-cancel → 26.7, doassign → 26.8) + the Group-23 FE test-backfill (→ 26.9) that the reds item named its "siblings"; the 3 existing test NOT-Grouped rows are marked folded-into-26. Test-only, LOW risk — makes a red sweep meaningful again. No buglist change (the items already live there).

## Session Order (Recommended)

| # | Group | Items | Est. Effort | Status | Task File |
|---|-------|-------|-------------|--------|-----------|
| 1 | 🔵 Weight Handling Unification — Phase 1 | 3 | ~3–4 hrs | `DONE` 2026-04-27 | [01-weight-unification.md](tasks/01-weight-unification.md) |
| 2 | 🟢 Buffer Cards & Main-Menu Refresh | 3 | ~1.5–2 hrs | `DONE` 2026-04-28 | [02-buffer-cards-refresh.md](tasks/02-buffer-cards-refresh.md) |
| 3 | 🔴 Print Queue & Label Management | 7 | ~2.5–3.5 hrs | `DONE` 2026-04-28 | [03-print-queue-labels.md](tasks/03-print-queue-labels.md) |
| 4 | 🔶 Archive / Delete / Cleanup Lifecycle | 4 | ~3 hrs | `PARTIAL` 2026-04-28 (4.4 deferred) | [04-archive-delete-lifecycle.md](tasks/04-archive-delete-lifecycle.md) |
| 5 | 🟡 Edit Filament & Details Modal (small adds) | 2 | ~3 hrs | `DONE` 2026-05-08 | [05-edit-modal-small.md](tasks/05-edit-modal-small.md) |
| 6 | 🟡 Edit Filament & Details Modal (new panels) | 2 | ~3 hrs | `DONE` 2026-05-11 | [06-edit-modal-large.md](tasks/06-edit-modal-large.md) |
| 7 | 🧪 Testing Housekeeping | 2 | ~1–1.5 hrs | `DONE` 2026-05-07 | [07-testing-housekeeping.md](tasks/07-testing-housekeeping.md) |
| 8 | 🟣 Keyboard Navigation & Dialog Polish | 4 | ~3.5 hrs | `DONE` 2026-05-12 (8.1 partial — 12 button-only modals deferred as Bootstrap-default-acceptable) | [08-keyboard-nav-polish.md](tasks/08-keyboard-nav-polish.md) |
| 9 | 🟢 Quick-Swap Grid Enhancements & Printer Status Widget | 3 | ~4–5 hrs | `DONE` 2026-05-13 | [09-quickswap-grid.md](tasks/09-quickswap-grid.md) |
| 10 | 🟠 Add/Edit Wizard UX Overhaul | 11 | ~6.5–8 hrs | `DONE` 2026-05-14 (Sessions A+B+C — 10.1 progressive-disclosure cleanup shipped on `feature/wizard-cleanup-aggressive` across 9 commits / 6 feedback rounds; merged to dev + main) | [10-wizard-ux-overhaul.md](tasks/10-wizard-ux-overhaul.md) |
| 11 | ⚫ External Parsers & Prusament Cleanup | 3 | ~3 hrs | `DONE` 2026-05-13 | [11-external-parsers.md](tasks/11-external-parsers.md) |
| 12 | 🔵 Weight Entry Unified Component — Phase 2 | 2 | ~6–10 hrs | `DONE` 2026-04-27 | [12-weight-entry-component.md](tasks/12-weight-entry-component.md) |
| 13 | 🐛 Recent Bugfixes (Weight + Dryer Display + LOC Search + Bind Desync + Eject) | 9 | ~6–8 hrs | `DONE` 2026-05-11 | [13-recent-bugfixes.md](tasks/13-recent-bugfixes.md) |
| 14 | 🧪 Test-Sweep Flake Stabilization | 6 | ~3–5 hrs | `DONE` 2026-05-12 | [14-test-sweep-flake-stabilization.md](tasks/14-test-sweep-flake-stabilization.md) |
| 15 | 🪟 Canonical Overlay-Mount Helper | 3 | ~5–7 hrs | `DONE` 2026-05-11 (Path A — 5 inline overlays migrated; 4 non-inline surfaces deferred) | [15-canonical-overlay-mount.md](tasks/15-canonical-overlay-mount.md) |
| 16 | 🧰 Testing Hardening & Helper Consolidation | 4 | ~3–4 hrs | `DONE` 2026-05-12 | [16-testing-hardening.md](tasks/16-testing-hardening.md) |
| 17 | 🪟 Details Modal, Queue & Wizard End-State Polish | 5 | ~3–4 hrs | `DONE` 2026-05-14/15 (all 5 items shipped on `feature/buglist-sweep-2026-05-14`; 17.1 edit-toggle side completed 2026-05-15 by un-hiding `needs_label_print` from the wizard's hide list) | [17-details-queue-wizard-polish.md](tasks/17-details-queue-wizard-polish.md) |
| 18 | 📍 Unknown Location + Audit Mode Refinement | 2 | ~4–6 hrs | `DONE` 2026-05-15/16 (18.1 Unknown bucket + 18.2 Part A auto-park + Part B visual panel + idle watchdog all shipped on `feature/buglist-sweep-2026-05-14`; parent/child rollup deferred as future enhancement on top of the multi-location audit) | [18-unknown-location-audit.md](tasks/18-unknown-location-audit.md) |
| 19 | 🧪 Dev-Env Reset & E2E Test-Debt Triage | 5 | ~4–6 hrs | `DONE` 2026-06-02 (`feature/dev-env-reset-test-debt` — reset_dev.py + seed pair + 11 unit tests; 19.2–19.5 test-debt all green; 19.1a/b triage doc. Destructive `--prune` left for Derek to invoke per the non-destructive-default decision) | [19-dev-env-reset-test-debt.md](tasks/19-dev-env-reset-test-debt.md) · [triage notes](tasks/19-seed-readiness-and-failure-buckets.md) |
| 20 | 🔗 Toolhead Binding / Unbind-Propagation Cluster | 4 | ~6–10 hrs | `DONE` 2026-06-05 (`feature/group-20-toolhead-binding`). 20.1 verified-fixed (already resolved by the L204 FilaBridge unmap + Phase 4 cutover — no separate fix needed). 20.2 shipped (commit `c7c9847` — single-slot dryer-box auto-attach/detach follows its spool). 20.3 shipped (commit `daa31dc` — full toolhead-delete cascade: no spool/binding left pointing at a dead toolhead). Closed via `eb35b4a`+`9c96b48`. | [20-toolhead-binding-unbind-cluster.md](tasks/20-toolhead-binding-unbind-cluster.md) |
| 21 | 🐞 New-Bug Sweep (2026-06-12) — card/modal + toolhead load/unload | 6 | ~4–6 hrs | `DONE` 2026-06-15 (`feature/new-bug-sweep-2026-06-12` → dev). All 6 shipped, each Playwright-reproduced/verified on the live container; 17 new/rewritten tests, 122 passed across adjacent suites, 0 regressions. **21.1** the "lower-right search badge" was the floating `.fcc-fab-search` (measured overlapping the bottom buffer-card weight) — Derek's call: it's his primary search affordance, keep it always-available; reworked into a **drag-to-park FAB** (new `fab_drag.js`, persisted `localStorage:fcc.fab.pos`, default bottom-left deck band clear of data, tap/drag/long-press, + `/` & `Ctrl/Cmd+K` shortcuts). **21.2** self-healing QR sweep on `_renderManagePayload`'s anti-wiggle early-return (dropped rAF QR pass recovers within one pulse). **21.3** `is_toolhead` now includes `No MMU Direct Load` (sourced from canonical `locations_db.TOOLHEAD_TYPES`). **21.4** dropped the `spoolModal.hide()` from the queue-label handler. **21.5** `dblclick` on a force-location row commits via `Swal.clickConfirm()`. **21.6** buffer "recently-assigned-out" guard so a stale `/api/state/buffer` pulse can't resurrect a just-assigned spool (full multi-client sync stays L302). Spun off standalone: the `locations_db.TOOLHEAD_TYPES` double-definition smell. | [21-new-bug-sweep-2026-06-12.md](tasks/21-new-bug-sweep-2026-06-12.md) |
| 22 | 🌉 FilaBridge Phase-2 — Detection & Deduct-Attribution Follow-ups | 4 | ~7–11 hrs | `PARTIAL / OPEN — foundation MERGED to dev 2026-06-14 (8592964), branch deleted` — **22.1+22.2 RELEASED to prod 2026-06-13** (`54f271c`, merged in the FilaBridge Phase-2 cutover; feature branch deleted). **✅ 22.1 + 22.2 DONE** — one "detection-layer hardening" commit shipped all of it: instant-ack log on the STOPPED edge, adaptive poll (10s busy / 30s idle), and the shared **ambiguous-IDLE review** (a latched in-progress job reaching IDLE/READY without an observed STOPPED/FINISHED — incl. ATTENTION-screen hard-reset — routes to the cancel-review pipeline flagged "couldn't confirm", computed at retained progress, **never auto-deducts**). 106 backend + 5 frontend tests green, live on dev. **⏳ 22.3 OPEN** — mid-print spool swap/run-out apportionment (full footer still dumped on the spool mapped at completion; pre-existing, FilaBridge has it too); recommended start = option (c) minimum-viable (route a mid-print spool-change completion to the existing cancel-review), now on a prod-verified foundation (recoverable no-spool/progress-unknown deduct, `main`/`3d88d70`). **✅ 22.4 DONE + ✅ 22.3(c) DONE — CHAT 1 of 2, 2026-06-14, on `feature/group-22-deduct-followups`, committed `e522d99`, verified done 2026-06-14 (8-agent adversarial source check all-implemented + 49 new / 176 deduct-domain tests green, 0 regressions).** 22.4 = all 6 polish items (multi-spool ghost-vs-direct over-deduct fix, shared `_record_applied_deduct`, orphan-tool double-warn, 2dp shortfall, single MMU probe, progress_unknown threading). 22.3(c) = `spool_changed` manual-split review when a toolhead's spool changes mid-print (start-spool snapshot threaded through live edge / restart / deferred-fetch; degrades to auto-apply). 183 deduct+adjacent tests green, zero regressions; built design→adversarial-verify→implement→adversarial-diff-review (the diff review found+fixed the deferred-completion-retry bypass + test gaps). **🟠 22.3(b) = CHAT 2 FOUNDATION SHIPPED + MERGED to dev 2026-06-14, group still OPEN** — the swap-event capture **(a)** + threading + safe degrade-to-review (now catches **A→B→A**) + the `parse_color_change_segments` **(b)** segmentation primitive + the 2 residual 22.4 nits are built + unit-tested (29 new tests, 233 deduct/parser green, 0 regressions, local max-effort review applied). **MERGED into dev (`8592964`); the `feature/group-22-deduct-followups` branch was deleted (work is safe in dev, no forgettable branch).** The **ONE remaining item = the validated automatic grams-split, DEFERRED**: it's BLOCKED ON DATA — a real **"crazy multi-swap" print / active print** to validate the apportioned grams vs weighed reality AND confirm `;COLOR_CHANGE` survives the bgcode decode. **💡 Steering insight (Derek 2026-06-14): make the system genuinely SWAP-AWARE — a pause ≠ a filament swap** (could be embedding magnets/inserts/hardware); tie segmentation to ACTUAL filament events (gcode `;COLOR_CHANGE`/M600, runout sensor, explicit FCC eject-load), not bare pause edges, and consider letting the user annotate each pause's reason. Full handoff in task file §22.3(b). Part of the FilaBridge epic. | [22-filabridge-phase2-detection-deduct-followups.md](tasks/22-filabridge-phase2-detection-deduct-followups.md) |
| 23 | ✏️ Edit-Filament Import/Scrape, Product-URL & Label-Invalidation | 6 | ~5–8 hrs | `DONE` 2026-06-29 (`feature/group-23-edit-filament-import-scrape` → `dev`; `359e97f` + follow-up `1766573`). All 6 shipped + a label-scope follow-up; grounded (6-agent) → 17-agent adversarial diff-review (8 confirmed fixes) → focused follow-up review (clean); 185+ tests green, 0 regressions; 16 new backend tests. **23.2** `editfilExternalReset()` on modal open. **23.1** wired scraped `color_hex` (picker sync) + `product_url`/`purchase_url` into import-apply (parser already scraped them — consumer dropped them). **23.5** `📄 product` wizard chip. **23.6** idempotent canonical `product_url` UPGRADE in `_handle_prusament_url_scan`. **23.4** **delete-sentinel** (`window.FCC_DELETE_EXTRA` / `DELETE_EXTRA_SENTINEL`) so blanking clears — backend pops it, never reaches Spoolman; all 3 write surfaces (edit-filament, wizard spool-edit edit-only, vendor); no-op-delete suppression + SYSTEM_MANAGED backstop + edit-log "(cleared)". **23.3** label-type-aware: filament swatch = heavy `mountOverlay` prompt (gated on `needs_label_print===false` OR `sample_printed===true`), spool labels = light `needs_label_print` flag via new `POST /api/filament/<id>/flag_spool_labels` (Brand/Type/Color-name, NOT hex). Label-bearing fields read from the real `.lbx` templates. | [23-edit-filament-import-scrape-label.md](tasks/23-edit-filament-import-scrape-label.md) |
| 24 | ⚖️ Weight Transparency & 1000g-Default Correctness | 2 | ~3–5 hrs | `DONE` 2026-07-01 (`feature/group-24-weight-transparency-1000g` → `dev`). Both items shipped: shared `_log_manual_weight_change` helper wired into BOTH manual funnels (`api_spool_update` + `api_edit_spool_wizard`); `weight_is_default` now honored on the wizard create-path + Amazon/3DFP parsers + the edit-filament import preview; printer-status bar scaled to the spool's own `initial_weight`. Grounded (5-agent) → 22-agent adversarial diff-review (18 findings / 4 confirmed, all applied — incl. the missing wizard-edit funnel + the edit-import sibling leak) → full `RUN_INTEGRATION=1` sweep 1759 passed / 21 pre-existing (zero regressions, `locations.json` 55→55). Pre-existing red set filed as a standalone Testing-cleanup row. _Historical grounding:_ **F** manual weight adjustments log no before→after — `api_spool_update` (app.py:1142) already fetches the `pre` snapshot but writes no delta line; mirror the deduct-path wording (app.py:4129) at the one funnel (`saveSpoolWeight`). Low-risk additive. **K** audit + kill hard-coded 1000g spool logic, **make it variable by size**: the wizard create-path leaks the parser's 1000g default into `initial_weight` (inv_wizard.js:2513 ignores `weight_is_default`; ~6 `\|\|1000` sites), parser fallbacks (external_parsers.py:178/339/456), and the printer-status bar hard-codes 1000g=full (inv_printer_status.js:211 — scale to `initial_weight`). L200 correction already guards `weight_is_default`. Derek: derive tare by weighing a new full spool when net is known (`tare = gross − net`). Same WeightEntry surface as the deferred "archived-spool empty-weight" standalone row (gated behind the unified WeightEntry component — cross-ref, NOT folded). | [24-weight-transparency-1000g.md](tasks/24-weight-transparency-1000g.md) |
| 25 | 🧙 Wizard UX — Print, Tab-Confirm & Spool-Label Parity | 3 | ~3–4 hrs | `DONE` 2026-07-01 (`feature/group-25-wizard-ux-print-tab` → `dev`). All 3 shipped (pure-frontend `inv_wizard.js`): **25.I** `Tab` confirms the highlighted combobox item (shared location+vendor; no `preventDefault` so focus advances); **25.3** extracted `wizardBuildFilamentLabelDiff` + added the missing `original_color` diff so a wizard Color-name edit flags spool labels (parity via the shared `_maybePromptLabelReprint`); **25.H** extracted `wizardRenderPostSaveQueueActions` — "Queue All" for multi-create + a queue chip in edit mode. 11-agent adversarial review (6 findings / 1 confirmed + fixed: a Tab pass-through fired a bubbling `change` that spuriously flipped `isDirty` → false "Unsaved Changes"; guarded on actual-change, applied to Enter too). 13 new tests + 34 green regression, 0 regressions. _Historical grounding:_ **H** add a print/queue button for an existing filament being edited (the edit-spool path renders none) + a "Queue All" for multi-create rounds (today N created spools = N chip clicks; no bulk-queue helper exists). **I** the location/vendor combobox `wizardBindCombobox` keydown (inv_wizard.js:935) has no `Tab` branch — Tab should confirm the highlighted `.active` item like Enter does (~5-line shared fix, covers location + vendor). **25.3** (from the Group 23 cleanup verification) the wizard `edit_spool` change-diff (inv_wizard.js:~2896-2927) omits `original_color`, so a Color-name-only edit via the wizard doesn't fire the 23.3 spool-label flag — add `original_color` (ideally share the trigger logic with `inv_details`). Do NOT nest under the DONE Group 10/17 (lifted-on-archive). | [25-wizard-ux-print-tab.md](tasks/25-wizard-ux-print-tab.md) |
| 26 | 🧪 Test-Infra Debt Cleanup | 9 (21 reds / 6 buckets + 2 flakes + FE backfill) | ~5–8 hrs | `TODO` (filed 2026-07-01, consolidation). The **21 pre-existing reds** cataloged on the Group-24 sweep (21 failed / 1759 passed, `RUN_INTEGRATION=1`; ALL pre-existing, none touch product) in 6 buckets — **26.1** stale `test_buffer_card_refresh` source-canary (8; repoint to `_renderSpoolsRefreshPayload`), **26.2** stale post-FilaBridge `test_universal_fallback` (4; delete/rewrite + UTF-8 console fix), **26.3** overlay tests need a bound+loaded-slot fixture (6; seed or skip), **26.4** visual baseline drift (1; recapture), **26.5** order-pollution escape-walk (1; reset modal state), **26.6** stale vs the 23.4 delete-sentinel (1; update assertion) — **plus** the 2 intermittent flakes (**26.7** wizard-cancel-reopens-details, **26.8** doassign buffer-safety) + **26.9** the Group-23 FE coverage backfill (23.1/23.2/23.5/23.6). Test-only, LOW risk; makes a red sweep mean something again. | [26-test-infra-debt-cleanup.md](tasks/26-test-infra-debt-cleanup.md) |

## Items NOT Grouped (Solo, Deferred, or On Hold)

These items remain in `Feature-Buglist.md` but aren't part of a batch session:

| Item | Reason |
|------|--------|
| Filament-attribute add broken in wizard (buglist 2026-05-30) | `DONE` 2026-06-07 on `feature/buglist-sweep-2026-06-05`. (a) `eda2cba feat(wizard): add ➕ button to multi-choice chip fields (filament_attributes)` — `wizardGenerateFieldHTML` (inv_wizard.js:~1618-1665) now wraps the chip widget in a flex row with a `.wizard-multichoice-add` ➕ button; `wizardPromptNewChoice` is multi-choice-aware (pushes to `fieldDef.choices` + calls `wizardAddMultiChoiceChip` in place, no schema refetch). (b) `3c6da1c feat(toast): Escape dismisses toasts (overlay > toast > modal)` — capture-phase document keydown handler in inv_core.js with overlay > toast > modal precedence; publishes `window.showToast`. (c) `d29c1e3 fix(toast): Escape owns the toast through its fade-out` — closes the ~300ms fade-window race so Escape doesn't leak to the modal. **Config-modal "clunky" portion** is the L18 Phase 5 OPTIONAL follow-up (separately tracked). |
| `locations.json` corruption recurring (L37) | `PARTIAL` 2026-05-12 — Hardening (1) per-call unique temp filename + (2) verify-after-write tripwire shipped on `feature/locations-json-write-hardening`. Monitor prod hub.log for `verify-after-write FAILED` critical lines. (3) explicit truncate + (4) Docker named volume deferred pending recurrence signal. |
| Screen blanking / wake lock (L17) | ON HOLD — OS-level issue |
| Config system (L18) | **DONE (Phases 1–4) 2026-06-01** on `feature/config-system` — safe atomic writer + `GET`/`PUT /api/config` + schema-driven Settings card (Phase 1); editable connection settings + masked/updatable `SCRAPER_API_KEY` + Spoolman/FilaBridge host decoupling (Phase 2); guarded toolhead (`printer_map`) editor — fails closed, blocks referenced removals (Phase 3); config import/export with redacted secrets + dry-run diff (Phase 4). Each phase adversarially reviewed (21+18+24+17 agents); 76 unit+E2E tests. Full design + phased plan in [tasks/L18-config-system-design.md](tasks/L18-config-system-design.md). **Phase 5 (OPTIONAL / future, fill-time):** action-tool co-location — refactor the config-modal action cards (Reconcile / Filament-Attributes / Restore-Field-Order) into per-section hosts so the "clunky attributes" manager sits beside its prefs (UI-organization refactor, NO endpoint logic change). Overlaps the config-modal portion of the standalone "Filament-attribute add broken in wizard" bug above; the wizard `+`-button / modal-timing / toast-escape parts of that bug remain a separate wizard/UX task. |
| Filabridge status light (L20) | ON HOLD — all-software (FilaBridge process ↔ FCC `/api/status` polling). Defer until correlated logs from both sides + a clear repro of flicker-vs-solid behavior are available. |
| FIL:58 label scan (L23) | ON HOLD — needs physical label |
| Display modal on Display modal (L26) | `PARTIAL` 2026-05-12 + FOLLOW-UP 2026-05-14 — details↔details and wizard↔details stacking now both close siblings via `window.hideAllDetailsModals`. Retest the L28 freeze with same scenario. |
| Frontend lock-up (L28) | `DONE` 2026-05-19 on `feature/l28-frontend-lockup-ghost-eject` — root cause was polling-flood / socket-buffer exhaustion, **not** modal-on-modal as initially suspected. The 2026-05-18 DevTools console showed `net::ERR_NO_BUFFER_SPACE` cascading + uncaught `updateLogState` rejections. Added in-flight guards + `.catch()`/`.finally()` to 5 polling functions (`updateLogState`, `fetchLocations`, `loadBuffer`, `liveRefreshBuffer`, `refreshManageView`). Printer Status `_aggregate` already had one. Regression coverage in `test_polling_inflight_guards.py` (6 tests). Steady-state load remains high — see new L29 polling-reduction follow-up. |
| Box-slot ↔ toolhead spool-switching inconsistency (buglist line 146) | WATCH — the narrative repro is kept in the buglist; **likely RESOLVED by Group 20.1** (verified-fixed on prod 2026-06-05 via a live #240/#241 eject→reassign diagnostic — FilaBridge unmap + Spoolman location + ghost-trail all cleared). Awaiting Derek's re-verification; close if it no longer reproduces. |
| Unknown crash after auto-deduct (L69) | ON HOLD — needs repro; server logs suggest container restart mid-session |
| **NEW** Make Activity Log more ubiquitous (L238) | Persistent mini-log widget OR "N new events" pill OR modal-aware docking so the log is always visible during modal-heavy workflows. Once log is always-visible, toast durations can drop and toasts can become purely "happened now" flashes. Candidate approaches stack — pick one or all. (Partial progress: corner `#fcc-log-pill` "N new" flash + click-to-open compact log overlay shipped under L286 follow-on — but the in-modal docking ask is still open.) |
| Prusa metrics tooling research (L161) | Research / future inspiration — `prusa_exporter` + Prusa-Firmware-Buddy metrics docs. Could feed 9.3 (Printer Status widget), filabridge reconcile, or richer state probing. Not actionable as a discrete task. **⚠️ 2026-06-10 (8-agent workflow): NO-GO as a MEASURED-filament source for cancel deduction** — the Buddy metrics catalog has NO `pos_e`/`filament_used`/`extruded_mm` (only X/Y/Z + `sdpos` progress proxy + extruder index + a filament-TYPE string, not grams); the firmware running total is a lifetime-length odometer emitted only to Prusa Connect cloud. Transport is plain UDP syslog (port 8514, a Flask thread could ingest without Prometheus) but there's no useful payload. This NO-GO is specific to measured-filament-for-cancel; the broader metrics→dashboard idea is untouched. See `filabridge-absorption-scoping.md` §9.1. |
| **NEW** In-progress printing modal: QR codes appear on right side (L19) | Sometimes the in-progress printing update modal renders its QR codes on the right side instead of the expected position. Seemed to coincide with a background toast — possibly unrelated. Needs repro. |
| Slot-assign latency — "Processing..." stays up noticeably long (buglist line 3) | **`DONE` (A+B) 2026-06-10** (`e3b9481` → `main`) — **14.6s → 6.5s (−55%)** on a worst-case bound-Core-One assign. A per-assign `⏱️` trace (`inventory-hub/perf_trace.py`, zero-cost when idle) proved ~96% of the time was **synchronous timeouts probing the OFFLINE Core One**, not Spoolman. **Fix A** = per-move printer-state probe cache (kills the auto-deploy double-probe); **Fix B** = skip the legacy PrusaLink `/api/printer` fallback on a connection-level timeout (`prusalink_api.py`). Shipped with a **feeds-bind-to-Printer fix** (`9fdca65` — the dual-role `Type:"Printer"` Core One row was being rejected by the slot-bind validator). Dev fleet == prod fleet, so dev numbers represent prod. **Remaining ~4s was FilaBridge's own `/status` synchronously probing the offline printer — RESOLVED 2026-06-13 (FilaBridge decommissioned in the Phase-2 cutover; see `completed-archive.md`).** |
| **NEW** 🌉 Absorb / replace FilaBridge (epic) (buglist line 6) | Standalone epic — too large for a batch group (tracked like L271 / Mobile L315). **Two-phase plan + effort/risk/migration in [tasks/filabridge-absorption-scoping.md](tasks/filabridge-absorption-scoping.md)** (29-agent research + adversarial verification of 23 claims). **✅ Phase 0 + Phase 1 + the entire cancelled-print partial-deduct feature SHIPPED + RELEASED to `dev`+`main` 2026-06-12** (feature branches deleted; prod deploy pending Derek's TrueNAS cron): **(Phase 0) L25 print-finish fast-poll** (printer-state-transition one-shot; the old "cadence drops when idle" mechanism was WRONG — cadence is USER-inactivity+tab-visibility driven); **(Phase 1 = Scope 1) `_fb_spool_location` repointed** (`20f8802`) to read Spoolman `location` instead of FilaBridge `/status` → FCC owns the toolhead↔spool map (killed the residual L3 tail + the desync class; still one-way `_fb_write` push); **(cancel feature)** pure-Python `.bgcode` decoder + dashboard-independent `_cancel_monitor` daemon + per-tool prefix-parse to the M73 cancel point + exactly-once `print_deduct_ledger` + preview/confirm review + restart recovery, real-XL-validated. Method LOCKED = gcode prefix-parse (Buddy metrics L150 = NO-GO, no measured-filament metric; Prusa Connect API = XL-blind → local-only). **🎯 Scope 2 / Phase 2 "finish removing FilaBridge" — ✅ COMPLETE + VERIFIED ON PROD 2026-06-13** (main `92e4d74`, prod `92e4d749`; FilaBridge stopped/decommissioned; the full (a)–(f) checklist landed — see `completed-archive.md`). **All six steps (a)–(f) shipped** — full per-phase as-built record in `completed-archive.md` (creds off FilaBridge onto the Printer rows = the L271 Phase-4 credential gate; bgcode-aware recovery; FCC owns FINISHED; atomic switch; FB surface retired — `/api/fb_*` + `/print-errors` + status-dot + `_fb_write`/`_fb_spool_location`; FilaBridge decommissioned, `needo37` image pinned as a do-nothing fallback). **Open follow-ups kept STANDALONE** (they survive epic archival): Group 22.3 (spool-swap apportionment) + the vestigial-FB-artifact cleanup row below. **Do NOT adopt the `doutorinfamous` fork** (Moonraker pivot breaks the Prusa fleet). |
| **NEW** Cleanup: vestigial FilaBridge artifacts (buglist line 34, 2026-06-13) | Low-pri / cosmetic, **no behavior impact** — dead artifacts left by the completed Phase-2 cutover. (1) orphaned `inventory-hub/data/filabridge_error_snapshots.json` (its reader/writer was removed in Phase E Slice 3 — safe to delete on dev + prod); (2) dead `config.json` keys `auto_recover_filabridge_errors` / `filabridge_port` / `filabridge_ip` (no live reader post-cutover); (3) optional deeper tidy — retire the `fb_url` half of `config_loader.get_api_urls()` + the now-ignored `filabridge_url` params still threaded through `prusalink_api` (kept as the credential-gate's back-compat signature). Deliberately STANDALONE (not nested under the archived epic, so it survives). |
| **NEW** Drop the round-trip `⏱️` perf-trace logs from the Activity Log (buglist line 3, 2026-06-14) | ✅ **DONE 2026-06-15 (`19a4a5e`).** FULL retire — `perf_trace.py` + test deleted, all 11 span() sites unwrapped, ⏱️ emissions dropped; probe-cache gating preserved via thread-local depth guard. Zero review findings. _Original:_ Derek: "we fixed that issue, don't need that code running anymore; with FilaBridge out of the way it doesn't make sense." Confirmed (grep + read 2026-06-14) = the **L3 slot-assign latency probe** (`perf_trace.py`), whose `⏱️ {label} total={ms}ms \| …` summary is written to the Activity Log (grey `INFO`/`888888`) at [logic.py:373](../../inventory-hub/logic.py#L373) (per-move/assign) + [app.py:3058](../../inventory-hub/app.py#L3058) (printer-state probe). The L3 latency it diagnosed is **DONE** (2026-06-10, `e3b9481`) and FilaBridge's `/status` probe — a main culprit — is decommissioned, so the diagnostic can retire. **Two removal scopes (Derek's call):** (a) **silence-only** — drop just the two `state.add_log_entry(summary, …)` emissions, keep `perf_trace.py` + the `span()` instrumentation for hub.log-only future diagnostics (lightest touch); (b) **full retire** — remove `perf_trace.py` + every `with perf_trace.span(...)` / `start_if_idle` / `finish` site (logic / app / prusalink_api / spoolman_api / config_loader) + `test_perf_trace.py`. Standalone; same FilaBridge-decommission cleanup theme as the vestigial-FB-artifacts row above (do together if convenient). |
| **NEW** Escape with an open dropdown should cancel the dropdown, not the modal (buglist line 13) | ✅ **DONE 2026-06-18 (`fd5ac89` + bind-picker follow-up).** Full sweep of the custom comboboxes (input + div-dropdown) that kept focus on their `<input>` so Escape bubbled to the Bootstrap modal and dismissed the whole modal: feeds combo (`inv_loc_mgr`), wizard **location + material + multi-choice** comboboxes (`inv_wizard`), and the bind-slot-picker (`inv_quickswap`) all now `preventDefault`+`stopPropagation` on Escape, handled AHEAD of the empty-list guard so a "No matches" list can still be escaped out of. The two `inv_details` comboboxes were already correct (reference pattern). E2E: `test_escape_closes_dropdown_e2e.py` (location, material, feeds populated + empty) + extended `test_bind_picker_escape_closes` to assert the modal stays open. The 4-lens review caught the wizard material + multi-choice comboboxes (both **confirmed live** closing the wizard) + the feeds empty-list edge — none in the first pass. _Original:_ Derek: hitting Escape with a dropdown open closes the **entire modal** instead of just the dropdown. Seen in Location Manager dryer-box toolhead/slot assignments; **needed a full sweep**. Native `<select>`s were out of scope (Chromium consumes Escape on the open popup, so they don't repro). |
| **NEW** Rename inventory hub → Filament Command Center (buglist line 9) | Derek 2026-05-31: "Rename inventory hub to Filament Command Center." — Repo-wide rename. Scope: `inventory-hub/` directory + every internal import / path reference; Docker container name (`inventory_hub` in `docker-compose.yml`), `Dockerfile`, `.dockerignore`; HTML `<title>` / nav-brand / page headings; `setup-and-rebuild/` references; test helpers / fixtures that mention "inventory-hub"; CLAUDE.md project conventions section; any external links Derek bookmarked (mark as breaking change). Sequencing: (1) inventory path rename + grep-and-replace, (2) Docker compose + container name (downtime), (3) verify TrueNAS prod compose can pick up the renamed image, (4) update CLAUDE.md + docs. Plan first — this is a large blast radius. |
| Filament-attribute cleanup firing "removed ['Wood']" every deploy (buglist line 11) | ✅ **FIXED + RELEASED 2026-06-05** (`d1072d8` → `478561e`). Root cause (adversarially verified, 7-agent workflow): a deploy-time add/remove loop — `setup_fields.py` re-seeded `Wood` into the `filament_attributes` choices (create_field merge-POSTs the union) and the boot cleanup stripped it, every deploy. Fix: drop `Wood` from `generated_attrs` + subtract `RETIRED_FILAMENT_ATTRS` before the POST (covers the external-CSV seed sources too) + a hermetic regression guard. **Watch:** expect `removed ['Wood']` ONE final time on the next deploy, then silent — close after confirming. |
| Print Status not updating weight on print-finish (L25) | `NEEDS REPRO` 2026-05-25 — code-path mapped: `_pulse_section_printer_status` at `app.py:4056` reads `spoolman_api.get_spools_at_location_detailed(tid)` per toolhead on every dashboard pulse and bumps the widget fingerprint via rounded grams, so the widget SHOULD pick up the new weight within one poll cycle. **Suspect adaptive-cadence lag** — when the printer transitions from PRINTING→IDLE/FINISHED, the cadence drops to 15s-30s buckets and the filabridge auto-deduct lag becomes visible. **Possible fix**: one-shot fast-poll for ~30s after a PRINTING→non-PRINTING transition. Repro with DevTools: confirm dashboard_pulse is actually firing post-print, confirm Spoolman serves post-deduct weight, confirm `inventory:sync-pulse` dispatch. L361 event-delegation fix is unrelated. |
| Refactor strip cards in Location Manager (L241) | UI/Theming standalone — merge horizontal layout with modern grid card features |
| High-Contrast Pop everywhere (L242) | Large UI/Theming pass — White Text + Heavy Black Shadow + adaptive variants per color |
| Help button per modal (L250) | Standalone — modal-specific help affordance |
| Legacy ID assignment tool (L252) | Standalone — pair existing Spoolman IDs to physical legacy spools (bulk-import case) |
| Location Manager redesign (L271) | **NEAR-COMPLETE, 2 open slices** — Phase 1A (2026-04-25), **1B** (2026-05-25, `aeeaa09`), **2** backend consumer migration via `resolve_parent` (2026-06-04, `22e3f91`+`3b7ba9e`+`172652c`+`6a5a35b`), **2.5** frontend tree consumer migration (2026-06-04, `280445a` merge), **3** first-class Printer rows + synthesizer retirement (**DEPLOYED to prod** 2026-06-04, `31bdd3c`), **3.5** true multi-level nested tree renderer + transitive subtree occupancy (**DEPLOYED to prod** 2026-06-04, `a070cff`), **5** Edit-UI + shelf-nesting slice — renames (Wall/Shelf/Section), data-driven Type dropdown with custom types persisted in `localStorage:fcc.locMgr.customTypes` (**DEPLOYED to prod** 2026-06-05, `3ea2a45`+`e7ccea3`+`8250f38`). Adversarial review on each phase. **⚠️ Phase 4 status (corrected 2026-06-12 against the live tree):** the **toolheads[] structure cutover DID ship to prod** 2026-06-04 (`a5b1ee6`) — `locations.json` Printer-row `toolheads[]` is the **runtime single source of truth** (`locations_db.get_active_printer_map()` is read at ~25 call sites across app.py/logic.py/spoolman_api.py; the `/api/printer_map` PUT writes the rows). BUT "config writer retired" was **imprecise**: `config.json:printer_map` is **NOT deleted** — it survives as a **PRIME-ONLY boot seed** ([app.py:332-337](../../inventory-hub/app.py#L332-L337)). **Phase 4 is now FULLY COMPLETE:** the toolheads[] structure cutover (`a5b1ee6`) **plus** the 2026-06-12 re-flagged **credential-relocation gate** — **✅ DONE, merged + on prod 2026-06-13** via the FilaBridge Phase-2 cutover (`43bb56d` creds on the Printer rows + `a26468c` Settings "Printer Connections" editor; main `92e4d74`, prod `92e4d749`; FilaBridge decommissioned). `config.json:printer_map` survives only as a vestigial PRIME-ONLY boot seed (optional later cleanup). **So the ONLY open L271 slice is (5) prefix-retirement** (delete `derive_parent_id_from_prefix` + remaining `split('-')[0]` sites + write-time orphan validation). [tasks/L271-location-manager-phase-plan.md](tasks/L271-location-manager-phase-plan.md) for the consumer map. |
| **NEW** Sub-location add workflow redesign — shelf/row/section clunky (buglist line 5, 2026-06-14) | Derek: adding shelf/row/section (in fact ANY sub-location) is messy; the whole add-workflow needs a redesign. **Answers his "have we finished the location-id refactor?" → yes, essentially:** the L271 *data* model is done (parent_id, first-class Printer/grouping rows, Room→Wall Shelf→Row→Section nesting — Phases 1–5 all shipped/deployed; only prefix-retirement remains). So this is a **UX-layer redesign of the add-sublocation flow on a now-solid data foundation** — the Add/Edit Location modal's Parent selector + Type dropdown + nesting affordance (`promptEditLocation` / `api_save_location` / `migrate_shelf_grouping_rows_if_needed`). Distinct from the L271 prefix-retirement (data cleanup), the custom-types edit/rename/delete row, and the strip-card refactor (L241). Standalone, L271-adjacent UX; **plan the flow first** (Derek wants a redesign, not a patch). |
| Bulk Moves (L298) | **UNBLOCKED** 2026-06-04 — L271 Phases 3+3.5+4 all deployed to prod. Ready for design/build whenever picked up: scan Box A (source) + Shelf B (destination), commit all spools. Touches scan handler + new bulk-move endpoint. |
| Shapeshifting QR codes (L299) | `DONE` 2026-06-07 on `feature/buglist-sweep-2026-06-05` (Option B — reusable helper). `aa3fc30 refactor(deck): extract registerShapeshiftQR helper; migrate Audit onto it`. `inv_cmd.js` exposes `window.registerShapeshiftQR(config) → {set, reset, current}` factory; Audit's prior inline `updateAuditVisuals` (which hand-mutated `qr-audit`/`lbl-audit`/`btn-deck-audit`) is now a one-liner `auditShapeshift.set(state.auditActive ? 'active' : 'idle')`. Future scannable QRs register through the same surface. Regression coverage `tests/test_shapeshift_qr_helper_e2e.py`. Close note `c2a71d9`. |
| Audit parent/child rollup (buglist line 240) | Future enhancement on top of the completed L154 Audit-mode work — scanning all of a cart's shelves (or a printer's toolheads) should count toward auditing the parent location itself; needs the audit session to span multiple locations. Re-homed 2026-06-05 from L154. |
| Location Manager cross-browser sync (L302) | ON HOLD — needs SSE/WS transport |
| Mobile mode (L315) | Large standalone architectural effort |
| Prusament QR codes as labels — 🚫 SHELVED (buglist line 18) | Derek 2026-06-05: shelved treating the manufacturer Prusament QR as a primary/reusable FCC label — once you rewind your own filament onto a reusable spool the QR lies (it still resolves to Prusa's ORIGINAL contents). The scan pipeline still RECOGNIZES Prusament URLs for the supported flows (match + temp/weight backfill on existing inventory, L198/L200, both shipped). Data-model note + the spool-hash matcher follow-up are filed separately (buglist line 22). |
| Dashboard modularization (L316) | ✅ **BACKEND DONE 2026-07-01** (`feature/l316-app-modularization` → `dev`) — grounding showed the frontend was already modular; the real monolith was `app.py` (7,429 lines → **367** + 12 flat modules: `app_core`, `startup_migrations`, `labels_csv`, 6× `routes_*`, `print_deduct`, `print_monitor`, `routes_state_pulse`). Characterization-first: 10-seam coverage audit (141 surfaces, 19 uncovered) → **284 new offline tests** (incl. `test_route_table_pin.py` strict identity + 12 `test_l316_charact_*.py`) adversarially verified → 11 scripted verbatim extraction steps, one commit each (steps 10+11 atomic — ~120 test patch-sites repointed to defining modules) → full sweep **zero new reds** vs the pinned 21-red baseline (final: 18 failed / 2,055 passed). CLAUDE.md gained the mandatory "Backend module map" rules; plan/as-built in [tasks/L316-app-modularization-plan.md](tasks/L316-app-modularization-plan.md). Spin-off: **50 suspected bugs** filed at the top of `Feature-Buglist.md` (full annotated list in [tasks/L316-characterization-findings.md](tasks/L316-characterization-findings.md)). Optional remainder: a further frontend pass (inv_wizard.js 4.4k) if Derek wants it. |
| Make as much configurable (L317) | Folds into Config system (L18) — see scaffold note |
| Clean up filament attributes (L319) | `PARTIAL` 2026-05-16 + AUTO-RUN 2026-05-19 — script shipped at `setup-and-rebuild/migrate_filament_attributes.py` + auto-cleanup at startup via `spoolman_api.ensure_filament_attributes_cleaned()`. `For Infill` + `Matte Pro` pending Derek's prod check before promotion to DELETE_CHOICES. |
| Continue supporting Spoolman Import from External (L341) | Tracks gap list of unimplemented sources (open-filament-database, Prusament spool-specific, Open Print Tags) |
| Dev Spoolman/filabridge environment (L345) | Standalone — set up isolated dev versions. **Note**: L345's "automate dev-env reset for clean E2E runs" follow-up shipped 2026-06-02 via Group 19.1 (`setup-and-rebuild/reset_dev.py` + seed pair). The base L345 ask (separate isolated dev Spoolman / FilaBridge instances vs the current shared dev) remains open. |
| Dynamic setup code refactor (L348) | Standalone — keep brand-new-install path working |
| Continue Spoolman vendor→filament→spool cascade (L349) | `DONE` 2026-06-07 on `feature/buglist-sweep-2026-06-05`. Empty-weight cascade was already shipped + battle-hardened earlier; the final gap (price cascade filament→spool) shipped via `f67773f feat(wizard): cascade filament→spool price (L349) + per-spool product_url input (L351)`. New `window.wizardPrefillSpoolPrice(ownPrice, filamentPrice)` (blank-gated — won't overwrite a typed value) wired into all three wizard entry paths (`openCloneWizard`, `openNewSpoolFromFilamentWizard`, `openEditWizard`). Regression coverage `test_wizard_price_product_cascade_e2e.py` (6 tests). Close-triage note `2e7966b`. |
| Configure extras propagation Filament↔Spool (L350) | Standalone — relates to Config system |
| Spool product data text field (L351) | `DONE` 2026-06-07 on `feature/buglist-sweep-2026-06-05` (verified). The L351 ask was for a per-spool `product_url` input in the wizard (storage already existed on the Spool extras). `f67773f` adds `<input id="wiz-spool-product_url">` to `modals_wizard.html:443-449` with the label "📄 Product Link (Optional — product/data page; falls back to the filament's link if left blank)"; `wizardApplyProductLinkFallback(spoolUrl, filamentUrl)` (inv_wizard.js:~635-659) prefills the spool's own URL or shows the filament URL as a ghost-placeholder `<truncated> (inherited from filament)`; wired into all 3 wizard entry paths; submit gated-on-truthy at `inv_wizard.js:~2631`. `applyFilamentFieldsFromTemplate` also populates on Prusament scan. Verified-DONE annotation in `2e7966b`. Regression coverage `test_wizard_price_product_cascade_e2e.py`. |
| Background refresh in Location Manager (L352) | `DONE` 2026-06-07 on `feature/buglist-sweep-2026-06-05` (Quick-Swap gap filled). `24de374 feat(quickswap): refresh live slot weights on the heartbeat (L352 gap)` — `inv_quickswap.js:1227` now listens for `inventory:sync-pulse` and calls `renderQuickSwapSection(currentLoc, { silent: true })` (guarded for `currentLoc` / `manageModal.show` / `focus-in-grid`); silent mode skips `innerHTML=''` up-front and uses a `_lastQsHtml` content-hash to skip DOM swap when unchanged (avoids flicker on a no-op tick). Now every surface driven by the L206 dashboard_pulse heartbeat — Location Manager, Printer Status widget, Buffer cards, Spool/Filament Details modals, Backlog modal, **and Quick-Swap grid**. Intentionally exempt: Add/Edit Wizard (would clobber typed values), Print Queue modal (user is reviewing). Regression coverage `test_quickswap_pulse_refresh_e2e.py` (3 tests). |
| Project Color Loadout (L391) | **UNBLOCKED** 2026-06-04 — L271 Phase 3 (first-class Printer rows) deployed to prod, so loadouts now have a stable Printer instance to bind to instead of a synthesized name. Ready for design/build per `/docs/Project-Color-Loadout/`. |
| Core One MMU rows cleanup (Derek aside) | `DONE` 2026-06-01 on `feature/core1-status-box-bounding`. Removed unused `CORE1-M1`–`CORE1-M5` from printer_map + locations.json; retyped `CORE1-M0` `No MMU Direct Load` → `Tool Head` (Name `🦝 Core One Upgraded Tool Head`). Surfaced alongside the box-bounding fix (the rows were rendering as 🔗 placeholder clutter). Seed `locations.json.example` updated. ⚠️ Prod replication still needed. See Feature-Buglist.md for the full write-up. |
| **NEW** 🏷️ Manage custom location Types — edit / rename / delete (buglist line 226) | Derek 2026-06-05 — `BACKLOG, not needed yet`. Deferred from L271 Phase 5. Phase 5 made the Type dropdown data-driven for ADDING custom Types (persisted in `localStorage:fcc.locMgr.customTypes` + auto-derived from any Type already used on a row), but edit/rename/delete of an existing custom type is missing. Deliberately deferred — file as the next L271 follow-up slice when needed. |
| **NEW** Surface live printer STATES in the Printer Status widget — expanded + collapsed ("tie into printer status" + "collapsed view" buglist items, 2026-06-13) | ✅ **DONE 2026-06-18 (`fd5ac89`).** The expanded badge already existed (L56); this added the **collapsed** half — a compact `.fcc-ps-state-chip` leads each printer group in the collapsed header strip so state (PRINTING / IDLE / PAUSED / OFFLINE …) is visible without expanding. Refactored the state→{class,label,title} logic into a shared `_stateMeta()` so the expanded badge + collapsed chip never drift; the printer-reported state string is HTML-escaped at the source (defensive, no-op for real states). `rows[name].state` already flows from the bulk-pulse payload + survives the `refresh()` cache-merge. Test: `test_collapsed_strip_shows_printer_state`; visual baselines stayed within tolerance (no recapture). _Original:_ surface live printer states in the widget — expanded AND the collapsed chip strip; mostly UI-surfacing of state the monitor already tracks. |
| **NEW** Standardize the two detail-modal button layouts (buglist line 3, 2026-06-13) | ✅ **DONE 2026-06-18 (`fd5ac89`).** Both footers now share one canonical order: Spoolman link far-left, then the right-anchored cluster — type-specific create/nav buttons, ✏️ Edit, ➕ Queue Label, the ⚙️ more-actions menu, Close. The shared Edit · Queue Label · ⚙️ · Close cluster sits in identical positions in both. Order-only change (all button ids / onclicks / data-bs-* byte-identical → JS wiring untouched; each modal keeps its theme colors); `id`s added to the Close buttons. Pinned by `test_detail_modal_footer_layout.py` (byte-offset order incl. Close); spool-details visual baseline within tolerance. _Original:_ Derek: "might be ok to force the user to pay attention" — went with positional consistency. |
| **NEW** Archived-spool empty-weight entry — type-in + combine prompts + propagate to vendor (buglist line 5, 2026-06-13) | Three asks on the empty-spool-weight prompt: (1) **bug** — the "weigh the empty" field only accepts the up/down number-scroller, the user can't type a value; (2) **combine** the spool-empty-weight + filament-empty-weight prompts (currently asked twice in a row); (3) **offer to propagate** the entered empty weight up to the brand/vendor `empty_spool_weight`. Touches the `<WeightEntry>` component + `weight_utils.js` resolveEmptySpoolWeight cascade (CLAUDE.md weight-entry surface). Standalone; line 5 is the meatiest of the three (a real input bug + two UX asks). |
| **NEW** Flaky E2E — `test_wizard_group10_session_a::test_edit_wizard_cancel_from_spool_details_reopens_details` (buglist Testing section, found 2026-06-07) | **→ folded into Group 26 as 26.7 (2026-07-01).** `NEEDS FIX (low-pri test-infra)` — intermittently fails at `_force_close_wizard`: after `m.hide()` (`forceClose=true`, `isDirty=false`) `#wizardModal` stays `.show`, so the chained details reopen never fires. Exercises the delicate wizard-on-top-of-spool-details stacking on a LIVE first-search-result spool → sensitive to dev data + modal-transition timing. **A/B-CONFIRMED pre-existing**, NOT a regression (fails on the clean dev baseline). Fix direction (in buglist): make `_force_close_wizard` await `hidden.bs.modal` rather than a fixed timeout (or retry `hide()` once), and/or seed a stable spool instead of `results[0]`. No prod impact — the wizard↔details close works in real use. |
| **NEW** Flaky E2E — `test_doassign_buffer_safety` (buglist Testing section, found 2026-06-15) | **→ folded into Group 26 as 26.8 (2026-07-01).** `NEEDS FIX (low-pri test-infra)` — intermittently fails (`assert <id> in []` — `state.heldSpools` empty when it should hold the seeded spool); a DIFFERENT test in the file fails on different runs, and the file passes most runs. Root-cause class: the live buffer poll (`liveRefreshBuffer`/`loadBuffer` → `/api/state/buffer`) races the test's locally-injected `state.heldSpools` during the ~400 ms wait, overwriting it with the server's empty buffer. **A/B-CONFIRMED pre-existing** (during the 2026-06-15 Option-B sweep — baseline passed 2/2 in isolation, sweep branch 4/5, failures hit different tests → impossible for a deterministic regression; the sweep diff doesn't touch `doAssign`/buffer). **Fix direction (test-infra):** stub/suspend the buffer poll for these tests, or gate the assertion on a deterministic hook rather than a fixed `wait_for_timeout`. No prod impact. Sibling of the wizard-cancel flake row above (same buffer-poll-races-local-state / modal-timing class). |
| **NEW** Test backfill — Group 23 FE behaviors not pinned (buglist Testing, 2026-06-29) | **→ folded into Group 26 as 26.9 (2026-07-01).** `test-debt, no functional shortfall` — surfaced by the Group 23 cleanup verification (the code was adversarially confirmed correct; a future regression just wouldn't be caught by the suite). Backend invariants ARE pinned (`test_delete_sentinel.py` 12 + `test_flag_spool_labels.py` 4); verified-by-read-only: **23.1** import-apply landing `color_hex` (synthetic color-input) + `product_url`/`purchase_url` + the 3-digit-expand/bad-hex toast; **23.2** `editfilExternalReset` anti-leak on reopen; **23.5** the `📄 product` chip; **23.6** the canonical Prusament `product_url` write + idempotent skip. Add focused assertions (extend `test_edit_filament_external_import.py`, `test_wizard_*`, `test_prusament_scan.py`). Sibling of the wizard-cancel / doassign flake rows. |
| **NEW** Movable activity-log pill — generalize `fab_drag` (buglist 2026-06-15) | ✅ **DONE 2026-06-15 (`19a4a5e`).** Made movable (shared `draggable_pill.js` / `makeDraggablePill`; persists `fcc.logPill.pos`; engine never touches `display`; Enter/Space added). _Original:_ Direct follow-up to Group 21.1 (draggable search FAB): the `#fcc-log-pill` activity-log badge is hard-anchored (global.css:1696, fixed right/bottom) and now looks orphaned next to the FAB that decoupled to draggable/default-bottom-left. `fab_drag.js` (~150 lines, keyed to `#fcc-fab-search` + `localStorage:fcc.fab.pos`) is directly generalizable to a `makeDraggablePill(el,{key,defaultPos,onTap})`. Needs `touch-action:none` + a dragging-transition-kill on the pill, a new `fcc.logPill.pos` key (add to the CLAUDE.md preference table), and must tolerate the JS-toggled hidden state. **Design Q (Derek):** make it movable (FAB parity) vs just re-anchor it sensibly — keep it visible/clickable either way ([[feedback_dont_remove_relied_on_affordances]]). Standalone (21.x sibling; Group 21 is DONE — don't reopen it). |
| **NEW** Label-CSV export — P-touch lock robustness (buglist 2026-06-15; narrowed 2026-06-29) | ✅ **DONE + ARCHIVED 2026-06-18 (`fd5ac89`)** for the main bug; **filename-mismatch RESOLVED** (`14ac268` — Derek's 2 P-touch files, one dev-linked + one live-linked, confirmed NO filename mismatch, so no realignment is needed). `api_print_batch_csv` writes the overwrite path atomically (`_write_label_csv`: temp + `fsync` + `os.replace`, leak-free fd, temp cleaned on failure — no torn CSV); a held handle (P-touch/Excel) fails LOUDLY — Activity Log ERROR + a 7s toast that **names the file held open** (main labels vs `slots_to_print.csv`); the success path also logs; fixed a latent `folder` NameError on a bare-filename `csv_path`. Tests: `test_label_csv_export.py`. **⏳ ONE low-pri residual still OPEN in the buglist:** give `api_print_location_label` (app.py:3778, generic `except` only) the same friendly file-lock message as the batch export (consistency follow-up). Standalone. |
| **NEW** `TOOLHEAD_TYPES` defined twice in `locations_db.py` (buglist 2026-06-15, spun off from Group 21.3) | ✅ **DONE 2026-06-15 (`19a4a5e`).** Collapsed to one canonical `frozenset` + guard test; pure-code, no prod/data/deploy step. JS copies left as a separate DRY item. _Original:_ Confirmed: `locations_db.py:20` (plain `set`) + `:547` (`frozenset`), identical 3 strings; the frozenset wins at import so the set is dead. Zero mutations anywhere (grep clean); all consumers are `in`/`not in`/iteration/`\|`-union → frozenset-safe. **Fix:** delete the dead `:20` set (or hoist the frozenset up next to it) + a membership/type guard test. ~15 min, near-zero risk. Standalone low-risk cleanup (do NOT nest under the DONE Group 20 — [[feedback_standalone_followups_not_under_completed_epics]]). Side note: `inv_quickswap.js:17` holds a parallel JS copy of the same 3 strings (separate, bigger lift). |
| **NEW** Undo-button overhaul — surface what moved where (buglist 2026-06-15) | ✅ **DONE 2026-06-15 (`19a4a5e`).** Log-readability shipped (record now carries label+source → "moved #1 … from <src> -> <dst>"); weight-undo intentionally NOT wired (separate feature). _Original:_ Hitting Undo logged only "↩️ Undid: Moved 1 -> CR" — "1" is `len(spools)` (a count, not an id), "CR" the destination; no spool identity, no source. Grounded: the rich label IS already computed in-loop and thrown away — `format_spool_display(spool_data)['text']` = "#1 Prusament PETG (Orange)" (logic.py:513) + the from-location `moves[sid]=current_loc` (logic.py:511); the summary at logic.py:489 just uses the count. **Pure data-plumbing** to enrich the undo record + render a readable from→to line (logic.py:1053). **Weight-undo does NOT exist** (only `perform_smart_move` pushes the undo stack, logic.py:643), so hitting Undo after a weigh reverts the last *move* — the surprising behavior Derek hit. **Scope Q (Derek):** ship the cheap log-readability fix now + decide separately whether to actually WIRE weight-undo (a real feature touching every weight write surface) or just signal "Undo = location-moves only." Standalone (pairs with the 18.2 activity-log-readability precedent). |
| **NEW** Test-debt — `test_bind_slot_picker` reds (found 2026-06-18 during the Option-B sweep) | ✅ **DONE 2026-06-18 (bind-picker follow-up).** Two root causes, both fixed; 14/14 stable across 2 full runs. **(1) Stale source mock (3 backend tests):** `test_single_slot_put_*` mocked `config_loader.load_config` for the printer_map, but the bindings validator reads `locations_db.get_active_printer_map()` post-**L271 Phase-4** cutover (config `printer_map` is now a prime-only boot seed) — the mock was dead, so XL-1/XL-3 weren't "registered" and every PUT was rejected. Repointed the 5 mocks to `get_active_printer_map`. **(2) Fixed-sleep flake (3 live tests):** rebind / unbind / enter-commits read the server only 600 ms after an async UI PUT — flaky against a slow/just-restarted dev container (a DIFFERENT chromium test failed on different runs = the classic flake signature). Replaced the fixed `wait_for_timeout` reads with a `_poll_slot_targets()` helper that polls until the expected state lands. **A/B-CONFIRMED pre-existing** via a stash run (failed identically on the clean baseline). Sibling of the wizard-cancel / doassign flake rows ([[reference_fcc_e2e_sweep_pollution]] class). |
| All "On Hold" section items | ON HOLD |

**Note on test flakes:** The `test_quickswap_visual` baseline mismatch, `test_ui_details_modal_e2e` offcanvas-backdrop flake, and `test_amazon_parser_matching` BS4-missing issue were all closed by Group 14 (14.4 / 14.3 / 14.5 respectively). Detailed write-up in `completed-archive.md`. Post-Group-14 follow-ups (parallel `tests/` directory, `_open_manage` consolidation, `reset_dom_state_js` audit, Windows pip footgun) are bundled into Group 16.

## How to Use

**With Claude Code:**
```
/project:work-group 1          # Start Group 1 (Weight Unification)
/project:work-group weight     # Same — matches by keyword
/project:finish-group           # When done — commit, archive, update status
```

**With Gemini / Antigravity:**
- Point at the specific task file: "Read `docs/agent_docs/tasks/01-weight-unification.md` and implement it"
- Or use the `/startnewtask` workflow which now knows about these groups
