# Dev-only dependencies. NEVER bake into the production image.
# Install on the host:
#   pip install -r requirements-dev.txt
#   playwright install chromium
pytest>=8.0
pytest-playwright>=0.5.0
pytest-base-url>=2.1.0
Pillow>=10.0

# Runtime deps the app uses — needed on the host so unit tests that
# `from app import app` can collect without the container. These match
# what the Dockerfile installs for production.
flask>=2.0
requests>=2.28

# Parsers — Amazon order-page parser uses BeautifulSoup4. The runtime
# module guards the import so the prod image (which doesn't ship bs4)
# falls back gracefully; pinning it here keeps test_external_parsers green.
beautifulsoup4>=4.12
